{
  "name": "@snatchit/design-tokens",
  "version": "0.1.0",
  "private": true,
  "description": "Snatch It brand tokens (colors, spacing, radius, type scale, shadows) as TypeScript values and CSS custom properties. Values are copied verbatim from mobile src/theme/index.ts — mobile remains the source of truth; drift is caught by tests/.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./css": "./src/css.ts",
    "./tokens.css": "./tokens.css"
  },
  "scripts": {
    "generate": "node scripts/generate-css.ts"
  }
}
