# ============================================================================
# admin/.env.local.example-harness — TEST HARNESS ONLY. SYNTHETIC VALUES.
#
# Copy to admin/.env.local to point the admin console at the Docker-free local
# stack (admin/scripts/local-stack.sh). Nothing here is a real Supabase key:
# the "anon key" is an HS256 JWT signed with the fixed dev secret
# snatchit-local-harness-jwt-secret-0000000000 (role=anon, iat fixed so the
# string is stable; regenerate with `node scripts/auth-stub.mjs --mint anon`).
# ============================================================================
NEXT_PUBLIC_SUPABASE_URL=http://localhost:3202
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1oYXJuZXNzIiwicmVmIjoibG9jYWwtaGFybmVzcyIsInJvbGUiOiJhbm9uIiwiaWF0IjoxNzU2MDAwMDAwLCJleHAiOjIwNzEzNjAwMDB9.SOC2vWZoCh6gM7wQqixH9h3ZL6kwzNxZOZTxKTdEO0M

# Synthetic logins seeded by admin/scripts/fixtures.sql (password for all:
# harness-pass-123; every MFA challenge accepts code 123456):
#   founder.a@example.test   platform_admin (public.admin_users), aal1 -> enrol MFA in-app
#   founder.b@example.test   platform_admin (public.admin_users), aal2 pre-verified factor
#   support@example.test     NOT in admin_users -> denied-path testing
#   buyer1@example.test      plain marketplace user
