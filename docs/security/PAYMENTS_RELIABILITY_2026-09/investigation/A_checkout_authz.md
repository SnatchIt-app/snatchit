# Agent A — checkout authorization & reservation lifecycle (investigation report, eadd456)

**Agent A report — checkout authorization & reservation lifecycle (eadd456, `/Users/josetascon/snatchit-pay`)**
Local `npx vitest run`: 5 files / 116 tests pass (none cover these paths). Paths below are absolute under the worktree; `cpi` = `supabase/functions/create-payment-intent/index.ts`, `0590` = `supabase/migrations/0590_strict_auth_on_listing_checkout_rpcs.sql`.

## 1. Hypothesis verdicts

**F01 — CONFIRMED (with one PARTIAL clause).**
- `cpi:269` selects `id, seller_id, current_bid, buy_now_price, buy_now_enabled, status, auction_status, winner_user_id, winning_bid_amount` — no `reserved_by`/`reserved_until`/`ends_at`.
- `cpi:302` buy_now branch: only `listing.status !== 'reserved'`. Any authenticated non-seller becomes `payments.buyer_id` (`cpi:576`) and PI `metadata[buyer_id]` (`cpi:509`) while someone else holds the reservation.
- `cpi:317-326` auction branch: `auction_status==='ended'` + `winner_user_id===buyerId` only; does not look at `status`. PARTIAL: `mark_listing_sold` sets `auction_status='sold'` (`0590:112`) and `cancel_listing` sets `'cancelled'` (`0590:59`), so sold/cancelled ARE rejected; the unrejected case is `status='reserved'` by a Buy-Now buyer with a live `reserved_until` at the moment the auction finalizes (bids on a reserved listing are allowed — `047:51-75` checks only `auction_status`/`ends_at`).
- Expiry: nothing server-side cancels a PI. Reuse at `cpi:395-455` returns the same `client_secret` to whoever passes line 302; a buyer whose reservation expired still holds a confirmable `client_secret` (Stripe PI never expires). Only PI cancel in the file is the DB-insert-failure path `cpi:596`.
- Multiplicity: per (listing, buyer, mode) a pending PI is reused (`cpi:380-395`); across buyers, and across modes for one buyer, N pending PIs coexist. Idempotency key `cpi:479` is per buyer.
- Reachable sequence: A: `rpc reserve_buy_now` (user JWT). B (any authenticated, not seller): `POST /functions/v1/create-payment-intent {listing_id, mode:'buy_now'}` → 200 + client_secret → confirm on Stripe → webhook promotes B's row to succeeded (`stripe-webhook:270`) → `mark_listing_sold(listing, B)` raises `'not reserved by you'` → `finish(false)` → 500, Stripe retries up to 3 days; B is charged, no transfer, no refund. If A then pays: A's promote hits `idx_payments_one_success_per_listing` (`003:52`) → webhook `lookupErr` → 500 loop; `confirm-payment:216` update error only logged; A's row stays `pending`; A's `mark_listing_sold` SUCCEEDS (no payment check), A's `ensure_transfer_exists` fails (`061:95`). Net: two captures, one sale, zero transfers.

**F03 — CONFIRMED.** `mark_listing_sold` `0590:86-115` and `complete_auction_payment` `0590:117-137` have no `payments` lookup; EXECUTE granted to `authenticated` (`0552:33-34`). Sequence: buyer JWT → `rpc reserve_buy_now` → `POST /rest/v1/rpc/mark_listing_sold {p_listing_id, p_user_id:<anything>}` → `status='sold', auction_status='sold', sold_at=now()`. Consequences beyond inventory: seller can no longer cancel (`0590:54`), bids blocked (`047:71`), no admin relist on main. Auction: winner (set only by finalize, `000:575-635`/`647-700`, but `finalize_auction(uuid)` is EXECUTE `authenticated` per `067:72`, callable after `ends_at`) → `complete_auction_payment` → sold without paying. `061` blocks only the transfer row; `confirm-payment:245-249` and webhook inserts are also gated on `succeeded`, so no unpaid transfer — the damage is stranded inventory.

**F04 — CONFIRMED (one overstatement).** `0590:139-175`: `p_minutes` unbounded/unchecked; holder passes `IF v_reserved_by IS DISTINCT FROM caller AND v_reserved_until > now()` (`165`) and re-sets `reserved_until = now()+p_minutes` (`173`) on every call; no per-user count/rate limit (`check_rate_limit` not called). Hard ceiling exists: `IF now() > v_ends_at` (`163`) — so the hold is bounded by the listing's `ends_at` (days), not "indefinite". `p_minutes NULL` → `reserved_until NULL`: never swept (`cleanup_expired_reservations` `000:347-348`), `mark_listing_sold` treats it as unexpired (`0590:103` NULL), but other buyers can overwrite it (`165` NULL → false). Reachable: `POST /rest/v1/rpc/reserve_buy_now {p_listing_id, p_user_id, p_minutes: 99999}` with a user JWT. The 10-minute value is client-only (`src/config/app.ts:11`, `web/src/lib/checkout.ts:7`).

## 2. Eligibility trace

Writers: `status/reserved_by/reserved_until` set by `reserve_buy_now` (`0590:170-174`); cleared by its expired pre-sweep (`150-151`), `release_reservation` (`81`), `mark_listing_sold` (`105` expired, `111-114` sold), `cancel_listing` (`58-61`), `cleanup_expired_reservations` (`000:335-350`, called only from `auto_finalize_expired_auctions`, cron `*/2` `014:15-19` + edge `auto-finalize-auctions` with service-role bearer). `complete_auction_payment` sets `status='sold'` but never clears `reserved_by/until` (`0590:135`). `winner_user_id/winning_bid_amount/auction_status='ended'`: `finalize_auction` and `auto_finalize_expired_auctions` only.
Expiry enforced: DB (`reserve_buy_now`, `mark_listing_sold`, cron sweep). Edge `cpi`: nowhere. Mobile: one-shot check on mount `CheckoutNative.tsx:113-138`, no countdown/timer, banner text only (`477`). Web: server render `web/src/app/checkout/[id]/page.tsx:22-27`. Nobody calls `release_reservation` on back/unmount; the only caller is the webhook on `payment_intent.payment_failed` for buy_now (`stripe-webhook:537`). No `payment_intent.canceled` handler.
PI on expire/release/supersede: untouched (payments row stays `pending`, PI stays confirmable).

Mobile (`CheckoutNative.tsx`, identical at tag `mobile/v1.0-build9-apple-review`; calls present since 6a5b854 2026-04-14, i.e. every 1.0 build):
1 `ListingDetailScreen.tsx:776` `rpc reserve_buy_now(p_listing_id, p_user_id, p_minutes=10)` → 2 mount: `listings` select `113-117` → `createPaymentIntent` edge (`src/lib/payments.ts:63`, body `listing_id, mode, expected_total_cents`; builds ≤7 omit `expected_total_cents`) → `initPaymentSheet` `215` → 3 `presentPaymentSheet` `269` → `confirm-payment` edge (`payments.ts:116`, non-throwing) → `rpc mark_listing_sold(p_listing_id,p_user_id)` `285` → `rpc ensure_transfer_exists(p_listing_id)` `298` → `transfers` select. Auction: same with `mode:'auction'`, `complete_auction_payment` `349`, `ensure_transfer_exists` `362`.
Web: `BuyNowButton.tsx:18` → `reserveBuyNowAction` → `rpc reserve_buy_now(…,10)` (`checkout.ts:55`, user-session server client) → `CheckoutClient.tsx:35` → `create-payment-intent` via `callEdgeFunction` (user JWT relayed, `edge-functions.ts:41`; **no** `expected_total_cents`, so the 409 check is skipped) → `PaymentForm.tsx:36` `stripe.confirmPayment` → `finalizePurchase` (`checkout.ts:121`): `confirm-payment` → verify `134-145` → `mark_listing_sold|complete_auction_payment` `163` → `ensure_transfer_exists(p_listing_id,p_user_id)` `171`; redirect path `complete/page.tsx` → same.
Webhook (service_role): `payment_intent.succeeded` → claim `270-275` → `mark_listing_sold|complete_auction_payment(p_user_id=metadata.buyer_id)` `409` → direct `transfers` insert `435` (guard is BEFORE UPDATE only, `0550:155`, so it passes) → push.

## 3. Failing-test designs (not written)

Harness: vitest + `vm` as in `reproduce.cjs`; loading `cpi` needs extras `{stripeFetch, stripeFetchRaw, STRIPE_MOBILE_API_VERSION, feeBreakdown, dollarsToCents, totalMismatch, captureException, createClient}` and a `db` whose `rpc('check_rate_limit')` returns `{data:true}`, `auth.getUser` returns the chosen user, `from('profiles')` returns `{stripe_customer_id:'cus_x'}`, `from('listings')` returns the fixture row (full row — the mock ignores the select list, which is exactly what exposes the missing columns). pgTAP: new `supabase/tests/120_reservation_lifecycle.sql` using `tap.login()`/`tap.login_service()`; listing fixture cloned from `110:82-91`.
- (a) Two buyers — vitest: listing `{status:'reserved', reserved_by:'A', reserved_until:+9m, buy_now_enabled:true, buy_now_price:100, auction_status:'active', seller_id:'S'}`; request as `B`, mode buy_now. Assert `res.status===400/403` and no `payments` insert with `buyer_id:'B'`. Today: 200, insert with `buyer_id:'B'`. pgTAP companion: `tap.login(buyer)`; `reserve_buy_now(E, buyer, 10)`; `tap.login(other_user)`; INSERT `payments(listing E, buyer other, status pending)` as service → assert `throws_ok` (needs a "pending payment buyer must be the reservation holder" check — contract item). Today: insert lives.
- (b) TTL/renewal/quota — pgTAP: `tap.login(buyer)`; `throws_ok(reserve_buy_now(E, buyer, 525600), 'P0001')` → today lives, `reserved_until ≈ now()+1y`. Then `reserve_buy_now(E,buyer,10)` twice with `pg_sleep(1)`; assert `reserved_until` unchanged (or bounded) → today moves forward. Quota: reserve listings E, F, G as buyer; assert third `throws_ok` → today lives.
- (c) Released/expired then intent — vitest: listing `{status:'reserved', reserved_by:'A', reserved_until:-1s}`; request as `A` → assert 400/409 and that the stale pending PI is cancelled (`stripeFetch` spy on `/payment_intents/pi_x/cancel`) → today 200, no cancel. pgTAP: reserve, `release_reservation`, `tap.login_service()`; `mark_listing_sold(E, buyer)` → `throws_ok 'not reserved by you'` (passes today; keep as regression). Webhook vm: `payment_intent.succeeded` where `rpc('mark_listing_sold')` returns `{error:'This listing is not reserved by you.'}`; assert `res.status===200` and payment marked orphaned/refund initiated → today 500 (`finish(false)`), no refund.
- (d) Direct unpaid mark-sold — pgTAP: `tap.login(buyer)`; `reserve_buy_now(E, buyer, 10)`; `throws_ok(mark_listing_sold(E, buyer), 'P0001', 'No verified payment…')` → today lives, `status='sold'`. Auction: fixture `auction_status='ended', winner_user_id=buyer`, no payment; `throws_ok(complete_auction_payment(…))` → today lives. NB `110:193-194` (#18) currently asserts the opposite and must gain a `succeeded` payment fixture.
- (e) Winner vs buy_now mismatch — pgTAP: fixture `ends_at=now()+1m`; `tap.login(buyer)` reserve 10m; `tap.login(other_user)` INSERT bid; as postgres set `ends_at=now()-1s`; `tap.login_service()` `finalize_auction(E)`; assert `status<>'reserved' OR winner_user_id IS NULL` (finalize must not create dual eligibility) → today `reserved` + winner. vitest `cpi`: same row; request as winner `C` mode auction → assert 409 → today 200; request as `B` mode buy_now → assert 409 → today 200. Note the DB side is *silent*: whichever settles second early-returns (`0590:98`, `129`) — no exception to assert on.
- (f) Late capture after inventory change — webhook vm: `payment_intent.succeeded` for `pi_B`, `payments` update returns `{error:{code:'23505'}}` (partial unique index) or listing sold to another; assert 200 + orphan record + refund call → today 500 + retry loop, row stuck `pending`, money captured. Optional pgTAP: promote two rows to `succeeded` on one listing → `throws_ok 23505` (passes today; documents the collision).

## 4. Package 1 — minimal edits

1. New migration (DDL only, no state-machine change):
   - `reserve_buy_now`: `v_minutes := LEAST(GREATEST(COALESCE(p_minutes,10),1),10)`; if already held by caller & unexpired → keep existing `reserved_until` (or allow ≤1 extension); reject if caller holds ≥1 other live reservation (`count(*) … reserved_by=caller AND status='reserved' AND reserved_until>now()`); keep signature `(uuid,uuid,integer)`. Compat: mobile/web pass 10 → no change; new error strings surface via `ListingDetailScreen:784` generic alert.
   - `mark_listing_sold` / `complete_auction_payment`: add `IF NOT EXISTS (payments WHERE listing_id=p AND buyer_id=v_caller_id AND status='succeeded') THEN RAISE 'No verified payment…'`. Compat: mobile calls `confirm-payment` first (Stripe-verified write `confirm-payment:216`), so the row is `succeeded` on the happy path; in the `processing`/fetch-failure race the old build shows its existing "Payment Received… contact support" alert (`CheckoutNative:290`) and the webhook (promote at `270` then RPC at `409`) settles it — same degraded path that exists today for `ensure_transfer_exists`. Update `110` #18 fixture. **Contract needed**: what `mark_listing_sold` does when payment is verified but reservation expired/held by another (today: releases + raises, buyer stays charged).
   - Optional: `complete_auction_payment` refuse when `status='reserved'` by another with live `reserved_until`; `finalize_auction`/`auto_finalize` skip or clear reservations — **contract needed** (precedence of Buy-Now hold vs auction win).
2. `cpi`: (i) `:269` add `reserved_by, reserved_until, ends_at`; (ii) `:302` require `reserved_by===buyerId && new Date(reserved_until)>now` and `auction_status==='active'`; use messages matching `src/lib/payments.ts:33-44` regexes (`/reservation expired/`, `/already reserved/`, `/not reserved for purchase/`) so field builds render them instead of `SAFE_PAYMENT_ERROR`; (iii) `:317` auction branch also reject `status==='sold'` and live foreign reservation; (iv) before the 400 returns, look up this buyer's `pending` row and cancel its PI + set `failed` (explicit expired/superseded handling; reuse existing cancel code `:596`); (v) add `metadata[reserved_until]`/`metadata[reservation_owner]` to `piBody:503` for forensics; (vi) keep idempotency key shape. Compat: request/response shapes unchanged for all builds and web.
3. `stripe-webhook`: add `payment_intent.canceled` → `release_reservation` (mirror `:537`). Terminal-RPC-error classification (orphaned capture → 200 + refund/record instead of 3-day 500 loop) is **settlement contract** — flag, do not implement in Package 1.
4. Clients (no compat risk, server enforces): `CheckoutNative` countdown from `reserved_until` + `release_reservation` on back; web same; web should send `expected_total_cents` (`checkout-actions.ts:29` passes none).
5. Tests: `120_reservation_lifecycle.sql` (b, d, e-DB), `tests/checkout-intent.test.ts` + `tests/webhook-orphan.test.ts` (a, c, e-edge, f).

## 5. Audit corrections
- F04 "renew indefinitely": bounded by `ends_at` (`0590:163`); one large `p_minutes` already reaches that bound.
- F01 "multiple pending intents": per (listing, buyer, mode) there IS reuse (`cpi:395`); multiplicity is cross-buyer/cross-mode.
- F01 "auction branch ignores status": only the in-flight `reserved` case slips through; sold/cancelled are rejected via `auction_status`.
- F03 blast radius is inventory + seller lock-out (cannot cancel, bids blocked), not unpaid transfers — `061`, `confirm-payment:249`, and the webhook claim all gate transfer creation on `succeeded`.
- Note for lead: the second buyer's capture in (a)/(f) leaves a `pending` row + 500-looping webhook; there is no refund path — not "DB uniqueness stops the write" but "uniqueness converts the collision into an unrecoverable retry loop."
