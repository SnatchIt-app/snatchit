# Session handoff — C (consumer frontend) · 2026-09-24

## 1 · Completed work, with exact commits and the build

`v3/midnight-app`, pushed to `origin`. Head **`9c6c9bf4e6f9c201efe1fe760d2bf6ae12175d7f`**.

| Commit | What it closed |
|---|---|
| `016087ea` | A's checkout ruling implemented (display-only; server figures, "Preparing your total", nothing during a price change) |
| `212783f2` | B's rendered defects A-1…A-5 · R-5 through the authoritative action resolver (`offersBid`) · F-28 |
| `6c7fc18b` | A-1 neutral hairlines · the five transfer-state blocks extracted into one shared module · the sandbox-only synthetic gallery |
| `2619b9e1` | Gallery hardening: date lines suppress only themselves · side-effect-free static imports pinned · a **tested** Settings entry (SG1/SG2) |
| **`4d1e4b3d`** | **The appearance migration, complete.** 0 consumer files still read Midnight-only colour tokens (was 53 files / 546 refs). Root layout no longer partial |
| **`9c6c9bf4`** | **B's measured Daylight failures closed:** F-31, F-32, F-33, F-34, N-1, N-4, N-5 |

**Build: `65cb7633-0eca-4314-b135-fd9c90db8214` — FINISHED.** iOS `preview`, internal distribution, build
number 23, from commit `9c6c9bf4`, sandbox project `ofaidukbieeekqaboscm`, SDK 54.0.0. Artifact:
`https://expo.dev/artifacts/eas/3KrGF6h1hsigS7LfQhYfiIYCZu5cpCIH73SHmh1itw8.ipa`. Credentials were already on
EAS, including a provisioning profile carrying the owner's iPhone UDID `00008130-000E59801198001C`. The one
authorised build; no second build will be created. **Not installed and not launched** — the owner asked that
the phone session not start, so there is still no device evidence of any kind.

**One commit lands after the build and is not in it:** `24b021a3` — the F-32 sweep had also caught
`<Spinner>`, whose arc is graphical (3:1 bar) rather than text, so the loading indicator keeps the brand red in
thirteen files; and `AP22` pins the root layout's response (nav theme, status bar, splash inside the provider
gate). The only rendered difference in build 23 is the spinner arc in Light: `#D31212` there, `#FF1A1A` on the
branch. Both clear 3:1, no second build is authorised, and the build was not recut. Branch head is now
`24b021a3`; the build's commit remains `9c6c9bf4`. Gates re-run at `24b021a3`: **2676/2676**, tsc 0, lint 0/29.

Docs: `V3_COVERAGE_MATRIX.md` is the one current coverage list (`14102d7f`); the plan's §0 and §5 are
rewritten and the commit is pinned (`a6826d11`).

## 2 · Verification results, and the failures that are NOT erased

At `9c6c9bf4`, run alone at load average 4.16 with no peer vitest process:

| Gate | Result |
|---|---|
| `npx vitest run` | **144 files, 2675/2675** |
| `npx tsc --noEmit` | **0 errors** |
| `npm run lint` | **0 errors, 29 warnings** — the baseline; one new warning appeared (a dead `v2` import in the root layout) and was removed |
| Negative controls | **three mutants, each killed by exactly the predicted gate** (AM1 / RD8 / AM4); clean baseline, digest-verified restore |
| Gated payment surface | `git diff --stat e079fcc1..HEAD` over `payments.ts`, `checkout/{setupDecision,payControl,holdState}.ts`, `auth/signOut.ts` → **`signOut.ts +5` only** |

**Preserved failures** (a later passing run does not erase them):

1. **104 tests failed across 13 files** on the migrated tree at 01:17. **Not contention** — load 4.2, zero
   peer vitest. Cause: migrated components call `useTheme()`, and those 13 suites mock `react-native`
   without `useColorScheme`, which vitest rejects explicitly. Fixed by mocking the appearance boundary to
   Midnight — whose values ARE the v2 tokens, so nothing they pin moved — and retiring two source pins with
   the change they defended.
2. **35 timeouts across 16 untouched edge suites**, then **1**, then 0, earlier in the session. The only
   variable was machine load average (58 → 30 → 5) from peer sessions; `pgrep` showed no peer vitest, so
   the busy-check was not the signal. Those runs are void, not evidence about the code.
3. **A mutant harness that reported three surviving mutants** with a clean baseline. The harness was wrong,
   not the gates: its failure-name regex never matched vitest's verbose lines. Fixed by parsing per line
   and cross-checking the exit code; all three then died as predicted.
4. **I reverted the entire working tree mid-session** with a `git stash` probe I put inside a lint command.
   Recovered in full from `stash@{0}`; the inventory, the three fixes and the gates were re-verified after
   recovery, and the numbers above are all post-recovery.

**What the evidence is not:** every contrast figure here is harness-rendered — real components mounted under
each palette, translucent layers composited, WCAG computed on the styles they emit. **Nothing is
device-verified.** D-1…D-9 need the build that now exists.

## 2b · Work that landed AFTER the handoff above was written

Branch head is now **`404bce38`**. Build 23 is unchanged at `9c6c9bf4`.

| Commit | What it closed |
|---|---|
| `d981727e` | The three confirmed defects from the carried-forward findings, judged in rendered context: the past ticket card dimmed its own text (4.44:1 in Light); my own risk-banner tints were ungraded literals (edges 1.32–1.75:1 Light, fills 1.03–1.07:1 apart); nine `text.faint` sites carried task-relevant strings. N-2, N-3a, the checkout countdown, the switch and the `critical_risk`/`listing_blocked` collapse were measured and left alone, with reasons |
| `2ffb10a8` | FeedRow, SellerListingCard and DiscoveryCard stopped dimming their own text · the auth brand mark is tinted (it was a white monogram on Daylight's white canvas) · the push-challenge code field got a persistent label · signup's password rule moved to a persistent helper · the sell pickers and the notifications switch thumb reconciled · a transient eligibility failure stopped claiming "We've noticed some recent issues" and still fails open · "Contact support" got a route · both risk alerts web-guarded |
| `ca27d282` | A dispute decision is not a confirmation: the buyer and seller copy branch on `buyer_confirmed_at`. Includes a regression this introduced and C7 caught — the optimistic update now carries the timestamp with the status |
| `aee15697` | The same claim on three more surfaces: the badge ("Resolved", neutral, not a green "Received"), the Bids board, and the payout line, which now says only what the payout fields state |
| `404bce38` | The held-payout fallback stopped promising a notification the phone cannot keep |

**Three gated authoritative-state reads** were added across those commits — `buyer_confirmed_at` in
the receive, send and Bids transfer selects — and **all three are A-reviewed PASS** (`ca27d282`,
`aee15697`). The gated payment surface against `e079fcc1` is still `signOut.ts +5` only. A also
verified independently that DR11 fails on its parent and passes at `404bce38`.

Gates at `404bce38`, run alone at load average 4.79 with no peer vitest: **149 files, 2704/2704** ·
`tsc` 0 · lint 0 errors / 29 warnings (baseline).

## 3 · Remaining tasks

- **B's group-by-group review** of the migration and fix commits (peer review, not an owner gate).
- **Design/copy decisions B raised or measured, none blocking the build:** N-2 `status.warning` as a 1px
  border (bright amber on Midnight, dark brown on Daylight — passes contrast, looks materially different) ·
  N-3's remaining aesthetic case (dimming an image washes out rather than recedes on white; every case where
  it touched TEXT is fixed) · input focus signalled by hue alone, mitigated by the caret and keyboard ·
  the duplicated "Cancelled" wording under the no-redundancy direction · Dark's over-artwork secondary
  moved 0.70 → 0.78 alpha with the onArt fix · the ErrorBoundary crash plate, which genuinely cannot read
  the palette because it wraps the provider.
- **The device pass** on the installed build, per `V3_PHONE_TEST_CHECKLIST.md`, in small batches.

## 4 · Owner decisions — CURRENT STATE (2026-09-24, corrected)

**Everything in the previous revision of this section is superseded. Two errors are corrected here.**

1. **~~Approve A's sheet~~ — DONE.** The owner approved **D1–D6 at revision `7dae4815`**, expired and held
   included, plus D's read-only witness checks. Fixture writes wait for four things: the build installed,
   the owner available, an explicit **"go"**, and A's preflight passing. A's exact sequence is followed, with
   **no extra bid, Buy Now, Pay tap or account switch** outside it.
2. **~~Decide the expired / held data path~~ — DONE, and it was never a separate decision.** It is D5 and D6
   on A's sheet, which the approval covers. The previous revision listed it twice — once as its own owner
   ask and once inside the sheet approval — which made one decision look like two. Expired and held now have
   an approved path to **real rows** (D5 on transfer `19be875b`, D6 on `83b83858`), so they are no longer
   gallery-only. The gallery remains the rendering evidence; PT-33/PT-34 supply the data-path evidence.
3. **~~Recut the preview build~~ — DONE.** The owner authorised one iOS sandbox preview build from
   `v3/midnight-app` at **`404bce38`**, which is build **24**. Build 23 (`9c6c9bf4`) is superseded as the
   test artifact and its delta list no longer applies to the session. The authorised pin is not moved.

**The claim this section used to make, and why it was wrong.** It said "nothing on the device pass can start
without" the sheet approval. That was false and it mattered, because it made the whole pass look blocked on
the owner. **Most of the pass needs no fixture write at all:** the appearance and startup checks, every
reachable surface on existing rows, large text, narrow width, the dock, the avatar, and the synthetic
gallery. In the checklist's numbering that is **PT-01 … PT-29**, and the only sandbox effects in that range
are the two push-token rows that signing in inherently registers. Only **PT-30 … PT-35** create records, and
each names the permission line that governs it.

## 5 · Standing constraints for the session

- **Evidence stays tied to the installed build.** Anything measured in the harness or read from source is
  labelled as such and never recorded as device evidence.
- **The authorised pin does not move.** If a further defect requires another build, that is stated to the
  owner explicitly, with the reason, before anything is built.
- **No production build and no App Store submission** is authorised by any of this.

Not submission-ready, and not claimed as such: the device pass has not run.
