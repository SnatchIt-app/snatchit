# Phase 2 — Ratification Record (C26–C78 · O6–O11 · O-1…O-5 · D1–D13 · RET-1…RET-5)

**Ratified 2026-08-24 by the pre-implementation consolidation session; extended 2026-08-27 by the owner-ruling integration pass (O-1 … O-5, D4–D8, C51, C52); extended again 2026-08-27 by the four-pass remediation reconciliation (C53–C72, D9–D12, RET-1…RET-5, O9, O10); extended again 2026-08-28 by the reviewer-conditions pass (C73–C78, O11, D13).**
**Authority:** `docs/architecture/_superseded/PHASE_2_FINAL_ARCHITECTURE_AUDIT.md` (§4 correction table) · `docs/architecture/_governance/ARCHITECTURAL_RISK_REGISTER.md` (R1–R36) · `docs/architecture/_governance/IMPLEMENTATION_READINESS_SCORE.md` (gate composition) · `docs/architecture/_governance/CTO_DECISION_MEMO.md` (Gate P/M/L assignments).
**Effect:** the two constitution documents — `docs/architecture/SNATCH_IT_DOMAIN_ARCHITECTURE.md` (DA) and `docs/architecture/SNATCH_IT_CANONICAL_DATA_MODEL.md` (CDM) — were **consolidated so the body reads correctly without precedence rules**: every correction below is folded into the body text (tagged inline), stale contradictory prose was rewritten or deleted, and each document carries a Correction Index appendix. The 2026-08-27 owner-ruling pass extended the same treatment to O-1 … O-5 and D4–D8. **Every row below is in one of six states, five of which are terminal**: `OPEN-GATED` is not, and **six** rows (C50/O6, C51/O7, C52/O8, **C56/O9, C72/O10**, and **C78/O11 — added 2026-08-28**) sit in it. The 2026-08-24 line "no correction is pending" was true then and is not restated.

**Statuses:**
- **RATIFIED** — constitution text final; the correction is implemented as part of the Gate-P path (before the first native ticket).
- **RATIFIED-MODELED-ONLY(GATE-M)** — constitution reserves the extension point as binding text; built before native resale + instant payout; NOT in the Phase-2 foundation.
- **RATIFIED-MODELED-ONLY(GATE-L)** — same, gated at pre-legal-scale (international / erasure claims / enterprise).
- **DOC-FIX-APPLIED** — a documentation-consistency defect, corrected in the constitutions by this session.
- **OPEN-GATED(Ox)** — the constitution states the admissible forms; the choice between them is the named open decision, which blocks its gate.
- **RETRACTED** — **added 2026-08-27, terminal.** Ratified or spec-canon text **deleted as false** — not superseded, not narrowed, not re-gated. The claim survives in no form; the reason is recorded with the row; the deletion is *also* carried in the correction index of the file that held it, so it cannot be cited from an older copy. **`RET-1`…`RET-5` only.** See the retraction table in the remediation-pass section for why this is not `DOC-FIX-APPLIED`.

**Gates:** **P** = pre-native-issuance · **M** = pre-money-rail (native resale + instant payout) · **L** = pre-legal-scale · **—** = documentation.

> **Two `O` namespaces exist and they are NOT the same series. Read the hyphen.**
> **`O1` … `O8` (unhyphenated)** are the architecture **open questions** tracked in `docs/architecture/SNATCH_IT_DOMAIN_ARCHITECTURE.md` §0.4 and the risk register — e.g. `O3` = resale-policy snapshot drift, `O4` = per-event identity-verification strength, `O6` = cross-region native-resale form.
> **`O-1` … `O-5` (hyphenated)** are the **owner rulings** ratified 2026-08-27 — e.g. `O-3` = payout visibility/requests, `O-4` = door-manifest authority.
> `O3` ≠ `O-3` and `O4` ≠ `O-4`. Neither series is renumbered; the disambiguation is the fix. Recorded as row **D4**.

| ID | Correction (one line) | Status | Final constitution section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C26** | Ownership-log idempotency key = `UNIQUE(cause, cause_ref, ticket_id)` + per-`market_sale` terminal machine (`pending → completed XOR compensated` under the sale-row lock), restoring the double-transfer-impossible proof for one-cause→N-ticket flows and cross-object re-void | RATIFIED | DA §0.5 (C3 row), §9.4 · CDM §1.1 Ownership Log, §1.4 Market Sale, §3, §15 C26 | P | YES |
| **C27** | `remaining` single truth: the locked counter is authoritative operational truth; the movement ledger is the audit stream; a reconciliation job asserts equality; `credential_version` pinned to the ownership log | RATIFIED | DA §1.3, §4.1/§4.2, §2 (Part 4 + invariants), §5 ¶1.5 · CDM §1.1 Issued Credential, §1.3 Inventory, §15 C27 | P | YES |
| **C28** | SSCAS genuinely closed (members 10–15 added: event-cancellation cascade, dispute-resolution reversal, C25 auto-compensation, auction deposit-release, group-buy claim, wallet checkout); global lock order places every locked class incl. ascending-batch-id and money-plane rows; §12 sharding prose reconciled to the enumerated set | RATIFIED | DA §6.2, §3.10 · CDM §2, §12, §15 C12 (as completed) + C28 | P | YES |
| **C29** | First-class Reserve/Clawback object + payout-timing policy gating instant payout (the true form of old O1) | RATIFIED-MODELED-ONLY(GATE-M) | DA §10.5, §3.9, §1.5 · CDM §1.1 money-plane block, §15 C29 | M | NO |
| **C30** | Fan-side chargeback/clawback liability representable (receivable or Wallet negative-balance; physical form fixed by the Gate-M amendment) | RATIFIED-MODELED-ONLY(GATE-M) | DA §10.5, §1.5 · CDM §1.1 money-plane block, §15 C30 | M | NO |
| **C31** | Additive double-entry money-ledger schema beside the frozen Stripe core (home for C29/C30; balances splits/royalty/rounding structurally; custody log stays separate) | RATIFIED-MODELED-ONLY(GATE-M) | DA §10.5, §3.8, §1.5 · CDM §1.1 money-plane block, §15 C31 | M | NO |
| **C32** | First-class multi-currency at the frozen money-in boundary: currency on every money object (C13), FX capture-vs-payout timing, per-country payout currency, named rounding bearer | RATIFIED-MODELED-ONLY(GATE-L) | DA §10.5 · CDM §15 C32 (+C13) | L | NO |
| **C33** | Signing-key lifecycle: per-event default scope, KMS/HSM custody, audited rotation (one active signer per scope), compromise runbook, signer HA/throughput, public-key-only door distribution | RATIFIED | DA §10.4, §0.5 (C1 row) · CDM §1.1 Issued Credential, §5, §15 C33 | P | YES |
| **C34** | Provable erasure: per-identity DEK lifecycle incl. backup destruction, PII-sink inventory + purge, retained-graph re-identification mitigation, 7-yr financial-retention reconciliation (replaces bare "crypto-shred solves GDPR") | RATIFIED-MODELED-ONLY(GATE-L) | DA §8.7, §2 matrix (user row) · CDM §4, §15 C34 (+C15) | L (spec at P) | NO |
| **C35** | The kernel authorizes the buyer principal itself at the market→kernel seam; a market-supplied buyer id is never trusted; buyer re-verified against the payment | RATIFIED | DA §9.4, §5.2.3, §0.5 (C8 row), §2 · CDM §1.4 Market Sale, §15 C35 | P | YES |
| **C36** | Scope-qualified roles are structural: three disjoint per-plane label sets (org/venue/platform enums share no label), not a lint convention | RATIFIED | DA §0.4 A9, §7.1, §4 challenge resolution · CDM §1.3 Staff Role, §8, §15 C36 | P | YES |
| **C37** | Online door performs a live authoritative per-scan kernel read at the decision point; "dispute-free by construction" claim dropped (offline honestly shrunk-not-closed) | RATIFIED-MODELED-ONLY(GATE-L) — claim correction applied in the constitutions now | DA §10.4, §10.1, §16.4 · CDM §1.1, §6 Door Scanner, §12 caching, §15 C37 | L | NO |
| **C38** | Identity-merge grant reconciliation defined (per-class union rules, escalation impossible, conflicts fail closed narrower) + the merge decision itself dual-controlled | RATIFIED-MODELED-ONLY(GATE-L) | DA §8.7, §2 matrix · CDM §4, §15 C38 (+C15) | L | NO |
| **C39** | Comp/guest-list issuance above a per-staff threshold requires step-up + C9 live-table grant re-check | RATIFIED-MODELED-ONLY(GATE-L) | DA §7.5, §5 ¶1.6 · CDM §1.3 gated-extensions note, §15 C39 | L | NO |
| **C40** | `validation_callback` egress restricted to a static, platform-controlled allowlist (never provider/venue-supplied); CI asserts the adapter's kernel REVOKE | RATIFIED-MODELED-ONLY(GATE-L) | DA §13.2 · CDM §9, §15 C40 | L | NO |
| **C41** | MVP is no-re-entry GA: terminal `scanned` stands; re-entry is a named future change whose reserved extension point is the scan `direction` (`in`/`out`) attribute | RATIFIED (the decision + the hedge column; re-entry itself deliberately deferred) | DA §3.1, §1.3 scan row, §5 ¶1.8 · CDM §1.1 Ticket Atom, §1.3 Scan Event, §15 C41 | P (decision) | YES (decision + hedge) |
| **C42** | Optional-nullable seat/unit-row layer reserved now (unit-rows ≡ seats, reconciled with C4/C22); H6 "non-additive" holds only if the hedge is skipped; seat-map UX remains out of scope | RATIFIED (the hedge; seating build deferred) | DA §0.5 (C11 row), §5 ¶1.4 · CDM §1.1, §1.3, §13.5, §14, §15 C42 | P | YES (hedge) |
| **C43** | p2p `locked` overlay hard-TTL auto-unlocks (physical `expired` terminal; `requested` ≡ `initiated`; no `failed`); cancel-to-self exempt from the C6 freeze; freeze narrowed per-open-manifest-ticket | RATIFIED-MODELED-ONLY(GATE-M) — the physical state set is already spec-canon (schema §4.5 / migration 088 — package J, market native rail) | DA §3.7, §0.5 (C6 row), §10.6 · CDM §1.4 P2P Transfer, §15 C43 | M | NO |
| **C44** | Virtual-queue / waiting-room + bot-defense primitive before competitive on-sales | RATIFIED-MODELED-ONLY(GATE-L) | DA §5 ¶1.5 · CDM §1.3 gated-extensions note, §15 C44 | L | NO |
| **C45** | Table minimum-spend balance + at-the-room settlement modeled — until then that revenue is explicitly conceded off-platform (no bottle-service system-of-record claim) | RATIFIED-MODELED-ONLY(GATE-L) — pulled to the 2A-tables gate if bottle-service scope ships | DA §5 ¶1.4 · CDM §1.3 gated-extensions note, §15 C45 | L (2A-tables) | NO |
| **C46** | Fire-code occupancy distinct from sold capacity; cash box-office + auto-gratuity/tip-out settlement causes; door refunds require an authenticated staff principal (never a door PIN) | RATIFIED-MODELED-ONLY(GATE-L) | DA §5 ¶1.3/¶1.6/¶1.8 · CDM §1.3 (gated-extensions + Staff Role/Door PIN), §15 C46 | L (2B) | NO |
| **C47** | DR is a designed system: stated RPO/RTO, PITR/standby, drilled restores, snapshot rebuild budgets; "replayable" ≠ "recoverable"; no "recoverable" claim before it exists | RATIFIED-MODELED-ONLY(GATE-L) | DA Principle 17 · CDM §5, §15 C47 | L | NO |
| **C48** | Projection-rebuild retention floors: outbox compaction respects canonical-input retention; ephemeral-input projections marked non-rebuildable if not covered | RATIFIED-MODELED-ONLY(GATE-L) | DA §6.3 · CDM §7, §15 C48 (+C21) | L | NO |
| **C49** | Outbox hardening: poison-quarantine, partitioned/multi-drainer (no head-of-line blocking), specified region hand-off protocol (never an implicit 2PC) | RATIFIED-MODELED-ONLY(GATE-L) | DA §6.3 · CDM §15 C12 (envelope, as amended) + C49 | L | NO |
| **C50** | Cross-region native resale: the two admissible forms are a saga/escrow over the `paid_pending_transfer` window, or explicit intra-region-only scoping — the choice is O6; Miami single-region builds neither | OPEN-GATED(O6) — the forms are ratified constitution text; the choice blocks Gate M/multi-region | DA §0.5 (C8 row), §6.2, §0.4 open questions · CDM §12, §15 C50 | M/region | NO |
| **O6** | The open decision itself: saga/escrow vs intra-region-only for cross-region native resale — a joint commercial + technical call, made before multi-region resale | OPEN-GATED(O6) | DA §0.4 open questions, §6.2 · CDM §15 C50 | M/region | NO |
| **D1** | "The one sanctioned cross-aggregate transaction" phrasing (CDM §2 line 262 and kin) rewritten to SSCAS-member language; no one-seam/single-exception phrasing survives as current design in either constitution | DOC-FIX-APPLIED | CDM §2 (Market-Sale row: "an SSCAS member (#2)"), §0.10/§2 preamble, §12, §13.2 · DA swept clean | — | YES (applied) |
| **D2** | DA §3.1 ticket state machine redrawn with NO `refunded` terminal: money reversal = `voided` with cause `refund_void`; transition and illegal-move tables corrected; order/payment/external-transfer `refunded` states (different objects) untouched; §0 precedence note reduced to a historical note | DOC-FIX-APPLIED | DA §3.1 (diagram + both tables), §0.4 reconciliation note, §1-appendix challenge 4 | — | YES (applied) |
| **D3** | One canonical cause-code registry — `issue · primary_sale · comp · door_sale · p2p_transfer · market_sale · auction_sale · admin_action · refund_void · import · promoter_commission · settlement · chargeback` — identical verbatim in DA §5.2.1 and CDM §11; all other cause lists tagged as subsets | DOC-FIX-APPLIED | DA §5.2.1, §5.6, §9.4 · CDM §11 | — | YES (applied) |

| **E-1** | SSCAS enumeration alignment: CDM C12's fifteen-member list is canonical; SPEC_FOUNDATION §5 marked superseded-provenance; RPC §14.1/§14.2/§14.3 renumbered to C12 (adds #9 dispute-freeze, #11 dispute-resolution reversal, non-MVP #14/#15) | DOC-FIX-APPLIED (consistency review, 2026-08-25) | PHASE_2_SPEC_FOUNDATION §5 · RPC §14 · CDM §15 C12 | — | n/a (doc alignment) |
| **OBS-1** | Frozen-boundary clarification: **no column is ever added to `public.payments`** — native order/sale resolution = PI `metadata` + `kernel.payment_native` join | DOC-FIX-APPLIED (security review, 2026-08-25) | Edge spec §9.1 | — | n/a (doc clarification) |
| **A-2** | Administrative: PR #3 rebase-merge SHA mapping (tag `phase2-architecture-v1` anchors the original freeze SHAs; `main` carries byte-identical rebased equivalents cf7d6b9/e24989c/f66bf1d) + relocation of the frozen documents to `docs/architecture/` with path-reference rewrites (repository-hygiene PR) | DOC-FIX-APPLIED (administrative, 2026-08-25) | ARCHITECTURE_FREEZE.md Amendment A-2 | — | n/a (administrative) |

---

## Owner-ruling amendments — ratified 2026-08-27

**Authority:** owner rulings **O-1** (refund authority) · **O-2** (canonical role model) · **O-3** (payout visibility / requests) · **O-4** (door-manifest authority) · **O-5** (`door_open_at` lifecycle), ratified by the owner and designed to — not relitigated — in `docs/architecture/PHASE_2_MONEY_AUTHORITY_SPEC.md`, `docs/architecture/PHASE_2_ROLE_MODEL_SPEC.md`, and `docs/architecture/PHASE_2_DOOR_LIFECYCLE_SPEC.md`. **This record is the owner approval Rule 1 requires**; the rows below are what authorizes the corresponding edits to DA and CDM. Rows D4–D8 are documentation corrections applied in the same pass; C51 and C52 are constitution-vs-implementation contradictions recorded as open, deliberately not resolved here.

| ID | Correction (one line) | Status | Final constitution section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **O-1** | **Refund authority.** `org_owner` and `org_finance` may **initiate/request** refunds for their own organization's orders — but the mechanism is a **request door** (`kernel.request_order_refund`), never direct execution of the money writer: the RLS §7.10 denial of org roles is corrected toward the DA reading, while `kernel.refund_primary_order` narrows to definer + platform. The undefined "> micro" row is replaced by **three tiers** decided server-side from `catalog.platform_config` — auto-execute ≤ threshold · in-org dual control with `approver ≠ requester` · platform review beyond the org ceiling. `org_admin` holds **no** refund authority (read, request or approve). DA §7.2's "Inherits: Org Admin, Org Finance" is **deleted as a money mechanism** (`kernel.org_member.role` is single-valued under C36, so no `org_owner` row can ever satisfy `has_org_role([org_finance])`) — the *authority* moves to the `org_owner` label explicitly; the *inheritance* is removed. R7 money-single-path preserved: no new object writes a money row | RATIFIED | DA §7.2 (Org Owner "Inherits", Org Finance prose), §7.4 (SoD note), §7.5 (refund step-up row), §7.6 (three refund tiers + approve-request row + view-refund-ledger row + `ᔆ` legend) · CDM §1.1 Refund, §15 O-1 | P | YES |
| **O-2** | **Canonical role model.** O-2's eight role concepts are **display names**; C36 governs **stored enum labels** — the two-namespace reading DA §7.1 already states is confirmed correct and made unmissable rather than reversed. The canonical stored set is **fifteen plane-prefixed labels** in three disjoint enums: org (6) `org_owner · org_admin · org_finance · org_marketing · org_promoter_manager · org_member`; venue (6) `venue_manager · venue_finance · venue_box_office · venue_marketing · venue_promoter_manager · venue_scanner`; platform (3) `platform_admin · platform_support · platform_risk`. `venue_door` → **`venue_scanner`** (rename); **`venue_promoter` removed** — a promoter is a `promoter_link` row-ownership relationship, never a staff-role label; Ambassador stays a derived predicate and enters no enum. Standing rule **RM-1**: every role label MUST begin with its plane token | RATIFIED | DA §7.1, §7.2 (catalogue + Marketing "Cannot"), §7.3, §7.6 (column set), §1-object catalog, Principals table, ER diagram, §1.8 · CDM §1.3 Staff Role, §8, §15 C36 + O-2 | P | YES |
| **O-3** | **Payout visibility & requests.** `org_owner` **reads** the org payout ledger and **requests** payouts (RLS §7.9's SELECT denial was the wrong text; RPC §10.3 was already right); `org_finance` likewise. `org_admin` is DENY across the whole money plane. `venue_finance` is narrowed to **settlement-caused** payouts for its own venue — `kernel.payout` has no `venue_id`, so an unqualified "own-venue payouts" was never expressible. **Payout destination change is `org_owner` ONLY** (`org_finance` excluded) and is **not** the same authority as a payout request: it carries strictly stronger controls. **This ruling collapses SoD-1 by construction** — before it, `org_owner`-only destination change satisfied separation of duties automatically because owners could not disburse; O-3 puts both halves of the named fraud primitive ("redirect the bank account, then release funds to it") in one identity. Ratified **with** its compensating control, not as an unqualified grant: a **permanent** requester-vs-setter identity split (`kernel.request_org_payout` rejects `auth.uid() = payout_destination_set_by`, for that destination, forever — explicitly **not** a cool-down, which an attacker simply waits out), **destination probation** (first payout after a change is created `held`, released only by platform risk/admin), and **out-of-band notification** to every `org_owner`/`org_finance` including the actor. The existing cool-down is retained but demoted to a detection window, the weakest control in the set | RATIFIED | DA §7.2 (Org Finance prose), §7.4 (SoD-1 ratified trade-off + named mitigation), §7.5 (payout/bank-change step-up row), §7.6 (payout rows split; view-payout-ledger row; close-settlement row) · CDM §1.1 Payout, §15 O-3 | P | YES |
| **O-4** | **Door-manifest authority.** Opening and closing the door manifest, moving the door-freeze time (`catalog.event_session.door_open_at`), and changing event security configuration are authorized to **`org_owner`, `org_admin`, `venue_manager`** only, expressed inside the RPC predicate (org→venue inheritance, never by widening venue RLS). The scanner principal — the `venue_scanner` staff grant **or** the loginless device door session — may sync an already-open manifest, scan and admit, and may **never** create, move or clear the boundary; it holds no write path to `catalog.event_session`. `venue_box_office` receives no manifest administration, and the consequence is stated rather than left implicit: a person granted `venue_manager` for box-office selling thereby also gains manifest-open authority until per-capability scoping ships. Disabling a transfer freeze is `platform_admin` under step-up. **Admission is never gated on manifest state** — the manifest gates offline scanning only; gating admission on it would fail closed against paying fans at the door | RATIFIED | DA §7.2 (Door row), §7.3, §7.6 (scan/validate + door-lifecycle rows), §1.8 · CDM §1.3 Staff Role/Door PIN, §15 O-4 | P | YES |
| **O-5** | **`door_open_at` lifecycle.** The column is **written by nothing** in the frozen contract set, which makes the C6/C23/C43 transfer freeze — and the stale-pass safety property the design claims — false. Ratified form: `door_open_at` becomes the **cached monotone head of an append-only door-episode ledger** (`MIN(opened_at)` over the session's manifest opens), the same head-of-ledger pattern already ratified for `current_owner_id` and `credential_version` under C27. It is written only by the authorized open RPC, never by a client, **never cleared by close**, and **never moved backwards** — monotonicity becomes arithmetic, not a rule someone must remember; a re-open starts a new episode without moving the boundary. The effective freeze boundary is **total and fail-closed**: `LEAST(door_open_at, COALESCE(doors_at, starts_at) + configured offset)`, so a NULL `door_open_at` can no longer mean "never frozen". Override is explicit, elevated (`platform_admin`), TTL-bounded, reason-coded, audited, and never moves the boundary | RATIFIED | DA §3.7, §5 ¶1.8, §7.6 (door-lifecycle rows), §10.6 · CDM §1.2 Event Session, §1.3 Scan Event, §15 O-5 | P | YES |
| **D4** | **The two `O` namespaces are disambiguated, not renumbered.** `O1`…`O8` (unhyphenated) are architecture **open questions** (DA §0.4 + risk register); `O-1`…`O-5` (hyphenated) are **owner rulings**. `O3` (resale-policy snapshot drift) ≠ `O-3` (payout visibility); `O4` (per-event identity-verification strength) ≠ `O-4` (door-manifest authority). Both series keep their numbers; a standing note in this record, in DA §0.4 and in ARCHITECTURE_FREEZE.md states the distinction | DOC-FIX-APPLIED | this record (Statuses block) · DA §0.4 · ARCHITECTURE_FREEZE.md | — | n/a (doc clarification) |
| **D5** | **The pre-C36 bare-label role lists are purged from the C36 constitution itself.** Four sites in DA carried `owner/manager/finance/marketing/door/promoter_manager` — the exact strings C36 was ratified to abolish — in the object catalog, the Principals table, the mermaid ER diagram and §1.8. All four now carry the six plane-prefixed venue labels. §7.2's catalogue gains the **stored label beside every display name** and a header restating DA §7.1's rule (catalogue entries are display names; stored labels are the disjoint prefixed sets) — the existing reading is correct and is reinforced, never reversed. §7.2's Marketing "Cannot" row is corrected per O-2. CDM §1.3's Staff Role bullet and §15's C36 row carry the same finalization | DOC-FIX-APPLIED | DA §1-object catalog, Principals table, ER diagram, §1.8, §7.1, §7.2 · CDM §1.3 Staff Role, §15 C36 | — | YES (applied) |
| **D6** | **DA §7.6 collision resolved — one matrix, not two.** `PHASE_2_MONEY_AUTHORITY_SPEC.md` §12 items 5/6 supply a corrected 15-column §7.6 money block; `PHASE_2_ROLE_MODEL_SPEC.md` edit `D-6` instead **deletes §7.6 entirely** and replaces it with a pointer to ROLE_MODEL §5. Both cannot be applied. **Applied: the money spec's corrected matrix, in place, in the constitution. Rejected: ROLE_MODEL `D-6`'s delete-and-point.** Reason: ROLE_MODEL §5's own money block is explicitly *transcribed from the frozen corpus, not decided* (§15), carries two `⚠` undecided cells (B1 payout destination, B10 settlement close) and defers to `design/o1-o3-money-authority` — so pointing the constitution at it would delete the only ratified statement of O-1/O-3 money authority and re-open B1 on the day O-3 closed it. §7.6 keeps its matrix and gains a normative precedence note: stored labels are ROLE_MODEL §3; ROLE_MODEL §5 is the finer-grained 20-principal capability matrix and governs **non-money** capability detail; §7.6's **money** rows govern over ROLE_MODEL §5's money block | DOC-FIX-APPLIED | DA §7.6 (matrix + precedence note + `ᔆ` legend) | — | YES (applied) |
| **D7** | **Attribution write timing is made unambiguous, and the constitutions are the side that is right.** DA §1.7 states the attribution row is written **when an attributed order is paid**; RPC §6.1 and RLS §9.17 write it inside `create_order`. Attribution is an **immutable Ledger** (CDM §1.3) that carries a promoter's commission claim: writing it at order creation credits a promoter for an order that may never be paid, and the credit then cannot be deleted — only superseded. The constitutional statement stands and is restated in both constitutions in terms that cannot drift back ("in the same transaction that marks the order paid — never at order creation"). The implementation specs are wrong and must move | DOC-FIX-APPLIED (constitution side; RPC §6.1 / RLS §9.17 correction owed by the RLS/RPC integrator) | DA §1.7 (attribution row) · CDM §1.3 Promoter/Link/Attribution | — | YES (applied) |
| **D8** | **`venue.promoter` cannot express the commercial terms DA §1.7 ratifies.** The constitution ratifies commission terms that are **flat-per-ticket or %** (flat-per-ticket is the dominant nightlife term), a promoter **`tier ∈ {professional_invited, public_ambassador}`**, and a **`party_kind`** discriminator (`promoter` \| `affiliate`) so one attribution engine serves two party kinds. `venue.promoter` (schema §3.17) carries only `commission_bps` — percentage-only, no tier, no party kind. The constitutional requirement is restated as binding and the gap is named here; **the schema integrator adds the columns.** Nothing in the constitutions changes to match the narrower schema | DOC-FIX-APPLIED (constitution side; additive schema columns owed by the schema integrator) | DA §1.7 (promoter/affiliate rows) · CDM §1.3 Promoter/Link/Attribution | — | YES (applied) |
| **C51** | **The event outbox is promised by the constitution and scheduled by nothing.** DA §6.3 states that *"the only new infrastructure Phase 2 introduces is one outbox table and a drainer on the cron that already runs"*, and DA §6.2/§6.3 route every eventual-consistency flow through it; CDM C12's event-envelope guarantees (per-aggregate monotonic `sequence`, `causation_id`, `correlation_id`, at-least-once + idempotent consumers) and C28/C48/C49 all presuppose it, and the door, promoter and notification designs emit envelope messages into it. **No implementation spec schedules an outbox table, a drainer, or the envelope columns**: the sole occurrence of the word in the physical schema spec sits inside the Gate-L list (projection checkpoints, "+ outbox retention/compaction"), and the migration plan allocates no package. Both readings have a consequence and neither is chosen here — **(a) the constitution is right** ⇒ an outbox package is Gate-P/MVP work that is missing from the plan and every "eventual" flow in the constitution is currently unimplementable as specified; **(b) the implementation specs are right** ⇒ DA §6.2/§6.3 must stop claiming an outbox exists in Phase 2, C12's envelope guarantees have no carrier at MVP, and every design that emits an envelope message (door manifest, promoter attribution, notifications) needs a stated alternative transport. Open decision **O7** | OPEN-GATED(O7) | DA §6.2, §6.3 · CDM §2 (cross-aggregate channels), §7, §15 C12/C48/C49 | P (if (a)) / — (if (b)) | **OPEN — blocks the MVP scope decision** |
| **C52** | **The `notify` schema is RATIFIED Gate-P MVP in the constitutions and do-not-build in every implementation spec.** Row **C7** — RATIFIED · Gate P · MVP — is the `core` split that names the seven contexts `kernel · catalog · venue · market · social · analytics · notify`, and CDM §1.6 gives Notification a canonical object and identity (`NotificationID`). All four implementation specs place `notify` at Gate L / DO-NOT-BUILD (`PHASE_2_SPEC_FOUNDATION.md` §deferred; `PHASE_2_RLS_PERMISSION_SPEC.md` out-of-scope; `PHASE_2_PHYSICAL_POSTGRES_SCHEMA_SPEC.md` Gate-L extension list; `PHASE_2_SUPABASE_MIGRATION_PLAN.md` package K "documented extension points, NOT scheduled"). Consequence either way — **(a) C7 is right** ⇒ a `notify` schema is Gate-P work absent from the plan, and the four specs' deferral text is wrong; **(b) the specs are right** ⇒ C7's Gate/MVP columns are wrong for the `notify` context, MVP notifications continue to run on the existing `public.notifications` table plus the `send-push`/`notify-*` edge functions, and every Phase-2 design that names a `notify` consumer (money §10.3, door §12.2, promoter, notifications spec) must say which carrier it actually means. Not resolved here; **C7 is not amended by this pass** and keeps its ratified status until the decision is made. Open decision **O8** | OPEN-GATED(O8) | CDM header (C7 contexts), §1.6 Notification, §15 C7 · DA §6.2/§6.3 (notify consumers) | P per C7 / L per the implementation specs — **the contradiction is the row** | **OPEN — blocks the MVP scope decision** |

**Row counts by status (as of the 2026-08-27 remediation reconciliation):**

| Status | Count | Rows |
|---|:---:|---|
| **RATIFIED** | **31** | C26, C27, C28, C33, C35, C36, C41, C42, O-1, O-2, O-3, O-4, O-5, **C53, C54, C55, C57, C58, C59, C60, C61, C62, C63, C64, C65, C66, C67, C68, C69, C70, C71** |
| **RATIFIED-MODELED-ONLY(GATE-M)** | 4 | C29, C30, C31, C43 |
| **RATIFIED-MODELED-ONLY(GATE-L)** | 12 | C32, C34, C37, C38, C39, C40, C44, C45, C46, C47, C48, C49 |
| **OPEN-GATED** | **6** | C50 + O6 → **O6** · C51 → **O7** · C52 → **O8** · **C56 → O9** · **C72 → O10** |
| **DOC-FIX-APPLIED** | **15** | D1, D2, D3, E-1, OBS-1, A-2, D4, D5, D6, D7, D8, **D9, D10, D11, D12** |
| **RETRACTED** | **5** | **RET-1, RET-2, RET-3, RET-4, RET-5** |

**Total 73 rows** (44 before this pass; **29 added** — C53–C72, D9–D12, RET-1…RET-5). **67 are terminal.**

**Pending, stated honestly.** 67 rows are terminal. **Five open decisions block a gate:** **O6** (cross-region native-resale form — carried from 2026-08-24); **O7** (the event outbox) and **O8** (the `notify` schema), both direct constitution-vs-implementation contradictions with a ratified statement on one side and four implementation specs on the other; and the two recorded by the remediation reconciliation — **O9** (the `OQ-5`/`OQ-W4` Wallet relaxation, whose two grant conditions are now specified and gate the feature flag, but whose **owner sign-off is still owed**) and **O10** (the second structural amendment to the package DAG, pending re-ratification). **The earlier "zero pending" claim does not survive, and is not restated.**

**Owner decisions carried by the remediation passes and NOT resolved here.** These are named so they are not lost between documents; each belongs to a sibling spec's own open-question table and none is decided by this record. Demographics **D-12** (R7 excludes comped and zero-price custody, so a genuinely free event never renders the mix card) and **D-14**; CRM **D-8** (platform bulk extraction not built in Phase 2), **D-13** (`display_name` consent-gated in the export — it changes what every export file looks like), and **D-6**/**D-10** (artifact retention 24 h vs 7 d, deliberately the same decision from two sides); schema **S-8** (were `org_marketing`/`org_promoter_manager` intended to be storable at org grain), **S-9** (`door.*` config visibility — the one line in the §2.4.1 ruling that could reasonably go the other way), **S-10** (`promoter_link.status` vs deactivating the promoter), **S-13** (`catalog.event_security_config` — schedule it, or rule the function out as `set_door_open_at` was); RPC **R-9** (the bid ledger's home), **R-10** (`venue.get_dashboard_summary`), **R-11** (two `INFERENCE — AUTHORED` contracts); RLS **X-7** (C43's narrowing is `GATE-M` while four documents describe it as implemented — **if the board wants it in MVP that is a new ratification, not a clarification**) and **X-8** (`kernel.approval_request`'s place in the global lock order: if a reviewer judges it an aggregate class rather than an intent record, **the parked-refund branch is a sixteenth SSCAS member and C28's closure needs a formal amendment**); and Wallet **OQ-W3** (Wallet may not ship before the door spec's M2 tables and step 3b are implemented and drilled — owner acknowledgement required) and **OQ-W6** (the `verify_jwt=false` set's security sign-off, **which should cover the set as a whole rather than one function**, `door-session` being the higher-risk member).

---

## Remediation-pass amendments — recorded 2026-08-27

**Authority:** four concurrent remediation passes, each carrying its own correction index, each of which changed material this record had already ratified: the **door/wallet** pass (`PHASE_2_DOOR_LIFECYCLE_SPEC.md` §13/§17/§19, `PHASE_2_APPLE_WALLET_SPEC.md` §11.10/§14/§15 — defects `H-2`/`H-3`/`H-4`, requests `DL-1`…`DL-6`, ruling `OQ-5`/`OQ-W4`); the **authz** pass (`PHASE_2_RLS_PERMISSION_SPEC.md` §17 `X-1`…`X-15`, `PHASE_2_RPC_FUNCTION_CONTRACTS.md` §20.14 `R-1`…`R-18` — defects `AUTHZ-C1A`/`C1A2`/`C1B`/`H5`/`H10`/`M1`–`M12`/`R1`–`R4`); the **demographics/CRM** pass (`PHASE_2_DEMOGRAPHICS_PRIVACY_SPEC.md` §16 `J-1`…`J-12`, `PHASE_2_CRM_EXPORT_SPEC.md` §14 `K-1`…`K-20`); and the **schema** pass (`PHASE_2_PHYSICAL_POSTGRES_SCHEMA_SPEC.md` §13.7 `S-1`…`S-13`).

**Why these needed rows at all.** Every one of the four filed correction IDs **that did not exist in this record**, and three of them changed or withdrew text this record had already marked terminal. Rule 1 requires owner approval for a change to ratified material; a correction index inside the spec that changed is a *record of what was done*, not the approval for doing it. These rows are that approval surface. **No prior row is renumbered**, and the four passes' own IDs are cited rather than replaced — an implementer reading `AUTHZ-H10` in the RLS spec must be able to find it here by that name.

> **A third ID namespace now exists, and it is the one most likely to be misread.**
> **`RET-1` … `RET-5`** are **retractions** recorded by this section. They are deliberately **not** `R-n`: `R-1`…`R-18` is already the RPC spec's §20.14 request namespace, and `R-1`…`R-17` is separately the role-model spec's RLS-edit namespace. Three unrelated `R` series would be the `O3`/`O-3` mistake a second time. Read `RET-` as a prefix, not a letter.

### A sixth status is added, because four of the five existing ones assert the wrong thing

| Status | Meaning |
|---|---|
| **RETRACTED** | **NEW, terminal.** Ratified or spec-canon text that is **deleted as false**, not superseded, not narrowed, not re-gated. The claim does not survive in any form, the reason is recorded here, and the deletion is *also* recorded in the correction index of the file that carried it. |

**Why a new status rather than `DOC-FIX-APPLIED`.** A doc-fix corrects a document that disagreed with a decision. A retraction says **the decision's stated justification was never true**. Filing the five retractions below as doc-fixes would record that some prose was tidied, when what actually happened is that five load-bearing security arguments were found false and removed. The distinction is the whole point: **a reader who finds one of these claims in a stale branch, an older PR, or a printed copy must be able to establish here that it is dead** — which is exactly the failure mode that let *"second and last"* survive in four documents while a third unauthenticated surface shipped.

---

### The five retractions — ratified/spec-canon claims deleted as FALSE

| ID | Retracted claim (verbatim) | Where it stood | Why it was false | Recorded by |
|---|---|---|:---:|:---:|
| **RET-1** | *"You cannot difference aggregates that do not exist."* | `PHASE_2_DEMOGRAPHICS_PRIVACY_SPEC.md` §5.3 option B — the headline answer to differencing in general | **True about axes, false about populations.** The `(session, dimension, bucket)` aggregate very much exists, one instance per session, and an operator who **composes two populations** — by minting a session, by minting custody, or by choosing which two of their own sessions to compare — differences two aggregates that both exist. Fixed rollups defend against **slicing**, not against an adversary who controls the input set. R7/R8/R9 are the population-side defences; vector 7 records what is still not covered | **J-9 (1)** |
| **RET-2** | *"each fake data point costs a real ticket purchase"* | `PHASE_2_DEMOGRAPHICS_PRIVACY_SPEC.md` §11, the Sybil-accounts rebuttal | **A comp costs nothing, and the same `venue_manager` mints both the session and the comps.** Before R7 the operator could compose the entire population for free, so every *"an inferable group is at least 5 people"* bound assumed five people who could be manufactured. **R7 makes the rebuttal true; it does not make manufacturing impossible, only priced** | **J-9 (2)** |
| **RET-3** | *"Removing any one of layers 1–3 still leaves a correct floor"* | `PHASE_2_DEMOGRAPHICS_PRIVACY_SPEC.md` §5.2, the defence-in-depth claim | **Layers 1 and 3 were the same function.** The writer decides what to persist, and *"the RPC can only return what exists"* is not an independent check — it is a restatement of the writer's decision. **Only R2 was ever a database constraint.** Recorded with its own lesson attached: this is the class of claim a reviewer stops checking once it is written down | **J-9 (3)** |
| **RET-4** | the bound *"an anonymity set of at least 5, matching the per-bucket floor"* | `PHASE_2_DEMOGRAPHICS_PRIVACY_SPEC.md` §5.3 vector 4, the R6-era churn bound | **Pairs were conflated with people.** Five contributor-pair changes is **at minimum three people**, since an answer change costs two. The real bound was ≥ 3, not ≥ 5. The ≥ 5 bound is restored **only** by the added distinct-identity limb (churn ≥ 5 pairs **and** ≥ 5 distinct identities in `C₁ △ C₂`). The prior rule also quantified over **membership** while the buckets track **answers**, so a stable member editing their answer moved a bucket by 1 at zero membership churn | **J-9 (4)** |
| **RET-5** | *"the **non-consenting majority — every transferee, every comp, every purchaser who left the box unticked — is unjoinable**, which is exactly the population with no relationship to either venue."* | `PHASE_2_CRM_EXPORT_SPEC.md` §4.3, the `customer_ref` cross-tenant honest-limit statement — **and the §4.4 case (d) proof rested on it** | **`display_name` was emitted on every row of every export, at every org, ungated by consent**, from the one global `public.profiles.display_name` string. Two orgs union their files on the name column directly and corroborate with admission time, `first_seen_at`, ticket types and acquisition route. **The non-consenting majority was exactly as joinable as the consenting minority; only the carrying column differed.** The proof that cited it **was void as written**. Corrected claim: *the pseudonym removes the platform-supplied **stable** join key, and that is the whole of what it removes* | **K-18** |

**Standing consequence of RET-1 … RET-5.** Each deletion is recorded **in its own file's correction index as well as here**, so it cannot be cited from an older copy of that file. **A reviewer who encounters any of these five sentences in any branch, diff, or document should treat it as a defect in that copy, not as a position.** Two of them (**RET-2**, **RET-5**) were *load-bearing inside a proof*: the proofs are restated in their own specs against the corrected designs (R7; the `emit_name := emit_email` gate), and **assertion 13c now tests unjoinability itself** — the intersection of two orgs' non-blank name cells for a doubly-non-consenting identity must be empty — rather than testing the mechanism the old proof credited.

---

### Door / Wallet pass

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C53** | **The offline admission predicate was more permissive than the online one, in the document labelled BINDING.** Four documents each held an independently editable copy of the offline door check; one was corrected and the others were not, and **the copy that stayed wrong was the one a scanner SDK implements**. Edge §5.4.3 carried **two** conjuncts at 3b where **five** are required (atom ∈ M2 · no applied `revoke` delta · `credential_version` match · `ticket_state='active'` · **`resale_state='none'`**), plus 3c (`key_id` re-pin). Ratified form: the predicate is stated **once**, tagged **`OFFLINE-VERIFY-v1`**, with three sanctioned byte-identical mirrors (door §9.2, Wallet §2.3, Wallet §11.9) under a CI byte-identity gate over `docs/architecture/**`. **The fix is to the mechanism — four editable copies — not to the instance.** Conjunct 3b.v is load-bearing, not defence in depth: without it the offline door admits `paid_pending_transfer` and `refund_hold` atoms the **online** door refuses | RATIFIED — defect `H-2` | edge §5.4.3 (normative) · door §9.2 · Wallet §2.3, §11.9 | P | YES |
| **C54** | **The door session proved provisioning, not possession.** `kernel.assert_door_session(p_device_id, p_session_id)` read `venue.scan_device` and `venue.door_pin` and took **no PIN, no token, no nonce**; the role model posited a *"door session token — the bearer artifact the device actually holds"* and **no `door_session` object existed** in the schema spec, the migration plan or the package registry. So a device id arrived as an **untrusted parameter** on a `verify_jwt=false` function holding the service-role key, and **anyone knowing one live `(device_id, event_session_id)` pair reached admission unauthenticated** — on the one path where **RLS is bypassed entirely**. Ratified form: table **`venue.door_session`** (hashed token, device+session+PIN binding, server-max TTL, revoke-only forward transition, `token_hash` never client-readable by any role **including `platform_admin`**), an `assert_door_session` **signature change** returning the bound pair, `p_actor_device_id` derived from that return value on `record_scan`/`reconcile_offline_scans`, and a **`revoke_door_pin` cascade** — without which revoking a PIN leaves live bearer tokens behind, which is this same defect one level up | RATIFIED — defect `H-3` | edge §3.9a · schema §3.10a · door §17 · plan `086` | P | YES |
| **C55** | **The Wallet build context's entire authority was an auth-token compare.** `get_wallet_pass_build_context` would rebuild and serve a pass for a **superseded** pass row or a holder who is **no longer the current owner**. Ratified form: liveness preconditions (`status='issued'` ∧ holder = live current owner, rebuild at `credential_version_at_build`); `list_updated_wallet_passes` gains `p_auth_token` and becomes registration-scoped and liveness-filtered — **it was the one endpoint in the design returning *many* serials and it was unauthenticated by contract**; and the `wallet.apple.enabled` kill switch is corrected to gate **serve / rebuild / register / push**, not minting alone, because *a switch that protects only the people who do not have the feature yet is not a kill switch* | RATIFIED — defect `H-4` | Wallet §11.6a, §11.6b, §11.5a · edge §3.10, §3.11 | P | YES |
| **C56** | **Ruling `OQ-5`/`OQ-W4` relaxed a ratified constraint on TWO conditions, and NEITHER was implemented when it was granted.** The ruling grants the session-bounded Wallet token profile — correctly, since a short-TTL barcode baked into `pass.json` locks a paying fan out of a dead zone — but it grants it **conditionally**, and verification found both conditions unmet. **(1) The offline-window bound:** the ratified cross-config invariant `wallet_default_span + wallet_exp_skew <= door.manifest_ttl_interval` constrains `wallet_default_span`, which applies **only when `session.ends_at IS NULL`**. On the common branch it **bound nothing**, and a long or mistyped `ends_at` produced an **unbounded `exp`** — a fat-fingered year mints a multi-month bearer credential. The invariant was checked against the constants an operator sets and **never against the number the signer emits**. Now discharged by a **clamp on the computed value** applied at sign time (Wallet §5.2a), with the constants invariant demoted to explicitly **necessary-but-not-sufficient**. **(2) Key revocation force-closing open door episodes:** the mechanism existed (door §8.2.1, envelope #44 already carried `key_revoked`) and **`kernel.revoke_signing_key` was specified nowhere as invoking it** — a key-table update and nothing more. The ruling's own words: *"Without this I would reject DL-4."* Now written as a normative same-transaction obligation in edge §5.6. **Both conditions now gate the feature flag: Wallet §13 items 10a/10b block `wallet.apple.enabled := true`.** **The owner is signing off on a relaxation whose safety rests on these two, and the sign-off is still owed** | **OPEN-GATED(O9)** — the *conditions* are ratified and specified; the **relaxation itself is not yet owner-signed** | door §16 OQ-5 · Wallet §5.2a, §13 items 10a/10b, §15 OQ-W4 · edge §5.6, §5.5 | P (Wallet enable) | **OPEN — blocks the Wallet enable** |
| **D9** | **The `verify_jwt=false` set was characterized outside its enumeration, twice, in two different grammars.** Four documents said *"the second and last"* such surface while a **third** unauthenticated surface was arriving; that instance was corrected to a five-member enumeration in edge §7. This pass found the **same defect in a different form** still standing: edge §2's deployment note asserted *"Only Stripe/KMS-webhook endpoints run `verify_jwt=false`"* — a **count-by-category**, false against §7's own list, in which only `stripe-webhook` is a Stripe/KMS webhook. Ratified rule: **§7 is the only place the set may be characterized at all — by count, by category, or otherwise** — and `door-session` is named there as **the highest-risk member**, since it relays admission while holding the service-role key with RLS bypassed behind it. Its security sign-off is owed **alongside** `wallet-pass-webservice`'s, not after it | DOC-FIX-APPLIED — `EDGE-1` | edge §2, §3.11, §7 · Wallet §6.1, §15 OQ-W6 | — | YES (applied) |

### Authz pass

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C57** | **The approval tier had no stored discriminator, so every parked refund reached the org arm.** `kernel.approval_request` returned `pending_approval` / `pending_platform_review` as **result statuses** and stored neither, leaving `approve_refund_request` to branch on `state`/`action`/`org_id` alone. Ratified form: **`required_approver_class`** (`NOT NULL`, `CHECK IN ('org','platform','platform_admin')`), **server-set at request time and pinned like `config_versions`, never a parameter**; plus `CHECK (state <> 'approved' OR approved_by IS NOT NULL)` — **without which the SoD CHECK is vacuously satisfiable by any writer that forgets the column**, and *"SoD as a table constraint, not a convention"* is aspiration rather than fact; plus the `subject_kind` and `action ↔ subject_kind` pairing CHECKs; plus index `(action, required_approver_class, state)`. `config.set_money_key` additionally requires a **second distinct `platform_admin`** | RATIFIED — `AUTHZ-C1A`, `C1A2`, `M1`, `M2` | RLS §11.3, §16.1 · RPC §17.1/§17.2/§17.7, §20.1 · **schema §1.13 + plan `077` owed (X-10/R-16)** | P | YES |
| **C58** | **The money-plane SoD counterparty is mintable by the very role that needs one.** Both money SoD primitives compare `auth.uid()` against a stored identity, and an `org_owner` holds the grant authority to **mint the second identity through the ordinary invite flow**. Ratified form: **grant maturity** — `kernel.money_role_grant_matured(org_id)` (new predicate helper) rejects a caller whose money-role grant is younger than `authn.money_role_maturity_hours`, requiring `kernel.org_member.granted_at`, written by `accept_org_invite` and **reset by `change_org_role` on promotion *into* a money role**. **It binds the destination-SETTER as well as the requester — applied to one half of a pair it is applied to neither** — and is **never** applied to deny or cancel. This is the control that makes **O-3's** ratified SoD-1 collapse survivable in practice rather than only on paper | RATIFIED — `AUTHZ-C1B` | RLS §11.3a, §2.2 · RPC §17.1/§17.2/§17.7, §20.7 · **schema `kernel.org_member` + plan `077`/`078` owed (X-11/X-12, R-17/R-18)** | P | YES |
| **C59** | **Two RPCs wrote one append-only ledger under contradictory authority, and every product surface was wired to the permissive one.** `venue.review_attribution_flag` admitted **both promoter-manager labels**; `venue.decide_flagged_attribution` denied them — *"the fox at the henhouse"* — for the same ledger and the same release/deny decision. **The restrictive one was decoration**: `venue.attribution_review` is append-only with **effective decision = `max(seq)`**, so the conflicted party never needs to overwrite anything — **they append `release` at `seq+1` and it is the effective decision.** And the permissive function is the one the dashboard, the promoter spec and migration plan `090` all name; the restrictive one was named by RLS §11.5 and ROLE_MODEL §11 R-16 **and by no product surface and no package** — the function nobody would have built, cited as the reason the control existed. **Ruling: `venue.decide_flagged_attribution` is DELETED, not aliased; `venue.review_attribution_flag` survives and takes the restrictive allow-list**, which was the load-bearing half. **Nothing is left without a writer** — `090` never named the deleted one | RATIFIED — `AUTHZ-H10` | RLS §11.5, §17 X-14 · RPC §17.18, §20.13, §20.14 R-13 · dashboard §20A.1 (applied) · **ROLE_MODEL §11 R-16 + §12 row 17 owed** | P | YES |
| **C60** | **§11's EXEC authority table had drifted from the role model it is downstream of, and no assertion could see it.** A lexical `venue_door → venue_scanner` rewrite across §11 **preserved capabilities §5 was simultaneously removing**, because no clause said §5 governed §11. Ratified form: the **`EXEC-DERIVED`** rule plus `T-RLS-EXEC-01`, which make the derivation mechanical; the `venue.set_door_open_at` EXEC row **replaced** with `catalog.set_session_door_schedule` (the function it granted **did not exist**, and the row was the reason it would have been built); fourteen contracted functions given the grant class they lacked; fifteen `PROPOSED AUTHORITY` sites ruled on (fourteen accepted, `get_dashboard_summary` **deferred, not accepted**); and the four guest-list rows rolled up | RATIFIED — `AUTHZ-H5`, `R1`–`R4` | RLS §11.0, §11.1a, §11.1b, §11.1c, §11.4, §16.11 | P | YES |
| **C61** | **Twelve medium authority defects, of which four are absent-config fail-OPEN.** Ratified as one row because they share one shape: **a threshold that gates an authority and does not exist is not a gate.** `comp.per_staff_step_up_max_units` / `_window_hours` (**C39 cited in five documents with no key anywhere** — an implementer reads NULL, compares `quantity > NULL`, gets NULL, and **no comp at any quantity requires step-up**, which is unmetered comp issuance on the capability the domain architecture names *"the insider-fraud primitive"*); `refund.platform_support_max_minor`; `authn.money_role_maturity_hours`. **Standing rule: an absent key evaluates to the SAFE extreme — no grant is mature / every comp needs step-up / support approves nothing — never the permissive reading**, and the tests **delete the config row rather than set it to zero**, because a missing seed and a seeded `0` behave identically only if the `COALESCE` is actually there. Also: `step_up_unavailable` **raises** rather than evaluating on an absent freshness claim (`M4`); the promoter's own-row predicate, whose first correction was itself broken (`M10`); the promoter's `venue.attribution` cell narrowed from `A` (`M9`); the door's **positive** completeness assertion — *"the gate is in every place it must be"* — which on a path with no RLS behind it is the load-bearing half (`M6`); the export builder's Layer-0 grant, which as written **would have shipped every export with a blank contact column** (`M11`); and a scope constraint on the platform arm of `list_attendees` (`M12`) | RATIFIED — `AUTHZ-M1`…`M12` | RLS §9.17, §11.1, §11.3, §11.3b, §11.6, §16.1, §16.10, §16.11 · RPC §20.4.1, §20.5.1, §18.2 | P | YES |

### Demographics / CRM pass

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C62** | **The privacy floors were proved against a population the operator controls.** Ratified form adds four rules and a kill switch: **R6** — a suppressed snapshot returns the constant **`{suppressed: true}`** and nothing else (the prior shape returned denominators with **no floor on them**, so a `venue_manager` could mint a throwaway session, comp one ticket to a target, and read `holders_responded ∈ {0,1}` as that person's *did-you-answer* bit); **R7** — population eligibility, custody acquired **for consideration** only; **R8** — churn redefined over the contributor multiset `(identity, bucket)` **with a distinct-identity limb**; **R9** — cross-session near-duplicate gate; a fully determined R3 merge with a two-bucket minimum in R5; and a fail-closed **read-side re-derivation** of R1/R2/R4/R5/R6. **Recorded consequence:** a genuinely free event never renders the card, which is correct and is a real product loss (**owner decision D-12**) | RATIFIED — `J-8` (with `RET-1`…`RET-4`) | DEMOG §4.1, §4.3, §5.1–§5.5, §8.4, §10.2, §10.4, §11, §13 · dashboard §9.5 (applied) | P | YES |
| **C63** | **A timestamped history of a person's gender answers was specified in the same section that names it the worst artefact the feature could produce.** The fan-side audit row `(identity_id, action ∈ {set, changed, cleared}, occurred_at)` **is deleted**: *"never the value"* does not cover the harm — the row **records a transition**, and §3.2 had already rejected the same object by name. No audit table was named, so the only fitting home was `kernel.admin_audit` — **permanent and platform-readable** — which would have nullified the §8.5 tombstone's purge window, the sole mitigation offered. `set_my_demographics` and `clear_my_demographics` now write **no audit row of any kind**; the aggregate **read** audit on `venue.get_holder_mix` is unchanged and remains binding | RATIFIED — `J-11` | DEMOG §8.1, §8.3, §10.4, §13 assertion 26 | P | YES |
| **C64** | **The strictest erasure case was the one case a post-restore purge could not re-apply.** Account deletion **cascaded the demographic row away without writing the §8.5 tombstone**. Ratified form: the tombstone is written by a `BEFORE DELETE FOR EACH ROW` **trigger**, so **every** removal path produces one — withdrawal, cascade, definer delete, future C38 merge — and `kernel.identity_demographic_erasure.identity_id` is deliberately **FK-free**, because a CASCADE FK would delete the tombstone in the very statement that creates the need for it. Assertion 25 narrowed from *"zero triggers"* to *"exactly two, neither referencing a gender column"* — asserting the property the proxy stood for | RATIFIED — `J-12` | DEMOG §8.2, §8.5, §10.2, §10.4, §12(a), §13 assertions 25/27/29/30 | P | YES |
| **C65** | **The export's tenant predicate was bound at org grain only, and a venue changing hands leaked the previous org's customer list.** `catalog.venue.org_id` is **mutable** while `catalog.event.org_id` is stamped at create, and the isolation traversal is downward `org → venue → event → session → ticket → holder`, so a **venue-grain export at a new operator reached every historic session of that venue**. Ratified form **XO-1a**: `kernel.tickets.org_id = :job_org_id` at **every** grain, `:job_org_id` resolved once at request time and **frozen on the job row**; the `customer_ref` HMAC key and the consent gate's `EXISTS` both pinned to the **job's** org, not the atom's — the atom binding would have given two orgs the **same** pseudonym for the same person, joining their files directly. Assertions 18a–18c are asserted **per grain**, because a single-grain test passes while three branches leak | RATIFIED — `K-14` | CRM §1.4, §4.1–§4.4, §5.1, §11.2, §11.4, §12 | P | YES |
| **C66** | **The download re-check read the role set and never the template, so marketing could download a colleague's money export.** `template_id` was **not mentioned in the download contract at all**, while `operations_v1`'s request-time allow-list is the narrowest in the matrix — so a `marketing` role, which holds the job-list read, could take a `job_id` and download order refs, order totals, unit prices and refund state. **§3.1's own invariant — *"Finance sees money and no contact. Marketing sees contact and no money. Neither sees both."* — was defeated by any org that had ever run one operations export, with no grant being wrong.** Ratified form: the download re-evaluates `assert_may_request(actor, job.scope, job.template_id)` — **the same predicate a fresh request would face** — asserted as an **equality between the request and download predicates** so the two cannot drift | RATIFIED — `K-15` | CRM §3, §3.1, §6.2, §6.7 EX-4, §7.4, §11.4, §12 · dashboard §9.6 (applied) | P | YES |
| **C67** | **Nothing in the design could delete a Storage object, so retention, sweep and revoke all had NO AGENT.** Revoke said it *"deletes the artifact, effective immediately"*; `venue.revoke_export` said it *"signals the edge to delete"*; the sweep said it *"deletes artifacts past retention"* — and `crm-export` had exactly two routes, **neither a delete**. **A `SECURITY DEFINER` Postgres function cannot call the Storage API**, and its only in-DB option, `DELETE FROM storage.objects`, drops the metadata row and **orphans the bytes** — strictly worse than doing nothing, because the object survives while every accounting says it is gone. **The ratified *"the lake is bounded by a 24-hour sweep"* defence was therefore unimplementable as specified.** Ratified form: **`POST /purge`** on `crm-export` (`pg_cron` + `pg_net`, 15-minute cadence, the pattern migrations 014/032/034 already use) — **the only Storage delete agent in the entire design** — plus `artifact_state`/`purge_lease_until`/`purge_attempts` on `venue.export_job`, three definer `service_role` RPCs, and a **daily two-directional orphan reconciliation** reconciling bucket against job table **in both directions**, without which the 24-hour bound is a statement about rows and **rows are not what leaks**. Revoke's honest bound restated as **`min(300 s, time-to-purge)`, not zero** | RATIFIED — `K-16` | CRM §6.2, §6.6, §9.2, §11.1, §11.2, §11.4, §11.5, §12 · edge §2, §3.7, §7, §8 (applied) · **plan `087` schedule owed** | P | YES |
| **C68** | **The name-prefix lookup had no rate limit, so the printed list the design refuses could be reassembled one record at a time.** The limit table carried rows for `email_exact` and `order_ref` and **none for `name_prefix`**, and `venue.lookup_attendee`'s contract scoped its limit to `email_exact` **explicitly**. `venue_box_office` holds that read, so iterating `a…z`, `aa…zz` against one session returned the roster **at no rate cost** — rebuilding *"a box office cannot print a paper list. That is deliberate."* from the surface meant to replace it, **by the role denied it**. Ratified form: a `name_prefix` limit (20/actor + 60/org per 24 h), a **3-character minimum raised before the lookup and without consuming budget**, and **multi-match as an explicit `ambiguous_query` carrying no rows and no count** — because **the count is the harvest** (`"sm"`→14, `"smi"`→9 reconstructs the name distribution without returning a record). **Per-org caps added for every lookup kind.** Audit records kind **and outcome**, never the probed string; a run of `ambiguous`/`rate_limited` is the sweep signature and the only evidence of one | RATIFIED — `K-17` | CRM §7.1, §7.2, §7.2a, §7.4, §11.4, §12 | P | YES |
| **C69** | **The name column was the global cross-tenant join key the pseudonym existed to withhold.** Ratified form: **`emit_name := emit_email`** — one predicate, evaluated once per holder row, driving **both** cells, so they cannot disagree and nobody can gate one and forget the other. `display_name` stays **ungated on screen**, in the single-record lookup and in the door projection, where it is already readable and **where a surface cannot be unioned with another org's**; it is gated **in the export**, because *"once a name is in a CSV it is a durable, unioning join key over a population that never chose to have a relationship with the org holding it."* **The gate goes on the egress, not on the knowledge.** `name_cells_emitted`/`_suppressed` join the audit pair; the suppression legend covers both columns. **Operator cost recorded rather than hidden** (owner decision **D-13**): an `audience_v1` export over a heavily transferred session is mostly `customer_ref` and ops columns with name and email blank on the same rows | RATIFIED — `K-18` (carries **RET-5**) | CRM §2.2 field 2, §4.3, §4.4 (a)/(d), §5.1, §8.3, §11.2, §12 · dashboard §9.6 (applied) | P | YES |
| **C70** | **Six CRM mediums, of which the first falsifies a determinism property the whole replay design rests on.** (1) **The consent gate's evaluation instant was unspecified** and `identity_contact_pref` had no history, so a **paged** build necessarily evaluated its conjuncts at **inconsistent instants** — falsifying byte-identical determinism, assertion 22, and the replay property that is the entire reason per-export membership is not stored. Fixed by `gate_as_of` (stamped at claim, re-stamped on re-claim) plus **two append-only event logs** making both mutable conjuncts as-of evaluable. (2) The Layer-0 builder grant **as written returns zero rows** — `auth.users(id, email)` was not in the enumerated set — so **every export would have shipped a blank contact column reading as "nobody consented"**; a blank-column canary is added. (3) `finalize_export` **accepted the gate counters as worker-supplied parameters** for numbers §5.1 calls *"the only evidence the consent gate ran"* — **evidence the caller hands you is not evidence about the caller**; they are now accumulated inside the definer by the code that evaluates each cell. (4) The platform branch of `list_attendees` had **no scope constraint** at 12 000 rows/hour across every session on the platform. (5) Per-org caps for every lookup kind. (6) **Five roles hold both the roster read and the mix card**, so the demographics floor of 5 is a bound over five people the reader can **name** (**D-14**) | RATIFIED — `K-19` | CRM §4.5, §5.1, §6.3, §7.1, §10.1, §11.1, §11.2, §11.4, §12 | P | YES |
| **D10** | **The roster and the mix card count different populations on purpose, and the identity that reconciled them was false.** R7 excludes comp-caused and zero-price custody from `holders_total` while the CRM roster deliberately keeps comped atoms **on** the roster — both correct. `COUNT(holder view) ≡ holders_total` was therefore wrong. Restated as **`COUNT(holder view) ≡ holders_total + holders_excluded_ineligible`**, and **that is why `holders_excluded_ineligible` is stored at all**: the gap between the list an operator counts and the denominator on the card beneath it must be a **stated number**, not a discrepancy they are left to guess at before concluding one surface is broken. The assertion's fixture **must contain comps**, so it cannot pass vacuously, and it fails if **either** side drifts | DOC-FIX-APPLIED — `K-20` | CRM §1.3, §12 assertion 3 · DEMOG §5.2 · dashboard §9.5 (applied) | — | YES (applied) |
| **D11** | **The canonical table inventory was missing eleven tables, and the standing count of the miss was itself stale.** `SPEC_FOUNDATION` §6 — the file every implementation spec is told to take its names from — lacked objects that four specs had already contracted, packaged and written pgTAP against. `SCOPE_AMENDMENT` **X-14**/**R-6** record the miss as **eight** (four `J-4` + four `K-8`); **`K-19` has since taken the CRM set to six** by adding the two consent event logs, and `venue.door_session` (`H-3`) is an eleventh. **Anyone reconciling against X-14's number still finds two missing and concludes §6 is wrong.** Corrected to **eleven** and applied. Additionally, §4 C36 still carried the **pre-O-2** label lists naming `venue_door` and `venue_promoter` — **the exact strings O-2 abolished** — the same defect row **D5** purged from the constitutions, surviving in the naming authority itself | DOC-FIX-APPLIED — `SF-1`, `SF-2` (from `J-4`, `K-8`, `H-3`, `F-1`…`F-3`) | SPEC_FOUNDATION §4, §6 (applied) · **RLS §6 deny-all rows owed** | — | YES (applied) |

### Schema pass

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C71** | **`catalog.platform_config` was blanket public-read, publishing every control ceiling to `anon`.** RLS §8.4 states *"values are not secret; every non-admin principal including `anon` holds SELECT"*. Ratified form: a **two-class visibility model** — `anon`/`authenticated` see `visibility='public'` only; **`restricted`** is `is_platform([platform_admin, platform_risk])` plus the definer RPCs. Money, `authn.*` and `crm.*` are `restricted` on settled arguments. **Until §8.4 changes, package `078` publishes every dual-control ceiling, step-up window and export cap to `anon`** — including, now, the comp step-up threshold and the money-role maturity window that C57/C58/C61 introduce | RATIFIED — `S-4` | schema §2.4.1 · **RLS §8.4 owed** | P | YES |
| **C72** | **The schema-security remediation is a SECOND structural amendment to a ratified package DAG, and it is pending re-ratification.** It adds `venue.door_session` (a structural table in `086`, per C54) and recommends a **new dependency edge `086 → 087`** (`DAG-3`). The package registry carries the block **`⚠ SECOND AMENDMENT PENDING RE-RATIFICATION — schema-security remediation`**, closing *"Owner ratification required, per rule §6.5"*. **Recorded here so the pending state is visible from the ratification record rather than only from the registry** — a pending amendment that only the amended document knows about is the shape of every defect in this section | **OPEN-GATED(O10)** | registry re-ratification block · schema §13.6 `DAG-3`, §3.10a · plan `086`/`087` | P | **OPEN — blocks the package DAG re-ratification** |
| **D12** | **Twelve integrator requests filed by the schema pass, recorded so none is lost between owners.** `S-1` approval-tier writers · `S-2` `APPR-SUBJ-1/2` soft-subject integrity relocated into the RPC layer, **because the polymorphic subject has no FK by necessity** (three subjects, three packages) · `S-3` grant-maturity predicate binding **both halves of the pair** · `S-5` `assert_door_session` token parameter — **a signature change to the door's entire authorization surface** · `S-6` `record_scan`/`reconcile_offline_scans` device id **derived, never client-attested**, since on the `service_role` door path RLS is bypassed and the device id would otherwise be an unauthenticated string selecting whose scans are written · `S-7` `set_door_open_at` → `catalog.set_session_door_schedule` in §11.4, **the last place the ruled-out function survives** · `S-11` `venue.set_scan_device_status`, whose `retired` state **had no writer** and which is the kill switch for a lost scanner · `S-12` `market.respond_offer` must reject past `expires_at` **arithmetically**, because an accept path trusting `status='pending'` consummates an expired offer **every time the cron tick is late, which is the ordinary condition of cron** · `S-8`, `S-9`, `S-10`, `S-13` are owner rulings, carried in this record's pending list | DOC-FIX-APPLIED (recorded; **each is owed by a named sibling integrator**) | schema §13.7 `S-1`…`S-13` | — | n/a (routing) |

---

## Reviewer-conditions pass — recorded 2026-08-28

**Authority:** an adversarial design review of the Phase 2 corpus taken at `cbf8926` raised six non-blocking conditions. Each was **re-verified against the tree before any edit** — a tidy pass had landed since the review was taken — and each is recorded here under its reviewer condition number so it can be found by the name the reviewer used. **No prior row is renumbered.** This pass touched **no** SQL, **no** implementation code and **no** CI workflow; every change is to design prose.

**Read this before reading the rows.** Three of the six conditions are defects in a *register a reader trusts* rather than in the authority it points at — the traceability matrix (C3), the door spec (C4), the promoter spec (C5). In each case the authority was already correct and the derived document was wrong. That direction of error is more dangerous than the reverse, not less: an implementer reads the register, not the authority, and a wrong claim in a register is indistinguishable from a right one at the point of use. **C5's is wearing a `VERIFIED:` badge**, which is the sharpest form of the defect.

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **D13** | **This record asserted that the `OFFLINE-VERIFY-v1` CI gate "does not yet exist — no workflow under `.github/` scans for the tag". It exists.** `.github/workflows/ci.yml`, job `quality`, required check **`Typecheck / Lint / Unit tests`**, step *"OFFLINE-VERIFY-v1 spec byte-identity gate"*, with **seven numbered fail-closed guards** (unterminated fence · column-1 tag outside a fence · non-vacuity floor of 4 blocks · normative home still tagged · byte-identity · anti-emptying byte floor · anti-coordinated-weakening anchor list). The claim was **stale in the safe direction** — it understated a control rather than overstating one — **which is exactly why it still had to be corrected**: a constitution that under-reports its own enforcement teaches the next reader that the property is prose, and the next pass to "add the gate that is missing" either duplicates it or weakens it while believing it is building it. Corrected `DOC-FIX-APPLIED` rather than `RETRACTED` because the sentence was **true when written** on 2026-08-27 and was overtaken by an event; `RETRACTED` is reserved for text that was false when ratified | DOC-FIX-APPLIED (reviewer condition **6**) | this record, "Independent verification performed by this pass" ¶ | — | n/a (governance record) |
| **C73** | **`PHASE_2_PROMOTER_CODES_SPEC.md` placed `venue.settlement` in package `086`. `086` is `086_venue_door_and_scan`; settlement is `087`.** The reviewer filed one wrong number; verification found **four**, all from one root cause: §0.3 decoded the dead repo scale onto the canonical scale **by arithmetic (+3)**, which registry §4 explicitly forbids — *"arithmetic alone is not safe"*, because `PHASE_2_SUPABASE_MIGRATION_PLAN.md` carried `S0`, `S1` and `S2` **simultaneously in different sections**. Three of that table's five rows were off by one (`venue.order` `081`→`082`, `kernel.payment_native` `084`→`085`, `venue.settlement` `086`→`087`), and the errors propagated into §14.7 (×4) and §15 (×4). **`086`'s real contents — door PINs, `door_session`, scan devices, scans — intersect nothing this spec reads or writes**, so the wrong number was not a near-miss. **Three of the four wrong claims carried a `VERIFIED:` badge, and the badge was correctly applied**: per that spec's §0.2 `VERIFIED:` certifies only *"the cited document states it"*, never that the cited document is **canonical**, and the superseded migration plan really did say `086`. A correctly-applied badge on a false statement is the most expensive kind, because it is the claim a careful reader stops checking. Fix: §0.3 rebuilt to decode **by package identity** against registry §2 (now named canonical for this file), §14.1 re-titled **CLOSED BY RATIFICATION** (it was filed open against a renumber author whose renumber is ratified), §14.7 and §15 corrected inline with prior values shown, and a §16 correction index added (`X-01`…`X-04`). **No design content changed — every corrected item is a package number**; `090`, this spec's own package and the number on all sixty of its rows, was correct throughout. The reviewer's `R-5` is cited in §16 as *"reviewer condition 5"* and is deliberately **not** enrolled in any of the three existing `R-n` namespaces | DOC-FIX-APPLIED (reviewer condition **5**; reviewer IDs `R-5` / `X-01`…`X-04`) | promoter §0.3, §14.1, §14.7, §15, **new §16** · authority: `PHASE_2_PACKAGE_REGISTRY.md` §2/§4 | — | n/a (register alignment; registry unchanged) |
| **C74** | **`PHASE_2_DOOR_LIFECYCLE_SPEC.md` still carried the abolished label `venue_door` and the abolished door-PIN authorization arm — including in §10A.7, a table titled *"EXECUTE-authority additions to RLS §11"* that **contradicted RLS §11.4, the table it claims to extend**.** `venue_door` was renamed `venue_scanner` by O-2 / ROLE_MODEL §4.5 / schema §0.6 / RLS §2.1; the PIN arm is the provisioning-not-possession predicate closed by `AUTHZ-H3` (`venue.door_pin` has **no device column** — it evidences that a PIN was issued for a venue, never that *this device* holds it). **The authority was already correct in both places** — RLS §11.4 and the edge spec carry the token-bound `kernel.assert_door_session` form — and rule **`EXEC-DERIVED`** (RLS §11.0) already declares this file the defect on disagreement. **That is the dangerous direction, not the safe one:** a door implementer reads the door spec, not RLS §11.4, and the PIN arm was **code-shaped** (an `OR` clause to transcribe) with the caveat that it must not be reachable sitting in prose *after* it — **a caveat is not a predicate.** Two findings beyond the filed condition: (a) §4.1 and §16 `OQ-3` argued from a **four-label** venue enum and offered *"a fifth enum label"* as a remedy — **`venue_box_office` exists** in the canonical six and RLS §11.4 already excludes it, so that half of the open question is **closed** and only the narrower `venue_manager`-over-provisioning half (VD Δ8) survives; (b) §16 `OQ-7` asserted *"the RPC is unchanged … §11's EXEC row stands as written"*, quoting the deleted PIN arm — **deleting the route while leaving the arm in the predicate is the reachable-only-by-convention posture `AUTHZ-H3` was raised against.** Fixes in §4, §4.1, §4.2, §7.1, §7.2, §7.5, §10.1, §10A.1, §10A.2, §10A.7, §15, §16 `OQ-3`/`OQ-7`, §17; new §20 correction index (`DL-X1`…`DL-X4`). **Every change narrows authority or leaves it unchanged; none widens it.** No RPC signature, table, column, state machine, lock order, invariant or package number altered; §9.2's `OFFLINE-VERIFY-v1` mirror untouched | DOC-FIX-APPLIED (reviewer condition **4**) | door §4/§4.1/§4.2/§7.1/§7.2/§7.5/§10.1/§10A.1/§10A.2/**§10A.7**/§15/§16/§17, **new §20** · authority: RLS §11.4 + `PHASE_2_EDGE_FUNCTION_SPEC.md` (both already correct, unchanged) | P | YES (the door predicate is built from this file) |
| **C75** | **`T-RLS-EXEC-02` — the guard designed to catch exactly the `venue_door` class — is scoped to predicates in RLS §11 and structurally cannot see the downstream specs that copy those predicates.** RLS §11.0 states it as *"No label appearing in a **§11** predicate may be absent from the fifteen canonical labels of §2.1"*, and every abolished label corrected by **C74** lived in `PHASE_2_DOOR_LIFECYCLE_SPEC.md`, in predicates that are downstream copies of §11 rows rather than §11 rows themselves. **The assertion is not weak at those sites — it is not evaluated at them at all.** This repeats the guard's own origin: `T-RLS-EXEC-02` was added because ROLE_MODEL edit `R-14` renamed `venue_door → venue_scanner` **lexically** across §11 and nothing could tell a lexical rename from a re-derivation — and it was then **scoped to the one table that had already been fixed, and not to the downstream copies that had not.** A guard that covers only the site of the last incident is a guard against the last incident. **Partially closed:** door §15 assertion `25a` asserts the canonical-label rule over that document's own predicates. **The general form is OPEN and is not made here** — enforcing the canonical-label set over **every** predicate under `docs/architecture/**` is a corpus-wide CI change owed to the CI-gate owner, of the same shape as the `OFFLINE-VERIFY-v1` byte-identity gate: a property currently held by review that is buildable as a scan. Filed as `DL-X4` | DOC-FIX-APPLIED (partial — the per-file assertion; the corpus-wide scan is **filed, not built**, and is owed to the CI-gate owner) | door §15 (`25a`), §20.2 · RLS §11.0 `T-RLS-EXEC-02` (unchanged — no gate weakened) | P | NO (the general scan; the per-file assertion is MVP) |
| **C76** | **`PHASE_2_IMPLEMENTATION_TRACEABILITY_MATRIX.md` §10 asserted the `_sel_org`/`_sel_venue`/`_sel_promoter` policy triple for `venue.attribution` and `venue.attribution_review`; RLS §16.10 drops `venue_attribution_sel_promoter` and moves `attribution_review` into the zero-policy set (`AUTHZ-M9`).** Two further defects were found in the same register: **(a)** §10 listed only `catalog_platform_config_sel_public` and glossed it *"it is world-readable"* — RLS §8.4 is a **two-class** model on `visibility` (`AUTHZ-CFG1` / **C71**), and telling an implementer that the table holding every dual-control ceiling, step-up window and export cap is world-readable is **the unsafe direction and the exact defect C71 was raised against**; **(b)** *"The **31** zero-policy objects … **All 31** are claimed"* — RLS §16.10 lists **35**, so **a completeness assertion over the wrong denominator reported `PASS` while four objects went unclaimed** (`org_contact_consent_event`, `identity_contact_pref_event`, `venue.door_session` (`AUTHZ-H3`), `venue.attribution_review` (`AUTHZ-M9`)). **All three claims were TRUE at the matrix's stated baseline `64d2aac`** — verified against that commit, not assumed: `AUTHZ-M9` did not exist in the RLS spec there, `sel_restricted` did not exist, and the zero-policy set had **exactly** 31 members. The matrix did not misread its sources; the remediation passes changed RLS underneath it. Under **GP-3** the RLS spec is the authority and the matrix is derived. Fixed by copying the authority; §14 correction index added (`TM-X1`…`TM-X4`); **§14.3 states that only §16.10 was reconciled forward — §8, §9, §11, §12 and §13 remain at `64d2aac` and are explicitly NOT current** | DOC-FIX-APPLIED (reviewer condition **3**) | matrix header, §10 (×3), **new §14** · authority: RLS §16.10/§8.4 (unchanged) | P | YES (the matrix is the register an implementer works down) |
| **C77** | **The traceability matrix has no mechanism that fails when its baseline goes stale — and its own binding cell vocabulary hides the problem.** §0.1 defines a named artifact as *"**VERIFIED** — the named object exists in the cited spec **at this baseline**"*. That qualifier is load-bearing and invisible: `venue_attribution_sel_promoter` satisfied it at `64d2aac` and **names a policy that exists in no spec today**. There is no marker for *"verified against a baseline that has since moved"* and no assertion anywhere binding this derived register to its authority. **A completeness instrument pinned to a stale baseline does not degrade into an incomplete one — it degrades into one that reports `PASS` with authority**, which is worse than having no instrument, because the empty cells are its entire stated value (§0) and a stale full cell hides one. **This is the third independent finding in this pass converging on one missing class of gate — a derived register with no assertion binding it to its authority** (cf. **C75**, `T-RLS-EXEC-02` blind to downstream predicate copies; **C73**, a `VERIFIED:` badge that certifies citation but never canonicity). **FILED, NOT BUILT** (`TM-X4`), owed to the CI-gate owner: assert every policy name in the matrix exists in RLS §16.10, that the zero-policy **count** matches, and that the matrix's stated baseline is an ancestor of `HEAD` whose intervening commits touch no register it mirrors — the same shape as the `OFFLINE-VERIFY-v1` byte-identity gate, a property held by review that is buildable as a scan | DOC-FIX-APPLIED (the finding and the §14.1 baseline evidence are recorded; **the gate is filed, not built**) | matrix §14.1/§14.2 · §0.1 cell vocabulary (unchanged — no gate weakened) | P | NO (the scan; the corrected rows are MVP) |
| **C78** | **`org_admin` on the money plane is an INFERENCE presented as settled, and the two positions are held by the SAME document.** `PHASE_2_MONEY_AUTHORITY_SPEC.md` §3.4 denies `org_admin` all money authority (`D` on `kernel.payout` / `kernel.refund` SEL/EXEC) and **labels its own position `INFERENCE`**; **§10.1 row 35 of that same document grants `org_admin` `●` on the refunds order list and previously called the row *"unchanged"*.** Venue dashboard §5 row 35 shows the same `●`, and **VD §5.2 does not supersede row 35** — so the dashboard is faithfully inheriting the money spec and **the contradiction is internal to the money spec**, not between the two documents, which is sharper than it was filed. O-1 and O-3 name `org_owner` and `org_finance` and are **silent on `org_admin`**; neither position is a ruling. **Silence defaults to GRANT**: RLS §9.7 grants `org_owner/admin` `A(own-org orders)` SELECT on `venue.order` (the backing for row 35) and RLS §9.13 grants `org_admin` `A(own-org)` SELECT on `venue.settlement`, whose header carries gross/fees/refunds/net — so leaving this open does not stall the build, it **ships the grant silently** with a `D` sitting unread in §3.4. **That is the unsafe direction**: `org_admin` is the role most likely to be handed out liberally, and the remedy costs are asymmetric — widening later is a one-line matrix change, **narrowing later is a migration plus removing a capability operators have been using** — against a standing deny-by-default posture (RLS `GP-1`). **NO SIDE IS TAKEN AND NO RECOMMENDATION IS OFFERED.** `D-8` is deliberately the only row in the money spec's §11 register carrying **no** recommendation, because the tie-break is an authority question, not a design question. Recorded as money §11 `D-8` + new §11.1 (both positions in full), VD §22.13 and a §5.2 blocking banner, and `O11` below | **OPEN-GATED(O11)** — the admissible forms are stated; the choice is the named open decision | money §3.4/§10.1 row 35/§11 `D-8`/**new §11.1** · VD §5.2/§22.13 · authority silent: O-1, O-3 | P | NO — **BLOCKED.** Surface H's refund list must not be built for `org_admin` from either cell until `O11` closes |
| **O11** | **The open decision itself: does `org_admin` hold the money-plane read (refunds order list, and by extension the `D-4` settlement question)?** Admissible forms, both already stated in the corpus: **(A) deny** — money §3.4's reading, corroborated by Domain §7.2's Org Admin *Cannot* column and O-2's *"not unrestricted financial authority"*; **(B) grant** — money §10.1 row 35 / VD §5 row 35, corroborated by RLS §9.7 and §9.13, which grant today. **An owner call, not a design call**: both readings are defensible and the corpus cannot break the tie from inside, because the deciding inputs (O-1, O-3) are silent. **Blocks Gate P for venue-dashboard surface H.** Note the direction of the default — silence ships (B) — so *not deciding* is itself a decision for the over-provisioned side. **`D-4` (settlement, row 37) is a narrower sibling and should be closed in the same sitting, since (A) reopens it** | OPEN-GATED(O11) | money §11 `D-8`/§11.1 · VD §22.13 · this record C78 | P | NO |
---

**Remediation-reconciliation attestation (2026-08-27).** Four concurrent remediation passes changed ratified material and filed correction IDs that did not exist in this record. Those IDs are now recorded, **under their own names** — `H-2`/`H-3`/`H-4`, `AUTHZ-*`, `J-8`…`J-12`, `K-14`…`K-20`, `S-1`…`S-13` — so an implementer reading a defect ID in a spec can find it here by that ID rather than by a translation. **No prior row is renumbered**, and the two existing `O` namespaces are untouched; the new `RET-` prefix is deliberately not a third `R` series, because `R-1`…`R-18` (RPC §20.14) and `R-1`…`R-17` (role model) already collide with each other.

**Two entries are retractions of ratified reasoning rather than additions, and they are the reason this pass exists.** `RET-1`…`RET-5` record five security claims **deleted as false**, each with the reason and each also carried in its own file's correction index so it cannot be cited from an older copy. And **C56** records a fact about a ratified *decision*: ruling `OQ-5`/`OQ-W4` relaxed a ratified constraint **on two conditions, and verification found neither implemented** — one bound only the `ends_at IS NULL` branch while the common branch admitted an unbounded `exp`, and the other had a mechanism with no caller. Both are now specified and both gate the Wallet enable, but **the owner's sign-off on the relaxation is still owed**, and the relaxation's safety rests on exactly those two conditions.

**What this pass deliberately did NOT do.** It resolved **no** open decision — O6, O7 and O8 stand, and O9 and O10 are added, not closed. It made **no** owner decision; every one it found is listed above and left with its owner. It resolved **no** contradiction between specs — the ones it found are **recorded** where a reader will hit them: the `crm-export` per-function-vs-per-route `verify_jwt` contradiction (edge §9 item 17), the two divergent specifications of `venue.door_session` (SPEC_FOUNDATION §6), and RLS §2.2's helper count stated four different ways. It touched **no** SQL, **no** migration, **no** code, and nothing under `supabase/`. It edited only the four files it owns — the edge spec, the venue dashboard spec, this record, and the spec foundation — and every change it could not make in those four is named in the row that found it, with the owner who must make it.

**Independent verification performed by this pass, not inherited.** The `OFFLINE-VERIFY-v1` single-source claim of **C53** was checked rather than assumed: all **four** fenced blocks under `docs/architecture/**` were extracted and hashed by two independent methods, and all four are **byte-identical** — one normative source (edge §5.4.3) and three sanctioned mirrors, which sit in **two** other documents, not three, because the Wallet spec carries two of them. **The CI gate that enforces this now EXISTS, and this sentence previously said it did not — corrected as row `D13`.** It is `.github/workflows/ci.yml`, job `quality`, required check name **`Typecheck / Lint / Unit tests`**, step *“OFFLINE-VERIFY-v1 spec byte-identity gate”*. The property therefore holds **by construction**, not by inspection, exactly as edge §5.4.3 clause 3 claims. The gate scans **recursively** — every `*.md` under `docs/architecture/**`, `_governance/` and `_superseded/` included, because a mirror that lands in a subdirectory must be gated, not exempted by a flat glob — and fails closed on **seven numbered guards**: **1** an unterminated fence (every block after one is invisible to the scan) · **2** the tag at column 1 outside any fence — an unfenced copy is an ungated copy · **3** non-vacuity: fewer than `OFFLINE_VERIFY_MIN_BLOCKS` = **4** blocks found, a floor and never an equality · **4** the normative home (`PHASE_2_EDGE_FUNCTION_SPEC.md`, §5.4.3) no longer carrying a block, without which the mirrors would agree only with each other · **5** byte-identity across all copies · **6** anti-emptying: a body under `OFFLINE_VERIFY_MIN_BODY_BYTES` = **1900** — four empty blocks are byte-identical and four in number · **7** anti-coordinated-weakening: a ≥ 10-entry load-bearing-clause anchor list, itself guarded against heredoc truncation. Guard 7 is the one that would have caught **C53** itself: byte-identity proves the four copies agree and says nothing about *what* they agree on, and four copies that all drop conjunct `3b.v` are identical, four in number and full length. **This correction is `DOC-FIX-APPLIED`, not `RETRACTED`**: the retracted sentence was *true when written* on 2026-08-27 and was overtaken by the gate landing, whereas `RETRACTED` is reserved for text that was false when ratified. Stale in the safe direction is still stale in a constitution.

**Owner-ruling integration attestation (2026-08-27).** The DA and CDM edits authorized by rows O-1 … O-5 and D4–D8 are applied to the constitution bodies, tagged inline by ID, and carried into each document's Correction Index appendix. Specifically: DA §7.6 carries **one** money matrix — the O-1/O-3 corrected rows, with the payout-request and payout-destination rows no longer rendering as the same authority, and with `ᔆ` marking the SoD-constrained cells; DA §7.4 records the O-3 SoD-1 collapse as a ratified trade-off with its named mitigation rather than as an unqualified capability grant; the four pre-C36 bare-label lists are gone from DA and the §7.2 catalogue states display-name-vs-stored-label at its head; `set_org_payout_destination` reads `org_owner` only in DA §7.6, matching RLS §11; and both constitutions state attribution timing and the promoter's commercial terms in terms an implementation spec cannot quietly narrow. **What this pass deliberately did NOT do:** it resolved no open decision (O6/O7/O8 stand), renumbered no prior row, wrote no freeze record, and touched nothing outside the two constitutions, this record and `ARCHITECTURE_FREEZE.md`. Corrections owed by sibling integrators are named in the rows that found them (D7 → RPC §6.1 / RLS §9.17; D8 → schema §3.17).

**Consolidation attestation (2026-08-24).** Both constitutions now read correctly on their face: the ticket state machine draws no `refunded` terminal; the credential is asymmetric everywhere it is described, with the C33 key lifecycle in the body; the ownership-log idempotency rule is the three-column C26 key plus the per-sale terminal machine wherever stated; the inventory counter's authority (C27) is stated consistently in every counter/ledger passage; the SSCAS is enumerated, closed, and lock-ordered in both documents; and every Gate-M/L correction appears in the body as an explicitly-gated extension requirement, never as an MVP promise. Corrections remain tagged inline by ID for traceability to this record and to the four authority documents.
