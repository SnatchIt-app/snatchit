# Phase 2 — Ratification Record (C26–C98 · O6–O16 · O-1…O-5 · D1–D21 · RET-1…RET-6)

**Ratified 2026-08-24 by the pre-implementation consolidation session; extended 2026-08-27 by the owner-ruling integration pass (O-1 … O-5, D4–D8, C51, C52); extended again 2026-08-27 by the four-pass remediation reconciliation (C53–C72, D9–D12, RET-1…RET-5, O9, O10); extended again 2026-08-28 by the money-authority reconciliation (D13, D14, C75/O11); extended again 2026-08-28 by the **unwritable-control pass** (C91–C98, D21, O15, O16 — six controls whose physical substrate or writer did not exist).**
**Authority:** `docs/architecture/_superseded/PHASE_2_FINAL_ARCHITECTURE_AUDIT.md` (§4 correction table) · `docs/architecture/_governance/ARCHITECTURAL_RISK_REGISTER.md` (R1–R36) · `docs/architecture/_governance/IMPLEMENTATION_READINESS_SCORE.md` (gate composition) · `docs/architecture/_governance/CTO_DECISION_MEMO.md` (Gate P/M/L assignments).
**Effect:** the two constitution documents — `docs/architecture/SNATCH_IT_DOMAIN_ARCHITECTURE.md` (DA) and `docs/architecture/SNATCH_IT_CANONICAL_DATA_MODEL.md` (CDM) — were **consolidated so the body reads correctly without precedence rules**: every correction below is folded into the body text (tagged inline), stale contradictory prose was rewritten or deleted, and each document carries a Correction Index appendix. The 2026-08-27 owner-ruling pass extended the same treatment to O-1 … O-5 and D4–D8. **Every row below is in one of six states, five of which are terminal**: `OPEN-GATED` is not, and **six** rows (C50/O6, C51/O7, C52/O8, **C56/O9, C72/O10, C75/O11**) sit in it. The 2026-08-24 line "no correction is pending" was true then and is not restated.

**Statuses:**
- **RATIFIED** — constitution text final; the correction is implemented as part of the Gate-P path (before the first native ticket).
- **RATIFIED-MODELED-ONLY(GATE-M)** — constitution reserves the extension point as binding text; built before native resale + instant payout; NOT in the Phase-2 foundation.
- **RATIFIED-MODELED-ONLY(GATE-L)** — same, gated at pre-legal-scale (international / erasure claims / enterprise).
- **DOC-FIX-APPLIED** — a documentation-consistency defect, corrected in the constitutions by this session.
- **OPEN-GATED(Ox)** — the constitution states the admissible forms; the choice between them is the named open decision, which blocks its gate.
- **RETRACTED** — **added 2026-08-27, terminal.** Ratified or spec-canon text **deleted as false** — not superseded, not narrowed, not re-gated. The claim survives in no form; the reason is recorded with the row; the deletion is *also* carried in the correction index of the file that held it, so it cannot be cited from an older copy. **`RET-1`…`RET-6`.** See the retraction table in the remediation-pass section for why this is not `DOC-FIX-APPLIED`.

**Gates:** **P** = pre-native-issuance · **M** = pre-money-rail (native resale + instant payout) · **L** = pre-legal-scale · **—** = documentation.

> **Two `O` namespaces exist and they are NOT the same series. Read the hyphen.**
> **`O1` … `O8` (unhyphenated)** are the architecture **open questions** tracked in `docs/architecture/SNATCH_IT_DOMAIN_ARCHITECTURE.md` §0.4 and the risk register — e.g. `O3` = resale-policy snapshot drift, `O4` = per-event identity-verification strength, `O6` = cross-region native-resale form.
> **`O-1` … `O-5` (hyphenated)** are the **owner rulings** ratified 2026-08-27 — e.g. `O-3` = payout visibility/requests, `O-4` = door-manifest authority.
> `O3` ≠ `O-3` and `O4` ≠ `O-4`. Neither series is renumbered; the disambiguation is the fix. Recorded as row **D4**.

| ID | Correction (one line) | Status | Final constitution section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C26** | Ownership-log idempotency key = `UNIQUE(cause, cause_ref, ticket_id)` + per-`market_sale` terminal machine (`pending → completed XOR compensated` under the sale-row lock), restoring the double-transfer-impossible proof for one-cause→N-ticket flows and cross-object re-void | RATIFIED | DA §0.5 (C3 row), §9.4 · CDM §1.1 Ownership Log, §1.4 Market Sale, §3, §15 C26 | P | YES |
| **C27** | `remaining` single truth: the locked counter is authoritative operational truth; the movement ledger is the audit stream; a reconciliation job asserts equality; `credential_version` pinned to the ownership log | RATIFIED | DA §1.3, §4.1/§4.2, §2 (Part 4 + invariants), §5 ¶1.5 · CDM §1.1 Issued Credential, §1.3 Inventory, §15 C27 | P | YES |
| **C28** | SSCAS genuinely closed (members 10–15 added: event-cancellation cascade, dispute-resolution reversal, C25 auto-compensation, auction deposit-release, group-buy claim, wallet checkout); global lock order places every locked class incl. ascending-batch-id and money-plane rows; §12 sharding prose reconciled to the enumerated set | RATIFIED | DA §6.2, §3.10 · CDM §2, §12, §15 C12 (as completed) + C28 | P | YES |
| **C29** | First-class Reserve/Clawback object + payout-timing policy gating instant payout (the true form of old O1) | RATIFIED-MODELED-ONLY(GATE-M) | DA §10.5, §3.9, §1.5 · CDM §1.1 money-plane block, §15 C29 | M | NO |
| **C30** | Fan-side chargeback/clawback liability representable (receivable or Wallet negative-balance; physical form fixed by the Gate-M amendment) | RATIFIED-MODELED-ONLY(GATE-M) | DA §10.5, §1.5 · CDM §1.1 money-plane block, §15 C30 | M | NO |
| **C31** | Additive double-entry money-ledger schema beside the frozen Stripe core (home for C29/C30; balances splits/royalty/rounding structurally; custody log stays separate) | RATIFIED-MODELED-ONLY(GATE-M) | DA §10.5, §3.8, §1.5 · CDM §1.1 money-plane block, §15 C31 | M | NO |
| **C32** | First-class multi-currency at the frozen money-in boundary: currency on every money object (C13), FX capture-vs-payout timing, per-country payout currency, named rounding bearer | RATIFIED-MODELED-ONLY(GATE-L) | DA §10.5 · CDM §15 C32 (+C13) | L | NO |
| **C33** | Signing-key lifecycle: per-event default scope, KMS/HSM custody, audited rotation (one active signer per scope), compromise runbook, signer HA/throughput, public-key-only door distribution | RATIFIED | DA §10.4, §0.5 (C1 row) · CDM §1.1 Issued Credential, §5, §15 C33 | P | YES |
| **C34** | Provable erasure: per-identity DEK lifecycle incl. backup destruction, PII-sink inventory + purge, retained-graph re-identification mitigation, 7-yr financial-retention reconciliation (replaces bare "crypto-shred solves GDPR") | RATIFIED-MODELED-ONLY(GATE-L) | DA §8.7, §2 matrix (user row) · CDM §4, §15 C34 (+C15) | L (spec at P) | NO |
| **C35** | The kernel authorizes the buyer principal itself at the market→kernel seam; a market-supplied buyer id is never trusted; buyer re-verified against the payment | RATIFIED | DA §9.4, §5.2.3, §0.5 (C8 row), §2 · CDM §1.4 Market Sale, §15 C35 | P | YES |
| **C36** | Scope-qualified roles are structural: three disjoint per-plane label sets (org/venue/platform enums share no label), not a lint convention | RATIFIED | DA §0.4 A9, §7.1, §4 challenge resolution · CDM §1.3 Staff Role, §8, §15 C36 | P | YES |
| **C37** | Online door performs a live authoritative per-scan kernel read at the decision point; "dispute-free by construction" claim dropped (offline honestly shrunk-not-closed) | RATIFIED-MODELED-ONLY(GATE-L) — claim correction applied in the constitutions now | DA §10.4, §10.1, §16.4 · CDM §1.1, §6 Door Scanner, §12 caching, §15 C37 | L | NO |
| **C38** | Identity-merge grant reconciliation defined (per-class union rules, escalation impossible, conflicts fail closed narrower) + the merge decision itself dual-controlled | RATIFIED-MODELED-ONLY(GATE-L) | DA §8.7, §2 matrix · CDM §4, §15 C38 (+C15) | L | NO |
| **C39** | Comp/guest-list issuance above a per-staff threshold requires step-up + C9 live-table grant re-check | RATIFIED-MODELED-ONLY(GATE-L) | DA §7.5, §5 ¶1.6 · CDM §1.3 gated-extensions note, §15 C39 | L | NO |
| **C40** | `validation_callback` egress restricted to a static, platform-controlled allowlist (never provider/venue-supplied); CI asserts the adapter's kernel REVOKE | RATIFIED-MODELED-ONLY(GATE-L) | DA §13.2 · CDM §9, §15 C40 | L | NO |
| **C41** | MVP is no-re-entry GA: terminal `scanned` stands; re-entry is a named future change whose reserved extension point is the scan `direction` (`in`/`out`) attribute | RATIFIED (the decision + the hedge column; re-entry itself deliberately deferred) | DA §3.1, §1.3 scan row, §5 ¶1.8 · CDM §1.1 Ticket Atom, §1.3 Scan Event, §15 C41 | P (decision) | YES (decision + hedge) |
| **C42** | Optional-nullable seat/unit-row layer reserved now (unit-rows ≡ seats, reconciled with C4/C22); H6 "non-additive" holds only if the hedge is skipped; seat-map UX remains out of scope | RATIFIED (the hedge; seating build deferred) | DA §0.5 (C11 row), §5 ¶1.4 · CDM §1.1, §1.3, §13.5, §14, §15 C42 | P | YES (hedge) |
| **C43** | p2p `locked` overlay hard-TTL auto-unlocks (physical `expired` terminal; `requested` ≡ `initiated`; no `failed`); cancel-to-self exempt from the C6 freeze; freeze narrowed per-open-manifest-ticket | RATIFIED-MODELED-ONLY(GATE-M) — the physical state set is already spec-canon (schema §4.5 / migration 088 — package J, market native rail) | DA §3.7, §0.5 (C6 row), §10.6 · CDM §1.4 P2P Transfer, §15 C43 | M | NO |
| **C44** | Virtual-queue / waiting-room + bot-defense primitive before competitive on-sales | RATIFIED-MODELED-ONLY(GATE-L) | DA §5 ¶1.5 · CDM §1.3 gated-extensions note, §15 C44 | L | NO |
| **C45** | Table minimum-spend balance + at-the-room settlement modeled — until then that revenue is explicitly conceded off-platform (no bottle-service system-of-record claim) | RATIFIED-MODELED-ONLY(GATE-L) — pulled to the 2A-tables gate if bottle-service scope ships | DA §5 ¶1.4 · CDM §1.3 gated-extensions note, §15 C45 | L (2A-tables) | NO |
| **C46** | Fire-code occupancy distinct from sold capacity; cash box-office + auto-gratuity/tip-out settlement causes; door refunds require an authenticated staff principal (never a door PIN) | RATIFIED-MODELED-ONLY(GATE-L) | DA §5 ¶1.3/¶1.6/¶1.8 · CDM §1.3 (gated-extensions + Staff Role/Door PIN), §15 C46 | L (2B) | NO |
| **C47** | DR is a designed system: stated RPO/RTO, PITR/standby, drilled restores, snapshot rebuild budgets; "replayable" ≠ "recoverable"; no "recoverable" claim before it exists | RATIFIED-MODELED-ONLY(GATE-L) | DA Principle 17 · CDM §5, §15 C47 | L | NO |
| **C48** | Projection-rebuild retention floors: outbox compaction respects canonical-input retention; ephemeral-input projections marked non-rebuildable if not covered | RATIFIED-MODELED-ONLY(GATE-L) | DA §6.3 · CDM §7, §15 C48 (+C21) | L | NO |
| **C49** | Outbox hardening: poison-quarantine, partitioned/multi-drainer (no head-of-line blocking), specified region hand-off protocol (never an implicit 2PC) | RATIFIED-MODELED-ONLY(GATE-L) | DA §6.3 · CDM §15 C12 (envelope, as amended) + C49 | L | NO |
| **C50** | Cross-region native resale: the two admissible forms are a saga/escrow over the `paid_pending_transfer` window, or explicit intra-region-only scoping — the choice is O6; Miami single-region builds neither | OPEN-GATED(O6) — the forms are ratified constitution text; the choice blocks Gate M/multi-region | DA §0.5 (C8 row), §6.2, §0.4 open questions · CDM §12, §15 C50 | M/region | NO |
| **O6** | The open decision itself: saga/escrow vs intra-region-only for cross-region native resale — a joint commercial + technical call, made before multi-region resale | OPEN-GATED(O6) | DA §0.4 open questions, §6.2 · CDM §15 C50 | M/region | NO |
| **D1** | "The one sanctioned cross-aggregate transaction" phrasing (CDM §2 line 262 and kin) rewritten to SSCAS-member language; no one-seam/single-exception phrasing survives as current design in either constitution | DOC-FIX-APPLIED | CDM §2 (Market-Sale row: "an SSCAS member (#2)"), §0.10/§2 preamble, §12, §13.2 · DA swept clean | — | YES (applied) |
| **D2** | DA §3.1 ticket state machine redrawn with NO `refunded` terminal: money reversal = `voided` with cause `refund_void`; transition and illegal-move tables corrected; order/payment/external-transfer `refunded` states (different objects) untouched; §0 precedence note reduced to a historical note | DOC-FIX-APPLIED | DA §3.1 (diagram + both tables), §0.4 reconciliation note, §1-appendix challenge 4 | — | YES (applied) |
| **D3** | One canonical cause-code registry — `issue · primary_sale · comp · door_sale · p2p_transfer · market_sale · auction_sale · admin_action · refund_void · import · promoter_commission · settlement · chargeback` — identical verbatim in DA §5.2.1 and CDM §11; all other cause lists tagged as subsets | DOC-FIX-APPLIED | DA §5.2.1, §5.6, §9.4 · CDM §11 | — | YES (applied) |

| **E-1** | SSCAS enumeration alignment: CDM C12's fifteen-member list is canonical; SPEC_FOUNDATION §5 marked superseded-provenance; RPC §14.1/§14.2/§14.3 renumbered to C12 (adds #9 dispute-freeze, #11 dispute-resolution reversal, non-MVP #14/#15) | DOC-FIX-APPLIED (consistency review, 2026-08-25) | PHASE_2_SPEC_FOUNDATION §5 · RPC §14 · CDM §15 C12 | — | n/a (doc alignment) |
| **OBS-1** | Frozen-boundary clarification: **no column is ever added to `public.payments`** — native order/sale resolution = PI `metadata` + `kernel.payment_native` join | DOC-FIX-APPLIED (security review, 2026-08-25) | Edge spec §9.1 | — | n/a (doc clarification) |
| **A-2** | Administrative: PR #3 rebase-merge SHA mapping (tag `phase2-architecture-v1` anchors the original freeze SHAs; `main` carries byte-identical rebased equivalents cf7d6b9/e24989c/f66bf1d) + relocation of the frozen documents to `docs/architecture/` with path-reference rewrites (repository-hygiene PR) | DOC-FIX-APPLIED (administrative, 2026-08-25) | ARCHITECTURE_FREEZE.md Amendment A-2 | — | n/a (administrative) |

---

## Owner-ruling amendments — ratified 2026-08-27

**Authority:** owner rulings **O-1** (refund authority) · **O-2** (canonical role model) · **O-3** (payout visibility / requests) · **O-4** (door-manifest authority) · **O-5** (`door_open_at` lifecycle), ratified by the owner and designed to — not relitigated — in `docs/architecture/PHASE_2_MONEY_AUTHORITY_SPEC.md`, `docs/architecture/PHASE_2_ROLE_MODEL_SPEC.md`, and `docs/architecture/PHASE_2_DOOR_LIFECYCLE_SPEC.md`. **This record is the owner approval Rule 1 requires**; the rows below are what authorizes the corresponding edits to DA and CDM. Rows D4–D8 are documentation corrections applied in the same pass; C51 and C52 are constitution-vs-implementation contradictions recorded as open, deliberately not resolved here.

| ID | Correction (one line) | Status | Final constitution section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **O-1** | **Refund authority.** `org_owner` and `org_finance` may **initiate/request** refunds for their own organization's orders — but the mechanism is a **request door** (`kernel.request_order_refund`), never direct execution of the money writer: the RLS §7.10 denial of org roles is corrected toward the DA reading, while `kernel.refund_primary_order` narrows to definer + platform. The undefined "> micro" row is replaced by **three tiers** decided server-side from `catalog.platform_config` — auto-execute ≤ threshold · in-org dual control with `approver ≠ requester` · platform review beyond the org ceiling. `org_admin` holds **no** refund authority (read, request or approve). DA §7.2's "Inherits: Org Admin, Org Finance" is **deleted as a money mechanism** (`kernel.org_member.role` is single-valued under C36, so no `org_owner` row can ever satisfy `has_org_role([org_finance])`) — the *authority* moves to the `org_owner` label explicitly; the *inheritance* is removed. R7 money-single-path preserved: no new object writes a money row | RATIFIED | DA §7.2 (Org Owner "Inherits", Org Finance prose), §7.4 (SoD note), §7.5 (refund step-up row), §7.6 (three refund tiers + approve-request row + view-refund-ledger row + `ᔆ` legend) · CDM §1.1 Refund, §15 O-1 | P | YES |
| **O-2** | **Canonical role model.** O-2's eight role concepts are **display names**; C36 governs **stored enum labels** — the two-namespace reading DA §7.1 already states is confirmed correct and made unmissable rather than reversed. The canonical stored set is **fifteen plane-prefixed labels** in three disjoint enums: org (6) `org_owner · org_admin · org_finance · org_marketing · org_promoter_manager · org_member`; venue (6) `venue_manager · venue_finance · venue_box_office · venue_marketing · venue_promoter_manager · venue_scanner`; platform (3) `platform_admin · platform_support · platform_risk`. `venue_door` → **`venue_scanner`** (rename); **`venue_promoter` removed** — a promoter is a `promoter_link` row-ownership relationship, never a staff-role label; Ambassador stays a derived predicate and enters no enum. Standing rule **RM-1**: every role label MUST begin with its plane token | RATIFIED | DA §7.1, §7.2 (catalogue + Marketing "Cannot"), §7.3, §7.6 (column set), §1-object catalog, Principals table, ER diagram, §1.8 · CDM §1.3 Staff Role, §8, §15 C36 + O-2 | P | YES |
| **O-3** | **Payout visibility & requests.** `org_owner` **reads** the org payout ledger and **requests** payouts (RLS §7.9's SELECT denial was the wrong text; RPC §10.3 was already right); `org_finance` likewise. `org_admin` is DENY across the whole money plane. `venue_finance` is narrowed to **settlement-caused** payouts for its own venue — `kernel.payout` has no `venue_id`, so an unqualified "own-venue payouts" was never expressible. **Payout destination change is `org_owner` ONLY** (`org_finance` excluded) and is **not** the same authority as a payout request: it carries strictly stronger controls. **This ruling collapses SoD-1 by construction** — before it, `org_owner`-only destination change satisfied separation of duties automatically because owners could not disburse; O-3 puts both halves of the named fraud primitive ("redirect the bank account, then release funds to it") in one identity. Ratified **with** its compensating control, not as an unqualified grant: a **permanent** requester-vs-setter identity split (`kernel.request_org_payout` rejects `auth.uid() = payout_destination_set_by`, for that destination, forever — explicitly **not** a cool-down, which an attacker simply waits out), **destination probation** (first payout after a change is created `held`, released only by platform risk/admin), and **out-of-band notification** to every `org_owner`/`org_finance` including the actor. The existing cool-down is retained but demoted to a detection window, the weakest control in the set | RATIFIED | DA §7.2 (Org Finance prose), §7.4 (SoD-1 ratified trade-off + named mitigation), §7.5 (payout/bank-change step-up row), §7.6 (payout rows split; view-payout-ledger row; close-settlement row) · CDM §1.1 Payout, §15 O-3 | P | YES |
| **O-4** | **Door-manifest authority.** Opening and closing the door manifest, moving the door-freeze time (`catalog.event_session.door_open_at`), and changing event security configuration are authorized to **`org_owner`, `org_admin`, `venue_manager`** only, expressed inside the RPC predicate (org→venue inheritance, never by widening venue RLS). The scanner principal — the `venue_scanner` staff grant **or** the loginless device door session — may sync an already-open manifest, scan and admit, and may **never** create, move or clear the boundary; it holds no write path to `catalog.event_session`. `venue_box_office` receives no manifest administration, and the consequence is stated rather than left implicit: a person granted `venue_manager` for box-office selling thereby also gains manifest-open authority until per-capability scoping ships. Disabling a transfer freeze is `platform_admin` under step-up. **Admission is never gated on manifest state** — the manifest gates offline scanning only; gating admission on it would fail closed against paying fans at the door | RATIFIED | DA §7.2 (Door row), §7.3, §7.6 (scan/validate + door-lifecycle rows), §1.8 · CDM §1.3 Staff Role/Door PIN, §15 O-4 | P | YES |
| **O-5** | **`door_open_at` lifecycle.** The column is **written by nothing** in the frozen contract set, which makes the C6/C23/C43 transfer freeze — and the stale-pass safety property the design claims — false. Ratified form: `door_open_at` becomes the **cached monotone head of an append-only door-episode ledger** (`MIN(opened_at)` over the session's manifest opens), the same head-of-ledger pattern already ratified for `current_owner_id` and `credential_version` under C27. It is written only by the authorized open RPC, never by a client, **never cleared by close**, and **never moved backwards** — monotonicity becomes arithmetic, not a rule someone must remember; a re-open starts a new episode without moving the boundary. The effective freeze boundary is **total and fail-closed**: `LEAST(door_open_at, COALESCE(doors_at, starts_at) + configured offset)`, so a NULL `door_open_at` can no longer mean "never frozen". Override is explicit, elevated (`platform_admin`), TTL-bounded, reason-coded, audited, and never moves the boundary | RATIFIED | DA §3.7, §5 ¶1.8, §7.6 (door-lifecycle rows), §10.6 · CDM §1.2 Event Session, §1.3 Scan Event, §15 O-5 | P | YES |
| **D4** | **The two `O` namespaces are disambiguated, not renumbered.** `O1`…`O8` (unhyphenated) are architecture **open questions** (DA §0.4 + risk register); `O-1`…`O-5` (hyphenated) are **owner rulings**. `O3` (resale-policy snapshot drift) ≠ `O-3` (payout visibility); `O4` (per-event identity-verification strength) ≠ `O-4` (door-manifest authority). Both series keep their numbers; a standing note in this record, in DA §0.4 and in ARCHITECTURE_FREEZE.md states the distinction | DOC-FIX-APPLIED | this record (Statuses block) · DA §0.4 · ARCHITECTURE_FREEZE.md | — | n/a (doc clarification) |
| **D5** | **The pre-C36 bare-label role lists are purged from the C36 constitution itself.** Four sites in DA carried `owner/manager/finance/marketing/door/promoter_manager` — the exact strings C36 was ratified to abolish — in the object catalog, the Principals table, the mermaid ER diagram and §1.8. All four now carry the six plane-prefixed venue labels. §7.2's catalogue gains the **stored label beside every display name** and a header restating DA §7.1's rule (catalogue entries are display names; stored labels are the disjoint prefixed sets) — the existing reading is correct and is reinforced, never reversed. §7.2's Marketing "Cannot" row is corrected per O-2. CDM §1.3's Staff Role bullet and §15's C36 row carry the same finalization | DOC-FIX-APPLIED | DA §1-object catalog, Principals table, ER diagram, §1.8, §7.1, §7.2 · CDM §1.3 Staff Role, §15 C36 | — | YES (applied) |
| **D6** | **DA §7.6 collision resolved — one matrix, not two.** `PHASE_2_MONEY_AUTHORITY_SPEC.md` §12 items 5/6 supply a corrected 15-column §7.6 money block; `PHASE_2_ROLE_MODEL_SPEC.md` edit `D-6` instead **deletes §7.6 entirely** and replaces it with a pointer to ROLE_MODEL §5. Both cannot be applied. **Applied: the money spec's corrected matrix, in place, in the constitution. Rejected: ROLE_MODEL `D-6`'s delete-and-point.** Reason: ROLE_MODEL §5's own money block is explicitly *transcribed from the frozen corpus, not decided* (§15), carries two `⚠` undecided cells (B1 payout destination, B10 settlement close) and defers to `design/o1-o3-money-authority` — so pointing the constitution at it would delete the only ratified statement of O-1/O-3 money authority and re-open B1 on the day O-3 closed it. §7.6 keeps its matrix and gains a normative precedence note: stored labels are ROLE_MODEL §3; ROLE_MODEL §5 is the finer-grained 20-principal capability matrix and governs **non-money** capability detail; §7.6's **money** rows govern over ROLE_MODEL §5's money block | DOC-FIX-APPLIED | DA §7.6 (matrix + precedence note + `ᔆ` legend) | — | YES (applied) |
| **D7** | **Attribution write timing is made unambiguous, and the constitutions are the side that is right.** DA §1.7 states the attribution row is written **when an attributed order is paid**; RPC §6.1 and RLS §9.17 write it inside `create_order`. Attribution is an **immutable Ledger** (CDM §1.3) that carries a promoter's commission claim: writing it at order creation credits a promoter for an order that may never be paid, and the credit then cannot be deleted — only superseded. The constitutional statement stands and is restated in both constitutions in terms that cannot drift back ("in the same transaction that marks the order paid — never at order creation"). The implementation specs are wrong and must move | DOC-FIX-APPLIED (constitution side; RPC §6.1 / RLS §9.17 correction owed by the RLS/RPC integrator) | DA §1.7 (attribution row) · CDM §1.3 Promoter/Link/Attribution | — | YES (applied) |
| **D8** | **`venue.promoter` cannot express the commercial terms DA §1.7 ratifies.** The constitution ratifies commission terms that are **flat-per-ticket or %** (flat-per-ticket is the dominant nightlife term), a promoter **`tier ∈ {professional_invited, public_ambassador}`**, and a **`party_kind`** discriminator (`promoter` \| `affiliate`) so one attribution engine serves two party kinds. `venue.promoter` (schema §3.17) carries only `commission_bps` — percentage-only, no tier, no party kind. The constitutional requirement is restated as binding and the gap is named here; **the schema integrator adds the columns.** Nothing in the constitutions changes to match the narrower schema | DOC-FIX-APPLIED (constitution side; additive schema columns owed by the schema integrator) | DA §1.7 (promoter/affiliate rows) · CDM §1.3 Promoter/Link/Attribution | — | YES (applied) |
| **C51** | **The event outbox is promised by the constitution and scheduled by nothing.** DA §6.3 states that *"the only new infrastructure Phase 2 introduces is one outbox table and a drainer on the cron that already runs"*, and DA §6.2/§6.3 route every eventual-consistency flow through it; CDM C12's event-envelope guarantees (per-aggregate monotonic `sequence`, `causation_id`, `correlation_id`, at-least-once + idempotent consumers) and C28/C48/C49 all presuppose it, and the door, promoter and notification designs emit envelope messages into it. **No implementation spec schedules an outbox table, a drainer, or the envelope columns**: the sole occurrence of the word in the physical schema spec sits inside the Gate-L list (projection checkpoints, "+ outbox retention/compaction"), and the migration plan allocates no package. Both readings have a consequence and neither is chosen here — **(a) the constitution is right** ⇒ an outbox package is Gate-P/MVP work that is missing from the plan and every "eventual" flow in the constitution is currently unimplementable as specified; **(b) the implementation specs are right** ⇒ DA §6.2/§6.3 must stop claiming an outbox exists in Phase 2, C12's envelope guarantees have no carrier at MVP, and every design that emits an envelope message (door manifest, promoter attribution, notifications) needs a stated alternative transport. Open decision **O7** | OPEN-GATED(O7) | DA §6.2, §6.3 · CDM §2 (cross-aggregate channels), §7, §15 C12/C48/C49 | P (if (a)) / — (if (b)) | **OPEN — blocks the MVP scope decision** |
| **C52** | **The `notify` schema is RATIFIED Gate-P MVP in the constitutions and do-not-build in every implementation spec.** Row **C7** — RATIFIED · Gate P · MVP — is the `core` split that names the seven contexts `kernel · catalog · venue · market · social · analytics · notify`, and CDM §1.6 gives Notification a canonical object and identity (`NotificationID`). All four implementation specs place `notify` at Gate L / DO-NOT-BUILD (`PHASE_2_SPEC_FOUNDATION.md` §deferred; `PHASE_2_RLS_PERMISSION_SPEC.md` out-of-scope; `PHASE_2_PHYSICAL_POSTGRES_SCHEMA_SPEC.md` Gate-L extension list; `PHASE_2_SUPABASE_MIGRATION_PLAN.md` package K "documented extension points, NOT scheduled"). Consequence either way — **(a) C7 is right** ⇒ a `notify` schema is Gate-P work absent from the plan, and the four specs' deferral text is wrong; **(b) the specs are right** ⇒ C7's Gate/MVP columns are wrong for the `notify` context, MVP notifications continue to run on the existing `public.notifications` table plus the `send-push`/`notify-*` edge functions, and every Phase-2 design that names a `notify` consumer (money §10.3, door §12.2, promoter, notifications spec) must say which carrier it actually means. Not resolved here; **C7 is not amended by this pass** and keeps its ratified status until the decision is made. Open decision **O8** | OPEN-GATED(O8) | CDM header (C7 contexts), §1.6 Notification, §15 C7 · DA §6.2/§6.3 (notify consumers) | P per C7 / L per the implementation specs — **the contradiction is the row** | **OPEN — blocks the MVP scope decision** |

**Row counts by status (as of the 2026-08-27 remediation reconciliation):**

| Status | Count | Rows |
|---|:---:|---|
| **RATIFIED** | **45** | **C26**, **C27**, **C28**, **C33**, **C35**, **C36**, **C41**, **C42**, **O-1**, **O-2**, **O-3**, **O-4**, **O-5**, **C53**, **C54**, **C55**, **C57**, **C58**, **C59**, **C60**, **C61**, **C62**, **C63**, **C64**, **C65**, **C66**, **C67**, **C68**, **C69**, **C70**, **C71**, **C78**, **C79**, **C76**, **C86**, **C87**, **C91**, **C92**, **C93**, **C94**, **C95**, **C96**, **C98**, **C88**, **C89** |
| **RATIFIED-MODELED-ONLY(GATE-M)** | **4** | **C29**, **C30**, **C31**, **C43** |
| **RATIFIED-MODELED-ONLY(GATE-L)** | **12** | **C32**, **C34**, **C37**, **C38**, **C39**, **C40**, **C44**, **C45**, **C46**, **C47**, **C48**, **C49** |
| **RATIFIED-PENDING-REGISTRY-RE-RATIFICATION** | **2** | **C73**, **C74** |
| **OPEN-GATED** | **15** | **11 distinct open decisions, O6–O16.** Correction rows and their decision rows are listed together: **C50 + O6 → O6** · **C51 → O7** · **C52 → O8** · **C56 → O9** · **C72 → O10** · **C75 → O11** (precedence between delta specs) · **C77 + O12 → O12** (platform-plane grant maturity) · **C85 + O13 → O13** (`org_admin` on the money plane) · **C90 + O14 → O14** (the payout tier operand) · **O15** (account deletion under live custody) · **O16** (what `payout.status='paid'` asserts) · **D13** (the two `OWNER-DECISION-K2-*` items; its edge half is applied) |
| **DOC-FIX-APPLIED** | **30** | **D1**, **D2**, **D3**, **E-1**, **OBS-1**, **A-2**, **D4**, **D5**, **D6**, **D7**, **D8**, **D9**, **D10**, **D11**, **D12**, **D17**, **D13**, **D18**, **D14**, **D15**, **D16**, **D19**, **C80**, **C81**, **C82**, **C83**, **C84**, **C97**, **D21**, **D20** |
| **RETRACTED** | **6** | **RET-1**, **RET-2**, **RET-3**, **RET-4**, **RET-5**, **RET-6** |

**Total 114 rows** (44 before the remediation pass; **29 added** by it — C53–C72, D9–D12, RET-1…RET-5; then **four concurrent 2026-08-28 passes**: the missing-object repair (**C73, C74, D13**), the money-authority reconciliation (**D14, D15, C75**), the helper-set closure (**C76, C77, O12, D16**) and the offline-manifest projection (**C78, C79, D18**), plus the role-model edit-list pass (**D17, RET-6**) and the conditions pass (**C80–C87, O13, D19**), plus the refund-splitting / scan-delegation pass (**C88, C89, C90, O14, D20**) and the unwritable-control pass (**C91–C98, O15, O16, D21**). **99 are terminal** — recounted mechanically over the row table at every merge, never carried forward. **All four passes independently claimed the same next ids; the ids above are the post-merge assignment, and every cross-reference in every sibling document was repointed to match.**

**Pending, stated honestly.** 99 rows are terminal. **Eleven open decisions block a gate (O6–O16):** **O6** (cross-region native-resale form — carried from 2026-08-24); **O7** (the event outbox) and **O8** (the `notify` schema), both direct constitution-vs-implementation contradictions with a ratified statement on one side and four implementation specs on the other; the two recorded by the remediation reconciliation — **O9** (the `OQ-5`/`OQ-W4` Wallet relaxation, whose two grant conditions are now specified and gate the feature flag, but whose **owner sign-off is still owed**) and **O10** (the second structural amendment to the package DAG, pending re-ratification); and **O11** (**precedence between delta specifications — there is none, and row `C75` records the three admissible forms and the defect that absence already produced**). **The earlier "zero pending" claim does not survive, and is not restated.**

**Owner decisions carried by the remediation passes and NOT resolved here.** These are named so they are not lost between documents; each belongs to a sibling spec's own open-question table and none is decided by this record. Demographics **D-12** (R7 excludes comped and zero-price custody, so a genuinely free event never renders the mix card) and **D-14**; CRM **D-8** (platform bulk extraction not built in Phase 2), **D-13** (`display_name` consent-gated in the export — it changes what every export file looks like), and **D-6**/**D-10** (artifact retention 24 h vs 7 d, deliberately the same decision from two sides); schema **S-8** (were `org_marketing`/`org_promoter_manager` intended to be storable at org grain), **S-9** (`door.*` config visibility — the one line in the §2.4.1 ruling that could reasonably go the other way), **S-10** (`promoter_link.status` vs deactivating the promoter), **S-13** (`catalog.event_security_config` — schedule it, or rule the function out as `set_door_open_at` was); RPC **R-9** (the bid ledger's home), **R-10** (`venue.get_dashboard_summary`), **R-11** (two `INFERENCE — AUTHORED` contracts); RLS **X-7** (C43's narrowing is `GATE-M` while four documents describe it as implemented — **if the board wants it in MVP that is a new ratification, not a clarification**) and **X-8** (`kernel.approval_request`'s place in the global lock order: if a reviewer judges it an aggregate class rather than an intent record, **the parked-refund branch is a sixteenth SSCAS member and C28's closure needs a formal amendment**); and Wallet **OQ-W3** (Wallet may not ship before the door spec's M2 tables and step 3b are implemented and drilled — owner acknowledgement required) and **OQ-W6** (the `verify_jwt=false` set's security sign-off, **which should cover the set as a whole rather than one function**, `door-session` being the higher-risk member).

---

## Remediation-pass amendments — recorded 2026-08-27

**Authority:** four concurrent remediation passes, each carrying its own correction index, each of which changed material this record had already ratified: the **door/wallet** pass (`PHASE_2_DOOR_LIFECYCLE_SPEC.md` §13/§17/§19, `PHASE_2_APPLE_WALLET_SPEC.md` §11.10/§14/§15 — defects `H-2`/`H-3`/`H-4`, requests `DL-1`…`DL-6`, ruling `OQ-5`/`OQ-W4`); the **authz** pass (`PHASE_2_RLS_PERMISSION_SPEC.md` §17 `X-1`…`X-15`, `PHASE_2_RPC_FUNCTION_CONTRACTS.md` §20.14 `R-1`…`R-18` — defects `AUTHZ-C1A`/`C1A2`/`C1B`/`H5`/`H10`/`M1`–`M12`/`R1`–`R4`); the **demographics/CRM** pass (`PHASE_2_DEMOGRAPHICS_PRIVACY_SPEC.md` §16 `J-1`…`J-12`, `PHASE_2_CRM_EXPORT_SPEC.md` §14 `K-1`…`K-21`); and the **schema** pass (`PHASE_2_PHYSICAL_POSTGRES_SCHEMA_SPEC.md` §13.7 `S-1`…`S-13`).

**Why these needed rows at all.** Every one of the four filed correction IDs **that did not exist in this record**, and three of them changed or withdrew text this record had already marked terminal. Rule 1 requires owner approval for a change to ratified material; a correction index inside the spec that changed is a *record of what was done*, not the approval for doing it. These rows are that approval surface. **No prior row is renumbered**, and the four passes' own IDs are cited rather than replaced — an implementer reading `AUTHZ-H10` in the RLS spec must be able to find it here by that name.

> **A fourth ID namespace: `MP-n`, and why it is not `K-n`.**
> **`MP-1`** is the offline-manifest **projection** defect (rows **C78**, **C79**, **D18**). It is deliberately
> **not** `K-4`. `K-1`…`K-21` is already `PHASE_2_CRM_EXPORT_SPEC.md` §14's correction namespace, and `K-2`/`K-3`
> were separately used by the missing-object repair recorded immediately above — so `K-` is carrying two
> unrelated series before this pass adds anything, and `K-1` in particular is **taken**, by the CRM spec. A
> third meaning on the same letter is the `O3`/`O-3` mistake for the third time. Read `MP-` as a prefix.
>
> **A third ID namespace now exists, and it is the one most likely to be misread.**
> **`RET-1` … `RET-5`** are **retractions** recorded by this section (**`RET-6`** is recorded by the role-model edit-list section that follows it). They are deliberately **not** `R-n`: `R-1`…`R-18` is already the RPC spec's §20.14 request namespace, and `R-1`…`R-17` is separately the role-model spec's RLS-edit namespace. Three unrelated `R` series would be the `O3`/`O-3` mistake a second time. Read `RET-` as a prefix, not a letter.

### A sixth status is added, because four of the five existing ones assert the wrong thing

| Status | Meaning |
|---|---|
| **RETRACTED** | **NEW, terminal.** Ratified or spec-canon text that is **deleted as false**, not superseded, not narrowed, not re-gated. The claim does not survive in any form, the reason is recorded here, and the deletion is *also* recorded in the correction index of the file that carried it. |

**Why a new status rather than `DOC-FIX-APPLIED`.** A doc-fix corrects a document that disagreed with a decision. A retraction says **the decision's stated justification was never true**. Filing the five retractions below as doc-fixes would record that some prose was tidied, when what actually happened is that five load-bearing security arguments were found false and removed. The distinction is the whole point: **a reader who finds one of these claims in a stale branch, an older PR, or a printed copy must be able to establish here that it is dead** — which is exactly the failure mode that let *"second and last"* survive in four documents while a third unauthenticated surface shipped.

---

### The five retractions — ratified/spec-canon claims deleted as FALSE

| ID | Retracted claim (verbatim) | Where it stood | Why it was false | Recorded by |
|---|---|---|:---:|:---:|
| **RET-1** | *"You cannot difference aggregates that do not exist."* | `PHASE_2_DEMOGRAPHICS_PRIVACY_SPEC.md` §5.3 option B — the headline answer to differencing in general | **True about axes, false about populations.** The `(session, dimension, bucket)` aggregate very much exists, one instance per session, and an operator who **composes two populations** — by minting a session, by minting custody, or by choosing which two of their own sessions to compare — differences two aggregates that both exist. Fixed rollups defend against **slicing**, not against an adversary who controls the input set. R7/R8/R9 are the population-side defences; vector 7 records what is still not covered | **J-9 (1)** |
| **RET-2** | *"each fake data point costs a real ticket purchase"* | `PHASE_2_DEMOGRAPHICS_PRIVACY_SPEC.md` §11, the Sybil-accounts rebuttal | **A comp costs nothing, and the same `venue_manager` mints both the session and the comps.** Before R7 the operator could compose the entire population for free, so every *"an inferable group is at least 5 people"* bound assumed five people who could be manufactured. **R7 makes the rebuttal true; it does not make manufacturing impossible, only priced** | **J-9 (2)** |
| **RET-3** | *"Removing any one of layers 1–3 still leaves a correct floor"* | `PHASE_2_DEMOGRAPHICS_PRIVACY_SPEC.md` §5.2, the defence-in-depth claim | **Layers 1 and 3 were the same function.** The writer decides what to persist, and *"the RPC can only return what exists"* is not an independent check — it is a restatement of the writer's decision. **Only R2 was ever a database constraint.** Recorded with its own lesson attached: this is the class of claim a reviewer stops checking once it is written down | **J-9 (3)** |
| **RET-4** | the bound *"an anonymity set of at least 5, matching the per-bucket floor"* | `PHASE_2_DEMOGRAPHICS_PRIVACY_SPEC.md` §5.3 vector 4, the R6-era churn bound | **Pairs were conflated with people.** Five contributor-pair changes is **at minimum three people**, since an answer change costs two. The real bound was ≥ 3, not ≥ 5. The ≥ 5 bound is restored **only** by the added distinct-identity limb (churn ≥ 5 pairs **and** ≥ 5 distinct identities in `C₁ △ C₂`). The prior rule also quantified over **membership** while the buckets track **answers**, so a stable member editing their answer moved a bucket by 1 at zero membership churn | **J-9 (4)** |
| **RET-5** | *"the **non-consenting majority — every transferee, every comp, every purchaser who left the box unticked — is unjoinable**, which is exactly the population with no relationship to either venue."* | `PHASE_2_CRM_EXPORT_SPEC.md` §4.3, the `customer_ref` cross-tenant honest-limit statement — **and the §4.4 case (d) proof rested on it** | **`display_name` was emitted on every row of every export, at every org, ungated by consent**, from the one global `public.profiles.display_name` string. Two orgs union their files on the name column directly and corroborate with admission time, `first_seen_at`, ticket types and acquisition route. **The non-consenting majority was exactly as joinable as the consenting minority; only the carrying column differed.** The proof that cited it **was void as written**. Corrected claim: *the pseudonym removes the platform-supplied **stable** join key, and that is the whole of what it removes* | **K-18** |

**Standing consequence of RET-1 … RET-5.** Each deletion is recorded **in its own file's correction index as well as here**, so it cannot be cited from an older copy of that file. **A reviewer who encounters any of these five sentences in any branch, diff, or document should treat it as a defect in that copy, not as a position.** Two of them (**RET-2**, **RET-5**) were *load-bearing inside a proof*: the proofs are restated in their own specs against the corrected designs (R7; the `emit_name := emit_email` gate), and **assertion 13c now tests unjoinability itself** — the intersection of two orgs' non-blank name cells for a doubly-non-consenting identity must be empty — rather than testing the mechanism the old proof credited.

---

### Door / Wallet pass

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C53** | **The offline admission predicate was more permissive than the online one, in the document labelled BINDING.** Four documents each held an independently editable copy of the offline door check; one was corrected and the others were not, and **the copy that stayed wrong was the one a scanner SDK implements**. Edge §5.4.3 carried **two** conjuncts at 3b where **five** are required (atom ∈ M2 · no applied `revoke` delta · `credential_version` match · `ticket_state='active'` · **`resale_state='none'`**), plus 3c (`key_id` re-pin). Ratified form: the predicate is stated **once**, tagged **`OFFLINE-VERIFY-v1`**, with three sanctioned byte-identical mirrors (door §9.2, Wallet §2.3, Wallet §11.9) under a CI byte-identity gate over `docs/architecture/**`. **The fix is to the mechanism — four editable copies — not to the instance.** Conjunct 3b.v is load-bearing, not defence in depth: without it the offline door admits `paid_pending_transfer` and `refund_hold` atoms the **online** door refuses | RATIFIED — defect `H-2` | edge §5.4.3 (normative) · door §9.2 · Wallet §2.3, §11.9 | P | YES |
| **C54** | **The door session proved provisioning, not possession.** `kernel.assert_door_session(p_device_id, p_session_id)` read `venue.scan_device` and `venue.door_pin` and took **no PIN, no token, no nonce**; the role model posited a *"door session token — the bearer artifact the device actually holds"* and **no `door_session` object existed** in the schema spec, the migration plan or the package registry. So a device id arrived as an **untrusted parameter** on a `verify_jwt=false` function holding the service-role key, and **anyone knowing one live `(device_id, event_session_id)` pair reached admission unauthenticated** — on the one path where **RLS is bypassed entirely**. Ratified form: table **`venue.door_session`** (hashed token, device+session+PIN binding, server-max TTL, revoke-only forward transition, `token_hash` never client-readable by any role **including `platform_admin`**), an `assert_door_session` **signature change** returning the bound pair, `p_actor_device_id` derived from that return value on `record_scan`/`reconcile_offline_scans`, and a **`revoke_door_pin` cascade** — without which revoking a PIN leaves live bearer tokens behind, which is this same defect one level up | RATIFIED — defect `H-3` | edge §3.9a · schema §3.10a · door §17 · plan `086` | P | YES |
| **C55** | **The Wallet build context's entire authority was an auth-token compare.** `get_wallet_pass_build_context` would rebuild and serve a pass for a **superseded** pass row or a holder who is **no longer the current owner**. Ratified form: liveness preconditions (`status='issued'` ∧ holder = live current owner, rebuild at `credential_version_at_build`); `list_updated_wallet_passes` gains `p_auth_token` and becomes registration-scoped and liveness-filtered — **it was the one endpoint in the design returning *many* serials and it was unauthenticated by contract**; and the `wallet.apple.enabled` kill switch is corrected to gate **serve / rebuild / register / push**, not minting alone, because *a switch that protects only the people who do not have the feature yet is not a kill switch* | RATIFIED — defect `H-4` | Wallet §11.6a, §11.6b, §11.5a · edge §3.10, §3.11 | P | YES |
| **C56** | **Ruling `OQ-5`/`OQ-W4` relaxed a ratified constraint on TWO conditions, and NEITHER was implemented when it was granted.** The ruling grants the session-bounded Wallet token profile — correctly, since a short-TTL barcode baked into `pass.json` locks a paying fan out of a dead zone — but it grants it **conditionally**, and verification found both conditions unmet. **(1) The offline-window bound:** the ratified cross-config invariant `wallet_default_span + wallet_exp_skew <= door.manifest_ttl_interval` constrains `wallet_default_span`, which applies **only when `session.ends_at IS NULL`**. On the common branch it **bound nothing**, and a long or mistyped `ends_at` produced an **unbounded `exp`** — a fat-fingered year mints a multi-month bearer credential. The invariant was checked against the constants an operator sets and **never against the number the signer emits**. Now discharged by a **clamp on the computed value** applied at sign time (Wallet §5.2a), with the constants invariant demoted to explicitly **necessary-but-not-sufficient**. **(2) Key revocation force-closing open door episodes:** the mechanism existed (door §8.2.1, envelope #44 already carried `key_revoked`) and **`kernel.revoke_signing_key` was specified nowhere as invoking it** — a key-table update and nothing more. The ruling's own words: *"Without this I would reject DL-4."* Now written as a normative same-transaction obligation in edge §5.6. **Both conditions now gate the feature flag: Wallet §13 items 10a/10b block `wallet.apple.enabled := true`.** **The owner is signing off on a relaxation whose safety rests on these two, and the sign-off is still owed** | **OPEN-GATED(O9)** — the *conditions* are ratified and specified; the **relaxation itself is not yet owner-signed** | door §16 OQ-5 · Wallet §5.2a, §13 items 10a/10b, §15 OQ-W4 · edge §5.6, §5.5 | P (Wallet enable) | **OPEN — blocks the Wallet enable** |
| **D9** | **The `verify_jwt=false` set was characterized outside its enumeration, twice, in two different grammars.** Four documents said *"the second and last"* such surface while a **third** unauthenticated surface was arriving; that instance was corrected to a five-member enumeration in edge §7. This pass found the **same defect in a different form** still standing: edge §2's deployment note asserted *"Only Stripe/KMS-webhook endpoints run `verify_jwt=false`"* — a **count-by-category**, false against §7's own list, in which only `stripe-webhook` is a Stripe/KMS webhook. Ratified rule: **§7 is the only place the set may be characterized at all — by count, by category, or otherwise** — and `door-session` is named there as **the highest-risk member**, since it relays admission while holding the service-role key with RLS bypassed behind it. Its security sign-off is owed **alongside** `wallet-pass-webservice`'s, not after it | DOC-FIX-APPLIED — `EDGE-1` | edge §2, §3.11, §7 · Wallet §6.1, §15 OQ-W6 | — | YES (applied) |

### Authz pass

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C57** | **The approval tier had no stored discriminator, so every parked refund reached the org arm.** `kernel.approval_request` returned `pending_approval` / `pending_platform_review` as **result statuses** and stored neither, leaving `approve_refund_request` to branch on `state`/`action`/`org_id` alone. Ratified form: **`required_approver_class`** (`NOT NULL`, `CHECK IN ('org','platform','platform_admin')`), **server-set at request time and pinned like `config_versions`, never a parameter**; plus `CHECK (state <> 'approved' OR approved_by IS NOT NULL)` — **without which the SoD CHECK is vacuously satisfiable by any writer that forgets the column**, and *"SoD as a table constraint, not a convention"* is aspiration rather than fact; plus the `subject_kind` and `action ↔ subject_kind` pairing CHECKs; plus index `(action, required_approver_class, state)`. `config.set_money_key` additionally requires a **second distinct `platform_admin`** | RATIFIED — `AUTHZ-C1A`, `C1A2`, `M1`, `M2` | RLS §11.3, §16.1 · RPC §17.1/§17.2/§17.7, §20.1 · **schema §1.13 + plan `077` owed (X-10/R-16)** · **`PHASE_2_MONEY_AUTHORITY_SPEC.md` §6.1, §6.2, §6.6, §7.3, §9.2, §12 — TARGET SET AMENDED by row `D15`, applied by row `D14`; this row's original target set omitted the money spec, which is the document that DEFINES the approval object, and the omission is why that spec still stated the defect as its contract for a full remediation cycle** | P | YES |
| **C58** | **The money-plane SoD counterparty is mintable by the very role that needs one.** Both money SoD primitives compare `auth.uid()` against a stored identity, and an `org_owner` holds the grant authority to **mint the second identity through the ordinary invite flow**. Ratified form: **grant maturity** — `kernel.money_role_grant_matured(org_id)` (new predicate helper) rejects a caller whose money-role grant is younger than `authn.money_role_maturity_hours`, requiring `kernel.org_member.granted_at`, written by `accept_org_invite` and **reset by `change_org_role` on promotion *into* a money role**. **It binds the destination-SETTER as well as the requester — applied to one half of a pair it is applied to neither** — and is **never** applied to deny or cancel. This is the control that makes **O-3's** ratified SoD-1 collapse survivable in practice rather than only on paper | RATIFIED — `AUTHZ-C1B` | RLS §11.3a, §2.2 · RPC §17.1/§17.2/§17.7, §20.7 · **schema `kernel.org_member` + plan `077`/`078` owed (X-11/X-12, R-17/R-18)** · **`PHASE_2_MONEY_AUTHORITY_SPEC.md` §6.1, §6.7, §6.7a, §8.2, §8.5, §9.2, §12 — TARGET SET AMENDED by row `D15`, applied by row `D14`; the money spec is where SoD-1 (§8.2) and SoD-2 (§6.2) are stated, so a maturity conjunct absent there is absent from the statement of the very primitives it protects** | P | YES |
| **C59** | **Two RPCs wrote one append-only ledger under contradictory authority, and every product surface was wired to the permissive one.** `venue.review_attribution_flag` admitted **both promoter-manager labels**; `venue.decide_flagged_attribution` denied them — *"the fox at the henhouse"* — for the same ledger and the same release/deny decision. **The restrictive one was decoration**: `venue.attribution_review` is append-only with **effective decision = `max(seq)`**, so the conflicted party never needs to overwrite anything — **they append `release` at `seq+1` and it is the effective decision.** And the permissive function is the one the dashboard, the promoter spec and migration plan `090` all name; the restrictive one was named by RLS §11.5 and ROLE_MODEL §11 R-16 **and by no product surface and no package** — the function nobody would have built, cited as the reason the control existed. **Ruling: `venue.decide_flagged_attribution` is DELETED, not aliased; `venue.review_attribution_flag` survives and takes the restrictive allow-list**, which was the load-bearing half. **Nothing is left without a writer** — `090` never named the deleted one | RATIFIED — `AUTHZ-H10` | RLS §11.5, §17 X-14 · RPC §17.18, §20.13, §20.14 R-13 · dashboard §20A.1 (applied) · **ROLE_MODEL §11 R-16 + §12 row 17 owed** | P | YES |
| **C60** | **§11's EXEC authority table had drifted from the role model it is downstream of, and no assertion could see it.** A lexical `venue_door → venue_scanner` rewrite across §11 **preserved capabilities §5 was simultaneously removing**, because no clause said §5 governed §11. Ratified form: the **`EXEC-DERIVED`** rule plus `T-RLS-EXEC-01`, which make the derivation mechanical; the `venue.set_door_open_at` EXEC row **replaced** with `catalog.set_session_door_schedule` (the function it granted **did not exist**, and the row was the reason it would have been built); fourteen contracted functions given the grant class they lacked; fifteen `PROPOSED AUTHORITY` sites ruled on (fourteen accepted, `get_dashboard_summary` **deferred, not accepted**); and the four guest-list rows rolled up | RATIFIED — `AUTHZ-H5`, `R1`–`R4` | RLS §11.0, §11.1a, §11.1b, §11.1c, §11.4, §16.11 | P | YES |
| **C61** | **Twelve medium authority defects, of which four are absent-config fail-OPEN.** Ratified as one row because they share one shape: **a threshold that gates an authority and does not exist is not a gate.** `comp.per_staff_step_up_max_units` / `_window_hours` (**C39 cited in five documents with no key anywhere** — an implementer reads NULL, compares `quantity > NULL`, gets NULL, and **no comp at any quantity requires step-up**, which is unmetered comp issuance on the capability the domain architecture names *"the insider-fraud primitive"*); `refund.platform_support_max_minor`; `authn.money_role_maturity_hours`. **Standing rule: an absent key evaluates to the SAFE extreme — no grant is mature / every comp needs step-up / support approves nothing — never the permissive reading**, and the tests **delete the config row rather than set it to zero**, because a missing seed and a seeded `0` behave identically only if the `COALESCE` is actually there. Also: `step_up_unavailable` **raises** rather than evaluating on an absent freshness claim (`M4`); the promoter's own-row predicate, whose first correction was itself broken (`M10`); the promoter's `venue.attribution` cell narrowed from `A` (`M9`); the door's **positive** completeness assertion — *"the gate is in every place it must be"* — which on a path with no RLS behind it is the load-bearing half (`M6`); the export builder's Layer-0 grant, which as written **would have shipped every export with a blank contact column** (`M11`); and a scope constraint on the platform arm of `list_attendees` (`M12`) | RATIFIED — `AUTHZ-M1`…`M12` | RLS §9.17, §11.1, §11.3, §11.3b, §11.6, §16.1, §16.10, §16.11 · RPC §20.4.1, §20.5.1, §18.2 | P | YES |

### Demographics / CRM pass

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C62** | **The privacy floors were proved against a population the operator controls.** Ratified form adds four rules and a kill switch: **R6** — a suppressed snapshot returns the constant **`{suppressed: true}`** and nothing else (the prior shape returned denominators with **no floor on them**, so a `venue_manager` could mint a throwaway session, comp one ticket to a target, and read `holders_responded ∈ {0,1}` as that person's *did-you-answer* bit); **R7** — population eligibility, custody acquired **for consideration** only; **R8** — churn redefined over the contributor multiset `(identity, bucket)` **with a distinct-identity limb**; **R9** — cross-session near-duplicate gate; a fully determined R3 merge with a two-bucket minimum in R5; and a fail-closed **read-side re-derivation** of R1/R2/R4/R5/R6. **Recorded consequence:** a genuinely free event never renders the card, which is correct and is a real product loss (**owner decision D-12**) | RATIFIED — `J-8` (with `RET-1`…`RET-4`) | DEMOG §4.1, §4.3, §5.1–§5.5, §8.4, §10.2, §10.4, §11, §13 · dashboard §9.5 (applied) | P | YES |
| **C63** | **A timestamped history of a person's gender answers was specified in the same section that names it the worst artefact the feature could produce.** The fan-side audit row `(identity_id, action ∈ {set, changed, cleared}, occurred_at)` **is deleted**: *"never the value"* does not cover the harm — the row **records a transition**, and §3.2 had already rejected the same object by name. No audit table was named, so the only fitting home was `kernel.admin_audit` — **permanent and platform-readable** — which would have nullified the §8.5 tombstone's purge window, the sole mitigation offered. `set_my_demographics` and `clear_my_demographics` now write **no audit row of any kind**; the aggregate **read** audit on `venue.get_holder_mix` is unchanged and remains binding | RATIFIED — `J-11` | DEMOG §8.1, §8.3, §10.4, §13 assertion 26 | P | YES |
| **C64** | **The strictest erasure case was the one case a post-restore purge could not re-apply.** Account deletion **cascaded the demographic row away without writing the §8.5 tombstone**. Ratified form: the tombstone is written by a `BEFORE DELETE FOR EACH ROW` **trigger**, so **every** removal path produces one — withdrawal, cascade, definer delete, future C38 merge — and `kernel.identity_demographic_erasure.identity_id` is deliberately **FK-free**, because a CASCADE FK would delete the tombstone in the very statement that creates the need for it. Assertion 25 narrowed from *"zero triggers"* to *"exactly two, neither referencing a gender column"* — asserting the property the proxy stood for | RATIFIED — `J-12` | DEMOG §8.2, §8.5, §10.2, §10.4, §12(a), §13 assertions 25/27/29/30 | P | YES |
| **C65** | **The export's tenant predicate was bound at org grain only, and a venue changing hands leaked the previous org's customer list.** `catalog.venue.org_id` is **mutable** while `catalog.event.org_id` is stamped at create, and the isolation traversal is downward `org → venue → event → session → ticket → holder`, so a **venue-grain export at a new operator reached every historic session of that venue**. Ratified form **XO-1a**: `kernel.tickets.org_id = :job_org_id` at **every** grain, `:job_org_id` resolved once at request time and **frozen on the job row**; the `customer_ref` HMAC key and the consent gate's `EXISTS` both pinned to the **job's** org, not the atom's — the atom binding would have given two orgs the **same** pseudonym for the same person, joining their files directly. Assertions 18a–18c are asserted **per grain**, because a single-grain test passes while three branches leak | RATIFIED — `K-14` | CRM §1.4, §4.1–§4.4, §5.1, §11.2, §11.4, §12 | P | YES |
| **C66** | **The download re-check read the role set and never the template, so marketing could download a colleague's money export.** `template_id` was **not mentioned in the download contract at all**, while `operations_v1`'s request-time allow-list is the narrowest in the matrix — so a `marketing` role, which holds the job-list read, could take a `job_id` and download order refs, order totals, unit prices and refund state. **§3.1's own invariant — *"Finance sees money and no contact. Marketing sees contact and no money. Neither sees both."* — was defeated by any org that had ever run one operations export, with no grant being wrong.** Ratified form: the download re-evaluates `assert_may_request(actor, job.scope, job.template_id)` — **the same predicate a fresh request would face** — asserted as an **equality between the request and download predicates** so the two cannot drift | RATIFIED — `K-15` | CRM §3, §3.1, §6.2, §6.7 EX-4, §7.4, §11.4, §12 · dashboard §9.6 (applied) | P | YES |
| **C67** | **Nothing in the design could delete a Storage object, so retention, sweep and revoke all had NO AGENT.** Revoke said it *"deletes the artifact, effective immediately"*; `venue.revoke_export` said it *"signals the edge to delete"*; the sweep said it *"deletes artifacts past retention"* — and `crm-export` had exactly two routes, **neither a delete**. **A `SECURITY DEFINER` Postgres function cannot call the Storage API**, and its only in-DB option, `DELETE FROM storage.objects`, drops the metadata row and **orphans the bytes** — strictly worse than doing nothing, because the object survives while every accounting says it is gone. **The ratified *"the lake is bounded by a 24-hour sweep"* defence was therefore unimplementable as specified.** Ratified form: **`POST /purge`** on `crm-export` (`pg_cron` + `pg_net`, 15-minute cadence, the pattern migrations 014/032/034 already use) — **the only Storage delete agent in the entire design** — plus `artifact_state`/`purge_lease_until`/`purge_attempts` on `venue.export_job`, three definer `service_role` RPCs, and a **daily two-directional orphan reconciliation** reconciling bucket against job table **in both directions**, without which the 24-hour bound is a statement about rows and **rows are not what leaks**. Revoke's honest bound restated as **`min(300 s, time-to-purge)`, not zero** | RATIFIED — `K-16` | CRM §6.2, §6.6, §9.2, §11.1, §11.2, §11.4, §11.5, §12 · edge §2, §3.7, §7, §8 (applied) · **plan `087` schedule owed** | P | YES |
| **C68** | **The name-prefix lookup had no rate limit, so the printed list the design refuses could be reassembled one record at a time.** The limit table carried rows for `email_exact` and `order_ref` and **none for `name_prefix`**, and `venue.lookup_attendee`'s contract scoped its limit to `email_exact` **explicitly**. `venue_box_office` holds that read, so iterating `a…z`, `aa…zz` against one session returned the roster **at no rate cost** — rebuilding *"a box office cannot print a paper list. That is deliberate."* from the surface meant to replace it, **by the role denied it**. Ratified form: a `name_prefix` limit (20/actor + 60/org per 24 h), a **3-character minimum raised before the lookup and without consuming budget**, and **multi-match as an explicit `ambiguous_query` carrying no rows and no count** — because **the count is the harvest** (`"sm"`→14, `"smi"`→9 reconstructs the name distribution without returning a record). **Per-org caps added for every lookup kind.** Audit records kind **and outcome**, never the probed string; a run of `ambiguous`/`rate_limited` is the sweep signature and the only evidence of one | RATIFIED — `K-17` | CRM §7.1, §7.2, §7.2a, §7.4, §11.4, §12 | P | YES |
| **C69** | **The name column was the global cross-tenant join key the pseudonym existed to withhold.** Ratified form: **`emit_name := emit_email`** — one predicate, evaluated once per holder row, driving **both** cells, so they cannot disagree and nobody can gate one and forget the other. `display_name` stays **ungated on screen**, in the single-record lookup and in the door projection, where it is already readable and **where a surface cannot be unioned with another org's**; it is gated **in the export**, because *"once a name is in a CSV it is a durable, unioning join key over a population that never chose to have a relationship with the org holding it."* **The gate goes on the egress, not on the knowledge.** `name_cells_emitted`/`_suppressed` join the audit pair; the suppression legend covers both columns. **Operator cost recorded rather than hidden** (owner decision **D-13**): an `audience_v1` export over a heavily transferred session is mostly `customer_ref` and ops columns with name and email blank on the same rows | RATIFIED — `K-18` (carries **RET-5**) | CRM §2.2 field 2, §4.3, §4.4 (a)/(d), §5.1, §8.3, §11.2, §12 · dashboard §9.6 (applied) | P | YES |
| **C70** | **Six CRM mediums, of which the first falsifies a determinism property the whole replay design rests on.** (1) **The consent gate's evaluation instant was unspecified** and `identity_contact_pref` had no history, so a **paged** build necessarily evaluated its conjuncts at **inconsistent instants** — falsifying byte-identical determinism, assertion 22, and the replay property that is the entire reason per-export membership is not stored. Fixed by `gate_as_of` (stamped at claim, re-stamped on re-claim) plus **two append-only event logs** making both mutable conjuncts as-of evaluable. (2) The Layer-0 builder grant **as written returns zero rows** — `auth.users(id, email)` was not in the enumerated set — so **every export would have shipped a blank contact column reading as "nobody consented"**; a blank-column canary is added. (3) `finalize_export` **accepted the gate counters as worker-supplied parameters** for numbers §5.1 calls *"the only evidence the consent gate ran"* — **evidence the caller hands you is not evidence about the caller**; they are now accumulated inside the definer by the code that evaluates each cell. (4) The platform branch of `list_attendees` had **no scope constraint** at 12 000 rows/hour across every session on the platform. (5) Per-org caps for every lookup kind. (6) **Five roles hold both the roster read and the mix card**, so the demographics floor of 5 is a bound over five people the reader can **name** (**D-14**) | RATIFIED — `K-19` | CRM §4.5, §5.1, §6.3, §7.1, §10.1, §11.1, §11.2, §11.4, §12 | P | YES |
| **D10** | **The roster and the mix card count different populations on purpose, and the identity that reconciled them was false.** R7 excludes comp-caused and zero-price custody from `holders_total` while the CRM roster deliberately keeps comped atoms **on** the roster — both correct. `COUNT(holder view) ≡ holders_total` was therefore wrong. Restated as **`COUNT(holder view) ≡ holders_total + holders_excluded_ineligible`**, and **that is why `holders_excluded_ineligible` is stored at all**: the gap between the list an operator counts and the denominator on the card beneath it must be a **stated number**, not a discrepancy they are left to guess at before concluding one surface is broken. The assertion's fixture **must contain comps**, so it cannot pass vacuously, and it fails if **either** side drifts | DOC-FIX-APPLIED — `K-20` | CRM §1.3, §12 assertion 3 · DEMOG §5.2 · dashboard §9.5 (applied) | — | YES (applied) |
| **D11** | **The canonical table inventory was missing eleven tables, and the standing count of the miss was itself stale.** `SPEC_FOUNDATION` §6 — the file every implementation spec is told to take its names from — lacked objects that four specs had already contracted, packaged and written pgTAP against. `SCOPE_AMENDMENT` **X-14**/**R-6** record the miss as **eight** (four `J-4` + four `K-8`); **`K-19` has since taken the CRM set to six** by adding the two consent event logs, and `venue.door_session` (`H-3`) is an eleventh. **Anyone reconciling against X-14's number still finds two missing and concludes §6 is wrong.** Corrected to **eleven** and applied. Additionally, §4 C36 still carried the **pre-O-2** label lists naming `venue_door` and `venue_promoter` — **the exact strings O-2 abolished** — the same defect row **D5** purged from the constitutions, surviving in the naming authority itself | DOC-FIX-APPLIED — `SF-1`, `SF-2` (from `J-4`, `K-8`, `H-3`, `F-1`…`F-3`) | SPEC_FOUNDATION §4, §6 (applied) · **RLS §6 deny-all rows owed** | — | YES (applied) |

### Schema pass

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C71** | **`catalog.platform_config` was blanket public-read, publishing every control ceiling to `anon`.** RLS §8.4 states *"values are not secret; every non-admin principal including `anon` holds SELECT"*. Ratified form: a **two-class visibility model** — `anon`/`authenticated` see `visibility='public'` only; **`restricted`** is `is_platform([platform_admin, platform_risk])` plus the definer RPCs. Money, `authn.*` and `crm.*` are `restricted` on settled arguments. **Until §8.4 changes, package `078` publishes every dual-control ceiling, step-up window and export cap to `anon`** — including, now, the comp step-up threshold and the money-role maturity window that C57/C58/C61 introduce | RATIFIED — `S-4` | schema §2.4.1 · **RLS §8.4 owed** | P | YES |
| **C72** | **The schema-security remediation is a SECOND structural amendment to a ratified package DAG, and it is pending re-ratification.** It adds `venue.door_session` (a structural table in `086`, per C54) and recommends a **new dependency edge `086 → 087`** (`DAG-3`). The package registry carries the block **`⚠ SECOND AMENDMENT PENDING RE-RATIFICATION — schema-security remediation`**, closing *"Owner ratification required, per rule §6.5"*. **Recorded here so the pending state is visible from the ratification record rather than only from the registry** — a pending amendment that only the amended document knows about is the shape of every defect in this section | **OPEN-GATED(O10)** | registry re-ratification block · schema §13.6 `DAG-3`, §3.10a · plan `086`/`087` | P | **OPEN — blocks the package DAG re-ratification** |
| **D12** | **Twelve integrator requests filed by the schema pass, recorded so none is lost between owners.** `S-1` approval-tier writers · `S-2` `APPR-SUBJ-1/2` soft-subject integrity relocated into the RPC layer, **because the polymorphic subject has no FK by necessity** (three subjects, three packages) · `S-3` grant-maturity predicate binding **both halves of the pair** · `S-5` `assert_door_session` token parameter — **a signature change to the door's entire authorization surface** · `S-6` `record_scan`/`reconcile_offline_scans` device id **derived, never client-attested**, since on the `service_role` door path RLS is bypassed and the device id would otherwise be an unauthenticated string selecting whose scans are written · `S-7` `set_door_open_at` → `catalog.set_session_door_schedule` in §11.4, ~~**the last place the ruled-out function survives**~~ **[this clause is RETRACTED as false — `RET-6`; it survived in three further places, one of them an active build instruction]** · `S-11` `venue.set_scan_device_status`, whose `retired` state **had no writer** and which is the kill switch for a lost scanner · `S-12` `market.respond_offer` must reject past `expires_at` **arithmetically**, because an accept path trusting `status='pending'` consummates an expired offer **every time the cron tick is late, which is the ordinary condition of cron** · `S-8`, `S-9`, `S-10`, `S-13` are owner rulings, carried in this record's pending list | DOC-FIX-APPLIED (recorded; **each is owed by a named sibling integrator**) | schema §13.7 `S-1`…`S-13` | — | n/a (routing) |

---

## Role-model edit-list pass — recorded 2026-08-28

**Authority:** a targeted remediation of `docs/architecture/PHASE_2_ROLE_MODEL_SPEC.md`, a **delta spec covered
by Rule 1** (`ARCHITECTURE_FREEZE.md`, *"covered by Rule 1 from the moment they are ratified into the
record"*). It changed material this record had already ratified — the O-2/O-4 design document — and it
**retracts a claim this record itself makes**, so a correction inside that spec's own text is a record of
what was done, not the approval for doing it. Row **D17** is that approval surface; **`RET-6`** is the
retraction.

**Why this pass existed at all.** `PHASE_2_ROLE_MODEL_SPEC.md` declares itself **upstream authority** —
*"This file supersedes the role-set definitions in the RLS spec §1.1/§2.1 … §11 lists every edit an
integration pass must apply"* — and `PHASE_2_RLS_PERMISSION_SPEC.md` §11.0 confirms the direction from its own
side (*"§11 IS DOWNSTREAM OF THE ROLE MODEL"*). **Four remediation passes ran and none of them edited it.**
Four requests were filed *at* it and none was applied *in* it, so its §11 edit list — the instruction set an
integration pass follows — still ordered three deleted-or-blocked functions built. **An upstream document that
every downstream pass reads and no downstream pass may edit is exactly the document that goes stale
silently.**

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **D17** | **The role model's own edit list was never reached by any remediation pass, and it still ordered three abolished functions built.** Four filed requests are now applied: **`R-13`/`X-14`** — drop **`venue.decide_flagged_attribution`** from §11 `R-16` and §12 row 17 (`AUTHZ-H10`: *"two functions writing one append-only ledger under opposite authority meant the deny-list stopped nothing — effective decision is `max(seq)`"*; rebuilding it from `R-16` restores exactly that, and the surviving `venue.review_attribution_flag` carries the restrictive allow-list); **`R-14`** — **add RLS §11 to §5.3's supersession clause**, the omission `AUTHZ-H5` names as its own mechanism; the role-model half of **`S-5`** — the **token-bearing four-argument `assert_door_session`**, corrected at all **six** occurrences from the canonical RPC §1.1d text; and the role-model half of **`S-7`** — `venue.set_door_open_at` is **ruled out** and the O4-3 capability re-homed to `catalog.set_session_door_schedule` (`AUTHZ-R1`: a *sole-writer property with two writers*). **Three further corrections fall out and are recorded rather than assumed:** (a) new **§5.0** states the supersession order as a **strict, acyclic** order and **narrows** the DA §7.6 claim to its **non-money** rows **per row `D6`** — §11.7's edit **`D-6` is marked ❌ REJECTED in place**, so the rejected instruction can no longer be executed by a reader who never sees `D6`; (b) §11's **stale line-number baseline (`11ea2eb`) is deleted, not refreshed** — every instruction now anchors by section id and quoted text, because a re-baseline is correct for one commit and silently wrong afterwards; (c) **all 55 §11 rows and 38 §12 rows carry a disposition verified against the current text of each target**, which is what makes the file trustworthy as an instruction set again. **Two questions are recorded and NOT decided:** **`OD-11`** (`venue.set_event_security_config` writes per-event door-config rows and no such table exists in any package — schema `S-13` / RPC `R-21`; while it stands open, `R-16` must not instruct an EXEC row and `086` must not schedule it) and **`R-18`** (RLS §2.2 states its helper count four different ways; `T-RLS-ROLE-02` cannot be written until one number wins — the RLS owner's call) | DOC-FIX-APPLIED — carries **`RET-6`** | `PHASE_2_ROLE_MODEL_SPEC.md` §0 evidence convention · §5.0 · §6.2 · §6.3 · §7.2 · §8.1 · §11.0 · §11.2 `R-16`/`R-18` · §11.4 `P-5` · §11.7 · §12 · §13 `OD-6`/`OD-11` | — | YES (applied) |

### The sixth retraction

| ID | Retracted claim (verbatim) | Where it stood | Why it was false | Recorded by |
|---|---|---|:---:|:---:|
| **RET-6** | *"the last place the ruled-out function survives"* — of `venue.set_door_open_at`, asserted of RLS §11.4 | **this record, row `D12`** (the `S-7` clause), restating `PHASE_2_PHYSICAL_POSTGRES_SCHEMA_SPEC.md` §13.7 `S-7` (*"§11.4 is the authority table and is the last place the ruled-out function survives"*) and `PHASE_2_SUPABASE_MIGRATION_PLAN.md` `086`'s ruled-out block (*"the last place the stale row survives"*) | **It survived in three further places, and the first of them was an active build instruction.** `PHASE_2_ROLE_MODEL_SPEC.md` §11 **`R-16`** told the RLS owner to **add** an EXEC row for `venue.set_door_open_at`; §12 **row 14** classified it `NEW RPC`; and `PHASE_2_IMPLEMENTATION_TRACEABILITY_MATRIX.md` **`G-14`** still carries it as an open gap. The claim was not a small over-statement: **it is the sentence that told three integrators the function was dead everywhere, and it was written into the record on the same day the role model was still ordering it built.** The two role-model instances are corrected by **D17**; **`G-14` is NOT, and is owed by the matrix owner** | **D17** |

**Standing consequence of `RET-6`.** The sentence *"the last place the ruled-out function survives"* is
**false in every copy that carries it**, including the two sibling specs named above, which this pass read and
did not edit. **A reviewer who encounters it in any branch, diff or printed copy should treat it as a defect
in that copy, not as a position** — the same rule `RET-1`…`RET-5` carry. Its lesson is narrower and worth
stating separately: **"this is the last occurrence" is a claim about a whole corpus made from inside one
file, and nothing checked it.** The corpus-wide form of the claim is checkable and the per-file form is not,
which is why `T-RLS-EXEC-02` (*no label in any §11 predicate is outside the fifteen*) is the right shape and a
prose *"last place"* is not.

**What this pass deliberately did NOT do.** It resolved **no** open decision — O6…O10 stand, and `OD-11` is
**added**, not closed. It decided **no** money authority: §5.3's `B1`/`B10` `⚠` cells and `OD-3`/`OD-4`/`OD-5`
are untouched and remain with the money-authority pass, and the DA §7.6 money carve-out is transcribed from
row `D6` rather than reasoned about here. It changed **no** authority cell anywhere in §5.3, §8.1 or §8.3 —
every finding was about *which function carries an authority*, never about *who holds it*, so `F1`, `F2`,
`F4` and `G5` read exactly as ratified. It touched **no** SQL, **no** migration, **no** code, nothing under
`supabase/`, and no `OFFLINE-VERIFY-v1` block. **It edited two files: `PHASE_2_ROLE_MODEL_SPEC.md` and this
record.** Everything it found in a file another integrator holds is named in the row that found it, with the
owner who must make it: **`G-14`** → the traceability-matrix owner (`RET-6`); **`R-18`** → the RLS owner; the
**`S-`/`D-` edit-id namespace collisions** (§11.7) → whoever owns the corpus-wide id scheme, the same hazard
this record already documents for the `R-` series and avoided with the `RET-` prefix.

---

## Missing-object repair — recorded 2026-08-28 (`K-2` / `K-3`)

**Authority:** an adversarial review of the Phase-2 design corpus that grepped the three authorities for physical existence — `PHASE_2_PHYSICAL_POSTGRES_SCHEMA_SPEC.md`, `PHASE_2_SUPABASE_MIGRATION_PLAN.md` §8 and `PHASE_2_PACKAGE_REGISTRY.md` — for every object the corpus contracts, and found **two tables and one function set that every authority agreed existed and no authority created.** The repair is recorded here because `PHASE_2_PACKAGE_REGISTRY.md` rule §6.5 says *"this registry is updated only by ratified amendment"* and rule §6.2 requires a numbered row in this file for any change to the `076`–`091` band. **This section is that approval surface.** No prior row is renumbered; the `C` series continues at **C73**, and the `D` series at **D13**.

> **These two rows are the same defect class as `C72`, one level down.** `C72` records a *table with no package*. `C73` records a **table with no package, no columns, no PK, no FK and no index — asserted as existing by four documents at once.** The mechanism that hid it is the same one in both cases: **`plpgsql` bodies are not validated at `CREATE FUNCTION`**, so a chain referencing a relation nothing creates **replays green**, and every gate in CI passes.

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C73** | **`kernel.identity_contact_pref_event` and `kernel.org_contact_consent_event` were contracted by four documents and created by none.** `PHASE_2_SPEC_FOUNDATION.md` §6 lists both in the canonical table inventory **and assigns them packages `077` and `082`**; `PHASE_2_CRM_EXPORT_SPEC.md` §5.1 introduces them as the fix that makes the export consent gate as-of evaluable and §11.1 schedules them as elements `5a`/`5b`; `PHASE_2_RLS_PERMISSION_SPEC.md` §6/§16.6 puts them in the six-table CRM deny-all set and §16.10 inside the **closed twelve-relation** `crm_export_builder` grant set; `PHASE_2_RPC_FUNCTION_CONTRACTS.md` §20 has `set_my_contact_prefs` and `grant_`/`withdraw_org_contact_consent` **write a row into them in the same transaction.** They returned **zero hits** in the schema spec, in plan §8 and in the registry. **Two failure directions, and the silent one is worse.** Loud: the chain replays green and the first fan who sets a contact preference raises `42P01`. Silent: `crm_export_builder` is a role **subject to RLS**, so a missing gate source yields no consent row for anybody, suppresses **every** contact cell, and emits a syntactically valid CSV whose contact column is uniformly blank — which reads as **"nobody consented,"** not as "denied" — while `finalize_export`'s invariant `cells_emitted + cells_suppressed = holder row count` **balances perfectly at `cells_emitted = 0`**, so nothing in the pipeline detects it. RLS §16.10 states this explicitly: omitting them *"reproduces the zero-rows failure this ruling exists to close."* Ratified form: **full physical definition** (columns, PK, FKs, CHECKs, indexes, AO posture, RLS posture, write authority), **placed exactly where `PHASE_2_SPEC_FOUNDATION.md` §6 assigned them** — `077` and `082` — with SEAM-1 confirming both and independently **flooring** `org_contact_consent_event` at `082` (its `source_order_id` FK targets `venue.order`). **`GATE-DEFAULT-1` is ratified with it:** the no-row as-of read resolves to **`'allow'`** for the master switch and to **NOT granted** for the per-org consent, and the asymmetry is load-bearing — one convention for both either suppresses every cell for every fan who never opened Settings, or emits contact for orgs nobody consented to | **RATIFIED-PENDING-REGISTRY-RE-RATIFICATION** — registry fourth amendment | schema §1.15 / §1.15.1 / §1.15.2 · plan §8 `077`, `082` · registry §2 (`077`,`082`), §2.1, JSON | P | **YES** |
| **C74** | **Nothing in the design deleted exported attendee PII, and two of the three places that should have said so still did not.** `venue.claim_artifacts_for_purge`, `venue.confirm_artifact_purged` and `venue.reconcile_export_orphans` are contracted in RPC §20 and specified in `PHASE_2_CRM_EXPORT_SPEC.md` §11.1 element `19a` / §11.4. The final reconciliation pass added them to plan §8/`087`'s Functions row, closing the **plan** half. **The registry named them nowhere** (§2's `087` row and the JSON `scope` both said only *"the three purge-agent definers"*), and **the physical objects they claim rows through were undefined** — `venue.export_job.artifact_state`, `purge_lease_until` (**distinct from `lease_until`, which is the build's**), `purge_attempts`, and the **`(artifact_state, expires_at)`** index that is `claim_artifacts_for_purge`'s only access path, which CRM §11.2 specifies and plan §8/`087`'s Indexes row omitted. CRM §11 calls `POST /purge` *"the only thing in this design that deletes bytes"*: without this substrate `revoke_export` and `sweep_expired_exports` only flip the **job**'s state while the CSV of attendee names and email addresses persists in the `crm-exports` bucket indefinitely — the *"accounting says it is gone"* failure §11 was written to close. Ratified form: schema **§3.18** defines the substrate and **§3.18.1** the three definers and the tick that reaches them, traced end to end (`revoke_export \| sweep_expired_exports → delete_pending → claim → Storage remove() → confirm → deleted`, with the daily orphan pass closing it in the other direction); the registry **names** all three; plan §8/`087` gains the missing index. **`087` additionally gains `venue.assert_may_request`** — the single request/download authorization predicate (RPC §17.22), named by `R-7` in the same breath as the three definers and scheduled by nothing: **two independent implementations of one authorization predicate is how a download outlives the authority that granted it.** SEAM-1 for all four: `max(087, 077) = 087`, and `087` already declares `077`, so **no edge is created** | **RATIFIED-PENDING-REGISTRY-RE-RATIFICATION** — closes RPC §20.14 **`R-7`** (`087` arm) and CRM `K-16`'s registry/schema halves | schema §3.18, §3.18.1, §13.1 · plan §8 `087` · registry §2 (`087`), JSON | P | **YES** |
| **D13** | **Two dependency edges promoted from prose to declaration, and two owner decisions recorded rather than taken.** **Edges — `077 → 082` and `078 → 082` (`‡`).** Only the first is *forced* by `C73` (`kernel.org_contact_consent_event.org_id` FK → `kernel.organization`); the second is a co-located pre-existing under-declaration corrected in the same pass, because fixing one half of a two-edge under-declaration and leaving the other is worse than fixing neither. **Both were already named in plan §8/`082`'s own Dependencies prose** (*"`081` (ticket_type), `078`, `077`"*) while all four declared sets said `{081}`. **Declaration-only:** `077 < 078 < 081 < 082`, so ordering was never wrong, no package is added or renumbered, no object moves, no rollback changes; what changes is that the machine-readable graph agrees with the human one. This is the **fourth** instance of the shape rule §6.6 / SEAM-1 exists to catch, after `079 → 085`, `085 → 088` and `086 → 087`, and it is resolved identically. **Edge count `36 → 38`, verified in all four declared surfaces.** Registry §2.2 gains a **second acceptance property** stating that those four surfaces must enumerate the *same* set, exactly — the property whose absence let three of the four instances survive. **Owner decisions, NOT taken** (registry §7.1): **`OWNER-DECISION-K2-D3`** — `D-3`'s outstanding cascade sign-off now spans **six** relations rather than four, and silently widening an unresolved sign-off is the shape rule §6.5 exists to stop; **`OWNER-DECISION-K2-READ`** — whether the subject may read their own consent *history* (as opposed to current state) is stated by no document, CRM §5.3's *"a consent record is the person's own evidence"* argues **for** a read path, and none exists. Both close with the strict posture and neither blocks the amendment. **One further under-declaration observed and deliberately NOT applied:** schema §13.6 asserts *"`086` already declares `078`"* — it does not (`086`'s declared set is `{079, 080, 081, 083}`). The claim is true **transitively** (`086 → 081 → 078`) and `078 < 086`, so no acceptance property is violated and no object is at risk; it is recorded because `086`'s edge set is ratified material outside the `K-2`/`K-3` remit | **DOC-FIX-APPLIED** (edges) · **OPEN — owner** (the two `OWNER-DECISION-K2-*` items) · **OPEN — recorded, not applied** (`086 → 078`) | plan §2 mermaid + §3 seq 7 · registry §2.1, §2.2, §7.1, JSON · schema §13.6 `DAG-4`/`DAG-5` | P | edges **YES**; the two owner items are rulings, not builds |

**Missing-object-repair attestation (2026-08-28).** Ground truth was established by grep **before** any edit, per file, and is recorded so the repair is checkable rather than assertable. **Before:** `kernel.identity_contact_pref_event` — CRM 8 · RLS 5 · RPC 3 · SPEC_FOUNDATION 2 · **schema 0 · plan 0 · registry 0**; `kernel.org_contact_consent_event` — CRM 9 · RLS 5 · RPC 4 · SPEC_FOUNDATION 2 · **schema 0 · plan 0 · registry 0**; each of the three purge definers — CRM 4–7 · RPC 3 · RLS 2 · edge 2 · **plan 1 (added by the final reconciliation pass — the plan half of `R-7` was already closed) · schema 0 · registry 0**. **`venue.assert_may_request` — plan 0**, in every pass. **After:** every one of the six objects is named in all three authorities. **This pass touched only design documents** — the schema spec, the migration plan, the package registry and this record. It wrote **no** SQL, authored **no** migration, changed **no** code and did not touch `.github/workflows/ci.yml`. It **renumbered no package**: the band is `076`–`091`, sixteen packages, each number used once, verified by parsing the registry JSON. It made **no** owner decision; the two it found are in registry §7.1 with their owner. **The four-way edge-set parity property of registry §2.2 was checked mechanically after the edits, not asserted:** the plan's §2 mermaid, the plan's §3 rollout table, registry §2.1 and the registry JSON `depends_on` each enumerate the **same 38 edges**, every dependency **strictly precedes** its dependent by package number, and all ten `+`/`†`/`‡` "why this edge exists" rows resolve to a declared edge. **What this pass deliberately did NOT do:** it resolved no conditional (`COND-A`, `COND-B`, `COND-C` stand), closed no prior open row, applied no correction to `086`'s under-declared edge set, and did not extend `D-3`'s scope on its own authority — it recorded that the scope has changed and left the sign-off owed.
## Offline-manifest projection pass — recorded 2026-08-28 (`MP-1`)

**Authority:** adversarial review of the `OFFLINE-VERIFY-v1` predicate against the contracts that deliver its
inputs, filed as **`MP-1`** (a new prefix: `K-1` is already the CRM export spec's, see the namespace note above). **A sibling pass recorded the same day** — the missing-object repair (`K-2`/`K-3`, rows C73/C74/D13) — **is independent of this one**: it repairs objects that were contracted and never defined, this one repairs a contract that was defined twice, differently, and could not compute the predicate it feeds. Neither touches the other's sections. **The title line and the status counts at the head of this record are updated by this pass for BOTH**, because the sibling added its three rows without updating them and a count that disagrees with its own table is how a row goes missing. **Rule 1 applies and this section is the approval surface it requires:** `C53`
ratified the predicate and its single-source mechanism as terminal, and the rows below **change the contracts
`C53` depends on** — a correction index inside a spec is a record of what was done, not the authority for
doing it. `C53` is **not amended, not renumbered and not re-opened**: the predicate's text is untouched, all
four fenced blocks remain byte-identical, and the CI gate is unchanged. What is corrected is the delivery of
its inputs, which `C53` assumed and no document stated.

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C78** | **`C53` ratified a predicate the device could not compute, because no contract delivered its inputs.** The five-conjunct offline door check was stated once, mirrored byte-identically and CI-gated — and **both** contracts that put M2 on the wire were missing different inputs to it, so three of its clauses were **dead at the client**. DOOR §7.5's entry projection carried no **`resale_state`** (3b.v dead — the offline door admits `paid_pending_transfer` and `refund_hold` atoms the **online** door refuses, which is **`H-2` reproduced at the client, failing in the admitting direction**); RPC §20.6.1's carried no **`ticket_state`** (3b.iv dead) and no **`session_id`** (conjunct 3 and the no-M2 clause have no input); and §20.6.1's **delta** row carried no **`signing_key_id`** (3c dead for **every atom supplemented after doors open**) — directly contradicting the CHECK DOOR §10.3a added *because* *"a supplemented atom with no pinned key would be structurally unadmittable offline"*. **The write side was defective too:** DOOR §6 step 7's snapshot INSERT omitted `resale_state`, a column §10.3 declares `not null` — so the defect stood at **write, wire and read**, with only the store correct. Ratified form: **one** wire shape stated identically in DOOR §7.5 and RPC §20.6.1; the delta row **op-conditional** (`add` carries the full entry payload, `revoke` carries `{seq, ticket_atom_id, op}`) in the same asymmetry §10.3a's CHECKs already encoded; and **DOOR §7.5a**, a binding superset rule — *every field the predicate reads appears in the entry projection and in the `op='add'` delta projection* — with the read set **derived from the fenced block, not transcribed beside it**. **The fix is to the mechanism — a predicate and its transport specified in different documents with nothing binding them — not to the instance**, exactly as `C53`'s was | RATIFIED — defect `MP-1` | DOOR §6 step 7, §7.5, **§7.5a (new)**, §7.7, §10.3, §10.3a, §10A.2, §15 group **P** (77–83), §17 · RPC §9.3, §20.6.1, §20.14 `R-22` · WALLET §10.2 item 2a, §13 item 11a · RLS §8 · RN §7.3, §11 · EDGE §5.4.2 · schema §13.1 · plan `086` · registry §2.1 · matrix `G-15` | P | YES |
| **C79** | **The offline clock-skew tolerance was cited in eight places and defined in none.** `OFFLINE-VERIFY-v1` conjunct 3a reads *"`now() <= token.exp`, ± 2 time-buckets"* and names **RPC §9.3** as the definition site; §9.3 said *"±2 time-bucket skew"* and defined no width, and neither did EDGE §5.4/§5.4.3, DOOR §9.2/§14, WALLET §2.3/§5.2 or DA §10.4. **A tolerance with no stated width is not implementable** — two scanner builds each pick a number and admission differs by vendor, at the one check that is supposed to be identical everywhere. Ratified form: **a time-bucket is `30 seconds`, so ±2 buckets is ±60 seconds**, stated in RPC §9.3 because the fenced block routes the reader there and the block is not editable from a downstream document. It is a **fixed protocol constant, deliberately not a `catalog.platform_config` key**: signer and long-offline verifier must agree, and a runtime-tunable value is read by the signer now and by the device only at its last sync — lowering it strands passes already in wallets, raising it widens a replay window on devices that will never learn the new value. Changing it is a change to `OFFLINE-VERIFY-v1` (EDGE §5.4.3 first) plus a scanner-SDK release, **not** a config edit. **`INFERENCE — AUTHORED`:** the corpus fixes the shape and never the width, so the magnitude is authored rather than derived, and is recorded as authored | RATIFIED — the **rule** (fixed constant, not config; versioned with the predicate) is ratified and not open; the **magnitude** is filed for owner confirmation as RPC §20.14 `R-22` | RPC §9.3, §20.14 `R-22` | P | YES |
| **D18** | **`G-15` was closed twice, independently, and two authoritative answers were worse than none.** The traceability matrix filed `venue.get_door_manifest` as uncontracted; RPC §20.6.1 then wrote a contract **while DOOR §7.5 kept its own**, leaving one function with **two** result shapes and **three** parameter names (`p_since_version` · `p_since_seq` · `p_since_delta_seq`) — and the first names the wrong quantity, since `manifest_version` counts *episodes* and `seq` counts *deltas within* an episode, so a device passing one where the other is expected re-downloads or skips silently. The no-open-episode return had diverged the same way: `{status:'no_open_manifest'}` in one document, `{open:false}` in the other, with live consumers branching on each. Resolved: **`p_since_delta_seq`** canonical, **both** return keys emitted so neither consumer changes, and `G-15`'s matrix row now carries the generalisation — **check whether the sibling document also filled the gap before declaring it closed**, which applies to every remaining `S2` row | DOC-FIX-APPLIED | matrix `G-15` · DOOR §7.5, §7.7, §15 #69 · RPC §20.6.1 | — | YES (applied) |

**Two additive schema consequences, both inside package `086` and inside the ratified `076`–`091` band.**
`venue.door_manifest_entry.ticket_type_id` (RPC §20.6.1 projected a column **no table carried**, so the read
returned a value with no source and the digest covered a column that did not exist), and
`venue.door_manifest_delta.ticket_state` / `.resale_state` / `.ticket_type_id` with five `op='add'` CHECKs.
**No package is added, renamed, reordered or renumbered**, and the new `ticket_type_id` FK → `venue.ticket_type`
rides the **already-declared** `081 → 086` edge — checked against the registry rather than assumed, so it is
not a fourth instance of the `SEAM-1` undeclared-edge shape. **`C72` is untouched:** this pass amends no
package DAG and adds no edge, so it does not compound the second amendment pending re-ratification.

**What this pass deliberately did NOT do.** It **did not touch any `OFFLINE-VERIFY-v1` fenced block** — all
four remain byte-identical, one distinct body,
`sha256 afb5184d58b62da5cb03cb8c4c7923953b4206c52f8afa23dee6403069fe6344`, 2017 bytes, 34 lines, verified by
extracting and hashing the four blocks after **every** edit rather than once at the end. It **weakened no CI
gate, ratchet or floor** and changed no workflow file. It **renumbered no migration package and no prior row
of this record** — the new pgTAP assertions are appended as group **P** (77–83) rather than inserted, so
assertions 71–76 keep their numbers. It **resolved no open decision**: O6, O7, O8, O9 and O10 stand exactly as
they were. It made **no owner decision about authority** — no row changes who may do what; the one judgement
call it did make (the time-bucket magnitude) is a numeric tolerance, is decided rather than left dangling
because an undefined tolerance is unimplementable, and is filed for confirmation as `R-22`.

## Money-authority reconciliation — recorded 2026-08-28

**Authority:** rows **C57** (`AUTHZ-C1A`, `C1A2`, `M1`, `M2`) and **C58** (`AUTHZ-C1B`), both already **RATIFIED · Gate P · MVP-must-implement YES**. **No new design decision is taken by this pass and no owner decision is made.** It applies two already-ratified corrections to the one covered document the authz pass did not sweep, amends that pass's own target columns so the omission is visible from the ledger, and records the precedence gap that let the omission survive.

**Why these needed rows at all.** The authz pass named `RLS §11.3, §16.1 · RPC §17.1/§17.2/§17.7, §20.1 · schema §1.13 + plan 077` as C57's targets and an equivalent set for C58. **`PHASE_2_MONEY_AUTHORITY_SPEC.md` appears in neither** — although it is the document that introduced `kernel.approval_request` (its §6.6, cited by schema §1.13 as the object's origin), that states both money SoD primitives, and that is covered by the freeze under Rule 1 as an owner-ruling delta for O-1/O-3. **Nothing in this ledger therefore tracked the specification that defines the approval object**, and that document went on carrying the pre-remediation text — a `BUILD-READY DELTA SPEC` stating the C-1a defect as its contract. Rule 1 requires owner approval for a change to ratified material; the correction index now inside that spec (its §13) is *a record of what was done*, not the approval for doing it. **These rows are that approval surface.** **No prior row is renumbered**, and the amendments to C57/C58 are made **in those rows, tagged with the amending row id (`D14`)** rather than silently rewritten.

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **D14** | **The document that DEFINES the approval object was excluded from the remediation that fixed it, and still stated the defect as its contract.** `PHASE_2_MONEY_AUTHORITY_SPEC.md` — `**Status:** BUILD-READY DELTA SPEC`, covered by the freeze, cited as live authority by RLS §11.3, RPC §17.2 and schema §1.13 — contained **zero** occurrences of `required_approver_class`, `money_role_grant_matured`, `granted_at`, `money_role_too_new` or any `AUTHZ-*` tag. Its §6.2 branched the approval authority on **`pending_approval` vs `pending_platform_review`**, the two strings C57 records as *"§17.1's return statuses … not stored"*, so **an implementer working from that file builds a branch that routes every parked refund to the org arm — including the above-ceiling and consumed-atom cases**, which is the collusion shape `MD-6` exists to surface. Its §6.6 defined the object with **no tier column**; its §6.7 listed **three** added preconditions on `request_org_payout` with **no grant-maturity conjunct**; its §8 contracted `set_org_payout_destination` with none, while RPC §17.7 states maturity *"binds the SETTER and not only the requester."* **Two documents both labelled build-ready gave contradictory authority branches for the same money RPC.** Applied form: §6.1/§6.2/§6.6/§6.7/§7.3/§8.2/§8.5/§9.2/§12 reconciled onto **`(action, required_approver_class)`** and **`kernel.money_role_grant_matured(org_id)`**, matching RPC §17.1/§17.2/§17.7 and schema §1.13.2/§1.13.3; a new **§6.7a** *references* the maturity helper (whose defining contract stays in RPC §1) and states the four properties the money spec depends on **so a drift at the defining site is visible**; the trap is explained and not merely corrected, so a reader who only ever sees that file learns why the old branch was wrong. **The document is determined LIVE, not retired** — evidence in its §13.3; retiring it would delete the only statement of its §4, §5, §7 and §8, which no sibling covers | **DOC-FIX-APPLIED** — applies ratified `C57` + `C58`; **no new decision** | `PHASE_2_MONEY_AUTHORITY_SPEC.md` §6.1, §6.2, §6.6, §6.7, **§6.7a (new)**, §7.3, §8.2, §8.5, §9.2, §12, **§13 (new correction index)** | P | YES (applied) |
| **D15** | **Rows C57 and C58 named their target sections and omitted the money spec, so the ledger tracked every document the fix touched EXCEPT the one that defines the object being fixed.** C57's target column read `RLS §11.3, §16.1 · RPC §17.1/§17.2/§17.7, §20.1 · schema §1.13 + plan 077 owed`; C58's the equivalent. Both are **amended in place, tagged with this row id**, to name `PHASE_2_MONEY_AUTHORITY_SPEC.md` and the specific sections each correction lands in. **This is an amendment, not a rewrite of history:** the original target sets are still readable in the rows, the addition is marked `TARGET SET AMENDED by row D15`, and the reason is recorded here. **The general lesson is the one this record already learned twice** (`D9`: a set characterized outside its enumeration; `D11`: a standing count of a miss that was itself stale) — **a correction whose target list is incomplete is a correction that reports itself as done.** Standing consequence: a remediation pass that corrects a defect in a shared object MUST enumerate **every** covered document that specifies that object, and the ratification row is where that enumeration is checkable | **DOC-FIX-APPLIED** (record maintenance) | this record, rows **C57** and **C58** target columns | — | YES (applied) |
| **C75** | **There is no precedence rule between delta specifications, and this defect is what that costs.** `PHASE_2_MONEY_AUTHORITY_SPEC.md` §6.2 and `PHASE_2_RPC_FUNCTION_CONTRACTS.md` §17.2 both carried a build-ready authority branch for `kernel.approve_refund_request`, and **they contradicted each other**. Nothing in the corpus ranks them: `PHASE_2_SPEC_FOUNDATION.md` §0 says *"if a source document conflicts with this file, surface the conflict; **do not silently pick a side**"* — correct, and silent on which side wins — and its authority order stops at *"roadmap"*, ranking **tiers**, not documents within one. `ARCHITECTURE_FREEZE.md` Rule 3 likewise ranks tiers (*freeze + constitutions → implementation specs → standards → implementation → old audits*) and its covered-document list places **every delta spec in ONE tier**, *"same tier as the implementation specs."* So a delta-vs-delta or delta-vs-implementation-spec conflict is **unresolvable by the stated rules**, and the only available action — *surface it* — is exactly what nobody did. **The three admissible forms**, stated so the choice is a choice and not a design exercise: **(a) recency** — the later ratified correction governs, mechanical but blind to whether the later document is the right owner of the section; **(b) subject-matter ownership** — a named owner per subject (authority branches → RPC; predicates/grants → RLS; physical columns → schema; the money-authority *model* → the money spec), correct but requiring an owner map the corpus does not yet have; **(c) remediation-tag precedence** — where two documents state the same rule and one carries a ratified correction tag (`AUTHZ-*`, `J-*`, `K-*`, `S-*`, `H-*`) and the other carries none, the tagged text governs and the untagged is presumed pre-remediation. **This pass relied on (c) for C57/C58 specifically, and on nothing more**: it is a reading of *this record* rather than a ranking of documents, and it is available with no precedence rule at all — but **it is not ratified as a general rule and must not be cited as one.** **Ranking delta specs against each other decides which document's authority statement binds an implementer, which is an OWNER decision. It is NOT made here.** Open decision **O11** | **OPEN-GATED(O11)** — the admissible forms are recorded; **the choice is the owner's** | `ARCHITECTURE_FREEZE.md` Rule 3 (gap + admissible forms recorded) · `PHASE_2_SPEC_FOUNDATION.md` §0 (the "surface, do not pick a side" clause that has no companion) · `PHASE_2_MONEY_AUTHORITY_SPEC.md` §13.2 | P | **OPEN — blocks nothing that D14 has already reconciled; blocks the NEXT delta-vs-delta conflict, which has no stated resolution** |

**What this pass deliberately did NOT do.** It resolved **no** open decision — O6…O10 stand and **O11 is added, not closed**. It made **no** owner decision. It did **not** define `kernel.money_role_grant_matured`: that contract belongs to `PHASE_2_RPC_FUNCTION_CONTRACTS.md` §1's predicate-helper substrate, is owed there, and the money spec §6.7a **references** it and states the four properties it depends on so a drift is visible. It renumbered **no** prior row and **retired no document**. It touched **no** SQL, **no** migration, **no** code, nothing under `supabase/` and nothing under `.github/` — **no CI gate, ratchet or floor was weakened, no package renumbered (`076`–`091` intact), and production migrations `071`–`075` were not reopened.** **The frozen Stripe money core is untouched:** what was reconciled is *specification text about authority*, and the money spec's invariant-attestation table (R7 money-single-path, OBS-1, GP-1/GP-2, C35, C36, C28) holds line for line after the edit as before it. It edited only the two files it owns — `PHASE_2_MONEY_AUTHORITY_SPEC.md` and this record — plus the Rule 3 gap note in `ARCHITECTURE_FREEZE.md` authorized by row **C75**; every correction it could not make itself is named in the row that found it, with its owner.

## Helper-set closure pass — recorded 2026-08-28

**Authority:** this record's own **Rule 1** — *owner approval is required for a change to ratified material, and a correction index inside the spec that changed is a record of what was done, not the approval for doing it.* The pass below changed material three rows of this record had already marked terminal (**C58**, **C57**, **C60**), so it files the rows that authorize those edits rather than relying on the specs' own correction indices. **No prior row is renumbered**, no prior row's status is altered, and the pass's own defect id — **`AUTHZ-C1C`** — is carried here under that name so an implementer reading it in the RLS or RPC spec can find it.

**What it found, in one sentence.** The control that closes **C58** — `kernel.money_role_grant_matured`, the predicate that stops an `org_owner` minting the second pair of eyes their own money action requires — was **invoked by four call sites and defined by none**, and the helper set it belongs to was **stated six mutually inconsistent ways across four documents**, which held the money plane's only mechanical defence (`T-RLS-ROLE-02`) in an unwritable state.

**The derivation, so the number is not a choice.** Six statements; three numbers; two sets. **Union = ten. Intersection of the three statements that actually enumerate = nine. The sole element of the difference is `kernel.money_role_grant_matured`.** *"Eleven"* corresponds to **no set** — there is no eleventh helper anywhere in this corpus under any name, so it is a miscount and not a disagreement about membership. *"Nine"* is not wrong so much as **stale: it is the correct membership immediately before `C58`.** RLS §11.2's EXEC table, which nobody had counted, independently already carried all ten — a second derivation agreeing with the first.

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C76** | **The control that closes `C58` had four callers and no contract, and the helper set it belongs to was stated six ways.** `kernel.money_role_grant_matured` is bound in RPC §10.3, §17.1, §17.2 and §17.7; RLS §2.2 lists it and states that helpers *"are defined as SECURITY DEFINER helpers in the RPC spec"*; RLS §11.2 grants it EXECUTE — and **RPC §1.1–§1.1d defined nine helpers and stopped.** So the predicate that makes SoD-1 and SoD-2 mean what they claim had no signature, no volatility, no grant class, no statement of what *matured* means and, critically, **no statement of what it returns when its config key is absent** — where the cheapest spelling (`now() - granted_at > interval` against a NULL) yields NULL, and under `NOT (immature)` NULL is **admit**, silently, on the highest-value money writes in the system, on a plane where **GP-3a** leaves no policy to review. Ratified form, in four parts. **(1)** RPC **§1.1e** authors the full contract: `kernel.money_role_grant_matured(p_org_id uuid) → boolean`, `STABLE`, `SECURITY DEFINER`, owner `postgres`, `search_path` pinned, `EXEC: authenticated` with `anon` revoked; the actor is **`auth.uid()` derived inside the body and there is no actor parameter, ever** (**C35**); the predicate is *a live `kernel.org_member` row for `(p_org_id, auth.uid())` at `role ∈ {org_owner, org_finance}` whose `granted_at` is at least `authn.money_role_maturity_hours` old* — **`granted_at`, never `created_at`**, because a two-year member promoted this morning passes a `created_at` test trivially, which is the attack rather than the control; the key is **read and rejected on its own line before any arithmetic**, so no NULL ever reaches a comparison whose spelling decides the answer (`AUTHZ-M4`'s lesson one predicate over); it is a **conjunct only, never a sole gate**, it binds **both halves of both primitives**, and it is **never** applied to a deny, cancel, sweep or hold. **(2)** The membership is fixed at **TEN, enumerated by name in RLS §2.2**, and standing rule **`HELPER-DERIVED`** (modelled on `EXEC-DERIVED`, C60) makes the derivation mechanical: a name is a member iff it carries a contract in RPC §1.1–§1.1e; **membership is fixed by this record**, so a ratified helper with no contract is a gap and a contracted helper with no ratification is an unratified control; ROLE_MODEL §6.2 is the **derivation source and is re-derived, never cited over the top** of a ratified correction; and **no statement of the set may be a bare count**. **(3)** `T-RLS-ROLE-02` becomes writable — not by picking the right number but by **taking no number at all**: it consumes the literal enumeration, and **`T-RLS-ROLE-06`** asserts that enumeration equals `pg_proc` in both directions with a non-vacuity guard. **A count assertion passes on the wrong set of the right size**, which is exactly how a helper reached four money call sites undefined. **(4)** SEAM-1 places the function at **`078`** — `max(077 kernel.org_member, 078 catalog.platform_config + seed)` — **not** at `077` beside `has_org_role`, which would forward-reference a table and a seed that do not yet exist and would return `false` for every caller during `077`'s own replay while looking correct. **No package added, renamed or renumbered; band stays `076`–`091`** | RATIFIED — **`AUTHZ-C1C`** (completes `AUTHZ-C1B` / **C58**; makes `AUTHZ-M5` / **C60**'s test writable; extends `HELPER-DERIVED` from `EXEC-DERIVED`) | RPC **§1.1e**, §10.3, §17.1, §17.2, §17.7, §20.14 `R-22`/`R-23`, test register `T-RPC-AUTHZ-17`…`-20` · RLS **§2.2** (heading + enumeration + `HELPER-DERIVED`), §2.2b `RM-2`, §11.2, §11.3a, §16.11 (`T-RLS-ROLE-02` rewritten, `T-RLS-ROLE-06`/`-07` added, `AUTHZ-M5` note extended), §17 `X-11` · ROLE_MODEL §6.2 (`F-4`), §6.6 `RM-2` · SPEC_FOUNDATION §4 (list completed **and the filing discharged in place**) · TRACEABILITY `C2 · O-2` · schema §13.7 `S-3` · plan §8 `078` · registry `078` | P | YES |
| **C77** | **The platform plane of the same control is described by the schema, requested by `S-3`, ratified by nobody, and implementable by nothing.** Schema §1.13.4 defines the money roles as `{org_owner, org_finance}` (org) **and `{platform_admin, platform_support, platform_risk}` (platform)**, observes that `kernel.platform_role.created_at` **already is** the platform grant time, and §13.7 `S-3` accordingly asks that **the money arm of `set_platform_config`** carry the maturity precondition. **`C58` ratified the org half only** — its ratified form names `kernel.org_member.granted_at` and nothing else — and the helper it ratified takes an **`org_id` the platform plane does not have**. Net: `approve_refund_request` at `required_approver_class='platform_admin'` (`config.set_money_key`) is gated by `is_platform(['platform_admin'])` and `auth.uid() <> requested_by` (`AUTHZ-C1A2`) **and by no maturity floor at all**, while `kernel.grant_platform_role` is itself held by `platform_admin` — **the C58 attack one plane up, on the act the money spec calls larger than any refund it then authorizes** (raising a ceiling is the authority behind every later refund). **Two admissible forms, both stated as constitution text, and the choice is the owner's: (a)** a second, scope-free helper `kernel.platform_money_role_grant_matured()` reading `kernel.platform_role.created_at` against the same key, bound on that arm — **a new control, therefore a new ratification, not a clarification**; or **(b)** **retract** §1.13.4's platform-plane sentence and `S-3`'s `set_platform_config` clause, so the corpus stops describing a control it does not have. **Neither is taken by this pass:** (a) extends a ratified control to a plane it was not ratified on, and (b) deletes ratified schema text — **both are owner acts.** The gap is now visible from all three sides that could see it (§1.13.4, RLS §11.3a, RPC §17.2's dispatch table) rather than from none | **OPEN-GATED(O12)** | schema §1.13.4, §13.7 `S-3` · RLS §11.3a · RPC §17.2 (dispatch table), §20.14 `R-22` | P | **OPEN — the platform money-key arm ships with no maturity floor until it is ruled on** |
| **O12** | The open decision itself: **extend the grant-maturity floor to the platform plane with a second helper, or retract the platform-plane claim.** A security ruling, not a schema preference — form (a) adds a control and its window to the `platform_admin` path (and inherits `MD-14`'s number); form (b) concedes that the platform plane is defended by dual control and audit alone. **Whichever is chosen, one document currently says the opposite and must be corrected in the same act** | **OPEN-GATED(O12)** | schema §1.13.4 · RLS §11.3a · RPC §20.14 `R-22` | P | **OPEN** |
| **D16** | **One predicate, two error codes, and a helper list short by two in the file every spec takes its names from.** Three documentation defects corrected together because a reader hits them in one breath. **(1)** Schema §13.7 `S-3` filed the maturity refusal as `precondition_failed('money_role_too_new')`; **four call sites and both behavioural tests say `sod_violation`**, with the argument stated at each — the role is genuinely held, so a permission-shaped error sends the operator to re-check a grant that is correct. **`sod_violation` stands and `S-3`'s spelling is withdrawn as a divergence, not preserved as a second option**: two codes for one predicate is how a UI ends up telling an operator two different things about one refusal. **(2)** `PHASE_2_SPEC_FOUNDATION.md` §4's predicate-helper list — *the list every implementation spec is told to take its names from* — was missing **`kernel.assert_door_session`** and **`kernel.is_promoter_for_event`** as well as the tenth helper, and **no `F-n` row had ever asked for either**. ROLE_MODEL now files **`F-4`** and the list is completed to ten. **(3)** `PHASE_2_RPC_FUNCTION_CONTRACTS.md` §10.3 took `p_org_id` and `p_settlement_id` as two independent untrusted identifiers and **never required them to agree**, so `has_org_role`, SoD-1 and the maturity check could all be **true statements about the wrong organization** while the money moved from another; §10.3 now binds `settlement.org_id = p_org_id` under the settlement's own `FOR UPDATE`, raising `not_found` (never `insufficient_privilege` — the caller must not learn the settlement exists). Its Role line also read a bare `has_org_role([org_finance, org_owner])` **with no scope argument**, which `RM-2` forbids outright. **C35 was not violated at any of the four call sites** — every one derives the actor from `auth.uid()` inside the helper — but **a scope argument that does not bind to the subject is the same defect wearing the other parameter** | DOC-FIX-APPLIED — **`AUTHZ-C1C`** | schema §13.7 `S-3` · RPC §1.1e, §10.3, `T-RPC-AUTHZ-20` · SPEC_FOUNDATION §4 · ROLE_MODEL §11.2 `F-4` | — | YES (applied) |

**Helper-set closure attestation (2026-08-28).** The membership was derived **mechanically and reported in full** — every statement enumerated, both diff directions taken, the union, the intersection and the single name accounting for the discrepancy all stated — rather than settled by preferring one number. **The remediation pass of 2026-08-27 recorded RLS §2.2's helper count as one of the contradictions it deliberately did not resolve** *(see its own "What this pass deliberately did NOT do")*; **that item is now closed, and `PHASE_2_SPEC_FOUNDATION.md`'s filing of it is marked `DISCHARGED` in place, with the original report kept verbatim beneath the discharge block** — a filing deleted rather than discharged is a filing that can be re-made from a stale copy.

**What this pass deliberately did NOT do.** It **made no owner decision**: `MD-14` (the value of `authn.money_role_maturity_hours`) is **recorded, not made** — §1.1e states the parameter, states the failure mode of each direction, and states that a maturity window is a security parameter whose number is the owner's; the key ships and fails to *no grant is mature* regardless. It **resolved no open decision** — O6…O11 stand, and **O12 is added, not closed**. It **changed no ratified ownership invariant**: the one residual it found that would need one — `kernel.money_role_grant_matured` reads `kernel.org_member` unlocked, so a money→money re-grant committing inside a caller's transaction is not observed — is **recorded as an accepted residual with its direction and bound stated** (three of four race directions fail closed; the fourth is bounded by one transaction) and filed at RPC §20.14 `R-23`, **because closing it means putting `kernel.org_member` into the global lock order, which is a C28 amendment.** It touched **no SQL, no migration, no code, nothing under `supabase/`**, did not reopen production migrations `071`–`075`, edited **no `OFFLINE-VERIFY-v1` block** (all four remain byte-identical), and **weakened no CI gate, ratchet or floor** — `.github/workflows/` is untouched. **No package was added, renamed or renumbered**; the canonical band remains `076`–`091`.

---

## Reviewer-conditions pass — recorded 2026-08-28

**Authority:** an adversarial design review of the Phase 2 corpus taken at `cbf8926` raised six non-blocking conditions. Each was **re-verified against the tree before any edit** — a tidy pass had landed since the review was taken — and each is recorded here under its reviewer condition number so it can be found by the name the reviewer used. **No prior row is renumbered.** This pass touched **no** SQL, **no** implementation code and **no** CI workflow; every change is to design prose.

**Read this before reading the rows.** Three of the six conditions are defects in a *register a reader trusts* rather than in the authority it points at — the traceability matrix (C3), the door spec (C4), the promoter spec (C5). In each case the authority was already correct and the derived document was wrong. That direction of error is more dangerous than the reverse, not less: an implementer reads the register, not the authority, and a wrong claim in a register is indistinguishable from a right one at the point of use. **C5's is wearing a `VERIFIED:` badge**, which is the sharpest form of the defect.

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **D19** | **This record asserted that the `OFFLINE-VERIFY-v1` CI gate "does not yet exist — no workflow under `.github/` scans for the tag". It exists.** `.github/workflows/ci.yml`, job `quality`, required check **`Typecheck / Lint / Unit tests`**, step *"OFFLINE-VERIFY-v1 spec byte-identity gate"*, with **seven numbered fail-closed guards** (unterminated fence · column-1 tag outside a fence · non-vacuity floor of 4 blocks · normative home still tagged · byte-identity · anti-emptying byte floor · anti-coordinated-weakening anchor list). The claim was **stale in the safe direction** — it understated a control rather than overstating one — **which is exactly why it still had to be corrected**: a constitution that under-reports its own enforcement teaches the next reader that the property is prose, and the next pass to "add the gate that is missing" either duplicates it or weakens it while believing it is building it. Corrected `DOC-FIX-APPLIED` rather than `RETRACTED` because the sentence was **true when written** on 2026-08-27 and was overtaken by an event; `RETRACTED` is reserved for text that was false when ratified | DOC-FIX-APPLIED (reviewer condition **6**) | this record, "Independent verification performed by this pass" ¶ | — | n/a (governance record) |
| **C80** | **`PHASE_2_PROMOTER_CODES_SPEC.md` placed `venue.settlement` in package `086`. `086` is `086_venue_door_and_scan`; settlement is `087`.** The reviewer filed one wrong number; verification found **four**, all from one root cause: §0.3 decoded the dead repo scale onto the canonical scale **by arithmetic (+3)**, which registry §4 explicitly forbids — *"arithmetic alone is not safe"*, because `PHASE_2_SUPABASE_MIGRATION_PLAN.md` carried `S0`, `S1` and `S2` **simultaneously in different sections**. Three of that table's five rows were off by one (`venue.order` `081`→`082`, `kernel.payment_native` `084`→`085`, `venue.settlement` `086`→`087`), and the errors propagated into §14.7 (×4) and §15 (×4). **`086`'s real contents — door PINs, `door_session`, scan devices, scans — intersect nothing this spec reads or writes**, so the wrong number was not a near-miss. **Three of the four wrong claims carried a `VERIFIED:` badge, and the badge was correctly applied**: per that spec's §0.2 `VERIFIED:` certifies only *"the cited document states it"*, never that the cited document is **canonical**, and the superseded migration plan really did say `086`. A correctly-applied badge on a false statement is the most expensive kind, because it is the claim a careful reader stops checking. Fix: §0.3 rebuilt to decode **by package identity** against registry §2 (now named canonical for this file), §14.1 re-titled **CLOSED BY RATIFICATION** (it was filed open against a renumber author whose renumber is ratified), §14.7 and §15 corrected inline with prior values shown, and a §16 correction index added (`X-01`…`X-04`). **No design content changed — every corrected item is a package number**; `090`, this spec's own package and the number on all sixty of its rows, was correct throughout. The reviewer's `R-5` is cited in §16 as *"reviewer condition 5"* and is deliberately **not** enrolled in any of the three existing `R-n` namespaces | DOC-FIX-APPLIED (reviewer condition **5**; reviewer IDs `R-5` / `X-01`…`X-04`) | promoter §0.3, §14.1, §14.7, §15, **new §16** · authority: `PHASE_2_PACKAGE_REGISTRY.md` §2/§4 | — | n/a (register alignment; registry unchanged) |
| **C81** | **`PHASE_2_DOOR_LIFECYCLE_SPEC.md` still carried the abolished label `venue_door` and the abolished door-PIN authorization arm — including in §10A.7, a table titled *"EXECUTE-authority additions to RLS §11"* that **contradicted RLS §11.4, the table it claims to extend**.** `venue_door` was renamed `venue_scanner` by O-2 / ROLE_MODEL §4.5 / schema §0.6 / RLS §2.1; the PIN arm is the provisioning-not-possession predicate closed by `AUTHZ-H3` (`venue.door_pin` has **no device column** — it evidences that a PIN was issued for a venue, never that *this device* holds it). **The authority was already correct in both places** — RLS §11.4 and the edge spec carry the token-bound `kernel.assert_door_session` form — and rule **`EXEC-DERIVED`** (RLS §11.0) already declares this file the defect on disagreement. **That is the dangerous direction, not the safe one:** a door implementer reads the door spec, not RLS §11.4, and the PIN arm was **code-shaped** (an `OR` clause to transcribe) with the caveat that it must not be reachable sitting in prose *after* it — **a caveat is not a predicate.** Two findings beyond the filed condition: (a) §4.1 and §16 `OQ-3` argued from a **four-label** venue enum and offered *"a fifth enum label"* as a remedy — **`venue_box_office` exists** in the canonical six and RLS §11.4 already excludes it, so that half of the open question is **closed** and only the narrower `venue_manager`-over-provisioning half (VD Δ8) survives; (b) §16 `OQ-7` asserted *"the RPC is unchanged … §11's EXEC row stands as written"*, quoting the deleted PIN arm — **deleting the route while leaving the arm in the predicate is the reachable-only-by-convention posture `AUTHZ-H3` was raised against.** Fixes in §4, §4.1, §4.2, §7.1, §7.2, §7.5, §10.1, §10A.1, §10A.2, §10A.7, §15, §16 `OQ-3`/`OQ-7`, §17; new §20 correction index (`DL-X1`…`DL-X4`). **Every change narrows authority or leaves it unchanged; none widens it.** No RPC signature, table, column, state machine, lock order, invariant or package number altered; §9.2's `OFFLINE-VERIFY-v1` mirror untouched | DOC-FIX-APPLIED (reviewer condition **4**) | door §4/§4.1/§4.2/§7.1/§7.2/§7.5/§10.1/§10A.1/§10A.2/**§10A.7**/§15/§16/§17, **new §20** · authority: RLS §11.4 + `PHASE_2_EDGE_FUNCTION_SPEC.md` (both already correct, unchanged) | P | YES (the door predicate is built from this file) |
| **C82** | **`T-RLS-EXEC-02` — the guard designed to catch exactly the `venue_door` class — is scoped to predicates in RLS §11 and structurally cannot see the downstream specs that copy those predicates.** RLS §11.0 states it as *"No label appearing in a **§11** predicate may be absent from the fifteen canonical labels of §2.1"*, and every abolished label corrected by **C81** lived in `PHASE_2_DOOR_LIFECYCLE_SPEC.md`, in predicates that are downstream copies of §11 rows rather than §11 rows themselves. **The assertion is not weak at those sites — it is not evaluated at them at all.** This repeats the guard's own origin: `T-RLS-EXEC-02` was added because ROLE_MODEL edit `R-14` renamed `venue_door → venue_scanner` **lexically** across §11 and nothing could tell a lexical rename from a re-derivation — and it was then **scoped to the one table that had already been fixed, and not to the downstream copies that had not.** A guard that covers only the site of the last incident is a guard against the last incident. **Partially closed:** door §15 assertion `25a` asserts the canonical-label rule over that document's own predicates. **The general form is OPEN and is not made here** — enforcing the canonical-label set over **every** predicate under `docs/architecture/**` is a corpus-wide CI change owed to the CI-gate owner, of the same shape as the `OFFLINE-VERIFY-v1` byte-identity gate: a property currently held by review that is buildable as a scan. Filed as `DL-X4` | DOC-FIX-APPLIED (partial — the per-file assertion; the corpus-wide scan is **filed, not built**, and is owed to the CI-gate owner) | door §15 (`25a`), §20.2 · RLS §11.0 `T-RLS-EXEC-02` (unchanged — no gate weakened) | P | NO (the general scan; the per-file assertion is MVP) |
| **C83** | **`PHASE_2_IMPLEMENTATION_TRACEABILITY_MATRIX.md` §10 asserted the `_sel_org`/`_sel_venue`/`_sel_promoter` policy triple for `venue.attribution` and `venue.attribution_review`; RLS §16.10 drops `venue_attribution_sel_promoter` and moves `attribution_review` into the zero-policy set (`AUTHZ-M9`).** Two further defects were found in the same register: **(a)** §10 listed only `catalog_platform_config_sel_public` and glossed it *"it is world-readable"* — RLS §8.4 is a **two-class** model on `visibility` (`AUTHZ-CFG1` / **C71**), and telling an implementer that the table holding every dual-control ceiling, step-up window and export cap is world-readable is **the unsafe direction and the exact defect C71 was raised against**; **(b)** *"The **31** zero-policy objects … **All 31** are claimed"* — RLS §16.10 lists **35**, so **a completeness assertion over the wrong denominator reported `PASS` while four objects went unclaimed** (`org_contact_consent_event`, `identity_contact_pref_event`, `venue.door_session` (`AUTHZ-H3`), `venue.attribution_review` (`AUTHZ-M9`)). **All three claims were TRUE at the matrix's stated baseline `64d2aac`** — verified against that commit, not assumed: `AUTHZ-M9` did not exist in the RLS spec there, `sel_restricted` did not exist, and the zero-policy set had **exactly** 31 members. The matrix did not misread its sources; the remediation passes changed RLS underneath it. Under **GP-3** the RLS spec is the authority and the matrix is derived. Fixed by copying the authority; §14 correction index added (`TM-X1`…`TM-X4`); **§14.3 states that only §16.10 was reconciled forward — §8, §9, §11, §12 and §13 remain at `64d2aac` and are explicitly NOT current** | DOC-FIX-APPLIED (reviewer condition **3**) | matrix header, §10 (×3), **new §14** · authority: RLS §16.10/§8.4 (unchanged) | P | YES (the matrix is the register an implementer works down) |
| **C84** | **The traceability matrix has no mechanism that fails when its baseline goes stale — and its own binding cell vocabulary hides the problem.** §0.1 defines a named artifact as *"**VERIFIED** — the named object exists in the cited spec **at this baseline**"*. That qualifier is load-bearing and invisible: `venue_attribution_sel_promoter` satisfied it at `64d2aac` and **names a policy that exists in no spec today**. There is no marker for *"verified against a baseline that has since moved"* and no assertion anywhere binding this derived register to its authority. **A completeness instrument pinned to a stale baseline does not degrade into an incomplete one — it degrades into one that reports `PASS` with authority**, which is worse than having no instrument, because the empty cells are its entire stated value (§0) and a stale full cell hides one. **This is the third independent finding in this pass converging on one missing class of gate — a derived register with no assertion binding it to its authority** (cf. **C82**, `T-RLS-EXEC-02` blind to downstream predicate copies; **C80**, a `VERIFIED:` badge that certifies citation but never canonicity). **FILED, NOT BUILT** (`TM-X4`), owed to the CI-gate owner: assert every policy name in the matrix exists in RLS §16.10, that the zero-policy **count** matches, and that the matrix's stated baseline is an ancestor of `HEAD` whose intervening commits touch no register it mirrors — the same shape as the `OFFLINE-VERIFY-v1` byte-identity gate, a property held by review that is buildable as a scan | DOC-FIX-APPLIED (the finding and the §14.1 baseline evidence are recorded; **the gate is filed, not built**) | matrix §14.1/§14.2 · §0.1 cell vocabulary (unchanged — no gate weakened) | P | NO (the scan; the corrected rows are MVP) |
| **C85** | **`org_admin` on the money plane is an INFERENCE presented as settled, and the two positions are held by the SAME document.** `PHASE_2_MONEY_AUTHORITY_SPEC.md` §3.4 denies `org_admin` all money authority (`D` on `kernel.payout` / `kernel.refund` SEL/EXEC) and **labels its own position `INFERENCE`**; **§10.1 row 35 of that same document grants `org_admin` `●` on the refunds order list and previously called the row *"unchanged"*.** Venue dashboard §5 row 35 shows the same `●`, and **VD §5.2 does not supersede row 35** — so the dashboard is faithfully inheriting the money spec and **the contradiction is internal to the money spec**, not between the two documents, which is sharper than it was filed. O-1 and O-3 name `org_owner` and `org_finance` and are **silent on `org_admin`**; neither position is a ruling. **Silence defaults to GRANT**: RLS §9.7 grants `org_owner/admin` `A(own-org orders)` SELECT on `venue.order` (the backing for row 35) and RLS §9.13 grants `org_admin` `A(own-org)` SELECT on `venue.settlement`, whose header carries gross/fees/refunds/net — so leaving this open does not stall the build, it **ships the grant silently** with a `D` sitting unread in §3.4. **That is the unsafe direction**: `org_admin` is the role most likely to be handed out liberally, and the remedy costs are asymmetric — widening later is a one-line matrix change, **narrowing later is a migration plus removing a capability operators have been using** — against a standing deny-by-default posture (RLS `GP-1`). **NO SIDE IS TAKEN AND NO RECOMMENDATION IS OFFERED.** `D-8` is deliberately the only row in the money spec's §11 register carrying **no** recommendation, because the tie-break is an authority question, not a design question. Recorded as money §11 `D-8` + new §11.1 (both positions in full), VD §22.13 and a §5.2 blocking banner, and `O13` below | **OPEN-GATED(O13)** — the admissible forms are stated; the choice is the named open decision | money §3.4/§10.1 row 35/§11 `D-8`/**new §11.1** · VD §5.2/§22.13 · authority silent: O-1, O-3 | P | NO — **BLOCKED.** Surface H's refund list must not be built for `org_admin` from either cell until `O13` closes |
| **O13** | **The open decision itself: does `org_admin` hold the money-plane read (refunds order list, and by extension the `D-4` settlement question)?** Admissible forms, both already stated in the corpus: **(A) deny** — money §3.4's reading, corroborated by Domain §7.2's Org Admin *Cannot* column and O-2's *"not unrestricted financial authority"*; **(B) grant** — money §10.1 row 35 / VD §5 row 35, corroborated by RLS §9.7 and §9.13, which grant today. **An owner call, not a design call**: both readings are defensible and the corpus cannot break the tie from inside, because the deciding inputs (O-1, O-3) are silent. **Blocks Gate P for venue-dashboard surface H.** Note the direction of the default — silence ships (B) — so *not deciding* is itself a decision for the over-provisioned side. **`D-4` (settlement, row 37) is a narrower sibling and should be closed in the same sitting, since (A) reopens it** | OPEN-GATED(O13) | money §11 `D-8`/§11.1 · VD §22.13 · this record C78 | P | NO |
| **C86** | **Four ratified venue-plane read policies had no implementation anywhere and would have failed the replay at package 3 — or, worse, been silently skipped.** `catalog_venue_sel_venue`, `catalog_event_sel_venue`, `catalog_event_session_sel_venue` and `kernel_tickets_sel_venue` are **named** in RLS §16.10 and **no document in the corpus stated their `USING` clause.** Every legal predicate calls `kernel.has_venue_role` / `kernel.has_event_role`, which ship in **`080`**, while the tables are created in **`078`**/**`079`**, and **`RM-3` forbids re-inlining the join** — so no other implementation existed. Outcomes: **`42883` (function does not exist)** on replay, or the quieter one — the policies omitted and, since **absence of a policy is denial (`I-1`)**, **the entire venue operator plane reading zero rows on the four tables it works from.** A staff member seeing an empty venue list does not read as a mis-ordered migration; it reads as a broken account. **RULING: the POLICIES move to `080`; the helpers do NOT move.** `has_venue_role` reads `venue.staff_role`, created in `080`, and **`SEAM-1` binds it there** — moving it earlier would require moving `venue.staff_role`, which renumbers the canonical band (forbidden) and guts `080_venue_staff_roles_and_predicates`. **Only `has_org_role_over_venue`/`_over_event` could move to `078` under `SEAM-1`, and moving them closes nothing**, since the four policies need `has_venue_role`/`has_event_role` regardless — it would only split the helper set across two packages. **The forward reference is therefore not declared-and-tolerated; it ceases to exist.** Consequence checked for every consumer: `081` already declares `080` (the precedent), `086` already declares `079`+`080`, and no package other than `080` now creates a policy on an `078`/`079` table — so **exactly one new edge is required, `‡079 → 080`, and it is DECLARATION-ONLY** (`079 < 080` already; nothing about the rollout order changes), the same class as `†086 → 087`. Declared in **all four** registry locations: plan §3 row 5, registry §2.1 row 5, registry JSON `depends_on`, and the mermaid DAG (`D079 --> E080`). The four `USING` clauses are written out in RLS **§16.10a**, with `T-RLS-POL-03` asserting they exist **and work** (a positive cross-venue read, because existence alone passes against a wrong predicate). Three sub-findings recorded and **not decided**: **`OPEN-1`** §8.3 grants `venue_scanner` `A(own-venue, **tonight**)` and **no document defines "tonight"** — the arm is omitted (fail-closed) rather than guessed; **`OPEN-2`** `kernel.tickets` is at **session** grain and the nine ratified helpers are at org/venue/event grain, so the clause uses a correlated sub-select rather than a **tenth** helper this pass declined to add; **`GP-3 NOTE`** `kernel_tickets_sel_venue` also carries the org arm and its name does not say so, **left unsplit** because `T-RLS-POL-01` asserts exact policy lists and a silent split would read as a CI regression | RATIFIED (the ruling + the placement) · DOC-FIX-APPLIED (the written-out clauses and the four declarations) | RLS **new §16.10a** + §16.11 (`T-RLS-POL-03`/`-04`) · registry §2.1/§2.2/JSON · plan §2 DAG/§3/§5 `078`/`079`/`080` | P | YES |
| **C87** | **The forward-reference sweep of schema §13.2 was FUNCTION-SCOPED BY DEFINITION and structurally could not see a policy→function edge — so it returned clean over four real instances and would have again on a re-run.** Its definition read *"a **function** authored in package N reads or writes a table created in a package later than N"* and its method reduced *"every **function** contracted in…"* to `max(package of every table it reads or writes)`. **An RLS policy is not a function, and the reduction had no term for a called function.** `catalog_venue_sel_venue` reads only `catalog.venue` (`078`), so under the table-only reduction it scored `078` and looked correct — **it is the `has_venue_role` call, not a table, that puts it in `080`.** This is the deeper defect behind **C86**: not that four policies were mis-placed, but that **the corpus's own guard against exactly this failure had a blind spot no amount of care in running it could overcome.** A sweep that cannot see a class reports a clean result on a corpus containing it, and **the clean result is what the next reviewer trusts.** They were found by an external reviewer reading the policy names and asking where the helper lived. **Fixes:** the §13.2 definition now covers *"an artifact … **or calls a function created in a package later than N**"*; the method's reduction gains the second term `max(…, package of every function it calls)`; **four artifact kinds are now in scope** (functions · **RLS policies** · triggers **and their attachments**, since `FR-6` was caught only via its function · generated columns / `CHECK`s / defaults that call a function); RLS §16.10 is added as a swept source; **`FR-10`…`FR-13`** are recorded with the original rows unchanged and unrenumbered; and **`SEAM-3`** is ratified beside `SEAM-1`/`SEAM-2` in registry §2.2 — *a policy is created in `max()` of the packages creating every table it reads and every function its predicate calls, never the package of the table it protects; where those differ it is **deferred**, stated in **both** packages' plan §5 entries, and never re-inlined to avoid the wait.* `T-RLS-POL-04` makes `SEAM-3` mechanical **with a non-vacuity guard**, because an empty helper map passes trivially — which is precisely how the old sweep returned clean. **The nine-edge count in registry §2.2 is now thirteen** | RATIFIED (`SEAM-3`) · DOC-FIX-APPLIED (the widened sweep) | schema §13.2 (definition, method, artifact scope, `FR-10`…`FR-13`) · registry §2.2 (`SEAM-3`, count 9→13) · RLS §16.11 (`T-RLS-POL-04`) | P | YES |
## Unwritable-control pass — recorded 2026-08-28 (`MB-2` … `MB-5`, `MN-2`, `MN-4`)

**Authority:** this record's own **Rule 1**. **Scope:** `PHASE_2_PHYSICAL_POSTGRES_SCHEMA_SPEC.md`,
`PHASE_2_SUPABASE_MIGRATION_PLAN.md`, `PHASE_2_PACKAGE_REGISTRY.md` and this record. Every change to another
document is filed as a request in schema §13.7 (`S-14` … `S-21`) and **not made here**.

**IDs claimed by this pass, stated so a concurrent pass can avoid them: `C91`–`C98`, `D21`, `O15`, `O16`,
and schema §13.7 `S-14`–`S-22`.** The maxima read at the start of the pass (branch head `734c814`) were
`C79`, `D18`, `O12`, `RET-6`, `S-13`, and this pass first claimed `C80`–`C87`/`D19`/`O13`/`O14` on that
basis. **`phase2/consolidation` then advanced to `1a16b71` (the `MB-1`/`MB-6` merge and its row-tally
reconciliation), which had claimed exactly that block — `C80`–`C90`, `D19`, `D20`, `O13`, `O14`. This
pass therefore renumbered its own rows upward, off the new maxima, rather than leaving the merger to
untangle two meanings for one id.** `S-14`–`S-22` were re-checked against `1a16b71` and are free there
(its `S` maximum is still `S-13`). **No prior row — anyone's — is renumbered, reworded or retracted.**

**What it found, in one sentence.** Six controls that are specified, ratified and depended on had **no
physical substrate or no writer** — a payout hold with no storable value, three payout lifecycle labels and
a settlement label with nobody to write them, an audit RPC that cannot satisfy its own `NOT NULL` actor, the
custody-head invariant three documents call *structural* with no trigger in any package, two sentinel
identities that are `NOT NULL FK→auth.users` and are seeded by nothing, and a ticket state nothing reaches.

**Band untouched: sixteen packages, `076`–`091`, no gaps, no duplicates, no renumbering. Zero dependency
edges added — the declared edge count is 39 after the conditions pass added the AUTHZ-PKG1 edge and all four surfaces were re-verified identical after the
edits.** Every new object was placed by SEAM-1 with its `max()` stated inline.

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C91** | **`kernel.payout.status='held'` was ratified four times and was never a member of the status set.** MONEY §8.4 Control 4, MONEY §12 (`NO SCHEMA CHANGE`), row **O-3** and RPC §17.7 control 2 all store it; the schema and the plan's DDL-authoritative CHECK both enumerate `pending·submitted·paid·failed·reversed`. RPC §11.2 already hedged — *"→ `held`-equivalent status"*. The label a reviewer's `grep` finds is **`venue.inventory_batch.held`, an integer capacity counter**. Repair: `held` is **not** added to `status` — that is lossy and makes RPC §11.3's ratified release contract unimplementable (it must restore `pending` XOR `submitted` after the hold overwrote which) — a hold becomes an **orthogonal** `hold_state` column (`none`·`held`·`probation_hold`) plus `hold_reason_code`/`held_by`/`held_at`, reproducing the shape of the frozen `public.transfers.payout_review_status` discipline (migration `039`) that `hold_payout` is contracted to *extend*. `probation_hold` is a second label because dashboard §14.5 requires three separately-rendered pills and forbids inference. MONEY §12's classification is corrected to **`SCHEMA CHANGE` — four additive columns**; the ratified *behaviour* is unchanged | **RATIFIED** | schema §1.9, §1.9.1 · plan §5/§8 `085` · registry §2/§3 `085` · filed `S-14`/`S-15`/`S-21` | **P** | **YES** |
| **C92** | **Nothing wrote `paid`, `failed`, `reversed`, `stripe_transfer_ref`, or `venue.settlement.status='paid'`.** The complete writer set (RLS §7.9 + schema §1.9) was `close_settlement`·`pay_promoter_commission` (INSERT `pending`), `request_org_payout` (→`submitted`), `hold_payout`/`release_payout`; the edge spec routed the Stripe events to *"`mark`-style state sync RPCs"* — a placeholder naming no function. A failed transfer therefore read `submitted` forever and §14.5's pinned *Failed payout* banner could never fire. Repair, per §3.11.1's rule *name the writer, do not remove the value*: **`kernel.mark_payout_transfer_state`** (`service_role` only, forward-only, refuses a row with `hold_state <> 'none'`) — **SEAM-1 `max(077, 085) = 085`** — and **`venue.on_payout_settled`**, a SEAM-2 no-op hook stubbed in `085` and replaced in `087`, whose real body is **SEAM-1 `max(085, 087) = 087`**. Both edges already declared; **none added.** The fifth and sixth instances of the class caught at `door_open_at`, `scan_device.retired`, `market.offer.expired` and `required_approver_class`, and the first two on the money ledger | **RATIFIED** | schema §1.9.2, §3.13 · plan §8 `085`/`087` · registry §3 hooks · filed `S-16` | **P** | **YES** |
| **O16** | **What `kernel.payout.status='paid'` asserts.** The edge spec maps four Stripe events onto one row, and only `transfer.created` supplies the `stripe_transfer_ref` join key: `payout.paid`/`payout.failed` describe the **connected account's own bank payout**, which aggregates many transfers and is not attributable to a single `kernel.payout`. So `paid` means either *"the transfer succeeded and was not reversed"* (written synchronously by the payout executor) or *"the funds reached the payee's bank"* (a `balance_transaction` fan-out from `payout.paid`). **The two differ in what the venue is being told, and one is a promise about a bank we do not observe.** Both forms are served by the single RPC of `C92`; only the caller and the triggering event change | **OPEN-GATED** | schema §1.9.2 · filed `S-16` | **P** | decision, then YES |
| **C93** | **`kernel.record_money_denial` cannot write its own audit row.** `kernel.admin_audit.actor_identity` is `NOT NULL` FK→`auth.users` and server-derived (C35); RPC §17.9 contracts the function as `service_role` only, **no human path**, with **four parameters, none of them an actor**. `auth.uid()` is NULL on that connection and the FK forbids a sentinel, so the INSERT cannot succeed — **the control fails on its first call, on the fraud path.** And the fact it is built to carry is *"repeated attempts by ONE PRINCIPAL"*, which is precisely the fact the row cannot hold. **RLS §3.1 lists this RPC among those with *"no human actor by construction"* — it is misfiled there**; the genuine members are the cron sweeps, served by the seeded system-actor sentinel of `C96`. Repair: the function is **bound by EDGE-CALLER-JWT** — the edge already built a client from the caller's `Authorization` header for the call that was denied, and the denial log is that call's second transaction on that same client — so `actor_identity := auth.uid()`, **signature unchanged, no actor parameter added**, `EXECUTE` to `authenticated` only, and it **RAISES when `auth.uid()` IS NULL**. The `p_actor_identity_id` repair is rejected as the `p_user_id`-trust pattern C35/R7 closed and `S-6` rejects in the identical shape. **No schema change — the writer was wrong, not the table** | **RATIFIED** | schema §1.12, §1.12.1 · plan §8 `085` Tests · filed `S-17` | **P** | **YES** |
| **C94** | **The claim that custody bypass is *"structurally impossible"* rested on a trigger no package built.** Door §7.6 names `kernel.transfer_ticket_ownership` *"the enforcement point"*; the enforcement itself is named **three times** in the frozen constitution — *"head-equals-log-tail **trigger**"*, *"single-writer fn + **verify trigger** + partial-unique on live custody"*, invariant #1. **Enumerating every Triggers row in plan §8 finds it in none of the sixteen packages**: `079` carried only `raise_append_only`. The corpus **did** build exactly this trigger for `door_open_at` (`086`, `catalog.tg_door_open_at_is_ledger_head`) after O-5 found that column unwritten, and did not build it for the column the pattern is named after — so by `AUTHZ-M1` (*"a control that depends on writers remembering **is** a convention"*) custody-head integrity was a convention. Repair: **`kernel.tg_custody_head_is_ledger_tail`**, a `CONSTRAINT TRIGGER … DEFERRABLE INITIALLY DEFERRED` on `kernel.tickets` created in `079`, asserting at COMMIT that the greatest-`sequence` log row's `to_identity`/`credential_version_after` equal the head's `current_owner_id`/`credential_version`. **Deferred rather than `AFTER ROW`, because an immediate trigger passes only by virtue of the engine's statement order — another convention. `state` is deliberately outside the clause set**: a `state_transition` clause would raise on every admission and every expiry sweep. The constitution's *"partial-unique on live custody"* is discharged **by construction** — the head is a column on the atom's PK row, so one live owner is a functional dependency, not an index. **SEAM-1 `max(079, 079) = 079`; no edge, no package change.** Structural assertion over `pg_trigger`/`pg_constraint` (`tgdeferrable` ∧ `tginitdeferred`), the `signing_key_id` technique, because a dropped trigger and a trigger that never fires are indistinguishable to every value-based test | **RATIFIED** | schema §1.6, §1.6.2 · plan §5/§8 `079` · registry §2/§3 `079` · filed `S-18`/`S-19` | **P** | **YES** |
| **C95** | **`CUSTODY-DEL-1` — the custody head has a live production writer no spec declared, and the reconciliation cuts the other way from how it was posed.** `PHASE_2_CRM_EXPORT_SPEC.md` §9.2 states, under a `VERIFIED:` tag, that the live account-deletion path repoints ledger-referenced rows to `00000000-…` and that *"once `current_owner_id` is the sentinel"* the export-membership question is unanswerable. **Read from the applied migrations, `020` repoints `public.listings`/`payments`/`transfers` and touches NO `kernel.*` relation** — so the clause is a claim about a future extension carried under a tag that means *checked against production*, and it is the sentence an implementer reads immediately before making it true. **Binding: `kernel.tickets.current_owner_id` and all three identity columns of `kernel.ticket_ownership_log` are permanently out of scope for anonymization-by-repointing**, forbidden **three** independent ways — the log is AO with `REVOKE UPDATE, DELETE`; §1.6 already names crypto-shred (C15/Gate L) as the erasure mechanism and says *"never anonymized-by-deletion"*; and `C94`'s trigger aborts a head repoint at COMMIT. **And the bind is two-sided:** all four columns are `ON DELETE RESTRICT`, so the `auth.users` DELETE **itself** fails — **account deletion stops working for anyone who has ever held a ticket, the day `079` lands.** The product handling is `O15` | **RATIFIED** | schema §5 table + §5.1 · §1.6 write authority · filed `S-19` | **P** | **YES** |
| **O15** | **How account deletion behaves for an identity holding custody.** `CUSTODY-DEL-1` forecloses repointing; it does not choose the product behaviour. Three admissible forms, all consistent with it: **(a) tombstone** — retain the `auth.users` row marked erased, revoke credentials, crypto-shred PII, keep an opaque dereferenceable uuid; **(b) refuse while custody is live** — deletion is refused, with a named reason, until every held atom is terminal or transferred; **(c) forced hand-off** — deletion voids or transfers the remaining atoms through the custody engine first. Each has a real cost, stated in §5.1: (a) the row survives deletion; (b) a fan holding a ticket to next month's show cannot delete today and must be told why **inside the deletion flow, before the confirm step**; (c) a privacy action destroys or moves something the person paid for. **What is NOT admissible under any form is reusing the `019` anonymization sentinel as the new `current_owner_id`** — see `C96` | **OPEN-GATED** | schema §5.1 · filed `S-19` | **P** | decision, then YES |
| **C96** | **The ownership log's two sentinel identities are `NOT NULL FK→auth.users` and no package creates them.** §1.6 gives `to_identity` (*"for `refund_void` this is the platform void-sentinel / issuer"*) and `actor_identity` (*"for system sweeps this is a named system sentinel identity"*); both are relied on by `void_ticket_atom`, `sweep_expired_p2p_transfers` and `sweep_paid_pending_sales`. A corpus-wide search finds those two lines, the RPC uses, and — everywhere else — the *anonymized-deletion* sentinel from production `019`/`020`, **a different object with a different meaning**. **Consequence: the first refund fails `23503` on `to_identity`, and every cron custody sweep fails on `actor_identity`.** Ruling: **TWO** sentinels, seeded in `078`. Not one — a custody **destination** and an **actor** answer different questions, and merging them makes an `actor_identity` filter return every human-initiated refund too. Not three — per-sweep identities re-encode what `cause`/`cause_ref`/`command_idempotency_key` already carry. **`SN-VOID`'s accumulation is correct**, and the reason is stated rather than assumed: CRM §9.5 refuses an accumulating sentinel because it would hold **grants** — live authority belonging to nobody; `SN-VOID` accumulates **terminal custody**, which grants nothing, since a `voided` atom cannot be transferred, listed, locked or scanned. **The `019` anonymization sentinel is NOT reusable** — four independent reasons: it means *a person who asked to be forgotten*; it carries a `profiles` row displaying **"Deleted User"**, which would render on the Transfer View for every voided ticket; it is written and given meaning by a `public`-schema deletion path outside the kernel's write authority; and it is person-shaped where a custody sink must be non-authenticable. **`00000000-…-000000000000` must never appear in a custody column** (with `C95`, the two halves of one rule). *"Issuer"* is struck as a synonym for the void sentinel: the column FKs `auth.users` and an issuer is a `kernel.organization`; a per-issuer sink would make `create_organization` a writer of `auth.users`. The `to_identity`-nullable alternative (*minted from ∅, burned to ∅*) is rejected **not** as an owner decision but on mechanics — under `C94`'s trigger it forces `current_owner_id` nullable, pushing a NULL case into the RN app, the dashboard, the manifest builder and the export. **SEAM: `max(precondition, 077, 078) = 078`; `078 → 077` and `078 → 079` already declared, no edge added** | **RATIFIED** | schema §1.6, §1.16 · plan §5/§8 `078` · registry §2/§3 `078` · filed `S-20` | **P** | **YES** |
| **C97** | **`MN-2` — the migration plan specified opposite DDL for one CHECK, in one document.** §5 PHASE E's `080` block enumerated the **superseded four** venue labels (`venue_manager/venue_finance/venue_door/venue_promoter`) while §8's table for the same package said *"the six canonical venue labels"* and *"the CHECK rejects `venue_door` and `venue_promoter`"*. `venue_door` was **renamed** `venue_scanner`; `venue_promoter` was **removed** (a promoter is a relationship, not a staff grant). §8's stated precedence (*"where it differs from §5's original blocks, §8 wins"*) resolves it for a reader who knows the rule — **and a reader of §5 does not**, while §5 is where a DDL author looking for *objects created* lands first. Corrected in §5 rather than left to be out-voted: **a precedence rule is a tie-breaker, not a substitute for the two statements agreeing** | **DOC-FIX-APPLIED** | plan §5 PHASE E `080` | **P** | **YES** |
| **C98** | **`MN-4` — `kernel.tickets.state='expired'` is in the enum, its transition is specified in §7.6, and no function in any package wrote it.** A fifth instance of the `MB-2b` class. Disposition per §3.11.1 — **name the writer, do not remove the value** — but the *weight* is §4.3.1's, not §3.5's: **`expired` is presentational, not load-bearing**, because `kernel.is_transfer_frozen` already freezes **every** atom of a session at doors, strictly before the session ends, so no listing or transfer of a past-show ticket was ever possible. Writer: **`kernel.sweep_expired_ticket_atoms(p_limit)`**, `DEF`/scheduler-only, re-entrant, actor `SN-SYSTEM`, **appending no ownership-log row and bumping no `credential_version`** — which is precisely why `state` sits outside `C94`'s trigger clause set. Folded onto the **existing** 2-minute `pg_cron` heartbeat: **no new cron entry, no new EXEC surface**. And, per §4.3.1's rule stated a second time: **no path may trust `state <> 'expired'` because the tick was supposed to have run.** **SEAM-1 `max(078, 079) = 079`; `078 → 079` already declared, no edge added** | **RATIFIED** | schema §1.5.1 · plan §8 `079` (Functions + Scheduled ticks + Tests) · registry §3 `079` · filed `S-22` | **P** | **YES** |
| **D21** | **The record of this pass itself.** Six controls repaired, all of one class: *a control that is specified, ratified and relied on, whose physical substrate or writer does not exist.* `MB-2a`/`MB-2b`/`MB-3`/`MB-4`/`MB-5`/`MN-4`, plus the `MN-2` documentation fix. **Nothing was decided that belonged to the owner**: `O15` (account-deletion product handling) and `O16` (what `payout.status='paid'` asserts) are recorded and left open. **No ratified invariant was weakened**: `C93` and `S-17` *extend* EDGE-CALLER-JWT's scope, `C94` *adds* a constraint, `C91` *corrects a classification* while preserving the behaviour O-3 ratified, and the frozen Stripe money core is untouched — `C91` in fact reproduces its shape rather than diverging from it. **No CI gate, ratchet or floor was weakened; migrations `071`–`075` were not reopened; the `OFFLINE-VERIFY-v1` blocks were not edited** (re-verified after every commit: **4 blocks, 0 loose, 0 unterminated, 1 distinct body, sha256 `afb5184d58b62da5cb03cb8c4c7923953b4206c52f8afa23dee6403069fe6344`**). **Band: sixteen packages, `076`–`091`, unchanged. Edges: 38, unchanged, and the four declared surfaces — plan §2 mermaid, plan §3, registry §2.1, registry JSON `depends_on` — were re-verified identical after every commit.** Every new object states the SEAM-1 `max()` that placed it | **DOC-FIX-APPLIED** | this record · schema §13.7 `S-14`–`S-22` | **P** | n/a |


---

**Remediation-reconciliation attestation (2026-08-27).** Four concurrent remediation passes changed ratified material and filed correction IDs that did not exist in this record. Those IDs are now recorded, **under their own names** — `H-2`/`H-3`/`H-4`, `AUTHZ-*`, `J-8`…`J-12`, `K-14`…`K-20`, `S-1`…`S-13` — so an implementer reading a defect ID in a spec can find it here by that ID rather than by a translation. **No prior row is renumbered**, and the two existing `O` namespaces are untouched; the new `RET-` prefix is deliberately not a third `R` series, because `R-1`…`R-18` (RPC §20.14) and `R-1`…`R-17` (role model) already collide with each other.

**Two entries are retractions of ratified reasoning rather than additions, and they are the reason this pass exists.** `RET-1`…`RET-5` record five security claims **deleted as false**, each with the reason and each also carried in its own file's correction index so it cannot be cited from an older copy. And **C56** records a fact about a ratified *decision*: ruling `OQ-5`/`OQ-W4` relaxed a ratified constraint **on two conditions, and verification found neither implemented** — one bound only the `ends_at IS NULL` branch while the common branch admitted an unbounded `exp`, and the other had a mechanism with no caller. Both are now specified and both gate the Wallet enable, but **the owner's sign-off on the relaxation is still owed**, and the relaxation's safety rests on exactly those two conditions.

**What this pass deliberately did NOT do.** It resolved **no** open decision — O6, O7 and O8 stand, and O9 and O10 are added, not closed. It made **no** owner decision; every one it found is listed above and left with its owner. It resolved **no** contradiction between specs — the ones it found are **recorded** where a reader will hit them: the `crm-export` per-function-vs-per-route `verify_jwt` contradiction (edge §9 item 17), the two divergent specifications of `venue.door_session` (SPEC_FOUNDATION §6), and RLS §2.2's helper count stated four different ways. It touched **no** SQL, **no** migration, **no** code, and nothing under `supabase/`. It edited only the four files it owns — the edge spec, the venue dashboard spec, this record, and the spec foundation — and every change it could not make in those four is named in the row that found it, with the owner who must make it.

**Independent verification performed by this pass, not inherited.** The `OFFLINE-VERIFY-v1` single-source claim of **C53** was checked rather than assumed: all **four** fenced blocks under `docs/architecture/**` were extracted and hashed by two independent methods, and all four are **byte-identical** — one normative source (edge §5.4.3) and three sanctioned mirrors, which sit in **two** other documents, not three, because the Wallet spec carries two of them. **The CI gate that enforces this now EXISTS, and this sentence previously said it did not — corrected as row `D19`.** It is `.github/workflows/ci.yml`, job `quality`, required check name **`Typecheck / Lint / Unit tests`**, step *“OFFLINE-VERIFY-v1 spec byte-identity gate”*. The property therefore holds **by construction**, not by inspection, exactly as edge §5.4.3 clause 3 claims. The gate scans **recursively** — every `*.md` under `docs/architecture/**`, `_governance/` and `_superseded/` included, because a mirror that lands in a subdirectory must be gated, not exempted by a flat glob — and fails closed on **seven numbered guards**: **1** an unterminated fence (every block after one is invisible to the scan) · **2** the tag at column 1 outside any fence — an unfenced copy is an ungated copy · **3** non-vacuity: fewer than `OFFLINE_VERIFY_MIN_BLOCKS` = **4** blocks found, a floor and never an equality · **4** the normative home (`PHASE_2_EDGE_FUNCTION_SPEC.md`, §5.4.3) no longer carrying a block, without which the mirrors would agree only with each other · **5** byte-identity across all copies · **6** anti-emptying: a body under `OFFLINE_VERIFY_MIN_BODY_BYTES` = **1900** — four empty blocks are byte-identical and four in number · **7** anti-coordinated-weakening: a ≥ 10-entry load-bearing-clause anchor list, itself guarded against heredoc truncation. Guard 7 is the one that would have caught **C53** itself: byte-identity proves the four copies agree and says nothing about *what* they agree on, and four copies that all drop conjunct `3b.v` are identical, four in number and full length. **This correction is `DOC-FIX-APPLIED`, not `RETRACTED`**: the retracted sentence was *true when written* on 2026-08-27 and was overtaken by the gate landing, whereas `RETRACTED` is reserved for text that was false when ratified. Stale in the safe direction is still stale in a constitution.

**Owner-ruling integration attestation (2026-08-27).** The DA and CDM edits authorized by rows O-1 … O-5 and D4–D8 are applied to the constitution bodies, tagged inline by ID, and carried into each document's Correction Index appendix. Specifically: DA §7.6 carries **one** money matrix — the O-1/O-3 corrected rows, with the payout-request and payout-destination rows no longer rendering as the same authority, and with `ᔆ` marking the SoD-constrained cells; DA §7.4 records the O-3 SoD-1 collapse as a ratified trade-off with its named mitigation rather than as an unqualified capability grant; the four pre-C36 bare-label lists are gone from DA and the §7.2 catalogue states display-name-vs-stored-label at its head; `set_org_payout_destination` reads `org_owner` only in DA §7.6, matching RLS §11; and both constitutions state attribution timing and the promoter's commercial terms in terms an implementation spec cannot quietly narrow. **What this pass deliberately did NOT do:** it resolved no open decision (O6/O7/O8 stand), renumbered no prior row, wrote no freeze record, and touched nothing outside the two constitutions, this record and `ARCHITECTURE_FREEZE.md`. Corrections owed by sibling integrators are named in the rows that found them (D7 → RPC §6.1 / RLS §9.17; D8 → schema §3.17).

**Consolidation attestation (2026-08-24).** Both constitutions now read correctly on their face: the ticket state machine draws no `refunded` terminal; the credential is asymmetric everywhere it is described, with the C33 key lifecycle in the body; the ownership-log idempotency rule is the three-column C26 key plus the per-sale terminal machine wherever stated; the inventory counter's authority (C27) is stated consistently in every counter/ledger passage; the SSCAS is enumerated, closed, and lock-ordered in both documents; and every Gate-M/L correction appears in the body as an explicitly-gated extension requirement, never as an MVP promise. Corrections remain tagged inline by ID for traceability to this record and to the four authority documents.

---

## Cumulative-authority and custody-routing pass — recorded 2026-08-28 (`MB-1` / `MB-6`)

**Authority:** an adversarial review of the Phase-2 money-authority and door contracts against the controls they claim to enforce, filed under a **fifth ID namespace, `MB-n`**. It is deliberately **not** `M-n` (`MD-n` is the RLS spec's open-decision series and `MP-n` is the offline-manifest projection pass recorded above), and this pass **allocates only the two `MB-` ids it was given — `MB-1` and `MB-6`, with sub-letters `MB-1b` and `MB-6a` for what it found itself.** `MB-2` … `MB-5` are **deliberately not used**: the reviewer's series is wider than this pass's scope and the unused numbers belong to whoever holds those defects. **Ids in this section were allocated by reading this record's current maxima (`C87`, `D19`, `O13`, `RET-6`) immediately before writing, on the merged tip** — five concurrent passes on 2026-08-28 each claimed the same "next" ids and all five had to be renumbered, and **this pass was itself renumbered once** (`C80`–`C82`/`D19`/`O13` → `C88`–`C90`/`D20`/`O14`) when a sibling pass landed first. **The rows below therefore claim exactly: `C88`, `C89`, `C90`, `O14`, `D20`.**

**What it found, in two sentences.** Every threshold in the refund tier table was compared against **one call's amount**, with no cumulative or velocity guard anywhere in the corpus — so a single `org_owner`/`org_finance` refunded an arbitrarily large order to zero without ever reaching `pending_approval`, and the design's own §5.4 Race 4 blessed the shape as *"successive partials compose correctly."* Separately, `venue.reconcile_offline_scans` declared a **direct `kernel.tickets` write** in violation of §0.7 of its own document — in the **batched** door path, behind `verify_jwt=false`, and therefore outside the `pg_proc.prosrc` assertion that pins `mark_ticket_scanned`'s must-not-recheck-the-freeze property.

| ID | Correction (one line) | Status | Final section(s) | Gate | MVP-must-implement? |
|---|---|---|---|:---:|:---:|
| **C88** | **The refund tier thresholds were per-call, and nothing bounded the sequence.** Every row of the tier table — twin copies at `PHASE_2_MONEY_AUTHORITY_SPEC.md` §6.1 and `PHASE_2_RPC_FUNCTION_CONTRACTS.md` §17.1 — compared **`p_amount_minor`**. The only aggregate in either document was the over-refund ceiling (`Σ(refunds for the payment) + p_amount_minor ≤ payment.total`), which bounds the **total to the order value** and says **nothing about the tier**; the `platform_support` cap (`AUTHZ-M3`) was per-call one arm over; and the edge rate limit (`check_rate_limit(user,'refund-execute',10,60)` = 600 calls/hour) is a throughput limit that bounds no value. **Consequence: `SoD-2` on refunds was unenforceable against the exact insider `O-1` created it for** — ⌈total / `refund.org_auto_execute_max_minor`⌉ sub-ceiling calls drain an order with no second approver, and §5.4 Race 4 documented the composition as a designed property. **Ratified form: the tier operand is cumulative over the PAYMENT** — `refund_exposure_minor(payment)` = Σ non-`failed` `kernel.refund.amount_minor` for that `payment_id` **+** Σ `kernel.approval_request.amount_minor` of its `pending` requests; `cumulative` = that + this call. **The subject is derived, not chosen:** it is the subject the over-refund ceiling already sums, its `FOR UPDATE` on `public.payments` is already taken in the same precondition (**no new lock, rank, SSCAS member or index — `D-1` is not reopened**), it is the subject Stripe aggregates against, and it is the only candidate **invariant under decomposition** (a time window is defeated by waiting, an actor window by the SoD-2 counterparty). The sum is computed **after** the payment lock or it is a per-call test with extra steps. The approval-time re-derivation uses the same operand and **excludes the request under approval from the parked term**. **The buyer arm's operand — never stated in any document, with the two readings holding opposite security properties — is settled as `cumulative` (the refunded amount, not the order's eligibility), one operand for the whole table.** **No threshold VALUE is chosen: `D-3` stands and is annotated, because the keys now denominate a cumulative ceiling.** Tests `T-RPC-MONEY-21`…`-24`, appended so `-15`…`-20` keep their numbers | RATIFIED | MONEY §5.4 Race 4, §6.1, **§6.1a (new)**, §6.2, §7.2, §11 (`D-3` amended, `D-9` added), §12, §14 · RPC §17.1, **§17.1a (new)**, §17.2, §18, §20.14 `R-27`, §21 | P | YES |
| **C89** | **`venue.reconcile_offline_scans` declared a direct `kernel.tickets` write, and *"only via the three kernel engines"* named a set smaller than the corpus contracts.** RPC §9.5's **Writes** read *"`kernel.tickets` (first-admit-wins → `scanned`)"* with **no delegation named**, while its online sibling §9.4 routes the same admission decision through **`kernel.mark_ticket_scanned`**. That engine carries the **must-not-recheck-the-freeze** property *structurally* — the migration plan pins *"`kernel.mark_ticket_scanned`'s body references `is_transfer_frozen` NOWHERE"* over `pg_proc.prosrc`, *"the defect a well-meaning engineer re-introduces"*, blast radius *"100% of admissions from doors-open to end of night"* — and **a reconcile path that writes the atom itself is not covered by that assertion**, which inspects one function's body and passes while the admission decision runs in another. The offline path is the **batched** one (up to 600 rows), reached only through the `door-session` edge function where `verify_jwt=false` and RLS is bypassed, so the function body is the sole enforcement point. **Ratified form:** §9.5 routes through `mark_ticket_scanned`, once per admitted row inside the batch loop, under the atom lock it already takes ascending — **first-admit-wins, ordering, idempotency, SSCAS class and `AUTHZ-H3b`'s per-call assert all unchanged**, `T-RPC-DOOR-35` asserting the routing structurally because no value-based test can distinguish the two implementations. **And the rule itself is made applicable:** §0.7's *"only via the three kernel engines"* is replaced by **§0.7a's enumeration of five sanctioned kernel custody writers** (the three engines + `lock_ticket`/`unlock_ticket` + `mark_ticket_scanned`) with their delegating callers, plus two standing rules — **the Writes line must name the writer** (`MB-6a`: §20.8.2 named it only in Locks) and **adding a member is an amendment**. **The prohibition is unchanged and nothing is exempted**; five conforming contracts had been violating the literal rule, which is precisely why the one real violation was invisible | RATIFIED | RPC §0.7, **§0.7a (new)**, §9.5, §20.8.2, §18.1, §20.14 `R-24`/`R-25`, §21 | P | YES |
| **C90** | **The same splitting shape applies to payouts, and there the aggregate subject is minted by the caller.** `payout.request_auto_max_minor` / `payout.dual_control_min_minor` are compared against **one payout's amount**; a payout is generated by `kernel.close_settlement` from a settlement whose **period is a caller-supplied parameter** (`venue.open_settlement(p_org_id, p_venue_id, p_event_id, p_period, …)`, open to `org_owner`/`org_finance`/`venue_finance` — **the same principals the tier gates**). An org that may not disburse £X in one payout opens N narrow-period settlements and disburses it in N. **This is `C88` with a worse property**: the refund case splits a *fixed* subject (the payment exists; Σ over the parts is the whole, which is why `C88`'s aggregate is invariant), while here the caller **chooses the decomposition of the subject itself**, so no aggregate scoped to "this settlement" can be invariant by construction. **The property a correct fix must have is stated so the fix is checkable: the tier operand must be invariant under decomposition of any caller-chosen subject.** Two admissible forms — **(a)** undisbursed org exposure (`Σ kernel.payout.amount_minor` in `pending`/`held`/`submitted` + this payout: no new key, no window, not caller-mintable, **does not close the slow drip**); **(b)** a rolling per-org window (`payout.tier_window_hours`: closes it, costs a key and a width decision). **Not decided here — it is *who may disburse how much without a second approver*, and unlike the refund case there is no subject already in the corpus to derive it from.** What is **not** open is whether payouts are splittable today: **they are.** Open decision **O14** | OPEN-GATED(O14) | MONEY §7.2, §9.2, §11 `D-10` · RPC §10.3 | P | **OPEN — blocks the payout tier's implementation** |
| **O14** | The open decision itself: **the payout tier's operand** — undisbursed-org-exposure aggregate vs rolling per-org window. A security ruling with a commercial cost, not a schema preference: form (a) adds no key and leaves the slow drip open, form (b) closes it and adds a window whose width is itself a `D-3`-class number | OPEN-GATED(O14) | MONEY §9.2, §11 `D-10` · RPC §10.3 | P | **OPEN** |
| **D20** | **Four integrator requests and two owner decisions filed by this pass, recorded so none is lost between owners.** **`R-24`** → the **RLS owner**: `PHASE_2_RLS_PERMISSION_SPEC.md` §7.5 — *"the complete statement of Phase-2 write authority"* — names **four** writers of `kernel.tickets` (*"`issue_ticket_atoms`, `transfer_ticket_ownership`, `void_ticket_atom`, `record_scan`"*, and §5's row *"same three + scan RPC"*) where **ten** are contracted; six are missing (`mark_ticket_scanned`, `lock_ticket`, `unlock_ticket`, and the four money RPCs that write `resale_state`), **and the fourth listed is `record_scan` — a `venue.*` wrapper, so the authority statement asserts a §0.7 violation as the design.** That is the traceability matrix's **`G-20`** *"two names for one function produces two functions or none"* landing in the one table where the difference decides whether the `prosrc` freeze assertion covers the writer; requested with a `T-RLS-EXEC-01`-shaped structural assertion so the list cannot drift again. **`R-25`** → **owner ruling**: four money RPCs (`request_order_refund`, `approve_refund_request`, `cancel_refund_request`, `sweep_expired_refund_requests`) write `kernel.tickets.resale_state` **without** the §7.4 `lock_ticket`/`unlock_ticket` overlay pair that exists so the column has one writer pair and one freeze re-check choke-point. **Not a defect in either direction today and deliberately unchanged** — §17.1 precondition 7 does check the freeze on the parked branch and the release paths are correct to skip it — but **nothing states which of the two the design is**, so an implementer picks per function. **`R-26`** → the corpus-wide id owner: **`R-22` is used twice in RPC §20.14**, once for `MP-1`'s clock-skew confirmation and once for the platform-plane grant-maturity ruling that schema §13.7 `S-3` and RLS §11.3a both cite **by that id**; not renumbered here because the second is cited externally. **`R-27`** → the **schema owner**: `kernel.approval_request` gains **`amount_minor integer`** (NULL only for `config.set_money_key`, two CHECKs), additive inside the already-scheduled `077`, **no new table, no package, no DAG edge, `C72` untouched** — required because `C88`'s parked term is an **authority** input and no authority predicate may read `payload` (`T-RPC-AUTHZ-01`, structural). **This is `C57` one column over: a tier decided from a value the row does not store is a control that does not run.** **Owner decisions recorded, not made:** MONEY **`D-9`** (should the buyer arm *additionally* gate on order value — recommendation **no**, the cumulative cap already bounds exposure) and **`D-10`** (`C90`/`O14`'s payout operand). **`D-3` is amended, not answered:** its numbers now denominate a **cumulative ceiling per payment**, so a per-call £50 and a cumulative £50 are different products and the values must be chosen against the new reading | DOC-FIX-APPLIED | RPC §20.14 `R-24`/`R-25`/`R-26`/`R-27` · MONEY §11 (`D-3` amended, `D-9`/`D-10` added), §14 · RPC §21 | — | YES (applied) |

**Row-count delta, stated rather than applied.** This section adds **five** rows (`C88`, `C89`, `C90`, `O14`, `D20`) — **four terminal and one `OPEN-GATED`**. The status-count table and the running total near the top of this record are **deliberately not edited by this pass**: five concurrent passes are appending on 2026-08-28, and a shared counter rewritten by each of them is a merge conflict in the one place the record must stay readable. **Whoever performs the next reconciliation should fold this delta in** — `RATIFIED` +2 (`C88`, `C89`), `OPEN-GATED` +2 (`C90`, `O14`), `DOC-FIX-APPLIED` +1 (`D20`).

**What this pass deliberately did NOT do.** It **chose no threshold value** — `D-3` is annotated, not answered, and `refund.org_auto_execute_max_minor`, `refund.org_dual_control_max_minor`, `refund.buyer_self_service_max_minor` and `refund.platform_support_max_minor` acquire no number anywhere. It **resolved no open decision**: `O6`…`O13` stand and **`O14` is added, not closed**; `D-1` is specifically **not** reopened, because the cumulative test reuses a lock the contract already takes. It **decided no authority**: no row changes *who* may refund, approve, scan or disburse — every predicate keeps the principals `O-1`, `O-3`, `C57` and `C58` gave it. What `C88` changes is **when the second pair of eyes is required**, which was the control `O-1` ratified and the per-call operand silently disabled; what `C89` changes is **which function performs a write that already happens**. It touched **nothing in the frozen Stripe money core**, no `public.*` table, no ratified ownership invariant, and no `kernel.refund`/`refund_primary_order` behaviour — **R7 single-path preserved: no new object writes a money row.** It **weakened no CI gate, ratchet or floor** and changed no workflow file. It renumbered **no** package (`076`–`091`), **no** migration (`071`–`075`), **no** prior row of this record and **no** existing test id — `T-RPC-MONEY-21`…`-24` and `T-RPC-DOOR-35` are **appended**. It **edited three files**: `PHASE_2_MONEY_AUTHORITY_SPEC.md`, `PHASE_2_RPC_FUNCTION_CONTRACTS.md` and this record; everything it found in a file another integrator holds is named in `D20` with the owner who must make it.

**`OFFLINE-VERIFY-v1` attestation (2026-08-28).** No fenced block was touched — **none appears in either document this pass edited.** Verified rather than assumed, by extraction and hashing after the last edit **and** after merging the concurrent tip: **four** blocks under `docs/architecture/**` (`PHASE_2_EDGE_FUNCTION_SPEC.md`, `PHASE_2_DOOR_LIFECYCLE_SPEC.md`, `PHASE_2_APPLE_WALLET_SPEC.md` ×2), **one distinct body**, **2017 bytes, 34 lines**, `sha256 afb5184d58b62da5cb03cb8c4c7923953b4206c52f8afa23dee6403069fe6344` — identical to the value `MP-1` recorded, so `C53`'s single-source claim still holds.

---

## Replay-ordering pass — recorded 2026-08-28 (`R2B`)

**Authority:** an adversarial review of the Phase-2 corpus for **migration replay ordering in the routine
layer**, independently re-verified against the RPC contracts, the RLS spec, the physical schema spec and the
plan before any edit. **The DDL layer is clean** — schemas, tables, columns, FKs, CHECKs, triggers, policies,
indexes and seeds all order correctly, and no native enum exists anywhere. **The routine layer was not.**
`plpgsql` bodies are **not** validated at `CREATE FUNCTION`, so every defect in this pass **replays green and
fails at runtime**. It is `K-2` one layer up: `K-2` was a body naming a relation that *nothing* created; this
is a body naming a relation, a column, a composite type, or a routine that a **later package** creates.

**Ids claimed by this pass, stated so a concurrent pass can avoid them: `C99`–`C107` and `D22`.** They were
allocated by reading this record's current maxima on the merged tip immediately before writing — **`C98`,
`D21`, `O16`, `RET-6`** — and taking the next free block. **No `O` id and no `RET` id is claimed:** every
owner decision this pass encountered was already open under an existing id (`MD-2`, `O14`, `R-9`, `R-10`) and
is routed there rather than re-numbered, and nothing ratified was deleted as false. Six prior passes have
collided on this block; the maxima are stated above so the next pass reads them rather than guessing.

**Row-count delta, stated rather than applied**, per the convention the `MB-1`/`MB-6` pass set: this pass
adds **ten** rows — `C99`, `C100`, `C101`, `C102`, `C103`, `C104`, `C105`, `C106`, `C107`, `D22` — of which
**nine are `RATIFIED`** and **one is `DOC-FIX-APPLIED`** (`D22`). The status-count table and the running
total near the top of this record are **not edited here**; whoever performs the next reconciliation folds in
`RATIFIED` **+9** and `DOC-FIX-APPLIED` **+1**.

| Row | Statement | Status | Where applied | Gate | Blocks impl? |
|---|---|---|---|---|---|
| **C105** | **A composite type used by two routine signatures is created by nothing.** `settlement_line_candidate` is the declared `RETURNS SETOF` type of `kernel.settlement_royalty_lines` (RPC §20.11.1) and, by inheritance (*"Signature / authority / posture. As §20.11.1"*), of `kernel.settlement_commission_lines` (§20.11.2). **It occurs exactly once in the repository** — that one signature line. No package's Tables row creates it, no `CREATE TYPE` exists anywhere, and the physical schema spec does not define it. **`087` therefore fails `42704` on its first stub** — and unlike the rest of this pass that failure IS at replay, because a return type is resolved at `CREATE FUNCTION` even though the body is not. Repair: **`CREATE TYPE kernel.settlement_line_candidate AS (cause text, cause_ref uuid, amount_minor bigint, currency text, payee_kind text, payee_id uuid)` in `087`, immediately before the two stubs**, schema-qualified into `kernel` where its consumers live. **SEAM-1 places it at `087`**: it depends on no relation, and `087` is the earliest package in which any consumer exists. **The alternative — two hand-written `RETURNS TABLE(…)` lists — is rejected on mechanics, not taste:** the shape is the return type of **four** routine definitions across **three** packages (stub ×2 in `087`, replacement in `088`, replacement in `090`), and `CREATE OR REPLACE` **cannot change a return type**, so four independently maintained column lists fail as *"cannot change return type of existing function"* at `088`/`090` replay. One object makes the divergence physically impossible rather than asserted. **Rollback order matters and is stated: the type drops LAST**, after both hooks, because `DROP TYPE` fails while any routine's signature depends on it. Physical definition filed to the schema-spec owner; the column list is transcribed verbatim from §20.11.1 and must not be re-derived. **No package added, renamed or renumbered; no edge** | **RATIFIED** | plan §0.4b · plan §8 `087` (new **Types** row, Rollback, Tests `T-SCHEMA-SEAM-01`/`-02`) · registry §2 `087`, §3 `seam_rules.types` · filed `S2-A` | **P** | **YES** |
| **C106** | **`market.on_atom_voided` had two arities, and the prescribed mechanism is `CREATE OR REPLACE`.** Two-parameter `(atom_id, refund_id)` in plan §0.4b's hook table and in `PHASE_2_PHYSICAL_POSTGRES_SCHEMA_SPEC.md` §13.2 **twice** (the `FR-4` row and the SEAM-2 table); three-parameter `(p_atom_id, p_refund_id, p_cause)` in RPC **§20.11.3**, which is the section that contracts it. **Neither half of the divergence is a documentation defect.** Stub at two and replacement at three: `CREATE OR REPLACE` **cannot add a parameter**, so `088` creates a **second, overloaded** routine; the two-argument no-op **stays live**, `kernel.void_ticket_atom`'s call site stays bound to it, replay is green, and **the C26 compensate arm is dead in production on every refunded resold ticket** — `market_sale.terminal_state` is never set to `compensated`, so compensate-XOR-complete goes unenforced in the one direction it exists for. Same arity but different parameter *names* (`atom_id` vs `p_atom_id`) is the other half: `42P13 cannot change name of input parameter`, a **hard `088` replay failure**, loud and therefore the better of the two. **Canonical: RPC §20.11.3's three-parameter form**, on plan §8's own stated rule that RPC §20 is where a scheduled function's signature is contracted. New binding rule **`SEAM-2a`**: a hook's **parameter list, parameter NAMES and return type are frozen at the stub**, `CREATE OR REPLACE` may change **only the body**, and the replacing package asserts **`COUNT(*) = 1` over `pg_proc` for the hook name** — an *overload count*, because a value-based test of the new routine passes while the old one is still bound. **`p_cause`'s admissible values and its effect on the body are contracted nowhere** — filed to the RPC owner as `R2B-1` and **not decided here**; `SEAM-2a` means a ruling that drops it must land **before `085` is authored** | **RATIFIED** | plan §0.4b (rule + corrected hook table) · plan §8 `085`/`088`/`090` (signatures + `T-SCHEMA-SEAM-03`) · registry §2.2 `SEAM-2a`, §3 `seam_rules` · filed `S2-B` · `R2B-1` → RPC owner | **P** | **YES** |
| **C103** | **`kernel.issue_ticket_atoms` (`081`) reads `kernel.signing_key` (`083`) — the identical criterion `FR-3` used to move `transfer_ticket_ownership` out of `079`, applied to one kernel engine and not the other.** RPC §7.1's preconditions require *"an `active` `kernel.signing_key` resolves for the event scope"*, and its Writes row pins `signing_key_id` on every atom it mints. `kernel.signing_key` is created in `083`. **`FR-3` (schema §13.2) moved `kernel.transfer_ticket_ownership` out of `079` for reading that same table** (among two others) and states the reason in one line; the mint engine reads it too and stayed in `081`. **A rule applied to one of two symmetric engines is not a rule, it is a case** — and the failure is silent: `plpgsql` does not resolve `kernel.signing_key` at `CREATE FUNCTION`, so `081` replays green and the first primary issuance raises `42P01` inside `venue.finalize_primary_order`'s paid transaction, after the money is captured. Repair: **authored in `083`** — `max(078 event_session, 079 tickets + ownership_log, 081 inventory_batch/_shard/movement, 083 signing_key) = 083`. **Both alternatives rejected on mechanics.** A SEAM-2 `kernel.resolve_active_signing_key` hook stubbed in `081` has **no correct neutral result**: *no key* either fails open (a minted atom with a NULL `signing_key_id` is a credential-less ticket) or fails closed (the mint is impossible), and SEAM-2 requires the stub's answer be *"correct at that point in the chain"*, not merely inert. Moving `kernel.signing_key` to `≤081` instead would change **`084`'s object list** — `084` creates zero relations and zero routines and that purity is what makes its rollback unconditionally reversible, protected by registry rule §6.7. **New declared edge `081 → 083`** (`§`), declaration and ordering agreeing on the day it is written (`081 < 083`). **No package added, renamed or renumbered; band `076`–`091`, sixteen** | **RATIFIED** | plan §1, §2 (mermaid + `R2B` edge table), §3 seq 8, §4, §8 `081`/`083` (Functions, Dependencies, Rollback, Tests `T-SCHEMA-MINT-01`/`-02`) · registry §2, §2.1, §3 · filed `V5` | **P** | **YES** |
| **C102** | **`venue.append_door_manifest_delta` (`086`) is called from three earlier packages, and one of those calls is mandatory.** `kernel.issue_ticket_atoms` calls it with `p_op := 'add'` so an offline scanner's admissible set tracks atoms minted after the base snapshot — *"without it, a fan who buys at the box office after doors open is refused by every offline scanner"* (RPC §6.3). `kernel.void_ticket_atom` on **any** freeze-exempt path, `kernel.admin_refund` and `kernel.force_void_ticket` — all `085` — call it with `p_op := 'revoke'`, and RPC §12.4c makes that **mandatory, not advisory**: *"Every exempt path that voids an atom MUST write a `revoke` delta … Omitting it re-opens the offline-revocation leak the exemptions were granted around"* (`T-RPC-MONEY-15`). `venue.door_manifest_delta` and the function's real body are created in `086`. **Four call sites, three packages, all earlier than the callee — every one of them replays green and raises at first use, two of them inside a refund transaction.** Repair: **one SEAM-2 hook**, `venue.append_door_manifest_delta(p_session_id uuid, p_atoms uuid[], p_op text, p_cause_ref uuid) RETURNS void`, **stub in `083`** — SEAM-2 places a stub in the package of the **earliest caller**, and after `C103` that is `kernel.issue_ticket_atoms` — **body `CREATE OR REPLACE`d in `086`**, signature frozen at the stub by `SEAM-2a`. **The neutral result is the callee's own contracted degenerate case**, not an approximation of it: *"if no episode is open the call is a silent no-op, never an error"*, and no episode **can** be open before `086` because the table does not exist. **This is the one hook whose neutral result would be UNSAFE if it outlived its replacement, and that is stated rather than left to be noticed**: a missed `add` fails **closed** (a scanner refuses a late-issued atom), a missed `revoke` fails **OPEN** (a scanner admits a voided atom) — so `086` asserts the replaced body is live, and `086` is the first package in which either condition can obtain. **`085 → 086` is NOT added and must not be** — it runs backwards; the hook is what makes the repair admissible at all. **No edge, no package added, renamed or renumbered** | **RATIFIED** | plan §0.4b (hook table + the fail-open note), §8 `083` (stub, Rollback, `T-SCHEMA-DELTA-01`) / `085` (the three callers) / `086` (`CREATE OR REPLACE`, `T-SCHEMA-DELTA-02`/`-03`) · registry §2, §3 `seam_rules.hooks` · filed `V4`, `V6` | **P** | **YES** |
