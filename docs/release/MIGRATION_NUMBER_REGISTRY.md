# Migration and pgTAP number registry

One owner per number. Updated 2026-09-17. Check here before claiming a number — **migration AND pgTAP numbers are allocated together** (203 = 136 A, 204 = 139 B, 205 = 137 A, 206 = 138 D, **207 = 140 B**).

**Build-source tag b2, 2026-09-17:** `candidate/2026-09-18-build-b2` = `aad5f75` — the sandbox pin `9bef640` plus docs-only runbook fixes and C's two client-only fixes (`8dc4cec`, `a609cbc`; D PASS; owner ruling 2026-09-17); `supabase/` identical to `9bef640`, so no migration state changes; CI green at the commit (run 35175523163). Source of the one authorized combined sandbox preview build. The sandbox application pin remains `9bef640`.

**Production-gate pin b2, 2026-09-16:** `release/production-gate-20260918 @ 9bef640` = tag `candidate/2026-09-18-pin-b2` — GitHub CI green at that commit (run 35053607616, 5/5 jobs, pgTAP files=85 tests_ran=5186 PASS), D Gate 3 PASS (D log `26d69d9`) — adds 131 (K-2 amendment), 132 (@ 74a4371), 133, 20260916000000 (134), 135 and their edges (`create-payment-intent`, `enforce-transfer-expiry`, `send-push`) on top of the candidate pin below, which is kept untouched. Applied nowhere; the sandbox application package (`SANDBOX_APPLICATION_PACKAGE_B2.md`) awaits the owner. Docs-only above the pin, applied bytes unchanged: `5e8b0f4` (D-135-5, B's `5225557`) and `e6d9f2e` (D-135-6, B's `84ddd9a`), both D PASS. **Sandbox apply order (owner confirmation pending 2026-09-16):** `131 → 132 → 133 → 135 → 20260916000000` — the `LC_ALL=C` order the chain replays; "134" sorts last by design (see its row).

**Candidate pinned 2026-09-15 (re-pinned the same night after the 193 CI fix):** `release/candidate-20260918 @ aabe029` = tag `candidate/2026-09-18-pin` — GitHub CI green at that commit — 121, 123, 124, 125, 126, 127, 128, 129, 130 (amended) + edges #64/#66/#67 + C's client stack. Applied nowhere; not a release. Anything numbered from here (131 session-bound, 132 if the fresh-mint structural fix is wanted) is outside the pin.

**Canonical production state:** `docs/release/PHASE2_PRODUCTION_STATE_20260912.md` (Claude B, commit
`55d37f5`, currently only on `origin/feature/venue-native-and-product-v2`). Production ledger **135**, numeric
tip **120**. That document supersedes the "current state" lines in the six historical Phase-2 records it
banners; it authorizes nothing.

| Migration | pgTAP | Owner | Subject | State |
|---|---|---|---|---|
| — | 187 | release integration | `my_tickets_read` | on the candidate |
| `20260910120000` | 188 | **Claude D** | `venue_api` read views | branch `venue/read-adapters-slice-1`, not applied |
| `121` | 189 | **Claude B** | manifest signing context STRICT | PR #58, not applied |
| ~~`122`~~ → `125` | **190** | **Claude B** | scanning drift fix (086↔112/113) | **REASSIGNED 2026-09-12**; written + rollback + pgTAP 190 (30 assertions), **PR #62** (`fc4f1130` → `admin/operating-console`), rehearsed 121→123→124→125; **A-reviewed 2026-09-14, review-only**; not applied |
| `123` | 191 | **release integration** | transfers↔profiles FK parity | branch `fix/122-transfers-profiles-fk`; **applied to sandbox**, not production |
| `124` | 192 | **release integration** | bids↔profiles FK parity (F2) | written with pgTAP 192 and rehearsed P1–P6 (2026-09-12); applied nowhere; sandbox apply awaits authorization |
| `126` | 193 | **Claude B** (reassigned from A 2026-09-14) | ops refund exactness (`refund_facts` + `daily_summary`/`money_overview`/`refresh_metrics`; A8 capped running-sum slice; `mixed` + `legacy_upper_bound_cents` + `legacy_count`) | **PR #63 draft** (`fix/126-refund-exactness @ db2f95f1`, base `c55ea50`), **review-only, D reviewing**; certified harness 141/141 replay, full suite 4755/4755; tests 184/186 amended in place with dated notes (120's vocabulary); not applied. Number stays `126`; `129` withdrawn |
| `127` | 194 | **release integration** | release guards — **L1** stale cancellation frees a newer hold + **L2** release frees a paid order's hold | written + rehearsed; **three adversarial review rounds** (`d3761c4` → `a85bbb0` → `6383b8f`); pgTAP 194 **30/30**; applied nowhere. **Not integration-ready** until a review round returns clean. L1 is closed only together with the webhook/checkout edge change recorded in the release package — 127 alone delivers L2 |
| `128` | 195 | **release integration** | `public.register_push_token` — device-proof token rebinding (F7); `notify` stays unexposed | **contract v2 FROZEN 2026-09-15** (`PUSH_TOKEN_CONTRACT_V2.md`) at **`f22c1a3`** after D's pass 3: **no blocking findings**; three D passes total (F1 HIGH heal regression fixed; G-1 parity re-grant, G-2 UPDATE oracle, G-3/G-4 rollback inverses closed). Certified harness 4806/4806; 195 **58/58**; 157 294/294 on the public verb; census 31/93/37/35; `push_tokens` client SELECT and UPDATE column-scoped. Applied nowhere; **residual risk (session compromise) is O-3, the owner's production decision** |
| `129` | 196 | **release integration (A)** | `public.revoke_push_token(text)` — the client's sign-out revoke, reachable through `public` (the `notify` schema is not PostgREST-exposed; contract v2 §2.4 erratum) | **written 2026-09-15**, in the Friday candidate (`57b3a00`); census +1 function (94); rollback true inverse; **D adversarial review: NO FINDINGS** (IDOR, caller identity through the delegate, anon/service_role refused, notify ACLs unchanged, rule-5 spillover, rollback) |
| `130` | 197 | **Claude B** | checkout supersede claim — per-(listing, buyer, mode) serialization of PaymentIntent secret hand-out; lock order payments → listings; token-bound release; 120 s reclaim window (holder bound by the edge budget, E-1) | **written, reviewed, merged, then amended in place** (PR #65 `d5ee036` → PR #67 `8e02a95`, any-status sibling check per D-5 Q3 — 130 was applied nowhere so the immutability rule did not bind); A review APPROVED both; certified 4972/4972, 197 **45/45** (RED Q.1/Q.3 on the first version), two-session S1–S5 + deadlock control; edge E-1 (PR #66) merged; census **96**; **in the candidate `4b012fd`**; applied nowhere |
| `136` | 203 | **release integration (A)** | `public.get_my_security_notices()` + `public.mark_security_notices_read(uuid[])` — `public` wrappers over `notify.get_inbox` / `notify.mark_read` for the mandatory security types (device rebound now), the 129 pattern, because `notify` is not PostgREST-exposed | **WRITTEN 2026-09-17** on `fix/136-security-notices-read` (off `candidate/2026-09-18-build-b2`): also carries `notify.template` v2 for `security_device_rebound` (owner-corrected, D-verified copy; v1 untouched; rollback deletes v2). Certified full-chain rehearsal 155/155, Gate-2 32\|107\|37\|38; pgTAP 203 28/28 + 196/202/157 green (202 A7b version-agnostic, 157 A28 63); three negative controls fail as designed; rollback identity proven. Manifest +2, `EXPECT_FUNCS` 107. **Rev3 head (2026-09-17, after D's lifecycle finding: unread-only surface, partial index; 203 36 assertions; six negative controls)** after rev2 `a0a2b47` following D's first review (0a2644e held on the 200-row cap): no cap — count the caller's security rows first, then page `get_inbox` until every counted row is returned; type set derived from `notify.notification_type` (account_security + mandatory), never a literal; `p_ids` bounded to 100; 'newly marked' semantics and the E-160 keyset dependency in the header; 203 → 35 assertions incl. pagination (page 2) and bound cases; five negative controls fail as designed; 162 P2 census pin 105→107. **D PASS on rev3 `ae09f5b` (2026-09-17)**: both sides carry the same unread predicate so target and result cannot drift; STABLE function → one snapshot. Integration order 136 → 139 (B `bcece84`, pending D's release-on-failure fix) → B `aeb4081` → C's heads; at integration A fixes the 179 A9 / 180 A6 mangled prose and re-measures the five-schema census on the MERGED tree (B's 304/81/21/9 was taken with A's migration copied but not A's test edits). CI on the branch; applied nowhere — **INTEGRATED onto `release/production-gate-20260918` at `e9b52ce` (2026-09-17); not applied anywhere; no tag** **Current (2026-09-17 ~14:45Z):** in the reviewed candidate tree `db16e1a` (CI 35188006272; D merge gate PASS; no open review item); **absent on the sandbox** (ledger 141, read 14:35:00Z) and on production; no tag; build HELD for F-NAV-1 (client). |
| `137` | 205 | **release integration (A)** | **133 follow-ups:** (i) `notify_outbid` moved from the legacy GUCs to the Vault form (B's N-6); (ii) `kernel.check_signing_key_invariants` (099, rewritten by 133) passes its **cron run id** in the `notify-report` payload so 139's signing-alert claim keys on the run, not on a UTC date computed in the edge (D's finding via B: a run near 23:59 UTC would key to the next day and silence that day's trust-root alarm; 099 runs 05:23 UTC so latent, not live) | **ALLOCATED 2026-09-17, not written**; correctness fixes only — enabling the outbid push is a separate outbound approval |
| `139` | **204** | **Claude B** | `notify.report_delivery_claim` + `notify.claim_report_delivery(text,text) → boolean` — idempotency claims for `notify-report` (G22/R3): `report_created` on report_id, `dispute_opened` on transfer_id, `signing_invariant_alert` on the monitor RUN as the UTC date (never on the alert text — a recurring trust-root alarm must never be silenced; accepted cost: two alerts in one UTC day collapse, use a runid if 099 ever passes one) | **WRITTEN 2026-09-17** `fix/139-notify-report-delivery-claims @ ef8e2d5` (bcece84 + D's finding: a claim taken before the send was never released, so an event whose sends all failed stayed suppressed for good — `notify.release_report_delivery(text,text)` releases only when EVERY attempt failed; `sendPush` non-OK now a failure; over-length claim keys raise instead of truncating; pgTAP renamed 203→204 earlier), off the build tag; no table grants (157 B9), verbs SECURITY DEFINER service_role-only; notify census 8→9 tables / 20→**22** routines, five-schema 80→81 / 303→**305**, public census unchanged; pins 142/148/156/157 (A14, A15 signature list, A16, A46)/179/180 updated; replay 155/155, full pgTAP 5221/5221, pgTAP 204 41/41, vitest 2090/2090; controls: never-release, release on partial success, non-OK as delivered, `do update` → C2/C5/C8; CI 35180699684 green. **Ordering: after 136** (both `notify`; disjoint objects; A verifies at integration). With D for review; applied nowhere — **INTEGRATED onto `release/production-gate-20260918` at `e9b52ce` (2026-09-17) @ 7612c8b (test-only follow-up; D PASS); not applied anywhere; no tag** **Current (2026-09-17 ~14:45Z):** in the reviewed candidate tree `db16e1a` (CI 35188006272; D merge gate PASS; no open review item); **absent on the sandbox** (ledger 141, read 14:35:00Z) and on production; no tag; build HELD for F-NAV-1 (client). |
| `138` | 206 | **Claude D** | operator onboarding: `ops` reads (organisations, venues, staff, events), wrappers for `catalog.create_venue` / `approve_venue` / `create_event` / `venue.grant_staff_role`, new `action_type` branches + allowed-roles/approval rows | **ALLOCATED 2026-09-17, not written** — scope `OPERATOR_ONBOARDING_SCOPE_D_20260917.md` @ `0d85bb2`; the owner's decisions landed 2026-09-17 (visible fields, with **account identifier = the stable identity UUID, never the email**; two-person: venue approval, platform-role grants, elevation to organisation owner/admin; assisted first venue: owner approves every member change, by policy, window not opened) — **D writes 138 now.** D's proof at `review/d-release-sprint` @ `7989625`: every rollback target established mechanically (`action_dispatch`/`action_precheck` last defined in 118; `action_allowed_roles`/`action_requires_approval`/`audit_write` in 115 only; the nine onboarding verbs defined once each in 077/078/080); `ops.action`'s `action_type`/`subject_kind` CHECKs are inline auto-named constraints from 115 untouched by 118, so 138 drops them **by name and without `if exists`** (a no-match `if exists` would leave the old constraint standing and fail the first new-type action in the window), and proves the widening inside its own `do` block before commit (new type accepted, bogus type rejected). **Simplification (D, 2026-09-17): `ops.audit_write` needs no change** — it takes `p_action text` and `ops.audit.subject_kind` is `text not null` with no CHECK, so new action names and subject kinds flow through as data; 138 redefines **FIVE** objects: `action_dispatch`, `action_precheck` **and `ops.execute_action`** from 118's bodies, `action_allowed_roles` and `action_requires_approval` from 115's, plus the two CHECK constraints. **Status 2026-09-17: NOT review-ready.** D found (F-138-4, D's own probe on the rehearsal DB with an aal2 operator session) that `523333b` is INERT: `ops.execute_action` — the only door into the framework — validates `p_action_type` and `p_subject_kind` against hardcoded lists in 118's body and demands the old action↔subject pairings and a subject id, so every one of the fourteen new types is refused with `unknown action_type` before dispatch or precheck run; 45/45 pgTAP, 157/157 replay and six discriminating mutants were all component-level and never crossed the front door. A's F-138-2 (no test executes the framework for the new types) is the same gap. Fix: redefine `execute_action` from 118 (types, subject kinds, pairings, `org_create` in the subject-id-exempt set), rollback restores 118's body, and 206 gains per-type dispatch tests THROUGH `execute_action` (request → precheck → approve where required → dispatch) plus a test that the four approval-gated types cannot dispatch without a second approver. F-138-1 (accepted-invite mask fallback) fixed at `b7ce654` with G5b; F-138-3 (invite address in `ops.action.params`) goes into the contract. **F-138-5 (D, 2026-09-17, the security half):** 118's `execute_action` calls `action_precheck` ONLY when `action_requires_approval(type)` is true, so the type↔role guard placed in precheck never ran for the two ROUTINE types — `org_member_role_change` carrying `org_owner`/`org_admin` reached the domain verb without two-person approval (proven on the rehearsal DB: refused only by `kernel.has_org_role`, which would pass for an operator holding that org role); the elevating types were guarded (precheck fires for them), so D1–D7 passed while the security direction was open. Fix: the type↔role agreement now lives in `execute_action` beside 118's type/subject/pairing/reason validation (the path every action takes), precheck keeps its copies as a documented second layer for the approval-gated types; proven through the front door (routine+elevating role refused; elevate+routine role refused; routine+routine dispatched; `org_create` succeeded; `venue_approve` refused without a reason and `awaiting_approval` with one). Reason set extended to venue_approve, platform_role_grant, org_member_elevate, org_member_invite_admin, org_status_set, org_member_remove (one list, in execute_action). 118's execute_action enforces allowed-roles before the insert and records `requested_by`; `ops.approval.requested_by` records the requester of the approval. **Remaining before a head: the 206 rewrite** — per-type tests through `execute_action`, a rejected-precondition case per arm, the no-second-approver test, the reason-required cases, role tests on the real path. Pending-invite handle: `kernel.org_invite.invitee_ref` is the address and `invitee_identity_id` is null until acceptance, so the owner's identity-UUID rule has no handle for pending invites — D builds a deterministic mask (first character + domain, pending invites only, not searchable) as one expression in one function so the owner's ruling is a one-line change either way; **APPROVED by the owner 2026-09-17:** "a masked address for pending invites only, accompanied by the stable invitation ID so identical masks remain distinguishable. Keep the mask non-searchable and full addresses behind the audited, purpose-limited workflow. All previously agreed field restrictions and approval controls remain." New `ops` reads follow the house pattern: `grant execute … to authenticated` + `perform ops.assert_reader()` (operator role AND aal2 MFA), stronger than the contract draft's "console role only" line. **Ordering rule attached to the number (D, 2026-09-17):** `ops.action_dispatch` and `ops.action_precheck` are defined in 115 and **redefined in 118**; `action_allowed_roles`, `action_requires_approval`, `audit_write` are 115 only — all numbered below 138, so no timestamp trap; **138 must be written against 118's applied bodies and its rollback must restore 118's bodies, not 115's**, or a rollback silently reverts 118's corrections. 126 touches none of these. 136 is `public`-over-`notify` only (no `ops`, no action framework), so 136 and 138 do not interact **A's independent reviews (2026-09-17):** **@ 7eb4caf** — CI 35238876930 green; local replay Gate-2 32|107|37|38, 206 118/118; five redefinitions' rollbacks text-identical to their 118/115 applied bodies; F-138-6 fix correct (probes PA1 case, PA2 whitespace, PA3 duplicate keys: no invite); raised F-138-8 (self-targeted elevation completes with one approval; reproduced), F-138-9 (routine single-person org_finance grant; fact), F-138-10 (header counts). **@ 215694c** — CI 35242495301 green; local 206 161/161, 181 128/128; owner item 1: `ops.action_invitee` unreadable by anon/authenticated/service_role, labels only in params/audit, held row deleted before the kernel call, messages sanitized exactly, no UUID-variant bypass; owner item 3: `venue_submit` draft-only, approval pending-only at request and dispatch; **F-138-11 (A, MEDIUM, reproduced 166/166):** requester ≠ beneficiary is checked against the requester's email at request time, but acceptance matches the email at acceptance time, so an operator can request an admin invite to an address they control, get it approved, move their email and accept (the proposal's bootstrap invite A2 has the same shape; recommended fix: enforce 'operators are never members' at acceptance, via amendment); **principle 4 insufficient as worded:** a new kernel function defaults to EXECUTE for PUBLIC (anon/authenticated/service_role true locally; 076 default privileges cover tables only), so console-only verbs need explicit revokes from public/anon/authenticated/service_role plus a structural guard; retention of held references: an AFTER UPDATE OF state trigger avoids redefining approve_action. **Not application-ready:** item 2 awaits the owner's decision on D's proposal (`review/d-release-sprint @ 94681eb`). Applied nowhere; off the candidate. **Correction to A's 215694c review (D, verified by A in source):** the recommended structural guard `ALTER DEFAULT PRIVILEGES IN SCHEMA … REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC` cannot work, because a schema-scoped default can only add to the global default, never subtract PostgreSQL's built-in PUBLIC EXECUTE (PFA-1). The compensating control already exists: suite 140's PFA-1 witness (zero PUBLIC/anon EXECUTE on any function in kernel/venue/market/notify, via `coalesce(proacl, acldefault('f', proowner))`) plus 141 F2 (kernel's authenticated set by exact name). Its gaps: no service_role check, and `catalog` is not swept. The proposal now requires, per verb, revokes from public/anon/authenticated/service_role plus `has_function_privilege` assertions. **Proposal rev 2** (`review/d-release-sprint @ 732679c`): F-138-11 reproduced by D independently and answered by principle 5 / A4 (`accept_org_invite` refuses any identity holding platform authority, by amendment), with the revealed-address comparison as a secondary check only; the retention trigger is offered as decision 6 (D verified that no sweep writes expiry, so untouched expired requests keep their reference); server-log exposure stated as F11, unverified, with decision 8 asking for an authorized settings read. No 138 code changed. **OWNER RULINGS on proposal rev 2 (2026-09-17), for implementation and review, not application:** (1) the platform-assisted bootstrap flow and amendments A1–A4 are approved; creating an organisation must not make the operator its owner, and the customer gains ownership by accepting the invitation; (2) remove the seven organisation-management console actions (§3.1) and support's write permissions; (3) support keeps authorized reads and the two audited contact reveals, purpose-restricted, and membership must not be a workaround for support access; (4) customer roster management stays on the customer side, existing role restrictions and maturity rules are preserved, and there is no single-person platform elevation; (5) invites are delivered manually through a verified channel at first (workflow design only; no invitation sent, no notifications enabled); (6) held invite addresses are cleaned up when an action becomes terminal; untouched expired requests stay retained under lazy expiry, bounded cleanup for them is proposed separately, and no new scheduled job; (7) A4 is approved: identities holding platform authority, including the bootstrap admin path, may not accept customer-org invitations; test the email-change sequence A and D reproduced; identify the other membership-creation paths; (8) server-log exposure stays UNVERIFIED, with the settings-read scope prepared separately (`SERVER_LOG_SETTINGS_READ_SCOPE_20260917.md`) and no production read authorized; plus explicit per-function revokes, and tests covering the catalog and service_role gaps; PFA-4 and two-person approval preserved. D implements; A independently reviews the new head. 138 stays outside the marketplace candidate and unapplied. **Membership-creation paths inventoried by A from source (for ruling 7; review checklist, nothing decided):** (a) `kernel.accept_org_invite` (077:1147) is A4's guard; (b) `kernel.create_organization` (077:811) called directly over RPC by an identity with platform authority makes it org_owner of a new org (outside the console; decide whether A1 replaces it for platform identities or it is refused for them); (c) `venue.grant_staff_role` (080:224) has a platform_admin arm, so self-grant is refused but one operator can grant venue staff to ANOTHER operator (venue-plane membership at a customer venue); (d) the reverse direction: platform authority granted to an identity that already holds org or venue membership (`kernel.grant_platform_role`, fail-closed under PFA-4 today, or a `public.admin_users` bootstrap insert by SQL) breaks the invariant with no acceptance involved; (e) out-of-band SQL writes by postgres or service_role. Note: `kernel.is_platform` checks auth.uid() (the caller), so a grantee-side check needs an identity-parameterized form. **Head `5960b51` (parent 0cfa8ba), reviewed by A 2026-09-17: PASS.** A5, the ruling-1 recovery tests, the names-only detector `ops.list_platform_identity_memberships()` and A6. A's own evidence: replay 157/157, Gate-2 32|107|37|38, strict TAP 87 files / 5428 tests / 0 failures (= CI's 5434 with `000_helpers`' 6), CI 35255707849 green on five jobs; `create_organization` and `accept_org_invite` are 077's bodies plus only the marked A4/A5/A6 blocks; the rollback's two bodies are byte-identical to 077 (2450 and 3034 bytes); all seven pre-apply prosrc md5s recomputed from the migration text and matching D's values; 8 A-mutants each asserted applied/changed/restored, all killing exactly the predicted tests. Findings: F-A6-TEST (LOW, test gap: an A6 that refuses EVERY acceptance by an owner kills nothing — add the positive org_owner-invite case) and F-ACL-REPLAY (MEDIUM, apply package only: replay-derived ACLs may differ on a hosted project; prosrc mismatch stays a hard stop, ACL mismatch is report-and-decide). **PFA-33 SIGNED** (governance `99ecf31`). **A6 APPROVED by the owner 2026-09-17** with required tests (sole owner, multiple owners, lower-role invites, concurrent acceptance, rollback evidence); the follow-on amendment is **PFA-34**, to be placed PROPOSED against D's next head. **138 stays unapplied and off the candidate; applying it needs a separate owner authorization, including the 115–120 sandbox prerequisite (the sandbox has no `ops` schema) and any hosted-project detection read.** **SPRINT CLOSE-OUT (owner, 2026-09-17): 138 STAYS UNAPPLIED and outside the marketplace candidate.** The owner's words: *"Keep migration 138 unapplied and outside the marketplace candidate. Do not apply sandbox prerequisites 115-120, run hosted detection reads, read server-log settings, access production, deploy functions, enable keys, or request a build."* **PFA-34 remains PROPOSED and NOT SIGNED** (ruling 3), which settles the signature conflict between D's session and A's in favour of the conservative state both already held. **The venue-staff detector extension is DEFERRED** (ruling 4): the detection scope stays organisation membership, so the reviewed head remains **`a9aa34e`** and D's local `1cacdf5` (venue staff + the I136 split, no CI, unreviewed) **stays local and unpushed**. Consequence: PFA-34's placed D1 clause and the reviewed implementation agree again, and the placed block checksum `026cb858319bc7c0181e1dad01e23ef1` is unchanged and is the one still awaiting the owner's approval. Nothing in 138 is applied on any project; the apply package `MIGRATION_138_APPLY_PACKAGE_20260917.md` is PARKED with all six approval points unexercised. Known gap carried, not closed: venue-plane membership at a customer venue is reachable by the same reverse path and the detector does not report it. |
| `140` | 207 | **Claude B** (implements A's contract) | proof-upload repair: `public.mark_transfer_sent` both overloads idempotent and `returns jsonb` (drop + recreate + exact grants), new `public.attach_transfer_evidence(uuid,text)` for the null-proof case (seller-only, `seller_sent` only, object must exist, no bypass — the guard allows null→value), rollback to **0553's bodies** | **ALLOCATED 2026-09-17, not written** — contract `PROOF_UPLOAD_REPAIR_CONTRACT.md`; owner authorization: development and local tests only; A review → D review → next candidate. Cleanup sweep (B's design) is NOT allocated until designed and its cron approved — **WRITTEN by B: `fix/140-proof-upload-repair` @ `912a7d6` (off e9b52ce), CI 35185254662 green; A REVIEW PASS 2026-09-17** — verified from source: both overloads dropped and recreated returning jsonb with 0553's exact grants re-issued; outcomes transitioned | already_sent | attached | already_attached; a seller_sent retry writes nothing (207 R4 xmin unmoved, R5 no second notification) and never replaces proof (R7/R8); attach is authenticated-only (revoked from service_role), seller-only, status = seller_sent only, own-folder + transfer-evidence/ + storage.objects existence checks, NO bypass GUC (the guard admits null→value and refuses a second different path itself, S5); **rollback bodies hash byte-identical to the sandbox-applied bodies** (2-arg md5 bab0d402… len 955 = 0550's, 3-arg c3281f0a… len 1042 = 0553's); manifest +attach row (authenticated-execute); expected_grants.txt is tables-only (0 function rows), so no row there; census 107→108 (ci.yml, 162 P2); 050's replay assertion rewritten to pin already_sent AND seller_sent_at unmoved; 207 37/37 with a per-branch mutant matrix (ten branches, each kills exactly one assertion). **D combined review 2026-09-17: PASS on substance** (matrix numbers reproduced on a fresh replay; already-sent branch refuses backfill; no-UPDATE retry makes double notification impossible; different-path retry leaves proof intact) with two items to close before the candidate is tagged — 050 must assert the captured `transfer_evidence_path`, 207's header must be regenerated from the matrix (numbers right, prose wrong) — and one contract statement (the 2-arg overload is an ongoing source of the stranded state, §10). **Test-only follow-up `259246e` (custody assertion on transfer B; fails under the writing mutant; D confirmed) merged on the stack.** Applied nowhere; no build **Current (2026-09-17 ~14:45Z):** in the reviewed candidate tree `db16e1a` (CI 35188006272; D merge gate PASS; no open review item); **absent on the sandbox** (ledger 141, read 14:35:00Z) and on production; no tag; build HELD for F-NAV-1 (client). |
| `141` | 208 | **Claude A** | F-NOTICE-1: withdrawing an account deletion retires that user's pending deletion notice. One internal routine `notify.retire_account_deletion_pending(uuid)` plus one call inside `kernel.withdraw_account_deletion` (077's body otherwise verbatim). The retire lives in `notify` because 157 A48 forbids a kernel routine from touching notify's tables — the first draft did and A48 caught it. | **`fix/f-notice-1-withdraw-retires-notice @ ea547e5`** (base `6561d1f`), **unapplied anywhere**. **CI 35263537823 SUCCESS (five jobs); pgTAP Files=89, Tests=5325, PASS.** **D reviewed: PASS** (own replay; F-141-1 privilege pin required and now closed by 208 F2, proved by granting service_role and seeing exactly F2 fail; lock_timeout added on D's trade-off point and pinned by F3; D's controls 5/5). **B reviewed** (header overclaim and unpinned revoke, both fixed; C3 now a delta). Local: replay 158/158, Gate-2 32\|108\|37\|38 (public census unchanged), full suite **5316/5316**; new test 208 (19) with negative controls by TYPE and by USER, a blast-radius count, coalesce preservation, idempotence and no-event-emitted; rollback negative control fails exactly 12 assertions and no others (208 B3/C3/D4 + the nine census pins 148 B2, 156 A20, 157 A14/A15/A16/A46, 179 A9, 180 A6, 204 E2). Six census pins updated in place with dated notes. Awaiting D's review; the owner has not authorized applying it anywhere **SPRINT CLOSE-OUT (owner, 2026-09-17): the MINIMAL FIX is the chosen one, and it stays UNAPPLIED.** The owner's words: *"F-NOTICE-1: use the minimal fix. When a user withdraws account deletion, retire only that user's pending deletion notice. Do not add a new notification type or outbound message. Keep the reviewed branch and migration 141 unapplied unless a separate apply decision is made."* That is Option A of `F_NOTICE_1_ROOT_CAUSE_AND_FIX_20260917.md` exactly as built at `ea547e5` - **Option B (a withdrawal notice of its own) is DECLINED**, and the branch already adds no type, no template, no job and nothing outbound. No apply decision has been made for any project, so 141 is applied nowhere and carries no scheduled apply. The buyer's existing stale notice in the sandbox is NOT touched (the fix is forward-only by design); that scoped plan (§4) remains unauthorized and un-run. |
| `142` | 209 | **Claude A** | A′ column for candidate `8f45e9b`: `public.payments.amount_refunded_cents int` — nullable, no default, **never backfilled** (NULL = amount unknown; the deployed webhook records none). The exact DDL of `20260906120000:306`, plus a post-check refusing any other same-named shape. No writer, trigger, grant or index; grant manifest, Gate-2 census (32|108|37|38) and `expected_grants.txt` do not move (payments has table-level grants only). Rollback refuses once any value exists (R2) or once `20260906120000` is applied (R1); cutoff = release of the app build that reads the column. **Hazard for the payments RC:** `20260906120000`'s rollback drops this column. | **ALLOCATED + WRITTEN 2026-09-18** (owner: author locally, no apply) on `fix/142-payments-amount-refunded-cents @ e3c03d51` (base `8f45e9bb`, local only, not pushed). Number checked free across every ref and 119 worktrees. Full-chain replay: 89/89 pgTAP files PASS incl. 209 11/11. On the production shape: 209 fails S1–S4 and aborts at S6 without 142, and passes 11/11 with it. Shape guard refuses bigint / default 0 / NOT NULL. ~~**CI not yet run** (push needs owner).~~ **Draft PR #77 (do not merge), CI run 35416207342: pgTAP Files=89 Tests=5314 all successful (base 88/5303), 209 ok; code jobs pass; guard stops only at the owner's AUTODEPLOY-1 attestation (structural checks OK locally with the job's script).** Unapplied anywhere. Go/no-go §12, §12.11. |
| `143` | 210 | **Claude D** | ops cron-history read bound: `CREATE OR REPLACE` of `ops.job_health()` and `ops.detect_jobs()` only (same signatures, no new objects, census unchanged); rollback restores the 116/117 bodies. Retention/deletion and detector schedule changes are **excluded** (owner, 2026-09-19). Root cause: `PROD_DISK_IO_INVESTIGATION_20260919.md` | **allocated 2026-09-19 by A** (free on every ref and in 127 worktrees; positive control finds 142/209). D's local branch `admin/ops-cron-history-perf` @ `6b700aed` (v2: comment-only over `67f2ccd5`; md5s `da349c0b…`/`9e932d28…`), base the gate `e191cbfa`. **A review PASS 2026-09-19 (v1 and v2)**: independent replay 5333/5333 (210 = 25/25); rollback = 116/117 byte-identical; equivalence incl. A's run-ID-gap perturbation. **Draft PR #82** (`fix/143-ops-cron-history-bounded-reads` = `6b700aed`, owner-authorised publication; CI green run 35466024802, pgTAP Files=90 Tests=5339, 210 ok as non-superuser, census unchanged). Not approved to apply; the jobs-page last-run change awaits the owner |
| `144` | 211 | **Claude D** | refund-resolution detector (plan `REFUND_RESOLUTION_PLAN_20260919.md` §4; design `docs/operations/REFUND_RESOLUTION_DETECTOR_DESIGN.md` @ `e733c887`): new `ops.detect_refund_resolution()`, `case_type`/`case_event.kind` widenings, `run_job`/`run_all_detectors` arm, `action_dispatch` case_status classification check; setting `refund_resolution_detector_enabled` seeded **false** | **allocated 2026-09-19 by A** (free on every ref and 132 worktrees; control 143/210). Design A-PASS with 2 required changes: rebase `action_dispatch` onto the body applied immediately before (138 collision); R2 rule re-review before any RC edge deploy. **Built** `admin/144-refund-resolution-detector` @ `69140884` (= `bd12e297` + a 1-line design-doc fix; SQL/tests unchanged) (local): **A review PASS** — independent replay 5354/5354 (211 = 46/46), md5s match D, redefinitions minimal vs 117/118, rollback == gate (8 items; control 13 diffs). No PR/apply/flag without the owner |
| `145` | 212 | **Claude D** | job-monitoring gap: an ACTIVE cron job that stops running keeps a case (today it drops out of `detect_jobs` after 7 days and its case auto-resolves). Needs a first-seen record for never-run jobs; moves 181's ops-table count 13 → 14 | **allocated 2026-09-19 by A**; local build only, on the current gate |
| `146` | 213 | **Claude D** | **BUILT** `admin/146-alert-delivery` @ `a9bf6423`, draft **PR #86** (stacked on the chain; CI green after A added the AUTODEPLOY line). **A review PASS**: A's replay 5445/5445 (213 = 26/26); delivery confirmed only by a 2xx whose body reports `delivered >= 1` (notify-report answers 200 for unknown events and its own errors); allow-listed payload pinned by a fixture carrying an email and a phone; header states that enabling still delivers nothing because nothing schedules the dispatcher. ops.alert delivery capability, seeded OFF, built on the existing `notify-report` path (D's mapping: **nothing delivers an ops.alert today** — no trigger, cron, pg_net, edge, email or push reads it; the console shows it only when an operator opens the page) | **number registered 2026-09-19 by A**. The number is registry hygiene, **not** an authorisation of the work: that is between D and the owner |
| `131` | 198 | **release integration (A)** | session-bound push bindings — owner O-3 decision (b): password change / sign-out-everywhere invalidate bindings and proofs; old sessions cannot re-register | **allocated 2026-09-15** (moved from 129 so the wrapper could take the lower number ahead of it in the same candidate — the merge guard requires an added migration to exceed the base's highest); design `SESSION_BOUND_PUSH_BINDINGS_131_DESIGN.md` under D review (X1 reclaim rejected by D; owner choice pending); **production gate, not in the Friday candidate**; not written **2026-09-15 late: A-131-K2 (K2-S1/K2-S2) + F-131-K2a fixed; D PASSED 131 at `f3963a3` (CI 34982075970); first merge of the production-gate stack.** — **APPLIED ON SANDBOX 2026-09-16** (B2 window, ledger 141, md5-verified against `9bef640`, D closing read PASS; production: NO) |
| `132` | 199 | **Claude B** (proposed) | fresh-mint concurrency — group claim `public.checkout_group_claim` keyed (listing, buyer, mode), atomic INSERT…ON CONFLICT…WHERE stale, 120 s window, taken before the prior-payments read, released in finally; closes D's OPEN MONEY DEFECT | **design on record** (`MIGRATION_132_CHECKOUT_GROUP_CLAIM_DESIGN.md`, B @ ffca4e9); ~1 working day incl. review + CI; **owner placement pending** (this candidate vs the production gate); not written **2026-09-15 evening: PR #70 @ `ebbd1c0`, CI green (run 34982027295); group key now (listing, buyer), mode recorded; D's F-132-1 (cross-mode) and F-132-2 (cross-buyer best-effort retire) fixed; D's full battery pending at this head; deltas for A's pins: tables +1, functions +2, `service_role|checkout_group_claim|DELETE,INSERT,REFERENCES,SELECT,TRIGGER,TRUNCATE,UPDATE`; deploy order 132 → edge (edge fails closed 503 without 132).** **2026-09-16: head `9d82247` (record_checkout_attempt FOR SHARE; F-132-3 sweep before every hand-out; B's concurrent-withdraw fix); D PASSED (integrated harness 23/0/2, census 32|99|37|35 on the pin, two-session G1–G8 + C1–C4, FOR KEY SHARE mutant caught, edge 84/84 with 9 failing on ebbd1c0; two LOW notes non-blocking); integrated onto the production-gate stack at `6b058d2` with pins 32/102/37/37. Applied nowhere.** — **APPLIED ON SANDBOX 2026-09-16** (B2 window, ledger 141, md5-verified against `9bef640`, D closing read PASS; production: NO) |
| `133` | 200 | **release integration (A)** | edge-functions base URL from Vault `project_url` (owner item 6, 2026-09-15): the four bodies (033/034/035 triggers, 099 monitor) and five http crons (032, 087 ×2, 099 ×2) stop hardcoding production, post only when the secret exists, and 133 purges any production-host request already queued in `net.http_request_queue` at apply; no new objects | **allocated + written 2026-09-15** — `fix/133-config-driven-functions-url` (`/Users/josetascon/snatchit-133`); rehearsed 150/150, census unchanged 31/96/37/35, rollback md5-identical (4 bodies, 5 commands), pgTAP 200 24/24 with a 16-not-ok negative control on the 130 chain. **D's review (2026-09-15): correct for its purpose; two findings fixed at `235c839` — F-133-1 HIGH (precondition was prose only → §0 refuses to apply where Vault carries `service_role_key` without a well-formed `project_url`; rehearsed REFUSED/REFUSED/applied), F-133-2 MEDIUM (queue purge would drop production's own queue → guarded to non-production `project_url`). jobids of the five crons change on unschedule+schedule; 117's job-health evaluates only jobs with runs in the window, so no false case opens; old-jobid history is not joined (recorded). **D PASSED 133 at `235c839` (2026-09-16): refusal matrix (no secrets → applied, 0 sites, zero posts; key without URL / trailing slash / short ref → REFUSED, catalog untouched; sandbox URL / production URL / URL without key → applied, posts to the configured host), queue guard (sandbox URL purges the production-host request, production URL keeps its own queue), rollback identity 0 differing lines + five cron md5s, integrated harness PASS 23/0/2 declared; evidence limit: the local shim has no pg_net — D replaced `net.http_post` with a recording stub and created a `net.http_request_queue` table for the behaviour probes, so CI run 34982307401 (real pg_net) is the authority for delivery. Owner 2026-09-16: recorded as PASS; **this does not authorize application anywhere.** Conditions carried unchanged: Vault `project_url` inserted BEFORE the apply in production and on the sandbox (the sandbox also gains the missing `enforce-transfer-expiry` cron and loses its out-of-band notify bodies); production drift counts 4 functions / 5 crons read before 133 is scheduled (§4's proof aborts on any unrecorded site); post-apply proof that one `enforce-transfer-expiry` request is answered within 2 min; 117 job-health continuity loss recorded, no false case.** **Production-gate stack: 131 → 132 → 133.** Precondition everywhere it is applied: Vault `project_url` = `https://<ref>.supabase.co` inserted BEFORE apply (owner ceremony in production; sandbox window for the sandbox), else the five crons and four triggers go silent. CI leaves it unset by design; until 133 is in the chain CI is protected only by the runner egress block (`ci/egress-block-20260915`) — **APPLIED ON SANDBOX 2026-09-16** (B2 window, ledger 141, md5-verified against `9bef640`, D closing read PASS; production: NO) |
| `134` → file **`20260916000000_processing_sweep_arm.sql`** | 201 | **release integration (A)** | `processing` sweep arm — `get_unsettled_payments` gains `processing_stale` (status processing, external rail, intent id present, 15 min .. 7 days); `enforce-transfer-expiry` Phase 0 fails a buyer's own processing row whose intent Stripe already failed/canceled (guarded fail-update as the webhook) and frees its hold (127); anything else falls through to the settle path. Closes B's disclosed 132 residual (D: availability only, NOT blocking) | **written 2026-09-16** on `fix/134-processing-sweep-arm` (on the production-gate stack). **Versioned by TIMESTAMP, registry number kept:** it redefines a function owned by `20260906110000`, and a numbered `134_` file sorts BEFORE that file under LC_ALL=C — a fresh replay silently overwrote the body (caught in A's rehearsal: test 201 failed on the 134 database exactly as on the control). Rule for the registry from now on: **a migration that redefines an object owned by a timestamped file must itself be timestamped later than that file.** Evidence: replay 153/153, census unchanged 32/102/37/37, full suite green, 201 9/9 with a 4-not-ok negative control, rollback md5-identical (body + comment), vitest 18/18 with 4 of 5 new cases RED against the stack edge. **D PASSED 134 at `5146040` (2026-09-16; CI 35046284427; D's own negative control: rollback removes the arm → 201 5 ok / 4 not ok; D reproduced the LC_ALL=C overwrite that motivated the timestamp). Integrated onto the production-gate stack at `d61970b` (131 + 132 + 133 + 134); applied nowhere.** — **APPLIED ON SANDBOX 2026-09-16** (B2 window, ledger 141, md5-verified against `9bef640`, D closing read PASS; production: NO) |
| `135` | 202 | **release integration (A)** | **b2 — proof of possession** (owner decision 2026-09-16, build shape (i)): `notify.push_token_challenges`, `public.request_push_token_challenge`, `public.confirm_push_token_challenge`, `challenge_required` branch in `register_push_token`, client DELETE → tombstoning revoke, proof supersession on confirm, in-app `security_device_rebound` notice; contract v3 (`PUSH_TOKEN_CONTRACT_V3.md`, draft for D). Numbered (it owns its objects and redefines only 128/131 bodies, both numbered) | **allocated 2026-09-16**; A writes on `fix/135-push-proof-of-possession` from the production-gate stack `d61970b`; B: `send-push` challenge kind; C: client v3; D: C1–C6 matrix. Merge order 135 → send-push → client v3 → one stack commit → pin → one build. Applied nowhere; sandbox application needs the owner's package approval **Written 2026-09-16: head `b49a012` (CI 35050777765). Contract v3 applied in full (D's V3-1..5, P3-1, RB-1, H, M; two corrections forced by 157: the previous-owner notice is in_app with no email row, and send-push reads via `notify.get_push_token_challenge` because notify tables carry no service_role grants). Census: public 32/105/37/38; five-schema 303 routines, 80 relations; notify 8 tables, 20 routines, 32 types, 62 templates. pgTAP 202 (57), 195/198/157 adjusted only where v3 changes a cross-account outcome. Rollback md5-identical for both verbs. Disclosure: two earlier pushes on the branch were red (7bd34d6, 94939b7 — the latter's message wrongly claimed green); corrected at b49a012. D review pending; applied nowhere.** **D Gate 1 PASS at `1cfc85c` (2026-09-16; C1–C6 probes through real writers, four mutants flip, rollback restores the pre-migration catalog with 0 identity lines incl. 128's deleting unbind); two LOW fixed at `38b1e02` (M-135-3 bigint code generator; D-135-4 comments say in-app). Owner-visible decision recorded by D: `security_device_rebound` has no outbound channel (in-app only, never push, no email per N1), so a previous owner who never opens the notification centre is never told. Evidence limits: local harness only (pg_net, Vault, APNs/FCM are stand-ins). Applied nowhere.** — **APPLIED ON SANDBOX 2026-09-16** (B2 window, ledger 141, md5-verified against `9bef640`, D closing read PASS; production: NO) |

> **126 vs. the `129` proposal — kept distinct (2026-09-14).** The renumber never happened; `129` was later allocated (2026-09-15) to the `public.revoke_push_token` wrapper and session-bound bindings took `131` (above). 126's *progress* is part 1 of 3, committed. Separately, A *proposed* reassigning the refund-exactness work from `126` to `129` so that `127`/`128` (security fixes) would not be held behind it. **That proposal is UNAPPROVED** and the owner has ruled the other way: 126 must be resolved before 127/128 merge, or an explicit registry/sequencing change must be proposed and approved. Until the owner says otherwise the number is `126`, the file is `126_ops_console_refund_exactness.sql`, and the merge order is 126 → 127 → 128 behind 121 → 123 → 124. Nothing claims `129`.

> **L1's server half landed in `127`** (payment-scoped release). **L3 and L4 still claim no number** — L3 is a product tradeoff (holding inventory longer on `payment_failed` vs. occasional charge-then-refund) and L4 needs a Stripe-calling path at hold expiry, which `cleanup_expired_reservations` cannot be since it is SQL-only. Neither is reserved speculatively.
>
> **Sandbox ordering consequence:** `126`/`127`/`128` must NOT be applied to the shared sandbox before `125` either. The GitHub guard is base-branch-relative, but the sandbox ledger is not — applying 128 there would put the sandbox tip above 125 and break the same ordering for any later sandbox apply.

> **Hazard on record (D, 2026-09-15; pre-existing, not this release):** migrations `032, 033, 034, 035, 087, 099`
> hardcode the production project URL in `net.http_post` trigger bodies. Fresh replays with a live pg_net point those
> triggers at production's `notify-*` edge functions; the shared sandbox's copies were rewritten out of band
> (unrecorded drift). A future numbered migration should make the URL configuration-driven; number allocated when written.
> **D's reading (2026-09-15):** the 033/034/035 trigger paths and 087 no-op without a vault secret (CI has none) and pgTAP rolls back; **032's `*/2` cron fires unconditionally with a NULL bearer**, so a live pg_net on the CI runner probably POSTs to production's `enforce-transfer-expiry` every two minutes of a migrations job — 401'd by the function's own bearer check, no side effect, log noise from GitHub IPs. **Settled by CI run 34933664373 (3fa94d3): `enforce-transfer-expiry` ran once during the job and `net._http_response` holds exactly one row — status 401 — the only outbound request. So every migrations job POSTs production's function with a null bearer and is refused: LIVE hazard, benign today, contingent on that function's bearer check.** Remedy with the config-driven migration: no-op when the URL setting is unset (087's `where exists` pattern); in CI, unschedule after replay. **2026-09-15: remedy is migration `133` (row above); CI-only interim = a runner egress block + a fails-closed gate with an in-DB probe (owner: "the bearer regression test … does not stop the outbound request itself"). D's scope limits: the block covers CI runners only; any other fresh replay still posts production until 133; 133 cannot close the in-replay window between 032 and itself on its own, so it purges the queue at apply; production-side confirmation needs an authorized production log read.**

## How 122 was resolved

Both Claude B's remaining-path report and this line initially claimed 122. Only this branch had actually
committed the number, so the collision was prospective. B named it first and their `121` immediately precedes
it, so keeping `121`/`122` contiguous in that workstream is worth more than holding the number here; this work
renumbered to **123**, and **190 is left reserved** for B's companion test rather than taken.

Verified free across `release/convergence-135`, `venue/read-adapters-slice-1`, PR #58's branch and
`admin/operating-console` before claiming.

## Ordering note

The guard is **scheme-aware**: a `seq` migration is compared only against the highest `seq` migration, and a
`ts` one only against the highest `ts`. So `121`, `122` and `123` each need only exceed `120`, and they sort
*before* every timestamped migration in a fresh `LC_ALL=C` replay — after `120`, ahead of `20260714…`. That is
safe for all three, because each only re-creates or alters objects established earlier in the numeric block.

## CLI note

`supabase db query` **does** exist in CLI 2.115.0, with `--file/-f`, `--linked`, `--project-ref` and
`--db-url`. `supabase db --help | head -30` truncates the subcommand list after five entries; `query`, `lint`,
`start`, `advisors` and `schema` follow. Do not conclude from a truncated listing that it is unavailable.

## Apply order RESOLVED with B's plan of record (2026-09-12)

**Normal forward order, no renumbering:** `121` → `122` → `123` → `124`. Production tip is `120` and none of
the four is in production, so plain ascending order satisfies the guard and nothing needs renumbering. B's
`121` (PR #58) and `122` (086↔112/113 scanning drift) keep their numbers; `123` keeps its number, so the
sandbox apply history stays intact.

Two constraints follow:
- **`123` must not reach production before `121`/`122`.** It is sandbox-only today; nothing schedules it.
- **Sandbox is already at tip `123`**, so B's `121`/`122` rehearsals belong on a fresh replay DB (B's normal
  harness), not on the shared sandbox, or they land below its tip.

`142` in B's remaining-path report is the **count** of the combined release + venue + 121 chain, not a
migration number. ~~Nothing claims `142`.~~ **Superseded 2026-09-18: `142` is now A's (A′ column; see its row).**

## Superseded: apply-order conflict as first flagged (2026-09-12)

Production tip is **120** and `121` is deferred optional hardening, applied only under
`AUTHORIZE PFA-18C MIGRATION 121` (B's state document §3). `123` is sandbox-applied only.

The conflict is ordering, not numbering: the scheme-aware guard compares a `seq` migration against the
highest `seq` already applied. If `123` reaches production first, `121` and `122` are then **below** the tip
and a strictly-increasing guard rejects them. So either B's `121`/`122` go first, or their apply has to be
cleared against a tip of `123`. Numeric gaps themselves are fine — production already carries
023/043/055/056/059/060/066.

B's "combined chain 142" is B's own chain label, not a migration number on this line; ~~`142` is unowned here.~~ **`142` is A's since 2026-09-18 (row above); B's chain label is unrelated.**
Confirm before anyone treats it as a claimed number.

## CORRECTION (2026-09-12): the "guard" is CLI planning, not a rejection

A's earlier wording here and in the release package — "a seq migration must exceed the highest applied seq
migration or a strictly-increasing guard rejects it" — **overstated it**. Sourced today:

- `supabase db push --help`: `--include-all  Include all migrations not found on remote history table.`
  So the DEFAULT plan contains only versions **above** the remote maximum; `--include-all` plans every version
  missing from the ledger, applied in `LC_ALL=C` order.
- There is **no** monotonic guard in `.github/workflows/ci.yml` or `supabase/ci/`. Nothing rejects a
  lower-numbered migration.
- Production has already applied out of numeric order: **115–120 (2026-09-08) before 110–114 (2026-09-09)**.

**Consequences.**
1. A lower-numbered migration is never "stranded". It is only **omitted from the default plan**, and
   `--include-all` plans it. The risk is silent omission, not rejection — which is why every apply here
   dry-runs and checks the planned list exactly.
2. Renumbering is therefore **not required** by any ordering rule. It only keeps a fresh `LC_ALL=C` replay in
   the same order production applied, which the rehearsal harness models explicitly
   (`convergence_prod_order_rehearsal.sh` replays production's real order, not the file order).
3. The owner's menu is wider than the earlier caveat suggested — see the release package.

## Merge order approved by the owner (2026-09-12) — sequencing only

**Intended merge order: `121` → `123` → `124`.** The unwritten scanning fix reserved as `122` is reassigned to
**`125`**, the next free number above `124`. Its companion pgTAP number **190 stays with it** — the guard scopes
to `supabase/migrations/**`, so test numbering is unaffected.

Why the renumber is required rather than cosmetic: `.github/workflows/migrations-guard.yml` §4 fails any PR
whose newly **added** migration is not strictly greater than the base branch's highest version of the same
scheme. Once `124` is in the base, a PR adding `122` fails. `121` is written and reviewed in PR #58, so it
merges **first** and needs no renumber; the unwritten fix costs nothing to move.

`125` verified free across `release/convergence-135`, `feature/venue-native-and-product-v2`,
`venue/read-adapters-slice-1` and this branch on 2026-09-12.

**This is sequencing approval only** — not authorization to apply any migration, and not a Build 16 change.
Claude B owns `121` and `125`; the registry and B's references need to agree before either is merged.
