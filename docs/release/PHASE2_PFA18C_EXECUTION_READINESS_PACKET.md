# PFA-18C — EXECUTION READINESS PACKET (billing step + C1 infrastructure package)

**Date:** 2026-09-08 (sessions 8–9) · **Branch:** `feature/venue-native-and-product-v2` · **Reviewed tree:** `21ac8e7` (= audit `1f3fc19` + session-7/8 docs; branch tip = `origin`) · **Coordinator:** Claude A (read-back only; C18 unchanged)
**Authorization in force:** the owner approved and reports completing the direct AWS Paid-plan upgrade (pay-as-you-go). That approval/upgrade authorizes **no** IAM, S3, CloudTrail, Organizations, KMS creation, production migration, secret, deployment, or activation. The owner executes every ceremony mutation personally under the ratified single-founder procedure with independent verification on the separate clean device (M2). Paying AWS completes none of: backend integration, scanner implementation, launch validation.

Evidence classes: **CLAUDE-OBSERVED** (read this session), **OWNER-CONFIRMED** (stated by the owner; not yet API-verified), **OWNER-RETURNED** (typed by the owner earlier), **OFFICIAL-DOC** (AWS page read this session), **REHEARSAL** (local harness), **NOT OBSERVED**.

---

## 0. State reconciliation

| Item | Recorded (audit/runbook, 2026-09-05) | Fresh | Class |
|---|---|---|---|
| Audit commits `1f3fc19` / `aa74cc2` | current | still current; `21ac8e7` (session 8 docs) is the branch tip and equals `origin`; no later change to any PFA-18C document or 110–114 file on any branch. `admin/operating-console` advanced to `78a56fd` (its deployment record only). | CLAUDE-OBSERVED 2026-09-08T01:26Z |
| Production ledger | 124 / tip 109 | **130 rows, numeric tip 120**; present `115,116,117,118,119,120`; **110–114 absent** (`guard_110_present=false`, `recovery_111_present=false`); ops-console release 2026-09-08 (`docs/admin-console/DEPLOYMENT_RECORD_2026-09-08.md`, admin branch) | CLAUDE-OBSERVED 2026-09-08T01:26Z (unchanged vs 00:38Z) |
| Signing substrate | 0 keys, dark | `kernel.signing_key` **0**; issuance `false`, scanning `false`, monitor `false`, fingerprint `null`, max_not_after `null`; tickets 0; census kernel fns **149** · venue fns **83** · kernel tables **31** | CLAUDE-OBSERVED 2026-09-08T01:26Z |
| Native edges | not deployed | 11 legacy edges only (session 8 read) | CLAUDE-OBSERVED |
| Supabase branch record | `git_branch: ""` | `git_branch: ""` (session 8 read); owner visual OFF confirmation 2026-09-07 (RC3 record) | CLAUDE-OBSERVED |
| AWS plan | FREE (OWNER-RETURNED 2026-09-05) | **PAID — OWNER-CONFIRMED (Console upgrade, root used for billing). NOT API-VERIFIED:** `sts get-caller-identity` / `freetier get-account-plan-state` now return "Unable to refresh login credentials because of a change in your password. Please reauthenticate with your new password using 'aws login'." | OWNER-CONFIRMED / NOT OBSERVED |
| Pre-existing user edits | preserved | untouched: `M docs/release/PHASE2_PRODUCTION_KMS_SIGNING_CEREMONY_EXECUTION.md`, `?? docs/phase2/TICKETS_READ_CONTRACT_CORE_COORDINATION.md` | CLAUDE-OBSERVED |

Runbook NG-3 is re-baselined (dated note) to **ledger 130 / numeric tip 120 / 110–114 absent / 0 keys / census 149·83·31**. Runbook NG-1 carries a dated note: cleared only on API evidence.

---

## 1. Billing and identity — status and the verification step

**Status:** upgrade **OWNER-CONFIRMED**; **PAID not yet recorded** (no API evidence). The previous "keep Free plan" decision is superseded. Do not repeat the upgrade; buy nothing.

**What the Paid plan is (OFFICIAL-DOC):** "standard pay-as-you-go pricing for any usage beyond your Free Tier credit balance"; "AWS will not charge your payment method until you upgrade to paid plan"; remaining credits "automatically apply to future AWS bills until they expire. Free Tier credits expire 12 months after you create your account." There is no $100 product, subscription, support plan or credit package in this step; the "$100" is the existing **credit balance**. No recurring $100 budget is authorized or implied.

**Owner steps (primary machine, `jose-admin`; nothing pasted except command OUTPUT):**

| # | Owner does | Returns | Gate |
|---|---|---|---|
| V0 | `aws login --profile snatchit-admin` — if the default browser fails, copy the printed `https://signin.us-east-1.amazonaws.com/authorize?…` URL into a **Safari Private Window** (the workaround that worked), sign in as `jose-admin` with the **new** password + passkey, select that session. (`--remote` prints the URL and asks for the code instead of using the local callback, if needed.) | "done" | — |
| V1 | `aws sts get-caller-identity --profile snatchit-admin --region us-east-1 --no-cli-pager` | Account, Arn, UserId | `652872010073` / `arn:aws:iam::652872010073:user/jose-admin` |
| V2 | `aws freetier get-account-plan-state --profile snatchit-admin --region us-east-1 --no-cli-pager` | full JSON | **`accountPlanType: PAID`** ⇒ NG-1 cleared and recorded; anything else ⇒ not cleared |
| V3 | `aws iam get-account-summary --profile snatchit-admin --region us-east-1 --query 'SummaryMap.[AccountAccessKeysPresent,AccountMFAEnabled]'` | `[0, 1]` | root still has 0 access keys and MFA (root was used for billing; it is never a ceremony principal) |

---

## 2. Owner decision sheet (single, consolidated — check of existing approvals first)

Already approved by the ratification (2026-09-04) and **not reopened**: the compensating-control model; Model B for the *initial dark bootstrap only*; Model A before T3; M2 second clean device; M3 distinct runtime role; M5/M6/recovery before issuance; C18; ES256 (D2); PEM public key (D3); fingerprint (D5); `not_after=NULL` (D6); PFA-18C consumed once. Approved 2026-09-08: Paid plan (usage-based).

| # | Decision | Status | Recommendation | Reason | Consequence if adopted |
|---|---|---|---|---|---|
| **D1** | Production signing region | unresolved (README says "pinned by owner confirmation", readiness report says confirmation still required — treated as **not** confirmed) | **`us-east-1`** | only existing signal (saved CLI region); every artifact ARN is already written for it; CloudTrail home region + IAM/STS global events land there | single-region KMS key in us-east-1; all ARNs fixed; changing region later = new key + re-ceremony |
| **D2** | Object-Lock COMPLIANCE default retention (**irreversible per object; cannot be shortened by anyone incl. root; bucket cannot be emptied before expiry**) | unresolved — **the owner must state the integer; nothing is pre-selected** | engineering view: **3 years** | covers key lifetime through the Model-A migration and a normal audit horizon; storage cost ≈ $0.03/GB-month regardless | objects written from C1-8 onward are undeletable until (write date + N years); an aborted ceremony leaves the bucket in place; 1 year = least commitment but bootstrap evidence becomes deletable while the key is still live unless extended; 7 years = financial-records horizon, heaviest commitment |
| **D3** | Runtime credentials: **O1** (IAM user → `sts:AssumeRole` → Sign-only role; one static AWS secret in Supabase) vs **O4** (signer on AWS compute with an ambient role; zero static secret; architecture change) | unresolved (O1 PROPOSED, implemented + tested, not adopted) | **O1 now; revisit O4 before commerce** | O1 exists and is tested (E2, 28 cases); O4 is unbuilt and needs a PFA review of the edge→Lambda path | one access key created at **C6** (not C1), Sign-only via the role, 90-day rotation duty, CloudTrail alarm on unexpected `AssumeRole`; C1 creates both the runtime *role* (needed in key policy v1 either way) and the runtime *user* (O1; deletable if O4 is chosen later) |
| **D4** | Account layout | model approved; concrete layout unresolved | **reuse `652872010073` as the workload/signing member**; create a NEW management account and a NEW audit account only when Model A is authorized | key ARN is account-bound → no re-ceremony; `jose-admin`, MFA and baseline already exist here; SCPs never bind the management account, so the key must not live there | Model B (this account) is **temporary and valid only for the initial dark bootstrap**; **Model A must be operational before T3** (see §2.1); joining an Organization later is a separate authorization; credit survival on a later org-join is not documented — treat as at risk |
| **D5** | Exact resource names | unresolved (README set) | **confirm unchanged**: bucket `snatchit-audit-652872010073`, trail `snatchit-audit-trail`, role `SnatchIt-KMS-Ceremony`, user `snatchit-kms-verifier`, user `snatchit-credential-sign-runtime`, role `SnatchIt-CredentialSign-Runtime`, key tag `snatchit:purpose=ticket-signing`, session name `pfa18c-ceremony` | already embedded in every artifact and read-back | any rename = regenerate artifacts before C1 |
| **D6** | Ceremony MFA mechanism for the trust condition `aws:MultiFactorAuthPresent=true` | unresolved | **register an authenticator-app (TOTP) virtual MFA on `jose-admin` in addition to the passkey, and satisfy the condition by passing `--serial-number`/`--token-code` on `sts assume-role`** | OFFICIAL-DOC: "To use MFA with AssumeRole, you pass values for the SerialNumber and TokenCode parameters"; "You cannot use MFA-protected API access with U2F security keys"; passkeys are enrolled and used in the console only; whether `aws login` credentials carry MFA context is **not documented and is not assumed** | one more MFA device on `jose-admin` (up to 8 allowed); T1 in C1-9 remains a diagnostic only; **the trust condition is never removed**; fallback T3 (a temporary long-lived key on `jose-admin` for the ceremony window only, deleted after) requires its own explicit owner OK and is recorded as an exception |
| **D7** | T3 interpretation for M5 | unresolved (governance wording) | treat M5's one throwaway credential on a **non-saleable test event** as a pre-issuance *test*, not "the first production credential signed" | the ratification lists M5 and Model A as peers ("REQUIRED AFTER THE BOOTSTRAP BUT BEFORE ANY NATIVE ISSUANCE"); a stricter reading would force Model A before C7 | recorded as an owner ruling in the execution record; under the strict reading Model A precedes C7 |
| **D8** | C3 before C4, or C4 before C3 | unresolved | **C4 (110–114) first, then C3** | the guard then protects the bootstrap row from the moment it exists; 110 permits the `…b0` row | none beyond ordering; both proven safe |

### 2.1 Before the bootstrap vs. before activation (canonical governance)

- **Must exist before the dark bootstrap (C2/C3):** M4 (done), **M1 Model-B deny-set concretely applied and read back from Device 2**, **M2 device + read-only principal**, **M3 runtime role** named in key policy v1 (Sign only to the runtime role; ceremony drops Sign at v2), C18.
- **Permitted to complete after the bootstrap but before any native issuance (C8):** M6 (110), gated recovery (111), M5 live test (C7), **Model A** (separate audit account + SCP).
- **Model B is temporary:** acceptable only for the initial dark bootstrap in `652872010073`. **Model A must be operational before T3** = first production credential signed *or* issuance enabled — i.e. before C8 (or before C7 under D7's strict reading). No Organization or account is created until that layout is approved.

---

## 3. Cost estimate — reconfirmed (official pricing read 2026-09-08; us-east-1)

Unchanged from session 8, assumptions re-checked: KMS key **$1/month prorated**; asymmetric `Sign` **$0.15 per 10,000** (excluded from the 20,000/month free tier); CloudTrail **first copy of management events free** (a multi-region trail is that one copy; second copies $2/100k; no data events/Insights configured); S3 **$0.023/GB-month**, PUT **$0.005/1k**, GET **$0.0004/1k**, no separate Object-Lock line item; CloudWatch alarms $0.10/month beyond 10 free; Budgets notifications free; IAM "offered at no additional charge"; STS has no published price (treated as $0 by absence).

| Phase | Monthly |
|---|---|
| Dark, post-C2 (key + trail + locked bucket, ≤ 10 alarms) | **≈ $1.06–1.15**, ≈ $1.00 of it the key; +cents/month as logs accumulate |
| Live | + **$0.015 per 1,000** credentials, plus manifest signatures |
| One-time ceremony requests | ≈ $0.01 |

**Billing model:** usage-based pay-as-you-go. The owner has **not** purchased a $100 package and has **not** authorized a recurring $100 budget. The remaining credit balance (≈ $100, OWNER-RETURNED 2026-09-05; re-read at V2) applies automatically until 12 months after account creation (inferred ≈ 2027-09-05 — verify in Billing → Credits). **Exclusions:** Model A (two more accounts; org-trail second copies; second locked bucket); data-event logging; CloudTrail Lake; Supabase/Vercel/Stripe/Apple; taxes; support plans. **Budget alerts notify; they do not cap spend.** The design itself bounds cost: one key, one trail, Sign-only runtime, no data events.

---

## 4. M1–M6, recovery, Model A — status (unchanged from session 8 except artifacts F1/F3)

PFA-18C **M1–M6** are bootstrap controls; the scanner contract's **M1/M2** are the keyring and door manifests (migrations 112–114) — distinct.

| Control | Artifact(s) | Implemented | Tested | Applied/deployed | Op-verified |
|---|---|---|---|---|---|
| M1 audit plane (Model B) | `m1_*.json` ×4 (bucket policy, role trust, role policy, object-lock) | drafted + **validated 2026-09-08** (README validation log) | n/a | **no** | no |
| M2 device + verifier | `m2_verifier_policy.json` (**F3 corrected**: self-MFA/password) | drafted + validated | n/a | **no** | no |
| M3 runtime role | `m3_*.json` ×3; key policies v1 (**F1 corrected**) / v2; E2 code | policies validated; E2 implemented | E2 28 cases; CI Deno | **no** | no |
| M4 | ceremony §6.1 artifact | yes | off-production proven | n/a | pending C3 |
| M5 | mocked rehearsal; live = C7 | mocked | yes (mocked) | no | **no** |
| M6 (110) | `110_…sql` | yes | pgTAP 176 + production-order rehearsal | **no** (fresh) | no |
| Recovery (111) | `111_…sql` | yes | pgTAP 177 | **no** (fresh) | no |
| Model A | §2 D4 | recommendation | — | no | no |

Validation performed 2026-09-08 (OFFICIAL-DOC-backed; details in `pfa18c_artifacts/README.md` "Validation log"): principal/account bindings (`652872010073`, us-east-1) consistent across all ten files; trust conditions (MFA + fixed session name; ExternalId placeholder); audit-bucket protections (CloudTrail `aws:SourceArn`, `bucket-owner-full-control`, TLS-only, four principals denied all mutation); CloudTrail requirement `ReadWriteType All` + management events + no `kms.amazonaws.com` exclusion; separation of authority: ceremony (CreateKey constrained + proof Sign, denied IAM/STS/Org/lifecycle/audit mutation), verifier (read-only + explicit deny on Sign/mutation), runtime (AssumeRole-only user → Sign-only role on the exact ARN). Two corrections applied (F1, F3); one ordering defect corrected in §5 (F2); Years-integer rule (F4); credential-output rule (F5); MFA mechanism (F6 → D6).

---

## 5. C1 infrastructure package — reviewed, owner-executable (authorization phrase: **"AUTHORIZE PFA-18C M1/M2/M3 SETUP"**)

**Conventions.** Owner runs each step on the **primary machine** as `jose-admin` unless marked *Device 2* or *ceremony role*; every command carries `--profile snatchit-admin --region us-east-1` (omitted below for brevity); artifact paths are relative to `docs/release/pfa18c_artifacts/`; `ART=docs/release/pfa18c_artifacts`; read-backs compare with `jq -S` normalization; **STOP after every step** and paste OUTPUT only. Never paste: passwords, MFA seeds/codes, ExternalId, access keys, session tokens, raw `assume-role` output. **Wait ≥ 60 s after creating any IAM principal before naming it in a policy** (eventual consistency, OFFICIAL-DOC).

**Preconditions to C1 (all must be true):** V2 = PAID recorded; D1–D6 answered (D2 integer stated; D6 device registered); NG-2 settled by D4; NG-3 re-baselined preflight green (§A of the runbook with §0 values); Device 2 available; **local ceremony directory** `~/pfa18c-local/` (chmod 700) holding only the two *filled* files: `m1_object_lock_configuration.filled.json` (`"Years": <int>`) and `m3_runtime_role_trust.filled.json` (ExternalId from `openssl rand -hex 32`, chmod 600) — never committed.

| Step | Mutation — resource & purpose | Prerequisites | Command(s) | Read-back verification | Expected refusal tests | Abort conditions | Cleanup limits |
|---|---|---|---|---|---|---|---|
| **C1-0a** | *(read-only)* preflight | V0–V3 done | runbook §A1–§A4 with §0 values: no trail, no `snatchit-audit-*` bucket, no `SnatchIt*` roles, users = `jose-admin` only, no customer KMS keys, `list-access-keys --user-name jose-admin` = none, ledger 130 / tip 120 / 0 keys / `guard_110_present=false` | outputs equal expectations | — | any drift | — |
| **C1-0b** | *(owner console action, D6)* register an **authenticator-app virtual MFA** on `jose-admin` (IAM → Users → jose-admin → Security credentials → Assign MFA device → Authenticator app); keep the passkey | D6 | console; then `aws iam list-mfa-devices --user-name jose-admin --query 'MFADevices[].SerialNumber'` | two entries: the passkey and `arn:aws:iam::652872010073:mfa/<name>` (= **SERIAL** used in C1-9) | — | — | deactivate device (reversible) |
| **C1-1** | IAM role **`SnatchIt-KMS-Ceremony`** — the only principal that will call `CreateKey`/proof `Sign`; deny-set in force | C1-0 | `aws iam create-role --role-name SnatchIt-KMS-Ceremony --assume-role-policy-document file://$ART/m1_ceremony_role_trust.json --max-session-duration 3600 --description "PFA-18C ceremony principal" --query Role.Arn` · `aws iam put-role-policy --role-name SnatchIt-KMS-Ceremony --policy-name pfa18c-ceremony --policy-document file://$ART/m1_ceremony_role_policy.json` | `aws iam get-role --role-name SnatchIt-KMS-Ceremony --query Role.AssumeRolePolicyDocument \| jq -S . \| diff - <(jq -S . $ART/m1_ceremony_role_trust.json)` → empty; `aws iam get-role-policy --role-name SnatchIt-KMS-Ceremony --policy-name pfa18c-ceremony --query PolicyDocument \| jq -S . \| diff - <(jq -S . $ART/m1_ceremony_role_policy.json)` → empty; `list-attached-role-policies` → none | deferred to C1-9 (needs a role session) | any diff; `MaxSessionDuration` ≠ 3600 | `delete-role-policy` + `delete-role` — fully reversible |
| **C1-2** | IAM user **`snatchit-kms-verifier`** — Device 2's read-only identity (M2) | C1-0 | `aws iam create-user --user-name snatchit-kms-verifier --query User.Arn` · `aws iam put-user-policy --user-name snatchit-kms-verifier --policy-name pfa18c-verifier-readonly --policy-document file://$ART/m2_verifier_policy.json` · `aws iam attach-user-policy --user-name snatchit-kms-verifier --policy-arn arn:aws:iam::aws:policy/SignInLocalDevelopmentAccess` · **console:** set a console password (Users → snatchit-kms-verifier → Security credentials → Console sign-in; typed locally, never in chat, never on a command line) | `get-user`; `get-user-policy … --query PolicyDocument \| jq -S .` diff → empty; `list-attached-user-policies` → exactly `SignInLocalDevelopmentAccess` (`arn:aws:iam::aws:policy/SignInLocalDevelopmentAccess`); `get-login-profile` → exists; `list-access-keys --user-name snatchit-kms-verifier` → **none** | *(Device 2, after C1-10 login)* `aws kms create-alias --alias-name alias/pfa18c-probe --target-key-id 00000000-0000-0000-0000-000000000000` → **AccessDenied** (not NotFound); `aws sts assume-role --role-arn arn:aws:iam::652872010073:role/SnatchIt-KMS-Ceremony --role-session-name x` → AccessDenied; `aws iam create-access-key --user-name snatchit-kms-verifier` → AccessDenied | any diff; any access key present | `delete-login-profile`, `deactivate-mfa-device`, `delete-user-policy`, `detach-user-policy`, `delete-user` — reversible |
| **C1-3** | IAM user **`snatchit-credential-sign-runtime`** (O1 base identity; **no access key now**) + IAM role **`SnatchIt-CredentialSign-Runtime`** (must exist before CreateKey: key policy v1 names it) | C1-0; D3; filled trust file in `~/pfa18c-local/` | `aws iam create-user --user-name snatchit-credential-sign-runtime --query User.Arn` · `aws iam put-user-policy --user-name snatchit-credential-sign-runtime --policy-name pfa18c-runtime-assume-only --policy-document file://$ART/m3_runtime_user_policy.json` · `aws iam create-role --role-name SnatchIt-CredentialSign-Runtime --assume-role-policy-document file://$HOME/pfa18c-local/m3_runtime_role_trust.filled.json --max-session-duration 3600 --query Role.Arn` (no permissions policy until after C2) | `get-user-policy` diff → empty; `aws iam get-role --role-name SnatchIt-CredentialSign-Runtime --query 'Role.AssumeRolePolicyDocument.Statement[0].[Principal.AWS, Action, keys(Condition.StringEquals)]'` → `[user ARN, "sts:AssumeRole", ["sts:ExternalId"]]` (value never printed); `list-role-policies` → empty; `list-access-keys --user-name snatchit-credential-sign-runtime` → **none** | `aws sts assume-role --role-arn arn:aws:iam::652872010073:role/SnatchIt-CredentialSign-Runtime --role-session-name probe` (as `jose-admin`) → **AccessDenied** (trust names only the runtime user + ExternalId) | any diff; any access key | `delete-role`, `delete-user-policy`, `delete-user` — reversible |
| **C1-4** | S3 bucket **`snatchit-audit-652872010073`** with **Object Lock enabled at creation** — the evidence store | C1-0; D1; D5; bucket name free | `aws s3api create-bucket --bucket snatchit-audit-652872010073 --object-lock-enabled-for-bucket` (us-east-1: no `--create-bucket-configuration`) | `get-object-lock-configuration` → `ObjectLockEnabled: Enabled` (no Rule yet); `get-bucket-versioning` → `Enabled`; `get-bucket-location` → null/us-east-1 | — | `BucketAlreadyExists` / `BucketAlreadyOwnedByYou` → stop and report (do not pick another name silently) | **reversible while empty** (`delete-bucket`); becomes irreversible once the first object is written under C1-7's rule |
| **C1-5** | bucket hardening: **Block Public Access (all four)** + **SSE-S3 (AES256)** | C1-4 | `aws s3api put-public-access-block --bucket snatchit-audit-652872010073 --public-access-block-configuration BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true` · `aws s3api put-bucket-encryption --bucket snatchit-audit-652872010073 --server-side-encryption-configuration '{"Rules":[{"ApplyServerSideEncryptionByDefault":{"SSEAlgorithm":"AES256"},"BucketKeyEnabled":false}]}'` | `get-public-access-block` → four `true`; `get-bucket-encryption` → `AES256`, no `KMSMasterKeyID` | — | any value differs | reversible |
| **C1-6** | **bucket policy** (CloudTrail write via `aws:SourceArn`; TLS-only; ceremony/verifier/runtime principals denied all mutation) | C1-1…C1-3 created **≥ 60 s** earlier; C1-5 | `aws s3api put-bucket-policy --bucket snatchit-audit-652872010073 --policy file://$ART/m1_audit_bucket_policy.json` | `aws s3api get-bucket-policy --bucket snatchit-audit-652872010073 --query Policy --output text \| jq -S . \| diff - <(jq -S . $ART/m1_audit_bucket_policy.json)` → empty | *(Device 2 later)* `aws s3api put-object --bucket snatchit-audit-652872010073 --key probe --body /dev/null` → **AccessDenied**; *(ceremony role, C1-9)* `delete-bucket-policy` → AccessDenied | `MalformedPolicy … Invalid principal` → a principal is missing or not yet visible: verify C1-1…C1-3 and wait; **never edit the artifact to make it apply** | `delete-bucket-policy` — reversible |
| **C1-7** | **Object-Lock default retention: COMPLIANCE, `Years = <D2 integer>`** | D2 stated; C1-4; filled file in `~/pfa18c-local/` | `aws s3api put-object-lock-configuration --bucket snatchit-audit-652872010073 --object-lock-configuration file://$HOME/pfa18c-local/m1_object_lock_configuration.filled.json` | `get-object-lock-configuration` → `Mode: COMPLIANCE`, `Years: <D2>` | — | `Mode` ≠ COMPLIANCE; `Years` ≠ D2 | the **rule** can be edited while the bucket is empty; **every object written after this step is locked for `Years`** and neither it nor the bucket can be removed before then — by anyone, root included |
| **C1-8** | CloudTrail trail **`snatchit-audit-trail`** — multi-region, log-file validation, management events **Read+Write incl. KMS**, logging started | C1-6, C1-7 | `aws cloudtrail create-trail --name snatchit-audit-trail --s3-bucket-name snatchit-audit-652872010073 --is-multi-region-trail --enable-log-file-validation --include-global-service-events --query TrailARN` · `aws cloudtrail put-event-selectors --trail-name snatchit-audit-trail --event-selectors '[{"ReadWriteType":"All","IncludeManagementEvents":true,"ExcludeManagementEventSources":[]}]'` · `aws cloudtrail start-logging --name snatchit-audit-trail` | `describe-trails --trail-name-list snatchit-audit-trail` → `IsMultiRegionTrail true`, `LogFileValidationEnabled true`, `IncludeGlobalServiceEvents true`, `HomeRegion us-east-1`, no `KmsKeyId`, `S3BucketName` = ours; `get-trail-status` → `IsLogging true`, then a `LatestDeliveryTime` within ~15 min; `get-event-selectors` → `ReadWriteType All`, `IncludeManagementEvents true`, `ExcludeManagementEventSources []` | *(ceremony role, C1-9)* `stop-logging` / `update-trail` / `put-event-selectors` → AccessDenied | `InsufficientS3BucketPolicyException` → bucket policy or SourceArn mismatch: re-apply the artifact verbatim, never loosen it; any selector value differs | **first locked objects appear here**: trail deletion is reversible, the objects are not |
| **C1-9** | *(no new resource)* **M1-6 MFA test + deny-set proof** as the ceremony role | C1-0b; C1-1; CLI profile `[profile snatchit-ceremony] role_arn=arn:aws:iam::652872010073:role/SnatchIt-KMS-Ceremony source_profile=snatchit-admin role_session_name=pfa18c-ceremony mfa_serial=<SERIAL> region=us-east-1` | **T1 (diagnostic only):** `aws sts assume-role --role-arn arn:aws:iam::652872010073:role/SnatchIt-KMS-Ceremony --role-session-name pfa18c-ceremony --query AssumedRoleUser` with `aws login` credentials alone — record allow/deny, do not rely on it. **T2 (the mechanism, D6):** same command **+ `--serial-number <SERIAL> --token-code <6 digits>`** → `AssumedRoleUser.Arn` = `arn:aws:sts::652872010073:assumed-role/SnatchIt-KMS-Ceremony/pfa18c-ceremony`. Then, as the role (`--profile snatchit-ceremony`, CLI prompts for the code): `sts get-caller-identity` | assumed-role ARN with session name `pfa18c-ceremony`; `cloudtrail get-trail-status --name snatchit-audit-trail` **allowed** | as the role: `aws cloudtrail stop-logging --name snatchit-audit-trail` → **AccessDenied**; `aws s3api delete-bucket-policy --bucket snatchit-audit-652872010073` → AccessDenied; `aws iam get-user --user-name jose-admin` → AccessDenied; `aws kms create-alias --alias-name alias/pfa18c-probe --target-key-id 00000000-0000-0000-0000-000000000000` → AccessDenied (not NotFound); `aws sts assume-role --role-arn arn:aws:iam::652872010073:role/SnatchIt-CredentialSign-Runtime --role-session-name x` → AccessDenied | T2 refused ⇒ STOP (D6 T3 needs its own OK; the MFA condition is **not** removed); any refusal test **succeeds** ⇒ the deny-set is not in force — STOP | none (sessions expire ≤ 1 h) |
| **C1-10** | *(Device 2, no mutation)* **M2-2/M2-3**: verifier login + attestation + full read-back | C1-2 (password set; Device 2 enrols its **own** passkey or authenticator via the console using `VerifierManageOwnMFAAndPassword`); Device 2 clean per readiness §6 | on Device 2: `aws login --profile verifier` (sign in as `snatchit-kms-verifier` + MFA) · `aws sts get-caller-identity --profile verifier` · the refusal probes listed in C1-2/C1-6 · read-backs: `iam get-role`/`get-role-policy` (both roles), `iam get-user-policy` (both users), `iam list-attached-user-policies`, `s3api get-bucket-policy`, `get-public-access-block`, `get-bucket-encryption`, `get-object-lock-configuration`, `get-bucket-versioning`, `cloudtrail describe-trails`/`get-trail-status`/`get-event-selectors`, each diffed against `$ART/*.json` (placeholders excepted) · `aws cloudtrail lookup-events --lookup-attributes AttributeKey=Username,AttributeValue=jose-admin --start-time <C1 start> --query 'Events[].[EventTime,EventName]'` → **exactly the C1 setup calls**; `--lookup-attributes AttributeKey=Username,AttributeValue=root` for the window → **none** | identity = verifier user ARN; every diff empty; probes denied; event list matches | (the probes) | any diff; any unexplained `jose-admin`/root event; any probe not denied | none |
| **PHASE-1 GATE** | — | C1-1…C1-10 green, recorded in the execution record with CloudTrail event ids | — | — | — | — | next authorization: **"AUTHORIZE PFA-18C CREATEKEY"** (C2; separate) |

**Placeholders and when they are replaced:** `<RETENTION_YEARS>` → D2 integer, in the local filled copy before C1-7 (validate: `jq '.Rule.DefaultRetention.Years|type' == "number"`); `<KMS_SIGNER_EXTERNAL_ID>` → local filled trust file before C1-3 (validate: 64 hex chars; never printed); `<PRODUCTION_KMS_KEY_ARN>` → after C2's `KeyMetadata.Arn`, into a local filled copy of `m3_runtime_role_policy.json`, validated against `^arn:aws:kms:us-east-1:652872010073:key/[0-9a-f-]{36}$` before `put-role-policy` (C2 stage). Committed artifacts keep the placeholders.

**What C1 does not do:** no `CreateKey`, no `Sign`, no DB write, no access key, no Supabase secret, no Organization, no migration, no flag. C2–C8 each require their own authorization phrase (runbook §C).

---

## 6. Release sequence for migrations 110–114 (E) — venue-native lineage preserved

Unchanged from session 8: production is deployed from the venue-native lineage (093–109, 115–120); **no merge-to-main is required or imposed**; `main` is not on this path. Requirements: the apply checkout **must contain 115–120** (candidates: `admin/operating-console` `78a56fd`/`ab3e17f` tree, or venue-native after PR #55 merges — 110–114 bytes identical); `supabase db push --linked --include-all --dry-run` must list **exactly `110,111,112,113,114`** before the real push (CLI 2.115.0), fallback = the per-file Management-API method used for 115–120; fresh `AUTODEPLOY-VERIFIED-OFF: <apply date>` on the PR + owner visual confirmation on apply day; rollbacks `114…110` staged. **Production-order rehearsal (115–120 → 110–114) is proven:** identical catalog (682 definition/grant/trigger/policy lines) and full pgTAP 4320/4316 (documented deltas only). Post-apply read-backs: census **153 / 87 / 32**, guard + recovery present, `venue.get_signing_keys_door` refuses anon/authenticated, ledger 135, numeric tip **still 120** (record explicitly). Dark deployment (C4, C6) stays separate from activation (C8).

---

## 7. Next actions

1. **Owner, now (smallest step):** V0 — `aws login --profile snatchit-admin` (Safari Private Window if needed), then paste V1/V2/V3 outputs. On `PAID` the coordinator records NG-1 cleared.
2. **Owner, then:** answer D1–D8 in one message (D2 as an integer). Nothing is executed on those answers alone.
3. **Owner, then (the one mutation request of this packet):** say **"AUTHORIZE PFA-18C M1/M2/M3 SETUP"** to start C1-0 → C1-10 one step per turn, with the reviewed artifacts in `docs/release/pfa18c_artifacts/` (F1/F3 corrected) and this §5. Not authorized by the billing upgrade.

---

## 8. Status ledger

| Item | Implemented | Tested | Deployed / applied | Operationally verified |
|---|---|---|---|---|
| Paid-plan upgrade | n/a | n/a | **OWNER-CONFIRMED; API verification pending (V2)** | pending |
| C1 artifacts (10 JSON) | validated + corrected (F1, F3) | n/a | **not applied** | no |
| Migrations 110–114 | yes | rehearsal + CI + production-order rehearsal | **no** (fresh: absent) | no |
| Migrations 115–120 (ops console; not PFA-18C) | yes | CI | yes (2026-09-08) | per admin record |
| E2 runtime provider + corrected verifiers | yes | vitest 803 + Deno CI | no | no |
| KMS key / DB trust root | n/a | M4 proven off-production | no (0 keys) | no |
| Monitor / edges / M5 | code present | mocked | no | no |
| Model A | recommendation (D4) | — | no | no |
| Backend integration, scanner SDK, launch validation | **not completed by paying AWS** | — | — | — |
