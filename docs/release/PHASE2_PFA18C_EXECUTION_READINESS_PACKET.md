# PFA-18C — EXECUTION READINESS PACKET (C1 authorized; later stages prepared)

**Date:** 2026-09-08 (sessions 8–10) · **Branch:** `feature/venue-native-and-product-v2` · **Reviewed tree:** `614c53d` (branch tip = `origin`; CI green: run `34177096660`, guard `34177101087`) · **Coordinator:** Claude A (read-back only; C18 unchanged)
**Authorization in force (2026-09-08):** the owner authorizes **PFA-18C M1/M2/M3 SETUP within the reviewed C1 package (§5)**, subject to its prerequisites and the confirmations in §2. Confirmed by the owner: account `652872010073` **PAID/ACTIVE**; engineering principal `jose-admin`; root has 0 access keys and MFA; **Object-Lock COMPLIANCE retention 3 years**; **O1 runtime-credential design**; the keep-Free-plan instruction is superseded; billing is pay-as-you-go (no $100 purchase, no recurring $100 budget). **Not authorized by this:** Organizations/account creation, `CreateKey`, the signing-key DB insert, production migration application, production secret installation, deployment, issuance/scanning activation. The owner executes every mutation under the ratified single-founder procedure with independent verification from the separate clean device (M2).

Evidence classes: **CLAUDE-OBSERVED** (read this session), **OWNER-RETURNED** (typed by the owner), **OFFICIAL-DOC**, **REHEARSAL**, **NOT OBSERVED**.

---

## 0. State (fresh, 2026-09-08T01:50–01:52Z)

| Item | Value | Class |
|---|---|---|
| Repository | `614c53d` = `origin/feature/venue-native-and-product-v2`; no PFA-18C document, artifact, migration or edge change since `1f3fc19` on this branch; `admin/operating-console` = `78a56fd` (docs only over `ab3e17f`; `supabase/` identical) | CLAUDE-OBSERVED |
| Pre-existing user edits | untouched: `M docs/release/PHASE2_PRODUCTION_KMS_SIGNING_CEREMONY_EXECUTION.md`, `?? docs/phase2/TICKETS_READ_CONTRACT_CORE_COORDINATION.md` | CLAUDE-OBSERVED |
| AWS identity | `arn:aws:iam::652872010073:user/jose-admin` (UserId `AIDAZQARUJFMXKFV5U5JY`) | CLAUDE-OBSERVED (corroborates OWNER-RETURNED) |
| AWS plan | **`accountPlanType PAID · ACTIVE · remaining credits 100.0 USD`** — **NG-1 CLEARED** | CLAUDE-OBSERVED (corroborates OWNER-RETURNED) |
| Account summary | `AccountAccessKeysPresent 0 · AccountMFAEnabled 1 · Users 1 · Roles 3 · MFADevices 2 (in use 2)` | CLAUDE-OBSERVED (corroborates OWNER-RETURNED) |
| C1-0a baseline | trails `[]` · buckets `[]` · `SnatchIt*` roles `[]` (only the 3 service-linked roles) · users `["jose-admin"]` · KMS keys `[]` · `jose-admin` access keys `[]` · groups `SnatchIt-admins` (AdministratorAccess) + direct `IAMUserChangePassword` · Organizations **not in use** · `head-bucket snatchit-audit-652872010073` → **404 (name free)** | CLAUDE-OBSERVED |
| `jose-admin` MFA devices | **one device, a passkey/security key:** `arn:aws:iam::652872010073:u2f/user/jose-admin/jose-admin-touchid-…` — **no TOTP device yet** ⇒ C1-0b (TOTP enrolment) is still required before C1-9 | CLAUDE-OBSERVED |
| Production DB | ledger **130**, numeric tip **120**, present `115…120`, **110–114 absent**, `signing_key` **0**, guard absent, flags dark (issuance/scanning/monitor `false`, fingerprint/max_not_after `null`), tickets 0, census kernel **149** / venue **83** / kernel tables **31** | CLAUDE-OBSERVED 01:49:58Z |
| Native edges | none deployed (11 legacy edges; `credential-sign` / `door-session` / `door-manifest` absent) | CLAUDE-OBSERVED (session 8) |
| Supabase branch record | `git_branch: ""`; owner visual OFF 2026-09-07 (RC3 record) | CLAUDE-OBSERVED (session 8) |

---

## 1. Billing and identity — VERIFIED

`aws freetier get-account-plan-state` returned `PAID / ACTIVE / 100.0 USD remaining` (CLAUDE-OBSERVED 01:50:26Z, matching the owner-returned output). Identity `jose-admin`, root 0 keys + MFA. Nothing purchased; no upgrade repeated. Runbook NG-1 carries a dated CLEARED note. Billing remains usage-based (§3).

---

## 2. Decisions — approved, and the one remaining confirmation

**Approved (do not reopen):** ratification 2026-09-04 (model, Model B initial / Model A before T3, M2, M3, M5/M6/recovery before issuance, C18, D2/D3/D5/D6); 2026-09-08: Paid plan; **D2 retention = 3 years COMPLIANCE** (now written into `m1_object_lock_configuration.json`, integer); **D3 = O1** (runtime user authenticates STS only → assumes the restricted role → temporary role credentials sign; no direct KMS, no fallback).

**Still unresolved per the records** (README claims a region pin "by owner confirmation" but the readiness report and execution record never recorded one; names, layout specifics, MFA mechanism, M5 scope and ordering were never answered) — **one confirmation covering all five**:

| # | Item | Proposed (from the reviewed package) | If changed |
|---|---|---|---|
| R1 | **Region pin** | `us-east-1` for the key, trail home region and every ARN | every artifact is regenerated; nothing in C1 may run first |
| R2 | **Exact resource names** | bucket `snatchit-audit-652872010073` · trail `snatchit-audit-trail` · role `SnatchIt-KMS-Ceremony` (session `pfa18c-ceremony`) · user `snatchit-kms-verifier` · user `snatchit-credential-sign-runtime` · role `SnatchIt-CredentialSign-Runtime` · key tag `snatchit:purpose=ticket-signing` · inline policy names `pfa18c-ceremony`, `pfa18c-verifier-readonly`, `pfa18c-runtime-assume-only`, `pfa18c-runtime-sign` | artifacts regenerated |
| R3 | **Account layout** | `652872010073` becomes the **workload/signing member** of a future organization; **Model B (this account) is temporary and valid only for the initial dark bootstrap**; **Model A (new management account + new audit account + SCP) must be operational by the canonical deadline: before T3** = "first production credential signed OR issuance enabled" (ratification §4, maturity trigger). No organization or account is created until the owner separately authorizes Model A. | a fresh workload account means every ARN and the ceremony move there |
| R4 | **Ceremony-role MFA** | enrol an **authenticator-app (TOTP) virtual MFA on `jose-admin`** (C1-0b) in addition to the existing passkey; satisfy `aws:MultiFactorAuthPresent=true` by passing `--serial-number`/`--token-code` on `sts assume-role` (OFFICIAL-DOC: AssumeRole MFA = SerialNumber+TokenCode; U2F/passkeys unsupported for MFA-protected API). `aws login` credentials are **not assumed** to carry MFA; the condition is never removed. | none other than T3 fallback (temporary long-lived key on `jose-admin` for the ceremony window) which needs its own OK |
| R5 | **M5 scope + ordering** | (a) M5 = one `credential-sign` call for **one throwaway atom on a non-saleable test event** + one `door-manifest` call, treated as a **pre-issuance test, not T3** (the ratification lists M5 and Model A as peers before issuance; a stricter reading would place Model A before C7 — **this is the owner's ruling, not engineering's**); (b) **apply migrations 110–114 (C4) before the signing-key DB insert (C3)**. See §5b C7 for the open engineering question on how the throwaway atom comes to exist while issuance is dark. | under the strict T3 reading Model A precedes C7 |

Everything independent of R1–R5 is prepared below. **No C1 mutation runs before R1–R2 are confirmed** (they are embedded in every artifact).

---

## 3. Cost — unchanged (§3 of the prior revision, official pricing 2026-09-08)

Dark ≈ **$1.06–1.15/month** (≈ $1.00 the key); live + **$0.015 per 1,000** credentials; credits ≈ $100 apply first (expiry ≈ 12 months after account creation, inferred ≈ 2027-09-05). Usage-based; no $100 package; no recurring $100 budget; budget alerts notify only. Exclusions unchanged (Model A accounts, data events, Lake, Supabase/Vercel/Stripe/Apple, taxes, support plans).

---

## 4. Controls status

PFA **M1–M6** are bootstrap controls; the scanner contract's **M1/M2** are the keyring/door manifests (migrations 112–114).

| Control | Artifact(s) (sha256 prefix, `614c53d` + this session's Years fill) | Implemented | Tested | Applied / deployed | Op-verified |
|---|---|---|---|---|---|
| M1 audit plane (Model B) | `m1_audit_bucket_policy` `76addba3…` · `m1_ceremony_role_trust` `89540f61…` · `m1_ceremony_role_policy` `1fddd53b…` · `m1_object_lock_configuration` (**Years 3 filled**) | drafted + validated | n/a | **no** (baseline empty) | no |
| M2 device + verifier | `m2_verifier_policy` `3082cc74…` (F3) | drafted + validated | n/a | **no** | no |
| M3 runtime role | `m3_runtime_user_policy` `a1cb8644…` · `m3_runtime_role_trust` `8ebd036a…` (ExternalId placeholder, local-only) · `m3_runtime_role_policy` `bb3a2c4f…` (ARN placeholder, filled after C2) · key policy v1 `e0560a96…` (F1) / v2 `430677d0…`; E2 code | validated; E2 implemented | E2 28 cases; CI Deno | **no** | no |
| M4 | ceremony §6.1 artifact (runbook L421–540) | yes | off-production proven | n/a | pending C3 |
| M5 | mocked rehearsal; live = C7 | mocked | yes (mocked) | no | **no** |
| M6 (110) / recovery (111) | `3134f6f6…` / `d13cf6cb…` | yes | pgTAP + production-order rehearsal | **no** (fresh) | no |
| Model A | R3 | recommendation | — | no | no |

Effective-permission recheck (after F1/F3): ceremony = CreateKey (constrained) + tag-scoped read/PutKeyPolicy + proof Sign, denied IAM/STS/Org/CloudTrail-mutation/S3-mutation/KMS-lifecycle; key policy v1 additionally grants the ceremony role `PutKeyPolicy/GetKeyPolicy/DescribeKey` (lockout safety) — removed in v2; verifier = read-only + own-user MFA/password self-service, explicit deny on Sign/mutation; runtime user = `sts:AssumeRole` on the runtime role only, `kms:*` denied; runtime role = trust (user + ExternalId) with **no permissions policy until C2**, then Sign on the exact ARN only (`NotAction kms:Sign` denied). **No principal ever receives `kms:Sign` on `key/*`.**

---

## 5. C1 package — AUTHORIZED; execution log

Conventions as before (owner runs on the primary machine as `jose-admin` unless *Device 2* / *ceremony role*; `--profile snatchit-admin --region us-east-1` on every command; `ART=docs/release/pfa18c_artifacts`; wait ≥ 60 s after creating a principal before naming it in a policy; paste OUTPUT only; never passwords, seeds, OTPs, ExternalId, keys, tokens, raw `assume-role` output).

**Prerequisites:** V-checks ✔ (PAID); D2 ✔ (Years 3 in the artifact); D3 ✔ (O1); **R1–R5 confirmation pending** (R1/R2 gate every mutation; R4 gates C1-0b/C1-9; R5 gates nothing in C1); Device 2 for C1-10; local dir `~/pfa18c-local/` (chmod 700) with `m3_runtime_role_trust.filled.json` (ExternalId = `openssl rand -hex 32`, chmod 600) — the object-lock file no longer needs a local copy.

| Step | Status | Evidence |
|---|---|---|
| C1-0a preflight (read-only) | **DONE 2026-09-08T01:51Z (CLAUDE-OBSERVED, jose-admin profile)** | §0 baseline row: no trail/bucket/`SnatchIt*` roles/keys; users = jose-admin; 0 access keys; org not in use; bucket name free; DB ledger 130 / tip 120 / 0 keys / guard absent |
| C1-0b TOTP enrolment on `jose-admin` (owner console) | pending R4 | `list-mfa-devices` currently shows only the passkey |
| C1-1 role `SnatchIt-KMS-Ceremony` | pending R1/R2 | — |
| C1-2 user `snatchit-kms-verifier` | pending R1/R2 | — |
| C1-3 user `snatchit-credential-sign-runtime` + role `SnatchIt-CredentialSign-Runtime` | pending R1/R2 | — |
| C1-4 bucket (Object Lock at creation) | pending | — |
| C1-5 PAB + SSE-S3 | pending | — |
| C1-6 bucket policy | pending | — |
| C1-7 Object-Lock default retention COMPLIANCE **3 years** (irreversible per object) | pending | artifact filled |
| C1-8 trail (multi-region, validation, Read+Write, no KMS exclusion, logging) | pending | — |
| C1-9 MFA test + deny-set proof (ceremony role) | pending R4 | — |
| C1-10 Device 2 login, attestation, full read-back | pending Device 2 | — |

The step table (purpose, prerequisites, commands, read-backs, refusal tests, abort, cleanup) is unchanged from the reviewed revision and is repeated verbatim in §5a.

### 5a. C1 step table (reviewed; unchanged)

| Step | Mutation — resource & purpose | Prerequisites | Command(s) | Read-back verification | Expected refusal tests | Abort conditions | Cleanup limits |
|---|---|---|---|---|---|---|---|
| **C1-0b** | *(owner console action, R4)* register an **authenticator-app virtual MFA** on `jose-admin` (IAM → Users → jose-admin → Security credentials → Assign MFA device → Authenticator app); keep the passkey | R4 | console; then `aws iam list-mfa-devices --user-name jose-admin --query 'MFADevices[].SerialNumber'` | two entries: the existing `u2f/…` passkey and `arn:aws:iam::652872010073:mfa/<name>` (= **SERIAL** for C1-9) | — | — | deactivate device (reversible) |
| **C1-1** | IAM role **`SnatchIt-KMS-Ceremony`** — the only principal that will call `CreateKey`/proof `Sign`; deny-set in force | C1-0 | `aws iam create-role --role-name SnatchIt-KMS-Ceremony --assume-role-policy-document file://$ART/m1_ceremony_role_trust.json --max-session-duration 3600 --description "PFA-18C ceremony principal" --query Role.Arn` · `aws iam put-role-policy --role-name SnatchIt-KMS-Ceremony --policy-name pfa18c-ceremony --policy-document file://$ART/m1_ceremony_role_policy.json` | `aws iam get-role --role-name SnatchIt-KMS-Ceremony --query Role.AssumeRolePolicyDocument \| jq -S . \| diff - <(jq -S . $ART/m1_ceremony_role_trust.json)` → empty; `aws iam get-role-policy --role-name SnatchIt-KMS-Ceremony --policy-name pfa18c-ceremony --query PolicyDocument \| jq -S . \| diff - <(jq -S . $ART/m1_ceremony_role_policy.json)` → empty; `list-attached-role-policies` → none | deferred to C1-9 | any diff; `MaxSessionDuration` ≠ 3600 | `delete-role-policy` + `delete-role` — reversible |
| **C1-2** | IAM user **`snatchit-kms-verifier`** — Device 2's read-only identity (M2) | C1-0 | `aws iam create-user --user-name snatchit-kms-verifier --query User.Arn` · `aws iam put-user-policy --user-name snatchit-kms-verifier --policy-name pfa18c-verifier-readonly --policy-document file://$ART/m2_verifier_policy.json` · `aws iam attach-user-policy --user-name snatchit-kms-verifier --policy-arn arn:aws:iam::aws:policy/SignInLocalDevelopmentAccess` · **console:** set the console password (typed locally, never in chat or on a command line) | `get-user`; `get-user-policy … --query PolicyDocument \| jq -S .` diff → empty; `list-attached-user-policies` → exactly `SignInLocalDevelopmentAccess`; `get-login-profile` → exists; `list-access-keys` → **none** | *(Device 2, C1-10)* `kms create-alias --alias-name alias/pfa18c-probe --target-key-id 00000000-0000-0000-0000-000000000000` → **AccessDenied** (not NotFound); `sts assume-role` into the ceremony role → AccessDenied; `iam create-access-key --user-name snatchit-kms-verifier` → AccessDenied | any diff; any access key | reversible (login profile, MFA, policies, user) |
| **C1-3** | IAM user **`snatchit-credential-sign-runtime`** (O1 base identity; **no access key now**) + IAM role **`SnatchIt-CredentialSign-Runtime`** (trust only; no permissions policy until C2 — staged without any `key/*` grant) | filled trust file | `aws iam create-user --user-name snatchit-credential-sign-runtime --query User.Arn` · `aws iam put-user-policy --user-name snatchit-credential-sign-runtime --policy-name pfa18c-runtime-assume-only --policy-document file://$ART/m3_runtime_user_policy.json` · `aws iam create-role --role-name SnatchIt-CredentialSign-Runtime --assume-role-policy-document file://$HOME/pfa18c-local/m3_runtime_role_trust.filled.json --max-session-duration 3600 --query Role.Arn` | `get-user-policy` diff → empty; `aws iam get-role --role-name SnatchIt-CredentialSign-Runtime --query 'Role.AssumeRolePolicyDocument.Statement[0].[Principal.AWS, Action, keys(Condition.StringEquals)]'` → `[user ARN, "sts:AssumeRole", ["sts:ExternalId"]]`; `list-role-policies` → empty; `list-access-keys` → **none** | `aws sts assume-role --role-arn arn:aws:iam::652872010073:role/SnatchIt-CredentialSign-Runtime --role-session-name probe` (as jose-admin) → **AccessDenied** | any diff; any access key | reversible |
| **C1-4** | S3 bucket **`snatchit-audit-652872010073`**, Object Lock enabled at creation | name free (✔ 01:51Z) | `aws s3api create-bucket --bucket snatchit-audit-652872010073 --object-lock-enabled-for-bucket` | `get-object-lock-configuration` → `Enabled` (no Rule); `get-bucket-versioning` → `Enabled` | — | `BucketAlreadyExists`/`OwnedByYou` → stop and report | reversible while empty |
| **C1-5** | Block Public Access ×4 + SSE-S3 | C1-4 | `aws s3api put-public-access-block --bucket snatchit-audit-652872010073 --public-access-block-configuration BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true` · `aws s3api put-bucket-encryption --bucket snatchit-audit-652872010073 --server-side-encryption-configuration '{"Rules":[{"ApplyServerSideEncryptionByDefault":{"SSEAlgorithm":"AES256"},"BucketKeyEnabled":false}]}'` | `get-public-access-block` → four `true`; `get-bucket-encryption` → `AES256`, no KMS key | — | any value differs | reversible |
| **C1-6** | bucket policy (CloudTrail write via `aws:SourceArn`; TLS-only; four principals denied mutation) | C1-1…C1-3 ≥ 60 s earlier; C1-5 | `aws s3api put-bucket-policy --bucket snatchit-audit-652872010073 --policy file://$ART/m1_audit_bucket_policy.json` | `aws s3api get-bucket-policy --bucket snatchit-audit-652872010073 --query Policy --output text \| jq -S . \| diff - <(jq -S . $ART/m1_audit_bucket_policy.json)` → empty | *(Device 2)* `put-object` → AccessDenied; *(ceremony role)* `delete-bucket-policy` → AccessDenied | `MalformedPolicy … Invalid principal` → wait/verify principals; never edit the artifact to make it apply | reversible |
| **C1-7** | Object-Lock default retention **COMPLIANCE, 3 years** | C1-4 | `aws s3api put-object-lock-configuration --bucket snatchit-audit-652872010073 --object-lock-configuration file://$ART/m1_object_lock_configuration.json` | `get-object-lock-configuration` → `COMPLIANCE`, `Years: 3` | — | Mode ≠ COMPLIANCE; Years ≠ 3 | **every object written after this is locked 3 years; neither it nor the bucket can be removed before then, root included** |
| **C1-8** | CloudTrail **`snatchit-audit-trail`** — multi-region, validation, management Read+Write incl. KMS, logging | C1-6, C1-7 | `aws cloudtrail create-trail --name snatchit-audit-trail --s3-bucket-name snatchit-audit-652872010073 --is-multi-region-trail --enable-log-file-validation --include-global-service-events --query TrailARN` · `aws cloudtrail put-event-selectors --trail-name snatchit-audit-trail --event-selectors '[{"ReadWriteType":"All","IncludeManagementEvents":true,"ExcludeManagementEventSources":[]}]'` · `aws cloudtrail start-logging --name snatchit-audit-trail` | `describe-trails` → multi-region, validation, global events, home us-east-1, no KmsKeyId, our bucket; `get-trail-status` → `IsLogging true`, delivery within ~15 min; `get-event-selectors` → `All`, mgmt `true`, exclusions `[]` | *(ceremony role)* `stop-logging`/`update-trail`/`put-event-selectors` → AccessDenied | `InsufficientS3BucketPolicyException` → re-apply the artifact verbatim, never loosen | trail reversible; **locked objects are not** |
| **C1-9** | M1-6 MFA test + deny-set proof | C1-0b; C1-1; profile `[profile snatchit-ceremony] role_arn=arn:aws:iam::652872010073:role/SnatchIt-KMS-Ceremony source_profile=snatchit-admin role_session_name=pfa18c-ceremony mfa_serial=<SERIAL> region=us-east-1` | **T1 (diagnostic only):** `aws sts assume-role --role-arn arn:aws:iam::652872010073:role/SnatchIt-KMS-Ceremony --role-session-name pfa18c-ceremony --query AssumedRoleUser` (login creds alone) — record, don't rely. **T2 (mechanism):** same **+ `--serial-number <SERIAL> --token-code <6 digits>`** → `AssumedRoleUser.Arn` = `arn:aws:sts::652872010073:assumed-role/SnatchIt-KMS-Ceremony/pfa18c-ceremony`; then `--profile snatchit-ceremony`: `sts get-caller-identity` | assumed-role ARN; `cloudtrail get-trail-status` allowed | as the role: `cloudtrail stop-logging` → AccessDenied; `s3api delete-bucket-policy` → AccessDenied; `iam get-user --user-name jose-admin` → AccessDenied; `kms create-alias … 00000000-0000-0000-0000-000000000000` → AccessDenied; `sts assume-role` into the runtime role → AccessDenied | T2 refused ⇒ STOP (T3 needs its own OK; the condition stays); any refusal test **succeeds** ⇒ STOP | none |
| **C1-10** | *(Device 2)* M2-2/M2-3 login, attestation, full read-back | C1-2; Device 2 enrols its **own** passkey/authenticator via the console; Device 2 clean per readiness §6 | `aws login --profile verifier` · `sts get-caller-identity --profile verifier` · probes (C1-2/C1-6) · read-backs of both roles, both users, attached policies, bucket policy/PAB/encryption/object-lock/versioning, trail/status/selectors, each diffed against `$ART/*.json` · `cloudtrail lookup-events --lookup-attributes AttributeKey=Username,AttributeValue=jose-admin --start-time <C1 start>` → exactly the C1 calls; root → none | verifier ARN; empty diffs; probes denied; event list matches | (probes) | any diff / unexplained event / probe not denied | none |
| **PHASE-1 GATE** | — | C1-1…C1-10 green + event ids recorded | — | — | — | — | next: **"AUTHORIZE PFA-18C CREATEKEY"** (C2) |

### 5b. Later stages — exact packages prepared (each needs its own authorization; nothing here is authorized yet)

| Stage | Authorization phrase | Exact artifact / mutation scope (prepared) |
|---|---|---|
| **C4 — migrations 110–114** (R5b: before C3) | "AUTHORIZE PFA-18C MIGRATIONS 110-114" | **Apply tree:** `admin/operating-console @ 78a56fd` (contains 110–120; `supabase/` identical to CI-green `ab3e17f`; 110–114 sha `3134f6f6… d13cf6cb… 97d33d08… 32d42324… 9974eb91…` = venue-native bytes; 5 rollbacks present) — or venue-native after PR #55 merges. **CLI:** local `supabase` is **2.115.0** (the pin). **Commands (owner):** `git -C /Users/josetascon/snatchit-admin-console rev-parse HEAD` = `78a56fd…`; `supabase link --project-ref hqycwntpfoztoinemqns` (password from keychain, never chat); **`supabase db push --linked --include-all --dry-run`** → must list **exactly** `110_…`, `111_…`, `112_…`, `113_…`, `114_…` and nothing else (any extra = STOP); then `supabase db push --linked --include-all`. **Read-backs (SQL, read-only):** ledger 135; numeric tip **120** (unchanged — record); `guard_110_present=true` + trigger enabled; `count(*) from kernel.signing_key_recovery_approval` = 0; census kernel **153** / venue **87** / kernel tables **32**; `venue.get_signing_keys_door` grants = service_role only; `begin; insert <second global key>; rollback` refuses `active_global_exists` (only meaningful after C3). **Rollback:** `supabase/rollbacks/114…110` in reverse (rehearsed). **PR hygiene:** fresh `AUTODEPLOY-VERIFIED-OFF: <apply date>` on the carrying PR; `git_branch ""` + owner visual OFF on apply day. **Order-independence:** proven by the production-order rehearsal (session 8; identical catalog; pgTAP 4320/4316). |
| **C2 — CreateKey + binding proof** | "AUTHORIZE PFA-18C CREATEKEY" | As the ceremony role session (`--profile snatchit-ceremony`, MFA prompt): `aws kms create-key --key-spec ECC_NIST_P256 --key-usage SIGN_VERIFY --origin AWS_KMS --description "Snatch It ticket-signing trust root (PFA-18C)" --tags TagKey=snatchit:purpose,TagValue=ticket-signing --policy file://$ART/kms_key_policy_v1_binding_proof.json --query KeyMetadata` → **Arn = D4** (all three principals named in v1 must exist ≥ 60 s earlier: ceremony role, verifier user, runtime role — C1 provides them). *Device 2:* `aws kms get-public-key --profile verifier --key-id <D4> --query PublicKey --output text \| base64 -d > pub.der`; `openssl pkey -pubin -inform DER -in pub.der -out pub.pem`; `shasum -a 256 pub.der` = **D5** (Device 2 states first; primary recomputes independently from its own `get-public-key`). **Binding proof:** Device 2 mints `challenge.bin` (`printf 'snatchit-ceremony %s %s' "$(date -u +%Y%m%dT%H%M%SZ)" "$(openssl rand -hex 16)" > challenge.bin`), transfers it out-of-band; ceremony: `aws kms sign --key-id <D4> --message fileb://challenge.bin --message-type RAW --signing-algorithm ECDSA_SHA_256 --query Signature --output text \| base64 -d > sig.der`; Device 2: `openssl dgst -sha256 -verify pub.pem -signature sig.der challenge.bin` → `Verified OK`; altered message → FAIL; throwaway key → FAIL. Then `aws kms put-key-policy --key-id <D4> --policy-name default --policy file://$ART/kms_key_policy_v2_final.json` (fallback if v2's lockout check refuses the ceremony role: `jose-admin` applies v2 — logged; never `--bypass-policy-lockout-safety-check`); fill `m3_runtime_role_policy.json` locally with D4 (validate `^arn:aws:kms:us-east-1:652872010073:key/[0-9a-f-]{36}$`) and `aws iam put-role-policy --role-name SnatchIt-CredentialSign-Runtime --policy-name pfa18c-runtime-sign --policy-document file://$HOME/pfa18c-local/m3_runtime_role_policy.filled.json`; ceremony `kms sign` again → **AccessDenied**; Device 2 `get-key-policy` = v2; `describe-key` → `ECC_NIST_P256 / SIGN_VERIFY / Enabled / MultiRegion false`; CloudTrail event ids (`CreateKey`, `Sign`, `PutKeyPolicy` ×2, `PutRolePolicy`). |
| **C3 — trust-root DB commit** | "AUTHORIZE PFA-18C TRUST-ROOT DB COMMIT" | Owner saves the §6.1 artifact **verbatim** from `docs/phase2/PRODUCTION_SIGNING_KMS_CEREMONY.md` L421–540 as `$CEREMONY_DIR/signing_key_bootstrap.sql`; stages `handle.txt` (= D4), `fingerprint.txt` (= D5 from **Device 2**), `pub.pem` (Device 2's); re-confirms `select count(*) from kernel.signing_key` = 0 seconds before; runs **§6.2 exactly**: `psql "$PROD_DB_URL" -v PUBLIC_KEY_PEM="$(cat pub.pem)" -v KMS_HANDLE_REF="$(cat handle.txt)" -v EXPECTED_FINGERPRINT="$(cat fingerprint.txt)" -v ALGORITHM="ES256" -f signing_key_bootstrap.sql` (never the SQL editor); required output = the four NOTICEs + `COMMIT`; read-backs §7.1–§7.5 (row `…b0`, global, active, ES256, handle = D4, key = pub.pem; resolver returns `…b0`; parked-state loop as corrected; no shadow key; dry-run mint rolled back); guard 110 (already applied under R5b) accepted the row. Rollback: §10 `signing_key_bootstrap_ROLLBACK.sql` — available only while no atom pins the key. |
| **C5 — monitor arming** | "AUTHORIZE PFA-18C MONITOR ARMING" | config appends: `signing.expected_key_fingerprint := D5`; (`signing.expected_max_not_after` stays null — D6); `signing.monitor_enabled := true`; run `kernel.check_signing_key_invariants()` once → fingerprint match, one active global key. Rollback: `signing.monitor_enabled := false`. |
| **C6 — O1 secrets + dark deploy** | "AUTHORIZE PFA-18C DARK DEPLOY" | (1) `aws iam create-access-key --user-name snatchit-credential-sign-runtime --query 'AccessKey.[AccessKeyId,Status]'` — the **secret is written by the CLI to a local file only** (`--output json > ~/pfa18c-local/runtime-key.json`, chmod 600; never displayed); (2) owner builds `~/pfa18c-local/runtime.env` (chmod 600) with exactly: `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_REGION=us-east-1`, `KMS_SIGNER_ROLE_ARN=arn:aws:iam::652872010073:role/SnatchIt-CredentialSign-Runtime`, `KMS_SIGNER_EXTERNAL_ID=<the value in the filled trust file>`, `KMS_SIGNER_SESSION_NAME=snatchit-kms-signer`, `KMS_SIGNER_DURATION_SECONDS=3600`, `KMS_PROVIDER=aws` (env contract: `KMS_RUNTIME_CREDENTIALS.md` §2); (3) `supabase secrets set --project-ref hqycwntpfoztoinemqns --env-file ~/pfa18c-local/runtime.env`; `supabase secrets list --project-ref hqycwntpfoztoinemqns` shows names only; (4) deploy from the reviewed tree (`614c53d` or `78a56fd` — edge bytes identical since `1f3fc19`): `supabase functions deploy credential-sign --project-ref hqycwntpfoztoinemqns` (verify_jwt **true**), `supabase functions deploy door-manifest --project-ref hqycwntpfoztoinemqns` (verify_jwt **true**), `supabase functions deploy door-session --no-verify-jwt --project-ref hqycwntpfoztoinemqns` (bearer `DoorSession` model, verify_jwt **false**); (5) read-back `supabase functions list` → three new functions, `verify_jwt` postures as above; no call made. Dark: flags stay false; nothing signs until called. Rollback: `supabase secrets unset …`, delete the three functions, deactivate + delete the access key. |
| **C7 — M5 live proof** | "AUTHORIZE PFA-18C M5" | one `credential-sign` call (staff JWT, body `{ticket_atom_id}`) → `signed`; one `door-manifest` call on a real open episode → `signed`; CloudTrail: exactly one `Sign` per call by `SnatchIt-CredentialSign-Runtime/snatchit-kms-signer`, none by any other principal; `/keys` from a real door session returns `…b0` with the DB `public_key`; offline verify passes; flags stay false. **Open engineering question (surfaced, not decided):** `kernel.issue_ticket_atoms` refuses while `feature.native_issuance_enabled=false` (093 L4931–4937), so with issuance dark **no atom can be minted for the credential-sign half of M5**. The only paths are (a) a superuser test INSERT into `kernel.tickets` for a non-saleable test event — a production custody-table write needing its own authorization, and it **pins the key** (084 FK RESTRICT ⇒ the C3 rollback becomes unavailable), or (b) redefining the credential-sign half of M5 (e.g. rehearsal DB + the deployed edge is not possible; a signed door-manifest alone proves the KMS path but not `get_ticket_signing_context`). To be settled with the owner before C7; no change to ratification wording is proposed. |
| **C8 — activation** | outside this packet | M5 + M6 + Model A green; owner flips `feature.native_issuance_enabled` / `feature.native_scanning_enabled` separately. |

Changes since the rehearsal evidence (`1f3fc19`, 2026-09-06): **none** to migrations 110–114, their rollbacks, the three native edges or `_shared` on the venue-native branch; the admin branch adds 115–120 + ops edges (`ops-refund-execute`, not deployed) and touches none of the PFA-18C files (verified by path-scoped `git log` and `git diff --stat`).

---

## 6. Status ledger

| Item | Implemented | Rehearsal-tested | Deployed / applied | Operationally verified |
|---|---|---|---|---|
| Paid plan | n/a | n/a | **VERIFIED PAID/ACTIVE (API, 2026-09-08T01:50Z)** | yes |
| C1-0a preflight | — | — | **done (read-only)** | evidence §0 |
| C1-0b…C1-10 | artifacts final (Years 3; F1/F3) | n/a | **not started** — R1/R2 confirmation + R4 enrolment gate the first mutation | no |
| Migrations 110–114 | yes | yes (+ production-order) | **no** (fresh: absent) | no |
| KMS key / DB trust root | n/a | M4 proven | no (0 keys; 0 KMS keys in AWS) | no |
| O1 secrets / edges / M5 | code present | mocked | no | no |
| Model A | R3 | — | no | no |
| Product launch readiness | **not claimed** — live integration, scanner readiness and controlled commerce checks unproven | — | — | — |
