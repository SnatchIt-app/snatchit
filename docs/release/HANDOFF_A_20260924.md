# Claude A — handoff and release-window close (2026-09-24)

Records of truth: `SPRINT_STATUS_20260917.md`, `MIGRATION_NUMBER_REGISTRY.md`,
`SANDBOX_D8_READS_20260924.md`, `SANDBOX_V3_FIXTURE_APPROVAL_SHEET_20260924.md`,
`APP_STORE_SUBMISSION_CHECKLIST_V3_20260924.md`, all on `release/candidate-20260918`.

## 1. Completed work, with exact commits

| Item | Result | Where |
|---|---|---|
| Signing monitor, 2026-09-24 05:23:00Z run | **PASS** (A and D independently). The job fired on its own schedule: succeeded, "1 row", 43 ms; 0 alert rows in a container retaining the period; would_alert false. Second clean unattended run after the Vault change | sprint status, `496755f4` / `8d6cf501` |
| PR #91 (`SnatchIt-app/snatchit`) | **merged into `release/production-gate-20260918` only**, 05:31:51Z, merge commit `aadf996e`, after D's monitor PASS and a seven-point GO; `main` untouched at `eadd456a`; nothing re-applied (ledger 160, defn `705953d5…`) | sprint status and registry, `13b590b0` |
| Vercel after the merge | snatchit-web: one preview record, **CANCELED** (skipped by the Ignored Build Step, not built); snatchit-admin: **no record**, with a positive control | same |
| D-8 Tickets RPC (sandbox) | **closed**: function, grants, 17-column shape, ledger row, `kernel.tickets` 0 | `SANDBOX_D8_READS_20260924.md` |
| Sandbox fixture state + W1/W2 side effects | recorded from deployed bodies. Bids 0; 3 live listings, all ending by 2026-09-25 02:01Z; reversed ×4 usable; expired and held absent | same |
| Fixture approval sheet, rev 2 | independent cleanup tracks, both restore outcomes, owner-unavailable rule, pre-session recheck; **APPROVED D1–D6 by the owner 2026-09-24** (execution waits for Build 24 installed + "go" + A's preflight) | `7dae4815` (spec) |
| App Store submission checklist, draft | historical-document register; Stripe live/test evidence with its limits; minimal validation plan V1–V6a; claims verdicts (unsupported ones omitted); refund and payout facts; owner actions O1–O8; policy decisions P1–P6. D reviewed and its points are integrated. **Verdict: not submission-ready** | `53d7b352` / `23407b39` |
| Draft metadata and review notes | built only from verified claims; placeholders for owner steps | `APP_STORE_REVIEW_NOTES_V3_DRAFT_20260924.md` |

## 2. Release window close — the actual production state

- **Live:** the 24 release-candidate migrations plus 147 and, since 2026-09-24 16:55:40Z, 148 (ledger 161); ten edge functions, nine from gate `5b255838` and one from #92's `e73553d2`:
  create-payment-intent v48, confirm-payment v37, confirm-and-release v37, enforce-transfer-expiry v41 (#92, since 16:57:01Z; was v40),
  stripe-webhook v42, create-connect-account v35, delete-account v22, notify-transfer v8, send-push v22,
  notify-report v10. The containment write (listing `f499dc79` → sold). The signing monitor.
- **Off, and staying off:** alert delivery; refund-resolution detector; ops refund execute; the native payout and
  refund executors; `EMAIL_ENABLED` absent [D's production read]; operator alerts undelivered (0 delivered).
- **Shipped but inert, never described as operational:** native dispute handling (unreachable, and PFA-31 parks it);
  the send-push b2 challenge (three blockers); 139's report-delivery dedupe (fails open while `notify` is not exposed);
  outbid push (legacy settings unset).
- **Deployed, reachable, never exercised in production — a separate category (D):** the attempt-based payout path
  deployed 2026-09-23 (`payout_attempts` 0 rows). Its first run will be its first run against real money.
- **Unresolved findings:**
  - **F-DISPUTE-SELLERWIN-1:** a seller-win dispute resolution has no payout path. 0 rows are affected, but no
    dispute has ever been resolved and 5 are open.
  - pg_net at about 3% "succeeded, not delivered".
  - The synthetic ops case `650e7344` awaits a console dismissal.
  - 3 stale pending live intents: $2.20, $88, and $330 on the only reviewable listing.
  - The 2026-08-04 live refund rows are ambiguous (O6).
  - D's App Store claim reads await the owner's scope ruling.

## 3. Remaining tasks (A)

1. **Done:** gate CI run 35960357963 is green (5/5 jobs, 05:34:32Z). D's independent post-merge verification is **done**: every number matched, and `main` was proved
   not to contain the merge by an ancestry test.
2. F-DISPUTE-SELLERWIN-1: **implemented as PR #92** (148 + `enforce-transfer-expiry`; CI green; D reproduced the
   mutants). The production execution package is prepared, rehearsed and frozen, and awaits the owner's R1
   authorisation (`PR92_PRODUCTION_EXECUTION_PACKAGE_20260924.md`).
3. Revise the App Store checklist as the owner's O-actions and P-decisions arrive; C aligns the in-app copy to P1/P3.
4. Run the fixture session on the rev-2 sheet (D1–D6 **approved**) once Build 24 is installed, the owner says "go", and the pre-session recheck passes.

## 4. Owner actions — SUPERSEDED: the current list is `APP_STORE_SUBMISSION_CHECKLIST_V3_20260924.md` §8 (revised 2026-09-24 ~16:10Z)

The table below is kept as it stood at the window close.


| # | Action |
|---|---|
| O1 | Decide D1–D6 and D's sandbox witness reads on the fixture sheet, rev 2, `7dae4815` |
| O2 | Stripe live Dashboard: the webhook endpoint's URL, status and events, **including `payment_intent.canceled`**; compare the secret digests of `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` locally (checklist V2/V3) |
| O3 | Confirm the Apple Pay certificate for `merchant.com.snatchit` (live) |
| O4 | Confirm the reviewer accounts sign in; decide the credential rotation (still OPEN) |
| O5 | Confirm the Supabase Auth test phone number; decide Twilio auto-recharge (OFF) |
| O6 | Stripe read of the two live `refunded` rows of 2026-08-04: refund or lost dispute, and the amount |
| O8 | Low-priced review inventory plan; decide the $300 listing `c8d04339…` and its stale $330 intent |
| — | Rule on D's production reads made for the App Store claims (ratify, or record them as unauthorised-source) |
| — | Operating-policy decisions P1–P7 (P7: the false "Buyer confirmed receipt" notice after a seller-win, a decision separate from the payout fix); review times, who handles disputes and reports, removal and suspension, refund policy, reviewer-purchase refunds, seller-win payouts until the fix ships |
| Later | V6/V6a (no charge) and V7 (one $2.20 live purchase plus refund): each needs its own permission |
