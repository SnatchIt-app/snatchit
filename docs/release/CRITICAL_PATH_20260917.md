# Task-level critical path (A, 2026-09-17; owner request) — five tracks, sandbox verification separated from production

Legend: **exists** = code/records present at the build tag `candidate/2026-09-18-build-b2` (`aad5f75`) or applied where
stated · **missing** = no code and/or no owner · **owner** = the session that does it (A/B/C/D) or **Owner** (a ruling, an
MFA act, a ceremony) · effort in working days for one session, review included, as each session estimated it (C's lane is
C's own estimate of 2026-09-17; B's and D's lanes are marked *(pending B/D)* until their inputs land and are merged verbatim)
· **∥** = can run in parallel with the row above. Nothing here authorizes a production change, a build, a flag, a secret
or a notification.

## Track 1 — Marketplace release readiness (production)

State: the candidate is on the sandbox at ledger 141 with the four edges (manifest §11, D PASS); the combined build is being
cut from `aad5f75`; production is untouched at ledger 135.

| # | Task | Exists | Missing | Owner | Depends on | Effort | ∥ |
|---|---|---|---|---|---|---|---|
| 1.1 | Combined build cut from the tag; installation link to the owner | **SUBMITTED 2026-09-17** by C from the tag in a clean worktree (`aad5f75`, 0 dirty): EAS iOS preview/internal build `dcbf20e0-76dd-4b18-a48a-20c203ba0175`, expected build 18; page https://expo.dev/accounts/jdt_inc/projects/snatchit/builds/dcbf20e0-76dd-4b18-a48a-20c203ba0175 (the installation link when it finishes) | the finished artifact | C | — | done | — |
| 1.2 | Handset session 2 on the combined build: DV-AUTH-1 (sign-in hang), DV-611C-2 (registration visibility), DV-S1/S2 (seller keyboard), row-11 residue (large-text banner), DV-ST1..ST4 (offline/error/empty/no-match, F-OFF-1) | scripts, read-backs | the run | C (guide), Owner (handset ≈ 2.5–3 h), A (4 read-backs + 1 staged read), D (witness) | 1.1 | 0.5 d C | — |
| 1.2b | **Next candidate contents (client-only unless noted), integrated onto the stack only after D's PASS on each and the owner's word; a new build-source tag; `9bef640` stays the sandbox application pin:** 136 (A, SQL, D PASS rev3), 139 (B, SQL + edge, D gap: release-on-total-failure pending), `aeb4081` (B, webhook preference read, D's two conditions in), `cf94311` (C, hide five, D PASS), `577ec40` (C, F-2S-1 with D's wording, D PASS), `da1d11d` (C, notice surface rev: any returned type renders, newest unread wins, unknown → Dismiss only, top inset, failed-dismiss copy; **D PASS**), `df5127c` (F-2S-1 comment nit; **D PASS**), `ac70643` (Tickets sample label, layout-only option D; **D PASS**; **owner 2026-09-17: included** — the label and the fixture rows render only when the `devFixtures` state is true, and its only setter is the toggle rendered inside a `__DEV__` branch (initial state false), so preview and production builds cannot show either; A verified from the head's source), **`296439c` (C, F-611C-2, **D PASS** 13/13 + four mutants: foreground re-attempt gating, both limit texts classified rate_limited with a visible remedy, challenge elapsed-time resume; found by S2-1 step 2 — on Build 18 a (user, token) pair stays silent for the rest of that sign-in once the per-token challenge budget is spent; sign-out clears it)**. From the fixed client's source: the visible code is requested at most once per challenge (one-shot fallback armed only in awaiting_push; awaiting_code never re-registers on foreground; only the user's Try again on a dead challenge issues again), and the RC2 test pins 135's literal 'precondition_failed: too many challenge requests'. **Status 2026-09-17 (later): every C head D-cleared; A's 136 D-cleared; B's `aeb4081` D PASS (both conditions verified by control: the ordering inversion fails P7 and nothing else, 97/97 in the neighbouring webhook suites; removing the `pref_read_failed` token fails P4); 139 `ef8e2d5` carries ONE D residual — the claim release sits on the success path and is gated on `attempted > 0`, so a throw between the claim and the release, or before any send, strands the claim; D's ask: release in `finally` and drop the `attempted > 0` gate; G11 does not cover it because `sends: 'throw'` throws inside a send, which the send's own catch converts to `false`, so G11 walks G10's path — **fixed by B at `5a62bf0`** (CI 35181349762 success, five jobs, verified by A; state hoisted above the try, release in `finally`, condition `delivered === 0` with no `attempted > 0` gate; B's controls: release back on the success path fails G14 alone, restoring `attempted > 0` fails G14 alone; fresh replay 155/155, full pgTAP 5221/5221, vitest 2092/2092; census 22/305 unchanged; the only SQL change is a header comment plus a warning, in the migration header and the edge, that moving 099's schedule (today `23 5 * * *`) to within ~1 h of 00:00 UTC makes the date-key exposure live until 137 lands the run id); **awaiting D's explicit PASS on `5a62bf0`** — B's report that D said nothing further is not a pass. Owner's rulings 2026-09-17: all five reviewed C heads join the next candidate (`cf94311`, `df5127c`, `da1d11d`, `296439c`, `ac70643`, the last with its exclusion from preview and production builds preserved); the D-reviewed corrected device-rebound wording (136 template v2) and the neutral challenge-banner wording (`df5127c`) are approved; after D clears B's remaining head, A integrates the batch and runs the combined checks — no additional build, sandbox application, production change or outbound notification is authorized.** **INTEGRATED 2026-09-17 04:27Z at stack head `e9b52ce`** (`release/production-gate-20260918`, tree `65a9ca90`; D's explicit PASS on every head, incl. 139 @ 5a62bf0 and its test-only follow-up 7612c8b): merges in order c4a8b8d (136 ae09f5b) → 859d9a0 (139 5a62bf0) → 3da9fb1 (aeb4081) → f40d960 (cf94311) → 8738c36 (df5127c) → 9634dd3 (da1d11d) → 7a4e7a4 (296439c) → 7f389e0 (ac70643), then the 179/180 A9/A6 prose repair (305 = 296 + 131's four + 135's three + 139's two; values unchanged) and the follow-up merge of 139 @ 7612c8b (G16 isolates the `finally` from the dropped `attempted > 0` gate; D verified both mutants). 157 merged cleanly: 136's A28 and 139's notify counts are disjoint lines and both right on the merged tree (136 adds no notify routine, 139 adds no template), so the expected hand-merge did not arise. **Combined checks** (certified local harness on the merged tree — identical to the stack tree before the two final commits, then 179/180 and the claim suite re-run on the final files): fresh replay clean, Gate-2 tables 32 / functions 107 / policies 37 / triggers 38; pgTAP 5257/5257 (203: 36, 204: 41; 179: 45, 180: 42 after the repair); census notify routines 22, five-schema routines 305, notify tables 9, templates 63 with `security_device_rebound` v2 present; vitest **2124/2124 (101 files) re-measured on `e9b52ce` itself** — the first figure, 2123, was measured on the tree one commit earlier, before the 7612c8b follow-up added G16 (one test); D caught the stale number and A re-ran rather than adopting D's figure; claim suite 16/16 at 7612c8b; typecheck exit 0; lint 0 errors (29 pre-existing warnings). CI on `e9b52ce`: run 35181982625 **success on all five jobs** (Typecheck / Lint / Unit tests; Admin console; Web build; Deno type-check; Migrations apply cleanly on a fresh DB). **D PASS on `e9b52ce` (2026-09-17):** all nine heads verified as ancestors AND each head's load-bearing content verified present in the merged tree (136's v2 subject, unread/undismissed filter and registry-derived type set; 139's release verb, `finally` on `delivered === 0`, G16; aeb4081's `pref_read_failed` and P7 trace equality; cf94311's visible-toggles call; df5127c's sentence; da1d11d's top inset, unread filter and failed-dismiss copy; 296439c's foreground gate, challenge resume and widened rate-limit regex); 179/180 arithmetic checked per file (different baselines, both reach 296 then 305); tsc exit 0. D's scope statement: client-side and content only — the replay, Gate-2, pgTAP and census figures are A's, from A's certified harness; the pass is not a deployment-readiness statement; nothing in this batch has been on a handset and the b2 notification half still needs the deferred push key. **No build-source tag published and no build cut** — both wait for the owner's word; `9bef640` stays the sandbox application pin; Build 18 (`aad5f75`) stays the handset build. Near-miss disclosed: the first rehearsal merge ran in a pre-existing worktree on `frontend/candidate-recovery` because the scratch path collided; it was restored to `231f120` (= origin) from the reflog before anything else, nothing pushed, and the rehearsal redone in a guarded scratch worktree | heads | D's passes on B; then the owner's word for a tag/build | A | 1.2 | 0.5 d | — |
| 1.3 | Carried forward at their real status (not upgraded): two-session K-2 case **untested** outside D's harness (needs a second iPhone); row 18 **deferred** (needs push delivery); A11Y-1 VoiceOver **untested**; every push-delivery row **deferred** (option (b)) | records | evidence | C/A | Owner: key deferral, second device, VoiceOver opt-in | — | — |
| 1.4 | Two-second registration rejection window — **assessed by C + D 2026-09-17 (source, at the tag): understandable and recoverable, no silent failure found in source.** Reframing: `push_session_predates_epoch` bars every session older than the epoch, so this experience is met on every password change / sign-out-everywhere, not only in the 2 s race. Register path: the 42501 is classified `session_stale`, persisted and published to Settings **before** the local sign-out; the user lands on the login screen with "You were signed out on this device. Sign in again to keep notifications on."; a post-epoch session registers normally (tests + row 17). **F-2S-1 (LOW, copy):** the 135 challenge path does not route into the stale handler, so the failed-challenge banner shows the same "signed out" sentence while the user is still signed in and the app works; D recommends neutral copy on the challenge banner (not forcing re-auth from a push flow; that would be a behaviour change needing its own review); owner's call, not in the tag. One constructible quiet case recorded, not dismissed: a network drop in the sub-second between refusal and local sign-out leaves the user signed in until the next refusal, Settings still showing the remedy. **Device-untested:** C proposes DV-131-1 (register path, combined build) and DV-131-2 (challenge path, deferred with the key) — the difference between "found none" and "proven none" | C + D | done (source); DV-131-1 in 1.2 | — |
| 1.5 | Production preflight package: exact L-1 read (owner authorizes by name), ledger 135/tip 120 read, `AUTODEPLOY-VERIFIED-OFF` attestation, per-function `verify_jwt` read (D's ask), Vault `project_url` production ceremony (owner), 133 drift counts 4/5 | packet §2 | the owner's read authorizations; the ceremony | A (package), Owner (reads, ceremony) | 1.2 acceptance closed | 1 d A | ∥ 1.2 |
| 1.6 | Production apply of the stack `121? (deferred) → 123 → 124 → 125 → 126 → 127 → 128 → 129 → 130 → 131 → 132 → 133 → 135 → 20260916000000` + edges, serialized, D witnessing; 126 validated by CI/harness only on the sandbox | packages, rollbacks, D's rehearsals | the ruling | A, D witness, Owner | 1.5, Stripe `payment_intent.canceled` subscription (Owner) | 0.5 d window + 24 h observation | — |
| 1.7 | **B's surface (B, 2026-09-17):** 132 done and sandbox-applied — missing only the production apply package, the order (after 131) and the coupling (`create-payment-intent` deployed after 132; rollback edge first); N-132-2 carried in B's words ("withdrawUnrecordedIntent reads payments and then cancels — NOT CHANGED": milliseconds, availability never money, kept as the only money-safe self-cleaning option); the **Vault `project_url` production ceremony has NOT been executed** (only the sandbox W1) — 133 refuses without it and, applied without it, five crons and four trigger bodies go silent; production `verify_jwt` is evidence to read, not a change (true on eight, false on `stripe-webhook`) | records | production package; the ceremony; the read | B (near zero), A (package), Owner (ceremony, reads) | 1.5 | 0.25 d | ∥ |
| 1.9 | **133 follow-up on A's surface (B's N-6):** `notify_outbid` still reads the legacy GUCs `app.settings.supabase_url` / `service_role_key` and returns early when they are null; 133 migrated four bodies and not this one, so on a Vault-configured environment outbid notifications silently no-op. Fix: a numbered migration moving `notify_outbid` to the Vault form (+ rollback, pgTAP, census +0); production GUC state is unverified and needs an owner-authorized read | 133 | the migration; the read | A | — | 0.5 d | ∥ 1.5 |
| 1.8 | **Notification fixes REQUIRED before the marketplace release** (A's determination from the backlog; the owner confirms): **R1** honour `notify_listing_sold` in the webhook's seller push and make the Settings screen truthful about the five toggles with no sending event (batch 1 item 1 — a control that does nothing misleads); **R2** surface the device-rebound notice on mobile from rows 135 already writes, via a `public` wrapper (batch 1 item 2 — b2 stops the redirect but does not yet tell the victim on the phone); **R3** `notify-report` idempotency (G22 — double-sent dispute/report pushes are user-facing); **R4** tap routing for the server types that send today (`transfer_expired_*`, `auto_release_*`, `report_*`, `dispute_*`; G26). **Disclosed, not required:** G9/G13 dedupe (low), the Plane-C rail with no sender (N-1, deliberate), the payout-destination drain arm (N-4, blocked on the dispatcher decision), the signing-monitor egress (N-2, a scanning-track gate), the outbid producer (N-6, an outbound in effect). Plan: `NOTIFICATION_BATCH_1_PLAN.md`. **APPROVED for implementation and local verification 2026-09-17** (owner's choices: hide the five inactive switches preserving stored prefs; the authenticated owner-scoped notice read/ack path; rebound meaning corrected — the server template already carries it; F-2S-1 neutral copy). In progress: A 136 (written, certified rehearsal running), B webhook read + G22, C hide/notice/F-2S-1, D reviews incl. the recovery actions. The staged-notice device test is prepared (§3b), not executed. Dispatcher, new outbound and another hosted build stay separate decisions | inventory; 135 rows; 092 inbox functions | the batch | A (136 wrapper, 1 d), B (webhook read + idempotency, 1.5 d), C (screen truth, notice screen, routing, 3 d), D (reviews, 1 d) | owner approval of batch 1; **a new pin** after integration (server + client), CI, D gate; device checks on the next build for R2/R4 screens (R2 needs a staged notice row on the sandbox, its own authorization, or pgTAP only; R1's honouring is test-only until push delivery is testable) | 6.5 d across A/B/C/D, ≈ 1 week elapsed | ∥ 1.2, before 1.5 closes |

**Critical path to production:** 1.1 → 1.2 ∥ 1.8 → new pin + D gate + CI → 1.5 → 1.6. **Schedule with the notification dependency:** session 2 this week (owner handset ≈ 3 h); batch 1 in parallel ≈ 1 week after the owner approves its scope; then the re-pin (CI + D gate, 1 d) and the R2/R4 screen checks on the next build (needs a build ruling, ≈ 1 h owner); then the preflight package (1 d A) and the production window on the owner's ruling. **Realistic:** production window in the week after next if session 2 and batch 1 raise no blocking finding.

**Separated, per the owner:** *implementation effort* = batch 1 (6.5 d across four sessions), preflight package (1 d), venue-migration integration (0.5 d); *owner decisions* = batch-1 scope, the staged-notice authorization, the next build, the L-1 and posture reads, the `project_url` production ceremony, `AUTODEPLOY-VERIFIED-OFF`, the Stripe `payment_intent.canceled` subscription, the production window itself; *device acceptance* = session 2 (≈ 3 h), the batch-1 screens (≈ 1 h), and everything deferred with the push key (untested, not attempted).

## Track 2 — Venue read-only acceptance (sandbox)

State: authorized in principle by O-1 (2026-09-15) as a phase of the sandbox window, never run; the B2 ruling excluded
venue exposure from the B2 window only; the owner's 2026-09-17 ruling: **"keep venue acceptance at the agreed point after session 2, with the migration-order issue resolved before opening that window."** It opens only on the owner's explicit word after A announces the MFA moments; the MFA act (B4) is the owner's own. **Order issue (2.1): resolved by D's rehearsal (0 lines differing between the two orders); A's integration of the migration onto the stack (2.0) follows session 2; the window may then be announced to the owner.**

| # | Task | Exists | Missing | Owner | Depends on | Effort | ∥ |
|---|---|---|---|---|---|---|---|
| 2.1 | **Migration-order issue — RESOLVED by rehearsal (D, 2026-09-17, probe `probes/gate_venue_order.sh` @ `d30f4dc`).** The kit executes the venue file directly and writes its ledger row (`20260910120000`, `venue_api_read_views`, 24 statements) with `ledger-row.py`, never `db push`, so no CLI ordering guard sees it and the row lands out of version order on a ledger already holding `20260916000000`. Measured: database A = full `LC_ALL=C` replay with the venue file spliced immediately before `20260916000000_processing_sweep_arm.sql` (a fresh replay's position); database B = the same 154 files with the venue file applied last (the sandbox's order) → **0 catalog-identity lines differ**; both hold 8 `venue_api` views and public census 32\|105\|37\|38. Structural reason: the venue migration creates only `venue_api` views and grants; `20260916000000` redefines only `get_unsettled_payments` and the Phase-0 arm — disjoint objects, neither redefining the other's. **Resolution:** the out-of-order ledger row is a bookkeeping artifact with no catalog consequence, and CI's in-position replay (after 2.0) and the sandbox's last-position apply are the same state, so the acceptance evidence stays valid against the integrated branch. **Caveats carried:** after the apply, the sandbox ledger's version order no longer matches application time (nothing infers "applied when" from it — compounding the earlier caveat that the sandbox ledger was never row-by-row from production's chain); the convergence is proven for this pair only, not a licence to apply out of order — the next such case runs the probe again. D disclosed and fixed a variable-shadowing bug that mislabelled the splice position in the probe's log (the splice itself was always correct) before citing the result | kit, probe | — | D (done) | — | done | — |
| 2.0 | **`20260910120000_venue_api_read_views.sql` is on ten branches and neither release branch** (D): acceptance proves the slice, not that it ships. A integrates it onto the production-gate stack as its own integration after session 2 (a `supabase/` change → new pin, CI, D's gate), so the evidence does not age against a branch nothing ships from | migration on D's branches | the integration | A | 1.2 | 0.5 d | ∥ 2.1 |
| 2.2 | Owner authorizes the venue window; A announces **two owner moments** (D): B4 after kit step 2 `verify-apply` and before step 4 `verify-exposure` — dashboard → Integrations → Data API → Exposed schemas → add `venue_api` only → Save, screenshots before/after, say "exposed" (**3 min**); after V6, review three evidence screenshots: manager events, finance denied, outsider denied (**5 min**); plus 2 min at the start to name the window and confirm the build. **Owner ≈ 10 min; automated ≈ 8 min; end-to-end 25–30 min** with A's read-backs either side. Nothing after B4 runs until the owner says so | D's plan | the ruling | Owner | 2.1 | — | — |
| 2.3 | B1–B3: D applies `venue_api` read views via the kit, A records census before/after (ledger 141 → 142) | migration, kit | — | D (apply), A (witness) | 2.2 | 0.25 d | — |
| 2.4 | **B4 owner MFA**: expose `venue_api` in the dashboard (`public, graphql_public, kernel, venue_api`), `db_pre_request` unchanged, three screenshots | runbook | the act | Owner (≈ 10 min in three blocks, D's plan) | 2.3 | — | — |
| 2.5 | B5–B6: kit api + browser phases (135 checks, C6/C7), cleanup by exact id, A re-verifies counts | kit | the run | D, A | 2.4 | 0.5 d | — |
| 2.6 | D's inputs on what the acceptance proves and does not (reads only; no writes, no door) | *(pending D)* | | D | | | |

**Estimate:** one working day of D once the owner opens it; owner time ≈ 10 min. **Separate from production:** production
`venue_api` apply + exposure is its own ruling (Track 3).

## Track 3 — Usable client onboarding

State: reads-only dashboard exists on the venue branches; `venue_api` applied nowhere; no dashboard write path; the operator
role is the biggest gap (D §5); several finance steps have no owner (tax/1099: **missing**).

| # | Task | Exists | Missing | Owner | Depends on | Effort | ∥ |
|---|---|---|---|---|---|---|---|
| 3.1 | Track 2 complete on the sandbox | — | — | D | 2.x | — | — |
| 3.2 | Production `venue_api` apply + exposure (owner MFA in production) | migration | the ruling; production window | Owner, D, A | 1.6 (stack in production) | 0.5 d | — |
| 3.3 | **Operator (platform admin) onboarding surface — SCOPED by D 2026-09-17** (`docs/venue-dashboard/OPERATOR_ONBOARDING_SCOPE_D_20260917.md` @ `0d85bb2`; local investigation only, per the owner). **Findings:** half the onboarding verbs are unreachable from a browser today — `kernel` and `ops` are PostgREST-exposed, `catalog` and `venue` are not (the venue kit asserts they stay 406), so `catalog.create_venue`, `catalog.approve_venue`, `catalog.create_event`, `venue.grant_staff_role` cannot be called; and `ops.*` contains **no organisation, venue, staff or event read of any kind**, so the console cannot list an organisation to act on. The write side exists; the read side does not. Bespoke screens calling the verbs directly would **bypass the console's own two-person approval and audit trail** (`action_dispatch`, `action_precheck`, `action_allowed_roles`, `action_requires_approval`, `approve_action`, `audit_write`, `/actions/[id]`), which every other privileged action goes through; `action_dispatch` branches on a hardcoded `action_type`, so new types are a migration, not configuration. **Shape:** a migration first (ops read paths; `public`/`ops` wrappers for the four `catalog`/`venue` verbs — **wrappers, not schema exposure**, since exposing `catalog`/`venue` makes every object client-reachable and invalidates a standing venue-acceptance assertion; new `action_type` branches with allowed-roles/approval registry rows; the four-file rule per object; pgTAP with a negative control per branch), then a thin UI: **four screens, one existing** — Organisations (list + detail), Venue approval queue, Staff roles, and the existing action/approval screen reused. Every write verb takes `p_command_key`: the UI must generate, persist and re-send a key per action. **Effort: a band until two owner decisions land** — (i) which organisation/member fields an operator may see (legal names and member identities are personal data, the gender-dimension standard of care applies); (ii) which actions require two-person approval (D recommends: venue approval and platform-role grants yes, the rest no). **D's recommendation on the critical-path decision: hand-run the first venue under per-action authorization and build in parallel** — the first onboarding teaches what the screens need, and it keeps the critical path off a migration that queues behind the release work anyway. **Contract draft `OPERATOR_ONBOARDING_CONTRACT_D_20260917.md` @ `cf1f9c6` (2026-09-17); decisions:** ① the owner's elevation rule is params-dependent but `ops.action_requires_approval(text)` sees only the type — **A's ruling: split the types (`org_member_role_change`/`org_member_elevate`, `org_member_invite`/`org_member_invite_admin`) with an `action_precheck` guard in both directions**, not a change to the 115 framework predicate; the owner may run the assisted first venue under "approve every member change" by policy without code change. ② **"account identifier" — RULED by the owner 2026-09-17: the stable identity UUID, not the email;** contact email stays behind the separate audited, purpose-limited workflow. In the same ruling the owner accepted the guarded split between routine and elevating action types (①), requires requester and approver visible in the console audit record (③), and for the assisted first venue requires the owner's approval for every member change — a policy posture over the (a)-shaped code, and it does not open that execution window. Earlier the same day the owner fixed the visible fields (organisation ID, legal/display name, status, venue details, member display name and account identifier, roles, invitation status, Connect readiness; contact email only in workflows that need it; gender, bank/tax details, secrets and unrelated account data excluded) and the two-person set (venue approval, platform-role grants, any elevation to organisation ownership/admin; stronger existing controls preserved; existing audited framework and narrow wrappers, no catalog/venue schema exposure). **D proceeds with 138 and the operator surface under the agreed development scope; D's baseline proof and constraint-name finding are at `review/d-release-sprint` @ `7989625`.** ③ the onboarding verbs take no actor, so under the framework the domain audit records the approver as actor while `ops.action` records requester and approver — **accepted and written into the contract** (two trails, `ops.action` authoritative, UI shows both); an actor parameter on the kernel/catalog/venue verbs is a later migration on A/B's surface. Also fixed by the contract: five `ops` reads (console role only), contact email as a separate audited verb with a closed reason set, no new doors, `p_command_key` derived from the action row, two baselines (118 for dispatch/precheck, 115 for the rest) with per-object rollbacks, the `ops.action` CHECK constraints widened, an allowlist control test, events/sessions deferred. 138 = pgTAP 206 | write verbs (077–082); the console's action framework (115–118) | `ops` reads; wrappers; `action_type` branches; two owner decisions | Owner (decisions i–ii; hand-run vs build-first), D (migration + UI), A (migration number **138 allocated provisionally**, review, four-file rule) | 1.6 (production window) for anything production; local work can start on the decisions | band → figure after (i)–(ii); the migration is the larger half | ∥ 3.2 |
| 3.4 | Venue admin self-service writes (events, sessions, ticket types, batches): dashboard callers for the existing `venue.*` RPCs | RPCs (081/082) | dashboard write slice + `venue_api` exposure of the RPC path or public wrappers (A's contract) | D (UI), A (contract) | 3.2 | *(pending D)* | ∥ 3.3 |
| 3.5 | Staff MFA policy for writes (reads need none): decision | admin console pattern | ruling | Owner | — | — | ∥ |
| 3.6 | Finance responsibilities with no owner: tax/1099, payout-destination change notice (backlog row 3) | Connect onboarding exists | owner assignment | Owner | — | — | ∥ |
| 3.7 | Support recovery runbooks (push unbind exists; org/venue recovery missing) | runbook | *(pending D)* | D/B | | | ∥ |

**Reconciling the 4–6 weeks with the short task estimates (owner's ask):** the implementation inside it is small; the elapsed time is sequencing and waiting.

| Component of "reads-only onboarding in production" | Kind | Time |
|---|---|---|
| Track 1 to a production window (session 2 → batch 1 → re-pin → preflight → window → 24 h observation) | mostly waiting on sequenced rulings and device time; ≈ 9 d of work across sessions | ≈ 2–3 weeks elapsed |
| Venue migration onto the stack (2.0) and the sandbox venue window (2.2–2.5) | 0.5 d + 1 d work; the owner's 10 min; opens on the owner's word after session 2 | ≈ 1 week elapsed, in parallel |
| Production `venue_api` apply + exposure (3.2) | 0.5 d work; an owner ruling and an owner MFA act | days, after the production window |
| **Missing work** with no estimate yet: the operator surface (3.3) — D's scoping day now (owner-authorized 2026-09-17), then a number; self-service writes (3.4); the MFA-for-writes ruling (3.5); finance ownership (3.6) | work + rulings | not in the 4–6 weeks: reads-only onboarding does not need them, a *usable* onboarding does |

So **4–6 weeks is ≈ 2 weeks of sequential release work plus ≈ 2–4 weeks of waiting on rulings and windows**, for a reads-only dashboard on an onboarded venue; a usable, self-service onboarding adds D's operator-surface estimate (pending its scoping day) and the write slice. 3.5–3.6 are rulings, not work.

## Track 4 — Populated My Tickets

State: the tab and the read contract exist; `20260909000000` is on the sandbox (ledger 141) and not in production; the only
legitimate producer of a ticket row is the dark native issuance chain. C's lane estimates (2026-09-17):

| # | Task | Exists | Missing | Owner | Depends on | Effort | ∥ |
|---|---|---|---|---|---|---|---|
| 4.1 | **My Tickets layout evidence — owner ruling 2026-09-17: client-side fixture preview only**, with a visible label reading exactly "Sample tickets — no server data"; **no fake signing key, no permanent ticket fixture on the shared sandbox, no audit-protection override; real issuance is a separate later step.** C implements and verifies locally after first checking whether an existing development client or local preview (the `__DEV__` toggle in `app/(tabs)/tickets.tsx`) already shows it; **no hosted build is authorized for this**; layout evidence is recorded apart from server and issuance acceptance (`MY_TICKETS_SANDBOX_FIXTURE_PROPOSAL.md` records the decision; the server-fixture shapes there are historical, not planned) | `__DEV__` fixture toggle | the label; the local check | C (implement, verify locally), D (review) | — | 0.5 d | ∥ 1.2 |
| 4.2 | CFT-801 **layout rows only** on the labelled local preview (list, Upcoming/Past, quantity, ownership × fulfillment, large text, VoiceOver) — recorded as "layout evidence, fixture mode, no server data", never as server or issuance acceptance; the server-backed CFT-801 waits for real issuance on the sandbox (4.8) | tab | the run | C, Owner (~1 h on the local preview) | 4.1 | 0.5 d | — |
| 4.3 | CFT-811 ticket detail | — | detail read contract (A 1–2 d incl. pgTAP + grant manifest) then C 2–3 d | A → C | 4.2 | 3–5 d | ∥ 4.2 |
| 4.4 | CFT-813 offline cache of the list | — | cache/TTL contract (A 1 d) then C 2–3 d; token half waits on 4.6 | A → C | 4.2 | 3–4 d | ∥ 4.3 |
| 4.5 | CFT-816 `ticket_ready` / `ownership_changed` push handling + routing | templates (Plane C) | a producer and the dispatcher un-parked (server, unscheduled) | A/B → C | backlog P3-2 | 1 d C + server | ∥ |
| 4.6 | CFT-812 Entry Pass / QR | — | `credential-sign` live + credential contract — **ceremony NO-GO**; QR renderer decision (Owner) | B → A → C | Track 5 | 3–4 d C | — |
| 4.7 | CFT-814 Apple Wallet; CFT-815 transfer/sell from a ticket | specs | wallet minting parked; `create_p2p_transfer` raises until the resale TTL and `native_resale_enabled` are decided | Owner rulings | — | not schedulable | — |
| 4.8 | **Production**: `20260909000000` owner-gated apply; native issuance flag flip (dual control); `primary-checkout` deployed; a primary-checkout client that does not exist; `venue`/`catalog` exposure or public wrappers (A's contract) | RPCs, edge code | the whole chain and its rulings | Owner, A, B, C | 1.6 | *(pending B for the chain)* + C's 4.3–4.4 | — |

**Estimate (substantiated):** **layout evidence** = 4.1 + 4.2 → days (local only, no sandbox write, no build); **populated Tickets from real data on the sandbox** = the issuance chain of 4.8 on the sandbox (a sandbox signing key by the ratified path, the issuance flag ON on the sandbox by ruling, `primary-checkout` deployed there, one primary sale) — weeks, ruling-gated; **in production** = Track 1 done + 4.8's chain, of which the flag flip, the checkout edge and a checkout client are unscheduled → **8–10 weeks** remains the honest figure and is gated by rulings, not effort. No fixture-cleanup assumption remains anywhere in this plan.

## Track 5 — Event-day scanning

State by environment (resolved by D from records, 2026-09-17; A concurs from the sandbox reads): **production** trust root live (KMS D4), signer/door functions deployed dark,
`kernel.signing_key` 1 active global key, flags false; **sandbox** no signing key (0), door/sign functions **not deployed** (the
sandbox `functions list` of 2026-09-16 shows nine functions, none of `door-session`, `door-manifest`, `credential-sign`,
`primary-checkout`), flags false; **local** dark code, `KMS_PROVIDER` unset. The door-session "park" text (PFA-26) is stale:
107 un-parked it and 107 is in production (tip 120); doors are switched off, not impossible (D §5).

| # | Task | Exists | Missing | Owner | Depends on | Effort | ∥ |
|---|---|---|---|---|---|---|---|
| 5.1 | 121 (manifest signing context strict) applied — **defensive hardening, not a defect fix** (B): the concurrency rationale was withdrawn, the function is STABLE and the race unreachable; PR #58 rev 2, deferred by the owner 2026-09-11 to A's integration after the handset matrix; needs "AUTHORIZE PFA-18C MIGRATION 121"; its CI "Immutability + ordering" FAIL is the attestation gate stopping before execution — **blocked, not failing** | migration, PR #58 | the ruling | Owner, A | 1.6 | in the stack window | — |
| 5.2 | 125 (scan-device manifest sync drift) applied | on the sandbox | production ruling | Owner, A | 1.6 | in the stack window | — |
| 5.3 | **B's dependency order (2026-09-17), complete in production: C1–C6** (CreateKey D4 2026-09-10, trust-root DB commit, 110–114, monitor armed under real dual control 2026-09-11, dark deploy verified 2026-09-12). **Missing, in order:** G2 the owner's ruling on **M5-iso vs M5-live — never ruled, gates everything downstream**; G3 Model A (AWS org, audit account, Object-Lock bucket, SCP, org trail, Device-2 read) — days, no repo code, parallel with all marketplace work; G4 M5-live = T3, the first production credential; G5 issuance flip; G6 migration 125; G7 scanning flip; G8 live commerce | C1–C6 | G2 ruling, G3, G4 | Owner (G2, dual control), B (G3, G4) | — | G3 days; others rulings | ∥ Track 1 |
| 5.4 | The three edges are already deployed dark in production and are held dark by **data and flags, not configuration** (B): `door-manifest` needs a staff role, venue, session and open episode; `door-session` needs `door_pin` and `scan_device` rows; `credential-sign` needs tickets and the issuance flag. "From dark to live" is therefore data + flags + the M5 credential, not a redeploy | dark deploys | G4–G7 | B, Owner | 5.3 | — | — |
| 5.5 | Scanner client | **none** | a client (web or native) using `door-session` (PIN → DoorSession token) | Owner decision; C or D | 5.4 contract | **missing, not estimated** | ∥ 5.3 |
| 5.6 | Scanning flag flip (dual control) + first real scan on the sandbox, then production | flag seeded false | rulings | Owner | 5.4, 5.5, Track 4 | — | — |

**Sequencing (D):** scanning is downstream of primary sales, not beside it — tickets come from `issue_ticket_atoms` on a primary `payment_intent.succeeded`, `primary-checkout` is not deployed and the issuance flag is false, so event-day scanning cannot be shown end to end until the primary path exists. Parallel now: D's dashboard door wiring (needs a read slice) and B's 121/125 decisions. **Caveat on sandbox state (D):** the sandbox ledger lists 123,124,125,127–130 above 109 and not 110–120, so it was not built row by row from production's chain; per-migration sandbox state is read from the catalog, never inferred from ledger rows. **Estimate:** after Track 4; not estimable until 5.5 has an owner and a shape. D's full inputs: `docs/venue-dashboard/CRITICAL_PATH_INPUTS_D_20260917.md` @ `a8cd818`.

## Standing item — required signup gender answer + analytics (must not disappear)

| Item | Owner | Status |
|---|---|---|
| CFT-901 data contract, privacy, schema, analytics review (A's answers owed): separate owner-only table; RLS owner-only; no admin row-level read; one aggregate function with small-group suppression; SECURITY DEFINER RPC write path setting consent + timestamps; "cleared" vs "undisclosed" distinguishable in the row, identical in every aggregate; retention = life of account; hard delete with the account; in the export; consent withdrawal stops inclusion forward | A | interim positions given 2026-09-16; full document owed with this critical path (next) |
| CFT-902–908 client: required answer with an equal-weight "Prefer not to say", inclusive options + optional self-description, the "why", explicit analytics consent, editable/clearable, private by default, never inferred | C | proposal `GENDER_PROFILE_DATA_PROPOSAL_C.md` written; waits on CFT-901 |
| Analytics dimensions and suppression thresholds | D | asked 2026-09-16 |
| Outstanding owner decisions | Owner | required vs skippable screen (App Store 5.1.1 data minimisation); which aggregates; retention text; whether any use outside analytics is ever approved (default: none) |
| Constraint | all | no migration or production change authorized; nothing in discovery, bidding, checkout, transfers or seller decisions |

## Parallelism summary
Track 1's session 2 and preflight package run together; Track 2 runs in D's lane on the owner's word independent of Track 1;
Track 4's sandbox fixture depends only on a ruling and A/B time; Tracks 3 and 5 are ruling-gated and start after Track 1's
production window. B's and D's lane inputs are merged above (B: execution record `55b315f` on `feature/venue-native-and-product-v2`, 132 design `961e29c`; D: `CRITICAL_PATH_INPUTS_D_20260917.md` @ `a8cd818`); nothing above is a commitment to a date. **Signing monitor egress (B's N-2):** the monitor's alert path is `notify-report`, gated by `EMAIL_ENABLED` (default false) and `RESEND_API_KEY` with deployed values unknown, and any notify-rail route is subject to the rail having no sender (backlog N-1) — an invariant alert on the production trust root may have no way to reach a human; cheap to check before scanning goes live (B with A).
